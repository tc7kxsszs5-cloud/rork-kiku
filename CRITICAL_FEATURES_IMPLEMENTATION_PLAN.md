# 🚀 План реализации критических функций

## Приоритеты

### 1. ✅ Analytics - Интеграция trackEvent (ВЫСОКИЙ)
**Статус:** Контекст создан, но не везде используется

**Что нужно:**
- Добавить `trackEvent` вызовы в `MonitoringContext` (message_analyzed, alert_created)
- Добавить в `ParentalControlsContext` (sos_triggered, settings_changed)
- Протестировать сохранение событий

**Время:** 30 минут

---

### 2. 🔔 Push Notifications - Полная интеграция (КРИТИЧЕСКИЙ)
**Статус:** Библиотека установлена, но нет интеграции

**Что нужно:**
1. Создать `PushNotificationsContext` для:
   - Регистрации устройства
   - Запроса разрешений
   - Обработки входящих уведомлений
   - Хранения push token

2. Интегрировать в MonitoringContext:
   - Отправка уведомлений при создании алертов
   - Отправка уведомлений при SOS

3. Backend интеграция:
   - Отправка push через Expo Push Notification Service
   - Или через Firebase Cloud Messaging (для production)

**Время:** 2-3 часа

---

### 3. 🔄 Backend Sync - Улучшение синхронизации (ВЫСОКИЙ)
**Статус:** Базовые роуты есть, но in-memory хранилище

**Что нужно:**
1. Улучшить синхронизацию чатов:
   - Merge логика для конфликтов
   - Timestamp-based синхронизация
   - Incremental sync

2. Синхронизация алертов и настроек:
   - Аналогичная логика

3. (Опционально) Добавить реальную БД (PostgreSQL) вместо in-memory

**Время:** 2-4 часа

---

## Порядок реализации

1. **Analytics** (быстро, сразу даст результат)
2. **Push Notifications** (критично для production)
3. **Backend Sync** (можно делать параллельно или после)

---

**Начинаем с Analytics!**


