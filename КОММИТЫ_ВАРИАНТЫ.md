# 📝 Варианты выполнения коммитов

## 🎯 Вариант 1: По одному коммиту (рекомендуется)

**Преимущества:**
- ✅ Легко откатить отдельный коммит
- ✅ Четкая история изменений
- ✅ Удобно для code review

### Коммит 1: Унификация логирования
```bash
git add constants/MonitoringContext.tsx constants/AnalyticsContext.tsx constants/UserContext.tsx constants/ParentalControlsContext.tsx constants/AuthContext.tsx constants/ChatBackgroundsContext.tsx constants/ThemeContext.tsx constants/AIModerationService.ts app/_layout.tsx "app/chat/[chatId].tsx"
git commit -m "feat: unify logging - replace console.* with logger.*" -m "- Replace all console.log/error/warn/info with structured logger" -m "- Add context and action metadata to all log entries" -m "- Improve debugging and monitoring capabilities" -m "- ~60+ replacements across constants/ and app/ directories"
```

### Коммит 2: Централизованная обработка ошибок
```bash
git add utils/errorHandler.ts constants/MonitoringContext.tsx constants/ParentalControlsContext.tsx constants/UserContext.tsx
git commit -m "feat: add centralized error handling with i18n support" -m "- Create utils/errorHandler.ts with user-friendly error messages" -m "- Integrate errorHandler into MonitoringContext, ParentalControlsContext, UserContext" -m "- Add i18n support for error messages (en/ru)" -m "- Add showUserFriendlyError, handleErrorSilently, withErrorHandling functions"
```

### Коммит 3: Переводы ошибок
```bash
git add constants/locales/en.ts constants/locales/ru.ts
git commit -m "feat: add error message translations to i18n" -m "- Add common.errors section to English and Russian locales" -m "- Support for network, sync, load, save, permission, storage, analysis, SOS, auth errors" -m "- Default error message for fallback cases"
```

---

## 🎯 Вариант 2: Объединить коммиты 2 и 3 (errorHandler + переводы)

**Преимущества:**
- ✅ Логически связанные изменения вместе
- ✅ Меньше коммитов
- ✅ errorHandler и переводы - единая фича

### Коммит 1: Унификация логирования
```bash
git add constants/MonitoringContext.tsx constants/AnalyticsContext.tsx constants/UserContext.tsx constants/ParentalControlsContext.tsx constants/AuthContext.tsx constants/ChatBackgroundsContext.tsx constants/ThemeContext.tsx constants/AIModerationService.ts app/_layout.tsx "app/chat/[chatId].tsx"
git commit -m "feat: unify logging - replace console.* with logger.*" -m "- Replace all console.log/error/warn/info with structured logger" -m "- Add context and action metadata to all log entries" -m "- Improve debugging and monitoring capabilities" -m "- ~60+ replacements across constants/ and app/ directories"
```

### Коммит 2+3: ErrorHandler с i18n (объединенный)
```bash
git add utils/errorHandler.ts constants/MonitoringContext.tsx constants/ParentalControlsContext.tsx constants/UserContext.tsx constants/locales/en.ts constants/locales/ru.ts
git commit -m "feat: add centralized error handling with i18n support" -m "- Create utils/errorHandler.ts with user-friendly error messages" -m "- Integrate errorHandler into MonitoringContext, ParentalControlsContext, UserContext" -m "- Add i18n support for error messages (en/ru)" -m "- Add error message translations to English and Russian locales" -m "- Add showUserFriendlyError, handleErrorSilently, withErrorHandling functions"
```

---

## 🎯 Вариант 3: Все вместе (не рекомендуется, но возможно)

**Преимущества:**
- ✅ Один коммит - все изменения
- ✅ Быстро

**Недостатки:**
- ❌ Сложно откатить отдельные изменения
- ❌ Менее понятная история

```bash
git add constants/MonitoringContext.tsx constants/AnalyticsContext.tsx constants/UserContext.tsx constants/ParentalControlsContext.tsx constants/AuthContext.tsx constants/ChatBackgroundsContext.tsx constants/ThemeContext.tsx constants/AIModerationService.ts app/_layout.tsx "app/chat/[chatId].tsx" utils/errorHandler.ts constants/locales/en.ts constants/locales/ru.ts

git commit -m "feat: improve code quality - unified logging and error handling" -m "- Replace all console.* with structured logger" -m "- Add centralized error handling with i18n support" -m "- Add error message translations (en/ru)" -m "- Improve debugging and user experience"
```

---

## ✅ Рекомендация

**Рекомендую Вариант 2** (объединить коммиты 2 и 3):
- Коммит 1: Логирование (отдельно)
- Коммит 2: ErrorHandler + переводы (вместе, т.к. логически связаны)

Это даст **2 коммита** вместо 3, но сохранит логическую структуру.

---

## 🚀 Готовые команды для Варианта 2

```bash
# Коммит 1: Унификация логирования
git add constants/MonitoringContext.tsx constants/AnalyticsContext.tsx constants/UserContext.tsx constants/ParentalControlsContext.tsx constants/AuthContext.tsx constants/ChatBackgroundsContext.tsx constants/ThemeContext.tsx constants/AIModerationService.ts app/_layout.tsx "app/chat/[chatId].tsx"
git commit -m "feat: unify logging - replace console.* with logger.*" -m "- Replace all console.log/error/warn/info with structured logger" -m "- Add context and action metadata to all log entries" -m "- Improve debugging and monitoring capabilities" -m "- ~60+ replacements across constants/ and app/ directories"

# Коммит 2: ErrorHandler + переводы (объединенный)
git add utils/errorHandler.ts constants/MonitoringContext.tsx constants/ParentalControlsContext.tsx constants/UserContext.tsx constants/locales/en.ts constants/locales/ru.ts
git commit -m "feat: add centralized error handling with i18n support" -m "- Create utils/errorHandler.ts with user-friendly error messages" -m "- Integrate errorHandler into MonitoringContext, ParentalControlsContext, UserContext" -m "- Add i18n support for error messages (en/ru)" -m "- Add error message translations to English and Russian locales" -m "- Add showUserFriendlyError, handleErrorSilently, withErrorHandling functions"
```

---

**Выберите удобный вариант!** 🎯
