# 🔀 Git Workflow - Руководство по коммитам

## 📋 Общие принципы

### При написании планов (Plan Writing)

- ✅ **Включать commit steps в задачи** как часть плана
- ✅ **Команды коммитов показывают ЧТО** должно быть закоммичено, а не КОГДА
- ✅ Планы могут содержать шаги коммитов, но **фактическое выполнение** решает executing-plans

### При выполнении (Execution)

- ❌ **НЕ делать авто-коммиты** - даже если план включает команды коммитов
- ✅ **Всегда спрашивать пользователя** перед коммитом
- ✅ Агент должен получить **явное подтверждение** перед коммитом

---

## ✅ Когда предлагать коммиты

### Условия для предложения коммита:

1. **После завершения батча** - когда агент проверил, что тесты проходят
2. **Естественные точки паузы** - завершение логической единицы работы (batch completion)
3. **Когда агент может проверить работу**:
   - ✅ Тесты проходят (`bun test`)
   - ✅ TypeScript проверка пройдена (`bunx tsc --noEmit`)
   - ✅ Линтер проверка пройдена (если применимо)
4. **Верфицируемая работа** - только если агент сам может убедиться в корректности

### Примеры уместных ситуаций:

```bash
# ✅ Хорошо: после завершения тестов
bun test  # Все тесты прошли
# → Можно предложить коммит: "Добавлены unit тесты для merge логики"

# ✅ Хорошо: после проверки TypeScript
bunx tsc --noEmit  # Нет ошибок
# → Можно предложить коммит: "Исправлены TypeScript ошибки"
```

---

## ❌ Когда НЕ предлагать коммиты

### Запрещенные ситуации:

1. **Тесты - ответственность пользователя**
   - Если требуется ручной запуск тестов пользователем
   - Если агент не может проверить результаты

2. **Нет естественной точки паузы**
   - В середине большой задачи
   - Когда работа не завершена

3. **Требуется ручная проверка**
   - Если нужна проверка пользователем
   - Если есть неопределенность в результате

4. **Работа не завершена**
   - Частичная реализация
   - Есть известные проблемы

### Примеры неуместных ситуаций:

```bash
# ❌ Плохо: в середине задачи
# Реализовано 50% функции
# → НЕ предлагать коммит

# ❌ Плохо: требуется ручная проверка
# "Пожалуйста, проверьте работу приложения"
# → НЕ предлагать коммит до проверки пользователем

# ❌ Плохо: тесты не запускались
# Код изменен, но тесты не проверены
# → НЕ предлагать коммит
```

---

## 📝 Формат сообщений коммитов

### Структура:

```
<тип>: <краткое описание>

<детали (опционально)>

<файлы (опционально)>
```

### Типы коммитов:

- `feat`: Новая функциональность
- `fix`: Исправление ошибки
- `test`: Добавление/изменение тестов
- `refactor`: Рефакторинг кода
- `docs`: Изменения в документации
- `style`: Форматирование, стиль кода
- `chore`: Вспомогательные задачи

### Примеры:

```
test: добавлены unit тесты для merge логики синхронизации

- Добавлены тесты для mergeMessages, mergeChats, mergeAlerts
- Добавлены тесты для getDeltaChats (инкрементальная синхронизация)
- Все 15 тестов проходят успешно

Файлы:
- backend/trpc/routes/sync/merge-logic.test.ts
```

```
fix: исправлена валидация deviceId в sync роутах

- Добавлена валидация длины и формата deviceId
- Добавлена валидация timestamp
- Исправлены TypeScript ошибки

Файлы:
- backend/trpc/routes/sync/chats.ts
- backend/trpc/routes/sync/alerts.ts
- backend/trpc/routes/sync/settings.ts
```

---

## 🔄 Workflow в планах задач

### В планах (REMAINING_TASKS.md, TESTING_PLAN.md):

Планы могут содержать шаги коммитов как часть задач:

```markdown
### Задача: Добавить unit тесты

1. Создать файл `backend/trpc/routes/sync/merge-logic.test.ts`
2. Написать тесты для merge логики
3. Запустить тесты: `bun test`
4. **Коммит (после проверки)**: `test: добавлены unit тесты для merge логики`
```

**Важно**: Это показывает ЧТО закоммитить, но НЕ означает авто-коммит!

### При выполнении:

```bash
# Агент выполняет задачи 1-3
# После успешного завершения:

# ✅ ПРАВИЛЬНО: Предложить коммит пользователю
# "Задачи завершены. Все тесты проходят (42/42). 
#  Предлагаю закоммитить: 'test: добавлены unit тесты для merge логики'"

# ❌ НЕПРАВИЛЬНО: Автоматический коммит
# git commit -m "test: добавлены unit тесты"
```

---

## ✅ Чеклист перед предложением коммита

Перед предложением коммита агент должен убедиться:

- [ ] Работа завершена (логическая единица)
- [ ] Тесты проходят (если применимо)
- [ ] TypeScript проверка пройдена (если применимо)
- [ ] Линтер проверка пройдена (если применимо)
- [ ] Нет очевидных ошибок
- [ ] Есть естественная точка паузы
- [ ] Агент может проверить результат

---

## 📚 Связанные документы

- [`AI_AGENT_WORKFLOW.md`](./AI_AGENT_WORKFLOW.md) - Интеграция процессов для AI агента
- [`TESTING_PLAN.md`](./TESTING_PLAN.md) - План тестирования (может содержать commit steps)
- [`REMAINING_TASKS.md`](./REMAINING_TASKS.md) - Оставшиеся задачи (может содержать commit steps)
- [`.cursorrules_git_workflow`](./.cursorrules_git_workflow) - Правила для AI агента
- [`.gitignore`](./.gitignore) - Игнорируемые файлы

---

## 🎯 Итоговые правила

1. **Планы могут содержать commit steps** - это нормально
2. **Выполнение НЕ делает авто-коммиты** - всегда спрашивать
3. **Предлагать коммиты только после проверки** - тесты, typecheck, lint
4. **Использовать естественные точки паузы** - завершение батча
5. **Уважать ответственность пользователя** - тесты, проверки

---

**Последнее обновление**: 2025-01-06

