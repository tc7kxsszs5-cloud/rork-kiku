# 📝 Коммиты для незакоммиченных файлов

## 📊 Анализ незакоммиченных файлов

### Измененные файлы (M):
1. `.github/workflows/ci.yml` - CI/CD конфигурация
2. `.gitignore` - Игнорируемые файлы
3. `README.md` - Документация
4. `TESTING_GUIDE.md` - Руководство по тестированию
5. `__tests__/README.md` - Документация тестов
6. `__tests__/unit/utils/versioning.test.ts` - Тесты
7. `app/(tabs)/_layout.tsx` - Layout табов
8. `app/(tabs)/analytics.tsx` - Экран аналитики
9. `app/(tabs)/index.tsx` - Главный экран
10. `app/(tabs)/profile.tsx` - Экран профиля
11. `backend/trpc/app-router.ts` - tRPC роутер
12. `backend/trpc/routes/notifications/send-push.ts` - Push уведомления
13. `bun.lock` - Lock файл зависимостей
14. `constants/types.ts` - Типы
15. `jest.config.js` - Конфигурация Jest
16. `jest.setup.js` - Настройка Jest

### Новые файлы (??):
- Много документации (.md файлы)
- Компоненты: `ChatBackgroundPicker.tsx`, `SyncStatusIndicator.tsx`
- Утилиты: `aiService.ts`, `syncService.ts`

---

## 🎯 Предлагаемые коммиты

### Коммит 1: CI/CD и конфигурация
```bash
git add .github/workflows/ci.yml .gitignore jest.config.js jest.setup.js bun.lock
git commit -m "chore: update CI/CD and configuration files" -m "- Update GitHub Actions workflow" -m "- Update .gitignore" -m "- Update Jest configuration" -m "- Update bun.lock"
```

### Коммит 2: Документация
```bash
git add README.md TESTING_GUIDE.md __tests__/README.md
git commit -m "docs: update project documentation" -m "- Update README.md" -m "- Update TESTING_GUIDE.md" -m "- Update tests README"
```

### Коммит 3: Тесты
```bash
git add __tests__/unit/utils/versioning.test.ts
git commit -m "test: update versioning tests" -m "- Update versioning test file"
```

### Коммит 4: Типы
```bash
git add constants/types.ts
git commit -m "feat: update type definitions" -m "- Update constants/types.ts"
```

### Коммит 5: Backend (tRPC)
```bash
git add backend/trpc/app-router.ts backend/trpc/routes/notifications/send-push.ts
git commit -m "feat: update backend tRPC routes" -m "- Update app-router.ts" -m "- Update send-push notification route"
```

### Коммит 6: UI компоненты (tabs)
```bash
git add app/(tabs)/_layout.tsx app/(tabs)/analytics.tsx app/(tabs)/index.tsx app/(tabs)/profile.tsx
git commit -m "feat: update tab screens" -m "- Update tabs layout" -m "- Update analytics screen" -m "- Update index screen" -m "- Update profile screen"
```

### Коммит 7: Новые компоненты
```bash
git add components/ChatBackgroundPicker.tsx components/SyncStatusIndicator.tsx
git commit -m "feat: add new components" -m "- Add ChatBackgroundPicker component" -m "- Add SyncStatusIndicator component"
```

### Коммит 8: Новые утилиты
```bash
git add utils/aiService.ts utils/syncService.ts
git commit -m "feat: add utility services" -m "- Add aiService.ts" -m "- Add syncService.ts"
```

### Коммит 9: Документация (новые файлы) - опционально
```bash
git add *.md
git commit -m "docs: add project documentation files" -m "- Add various documentation files"
```

---

## 🚀 Быстрый вариант: Все вместе

```bash
# Все измененные файлы
git add -u

# Все новые файлы (кроме временных)
git add components/ utils/aiService.ts utils/syncService.ts *.md

# Один коммит
git commit -m "chore: update project files and add documentation" -m "- Update CI/CD, configs, and documentation" -m "- Add new components and utilities" -m "- Update backend and frontend files"
```

---

## ✅ Рекомендация

**Вариант 1: По группам (рекомендуется)**
- Коммиты 1-6 для измененных файлов
- Коммиты 7-8 для новых компонентов/утилит
- Коммит 9 для документации (опционально)

**Вариант 2: Все вместе**
- Один большой коммит со всеми изменениями

---

**Выберите удобный вариант!** 🎯
