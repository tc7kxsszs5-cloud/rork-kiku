# ✅ Чек-лист на завтра (21 января 2025)

**Время:** Утро  
**Цель:** Улучшить качество кода проекта KIKU  
**Оценка времени:** 2-3 часа

## 🎯 Задачи на завтра

### ✅ Задача 1: Унифицировать логирование (1-1.5 часа)

**Что делать:**
1. [ ] Найти все использования `console.*` в проекте
2. [ ] Заменить в `constants/` (20 файлов)
3. [ ] Заменить в `app/` (12 файлов)
4. [ ] Заменить в `utils/` (если есть)
5. [ ] Проверить работу: `bun run lint` и `bunx tsc --noEmit`
6. [ ] Запустить приложение и проверить логи

**Файлы для замены:**
- `constants/MonitoringContext.tsx` - 9 замен
- `constants/AnalyticsContext.tsx` - 8 замен
- `constants/UserContext.tsx` - 14 замен
- `constants/ParentalControlsContext.tsx` - 10 замен
- `constants/AuthContext.tsx` - 5 замен
- `app/_layout.tsx` - 4 замены
- `app/chat/[chatId].tsx` - 6 замен
- И другие...

**Паттерн замены:**
```typescript
// ❌ Было:
console.error('[Context] Error:', error);
console.log('[Context] Info:', data);

// ✅ Станет:
logger.error('Error occurred', error instanceof Error ? error : new Error(String(error)), { context: 'Context' });
logger.info('Info message', { context: 'Context', data });
```

---

### ✅ Задача 2: Централизовать обработку ошибок (1-1.5 часа)

**Что делать:**
1. [ ] Создать `utils/errorHandler.ts` с функциями:
   - `showUserFriendlyError()`
   - `handleErrorSilently()`
   - `withErrorHandling()`
   - `createErrorHandler()`
2. [ ] Интегрировать в `MonitoringContext.tsx`
3. [ ] Интегрировать в `ParentalControlsContext.tsx`
4. [ ] Интегрировать в `UserContext.tsx`
5. [ ] Добавить пользовательские сообщения об ошибках
6. [ ] Протестировать обработку ошибок

**Структура `errorHandler.ts`:**
```typescript
// Пользовательские сообщения для разных ошибок
const ERROR_MESSAGES = {
  'Network error': 'Проблема с интернетом. Проверьте соединение.',
  'Sync failed': 'Не удалось синхронизировать данные.',
  // ... и т.д.
};

export async function showUserFriendlyError(...) { }
export function handleErrorSilently(...) { }
export function withErrorHandling(...) { }
```

---

## 📋 Быстрая проверка после работы

### После завершения:

1. [ ] `bun run lint` - должно быть 0 ошибок
2. [ ] `bunx tsc --noEmit` - должно быть 0 ошибок
3. [ ] `bun test` - все тесты должны проходить
4. [ ] Запустить приложение - проверить работу
5. [ ] Проверить логи в консоли - должны быть структурированными

---

## 🎯 Ожидаемый результат

**После выполнения:**
- ✅ Все логи через `logger.*` (структурированные)
- ✅ Централизованная обработка ошибок
- ✅ Пользовательские сообщения об ошибках
- ✅ Оценка качества: **9/10** (было 8.5/10)

---

## 📝 Заметки

**Важно:**
- Сохранять существующую функциональность
- Не ломать работающий код
- Тестировать после каждого изменения

**Если что-то пойдет не так:**
- Откатить изменения через git
- Проверить логи
- Спросить у меня

---

**Готовы начать завтра утром!** 🚀
