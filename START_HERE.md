# 🚀 Быстрый запуск проекта KIKU

## ⚡ Самые простые команды для запуска

### 1️⃣ Проверка установки

```bash
# Проверить, установлен ли Bun
bun --version

# Если Bun не установлен, установите его:
# macOS/Linux:
curl -fsSL https://bun.sh/install | bash

# Или используйте Node.js (если Bun не установлен):
node --version
npm --version
```

---

### 2️⃣ Запуск проекта (Самый простой способ)

#### Вариант A: Если установлен Bun (рекомендуется)

```bash
# 1. Перейти в директорию проекта (если еще не там)
cd ~/Desktop/rork-kiku

# 2. Установить зависимости
bun install

# 3. Запустить проект
bun run start
```

#### Вариант B: Если Bun не установлен (используйте npm/yarn)

```bash
# 1. Перейти в директорию проекта
cd ~/Desktop/rork-kiku

# 2. Установить зависимости
npm install
# или
yarn install

# 3. Запустить проект
npx expo start
# или
npm start
```

---

### 3️⃣ Что должно произойти

После команды `bun run start` или `npx expo start`:

1. ✅ Запустится сервер разработки
2. ✅ Появится QR код в терминале
3. ✅ Можно будет:
   - Отсканировать QR код в приложении Expo Go (iOS/Android)
   - Нажать `w` для запуска в браузере
   - Нажать `i` для iOS симулятора (если установлен Xcode)
   - Нажать `a` для Android эмулятора (если установлен Android Studio)

---

## 🔧 Решение проблем

### Проблема: "Command not found: bun"

**Решение:**
```bash
# Установить Bun
curl -fsSL https://bun.sh/install | bash

# После установки перезапустите терминал или выполните:
source ~/.bashrc
# или
source ~/.zshrc
```

### Проблема: "Command not found: node"

**Решение:**
```bash
# Установить Node.js через Homebrew (macOS)
brew install node

# Или скачать с официального сайта:
# https://nodejs.org/
```

### Проблема: "Permission denied"

**Решение:**
```bash
# Проверить права доступа
ls -la

# Если нужно, дать права на выполнение скриптам:
chmod +x scripts/*.sh
```

### Проблема: "Port already in use"

**Решение:**
```bash
# Найти процесс, использующий порт (обычно 8081)
lsof -ti:8081

# Остановить процесс (замените PID на номер процесса)
kill -9 PID

# Или просто использовать другой порт:
PORT=3000 bun run start
```

### Проблема: "Module not found"

**Решение:**
```bash
# Удалить node_modules и установить заново
rm -rf node_modules
bun install
# или
npm install
```

---

## 📱 Установка Expo Go

Для тестирования на реальном устройстве:

- **iOS:** [App Store - Expo Go](https://apps.apple.com/app/expo-go/id982107779)
- **Android:** [Google Play - Expo Go](https://play.google.com/store/apps/details?id=host.exp.exponent)

---

## 🎯 Пошаговая инструкция для первого запуска

1. **Откройте терминал** (Terminal.app на macOS)

2. **Перейдите в директорию проекта:**
   ```bash
   cd ~/Desktop/rork-kiku
   ```

3. **Проверьте установку:**
   ```bash
   bun --version
   ```
   Если ошибка - установите Bun (см. выше)

4. **Установите зависимости:**
   ```bash
   bun install
   ```
   Это может занять 1-2 минуты при первом запуске

5. **Запустите проект:**
   ```bash
   bun run start
   ```

6. **Дождитесь появления QR кода** в терминале

7. **Отсканируйте QR код** в приложении Expo Go на телефоне

---

## 💡 Полезные команды

```bash
# Просмотр всех доступных команд
cat package.json | grep '"scripts"'

# Запуск в браузере
bun run start-web

# Запуск с очисткой кеша
bun run start -- --clear

# Проверка TypeScript ошибок
bunx tsc --noEmit

# Линтинг кода
bun run lint
```

---

## 🆘 Если ничего не работает

1. **Проверьте версии:**
   ```bash
   bun --version  # должно быть 1.0+
   node --version  # должно быть 18+
   ```

2. **Проверьте директорию:**
   ```bash
   pwd  # должна быть /Users/mac/Desktop/rork-kiku
   ls -la  # должны быть файлы package.json, app.json
   ```

3. **Очистите и переустановите:**
   ```bash
   rm -rf node_modules
   rm -rf .expo
   bun install
   ```

4. **Проверьте логи:**
   - Все ошибки будут показаны в терминале
   - Скопируйте текст ошибки для диагностики

---

## 📞 Нужна помощь?

- 📧 Email: g_mikheil@icloud.com
- 📖 Полная документация: QUICKSTART.md
- 🚀 Развертывание: DEPLOYMENT_GUIDE.md

---

**Попробуйте запустить сейчас и напишите, если возникнут проблемы! 🚀**

