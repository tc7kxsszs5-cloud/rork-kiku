# Rork-Kiku

## О проекте
Kids — безопасный и развивающий мессенджер специально для детей. Этот проект направлен на создание уникальной цифровой среды, которая способствует общению, обучению и развитию детей, защищая их от негативного влияния цифрового мира.

Проект предлагает возможность:
- Общаться в текстовых, голосовых и видеочатах.
- Делиться медиа-контентом с использованием ИИ-фильтрации.
- Участвовать в развивающих и образовательных активностях.

## Цели проекта
- **Обеспечение безопасности**: Гарантия защиты детей от токсичного контента, травли и угроз.
- **Развитие детей**: Вовлечение в творческие и образовательные проекты, развитие IT-навыков.
- **Комфортная образовательная среда**: Создание пространства для взаимодействия детей, родителей и педагогов.
- **Интеграция с будущим**: Формирование навыков, необходимых для цифрового века.

## Целевая аудитория
- Дети от 7 до 16 лет.
- Родители, стремящиеся создать безопасную цифровую среду для своих детей.
- Педагоги, использующие инновационные методы обучения.

## Ключевые особенности
- ✅ **Реализовано**: Real-time чат с WebSocket для мгновенного обмена сообщениями
- ✅ **Реализовано**: ИИ-фильтрация контента с автоматической блокировкой опасных сообщений
- ✅ **Реализовано**: Система хранения истории сообщений
- ✅ **Реализовано**: Документация API и архитектуры
- ✅ **Реализовано**: Комплексные тесты (unit и integration)
- 🚧 Возможность участвовать в индивидуальных и групповых проектах
- 🚧 Уникальные мини-игры и упражнения для развития
- 🚧 Простой и дружелюбный интерфейс, оптимизированный для детей

## Структура репозитория
- **backend/**: Серверная логика и обработка данных
  - `services/`: Основные сервисы (WebSocket, фильтрация контента, хранение сообщений)
  - `trpc/`: Type-safe API endpoints
  - `tests/`: Unit и integration тесты
  - `examples/`: Примеры использования
- **frontend/**: Пользовательский интерфейс (React Native + Expo)
- **docs/**: Документация проекта
  - `API_DOCUMENTATION.md`: Полное описание API
  - `ARCHITECTURE.md`: Архитектура системы
  - `SECURITY.md`: Меры безопасности и best practices
- **constants/**: Общие типы и константы
- **tests/**: Автоматизированные тесты

## Установка и запуск

### Требования
- Node.js 18+
- npm или bun

### Установка зависимостей

```bash
# Установка пакетов
npm install --legacy-peer-deps

# Или с bun
bun install
```

### Запуск backend сервера

```bash
# Запуск сервера разработки
npm run start:backend

# Сервер будет доступен на:
# HTTP API: http://localhost:3000
# WebSocket: ws://localhost:3000/ws
# tRPC: http://localhost:3000/api/trpc
```

### Запуск frontend приложения

```bash
# Запуск Expo приложения
npm start

# Или для web версии
npm run start-web
```

### Тестирование WebSocket

```bash
# Запустите backend сервер в одном терминале
npm run start:backend

# В другом терминале запустите example клиент
npm run example:client
```

## Основные технологии

### Backend
- **Hono**: Быстрый и легкий web framework
- **tRPC**: Type-safe API с автоматической генерацией типов
- **WebSocket (ws)**: Real-time коммуникация
- **TypeScript**: Строгая типизация для надежности кода
- **Zod**: Валидация схем данных

### Frontend
- **React Native**: Кросс-платформенная разработка
- **Expo**: Инструменты для быстрой разработки
- **TypeScript**: Типизация на клиенте

## Функциональность Backend

### 1. Real-time Messaging (WebSocket)

```typescript
// Подключение к WebSocket
const ws = new WebSocket('ws://localhost:3000/ws');

// Аутентификация
ws.send(JSON.stringify({
  type: 'auth',
  payload: { userId: 'user123' }
}));

// Отправка сообщения
ws.send(JSON.stringify({
  type: 'message',
  payload: {
    chatId: 'chat123',
    text: 'Привет!',
    senderId: 'user123',
    senderName: 'Алиса'
  }
}));
```

### 2. AI Content Filtering

Автоматическая фильтрация контента по категориям:
- 🚫 Буллинг и домогательства
- 🚫 Нецензурная лексика
- 🚫 Насилие и угрозы
- 🚫 Хищническое поведение
- 🚫 Личная информация
- 🚫 Неуместный контент

**Уровни риска:**
- `safe`: Безопасный контент ✅
- `low`: Незначительные проблемы ⚠️
- `medium`: Средний риск, уведомление родителей 🔔
- `high`: Высокий риск, **сообщение блокируется** 🚫
- `critical`: Критический риск, **сообщение блокируется** 🚫🔴

### 3. tRPC API

```typescript
// Получить чаты пользователя
const chats = await trpc.chat.getUserChats.query({ 
  userId: 'user123' 
});

// Получить историю сообщений
const messages = await trpc.chat.getChatMessages.query({
  chatId: 'chat123',
  limit: 50
});

// Создать новый чат
const chat = await trpc.chat.createChat.mutate({
  chatId: 'chat123',
  participants: ['user1', 'user2'],
  participantNames: ['Алиса', 'Боб']
});
```

## Документация

- 📖 [API Documentation](docs/API_DOCUMENTATION.md) - Полное описание API с примерами
- 🏗️ [Architecture](docs/ARCHITECTURE.md) - Архитектура системы и дизайн
- 🔒 [Security](docs/SECURITY.md) - Меры безопасности и рекомендации
- 📝 [Backend README](backend/README.md) - Документация backend

## Тестирование

```bash
# Запуск всех тестов
npm test

# Запуск конкретного теста
npm test -- content-filter.test.ts

# Запуск с coverage
npm test -- --coverage
```

### Покрытие тестами
- ✅ Unit тесты для content filter
- ✅ Unit тесты для message store
- ✅ Integration тесты для WebSocket
- ✅ End-to-end тесты потока сообщений

## Безопасность

### Текущие меры безопасности
- ✅ Аутентификация на уровне WebSocket
- ✅ Проверка отправителя для всех сообщений
- ✅ Авторизация участников чата
- ✅ AI-фильтрация контента в реальном времени
- ✅ Автоматическая блокировка опасного контента
- ✅ Изоляция данных пользователей
- ✅ Audit trail всех сообщений

### Рекомендации для production
- 🔒 JWT аутентификация
- 🔒 HTTPS/WSS (TLS encryption)
- 🔒 Rate limiting
- 🔒 Шифрование данных
- 🔒 Интеграция с базой данных
- 🔒 COPPA compliance
- 🔒 Родительский контроль

Подробнее в [docs/SECURITY.md](docs/SECURITY.md)

## Конфигурация

Скопируйте `.env.example` в `.env` и настройте переменные окружения:

```bash
cp .env.example .env
```

Основные переменные:
- `PORT`: Порт сервера (default: 3000)
- `NODE_ENV`: development/production
- `OPENAI_API_KEY`: API ключ OpenAI (опционально)
- `JWT_SECRET`: Секрет для JWT токенов

## Разработка

### Добавление новых фильтров контента

См. `backend/services/content-filter.ts`

### Добавление новых API endpoints

См. `backend/trpc/routes/chat/`

### Интеграция с базой данных

См. рекомендации в `docs/ARCHITECTURE.md`

## Roadmap

### Фаза 1: Основная функциональность ✅
- [x] WebSocket сервер
- [x] AI фильтрация контента
- [x] Хранение сообщений
- [x] tRPC API
- [x] Тесты
- [x] Документация

### Фаза 2: Расширенные функции
- [ ] Интеграция с базой данных
- [ ] JWT аутентификация
- [ ] Фильтрация изображений
- [ ] Голосовые сообщения
- [ ] Видео чат

### Фаза 3: Compliance & Safety
- [ ] COPPA compliance
- [ ] Родительская панель управления
- [ ] Система репортов
- [ ] Верификация возраста

## Вклад в проект

Приветствуются все желающие внести вклад! 

1. Fork репозитория
2. Создайте feature branch (`git checkout -b feature/amazing-feature`)
3. Commit изменения (`git commit -m 'Add amazing feature'`)
4. Push в branch (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## Лицензия

См. LICENSE файл для деталей.

## Контакты

Для вопросов и предложений создавайте issues в репозитории.

---

**Важно**: Текущая версия использует in-memory хранилище для разработки. Для production необходимо внедрить постоянное хранилище данных.