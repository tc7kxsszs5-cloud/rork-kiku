# 📋 Отчёт: полноценное тестирование проекта KIKU

**Дата:** 31 января 2026  
**Репозиторий:** `/Users/mac/Desktop/rork-kiku`

---

## 🎯 Что входит в полноценное тестирование

1. **TypeScript** — проверка типов  
2. **Lint** — проверка стиля и правил  
3. **Unit-тесты (Jest)** — тесты компонентов, контекстов, утилит  
4. **E2E-тесты (Playwright)** — тесты сценариев в браузере  
5. **Запуск приложения** — проверка, что веб и старт работают  

---

## 1️⃣ TypeScript (`bun run typecheck`)

**Статус:** ❌ Есть ошибки

**Ошибки (15):**

| Файл | Строка | Описание |
|------|--------|----------|
| `app/+not-found.tsx` | 12 | Тип `"/"` не подходит для `router.replace` |
| `app/(tabs)/alerts.tsx` | 7 | Тип `"/security-settings"` не подходит для `router.push` |
| `app/(tabs)/profile.tsx` | 88, 96 | Типы `"/security-settings"`, `"/ai-recommendations"` |
| `app/register-child.tsx` | 108, 223 | Тип `"/(tabs)"` не подходит для `router.push` |
| `app/register-parent.tsx` | 135 | Тип `"/(tabs)"` не подходит для `router.push` |
| `__tests__/unit/screens/MonitoringScreen.test.tsx` | 219, 249, 268, 297, 358 | `string` вместо `ComponentType` |
| `__tests__/unit/screens/SecuritySettingsScreen.test.tsx` | 7 | Нет экспорта `getByText`, `getByTestId` |
| `__tests__/unit/utils/syncService.test.ts` | 190 | Свойство `contentFiltering` не в типе `ParentalSettings` |

**Команда:** `bun run typecheck`

---

## 2️⃣ Lint (`bun run lint`)

**Статус:** ✅ Без ошибок, есть предупреждения

- **Ошибки:** 0  
- **Предупреждения:** 12 (импорты не в начале файла в `app/chat/[chatId].tsx`)

**Команда:** `bun run lint`  
**Автоисправление:** `bun run lint:fix` или `expo lint --fix`

---

## 3️⃣ Unit-тесты (Jest)

**Команда:** `bun run test:unit`

**Структура тестов:**
- `__tests__/unit/utils/` — утилиты (14+ файлов)
- `__tests__/unit/contexts/` — контексты (6 файлов)
- `__tests__/unit/components/` — компоненты (10 файлов)
- `__tests__/unit/screens/` — экраны (3 файла)

**Типичные проблемы:**
- Часть тестов падает из‑за моков (React Native, Expo)
- NotificationsContext — нет projectId, deviceId
- Некоторые тесты ожидают реальные API/модули

**Ожидаемый результат (по предыдущим прогонам):** ~442 passed, ~201 failed, 643 total (~69%)

**Дополнительные команды:**
- `bun run test:unit -- --testPathPattern="utils"` — только утилиты
- `bun run test:coverage` — с покрытием

---

## 4️⃣ E2E-тесты (Playwright)

**Команда:** `bunx playwright test`  
**Список тестов:** `bunx playwright test --list`

**Файлы и сценарии:**
- `01-onboarding.spec.ts` — онбординг, выбор роли (4 теста)
- `02-chat-flow.spec.ts` — чат, модерация (4 теста)
- `03-sos-alerts.spec.ts` — SOS, алерты (4 теста)
- `04-settings-theme.spec.ts` — настройки, тема, язык (4 теста)

**Проекты:** Mobile Chrome, Mobile Safari, Tablet (часть тестов дублируется по проектам).

**Перед запуском:** нужен запущенный веб-сервер (или настроенный `webServer` в `playwright.config.ts`).

**Дополнительно:**
- `bunx playwright test --ui` — с UI
- `bunx playwright test --headed` — с видимым браузером

---

## 5️⃣ Запуск приложения

**Веб:** `bun run start:web` (порт 8082)  
**Обычный старт:** `bun run start`

Проверка: открыть http://localhost:8082 и убедиться, что приложение загружается.

---

## 📊 Сводка по проверкам

| Проверка | Команда | Статус | Примечание |
|----------|---------|--------|------------|
| TypeScript | `bun run typecheck` | ❌ 15 ошибок | app + тесты |
| Lint | `bun run lint` | ⚠️ 12 предупреждений | только стиль |
| Unit-тесты | `bun run test:unit` | ⚠️ ~69% проходят | часть падает из‑за моков |
| E2E | `bunx playwright test` | ✅ тесты есть | нужен запущенный веб |
| Веб-старт | `bun run start:web` | ✅ | порт 8082 |

---

## 🚀 Как прогнать всё подряд (вручную)

```bash
cd /Users/mac/Desktop/rork-kiku

# 1. Типы
bun run typecheck

# 2. Стиль
bun run lint

# 3. Unit-тесты (долго, 2–3 минуты)
bun run test:unit

# 4. E2E (нужен веб: в другом терминале — bun run start:web)
bunx playwright test
```

---

## 📁 Файлы конфигурации тестов

- `jest.config.js` — Jest (unit)
- `jest.setup.js`, `jest.setup.before.js` — моки и окружение Jest
- `playwright.config.ts` — Playwright (E2E)
- `tsconfig.json` — TypeScript

---

## ✅ Итог

- **Полноценное тестирование** = typecheck + lint + unit + E2E + проверка запуска.
- Сейчас: typecheck и часть unit-тестов требуют доработки; lint и E2E-набор в порядке.
- Для ежедневной проверки достаточно: `typecheck` → `lint` → `test:unit`; E2E — по необходимости при изменении сценариев.

**Дата отчёта:** 31.01.2026
