# Быстрое решение проблем

## ⚡ Быстрое исправление (1 команда)

```bash
bun run fix
```

Это автоматически:
- Остановит процессы на портах
- Очистит кэш
- Проверит зависимости
- Проверит конфигурацию

## 🚀 Затем запустите:

```bash
bun run start:web
```

## 🔍 Если все еще не работает:

### 1. Откройте консоль браузера (F12)
   - Найдите ошибки (красным)
   - Скопируйте текст ошибки

### 2. Проверьте тесты:
```bash
bun run test:all-config
```

### 3. Очистите все вручную:
```bash
# Остановите приложение (Ctrl+C)
# Убейте процессы на портах
lsof -ti:8081 | xargs kill -9
lsof -ti:8082 | xargs kill -9

# Очистите кэш
rm -rf .expo node_modules/.cache

# Переустановите зависимости (если нужно)
bun install

# Запустите заново
bun run start:web
```

## ✅ Что уже исправлено:

- ✅ Все файлы вкладок созданы (achievements, lessons)
- ✅ Обработка ошибок во всех экранах
- ✅ Обработка ошибок в TabLayout
- ✅ Все тесты проходят
- ✅ Все провайдеры на месте

## 📱 Проверка работы:

1. **Запустите:** `bun run start:web`
2. **Откройте:** `http://localhost:8082` (или порт из консоли)
3. **Проверьте:**
   - Открывается ли приложение
   - Работают ли вкладки
   - Нет ли ошибок в консоли (F12)

## 🆘 Если нужна помощь:

Опишите:
1. Что именно не работает? (не открывается / ошибка при открытии вкладки / белый экран)
2. Какая ошибка в консоли браузера? (F12 → Console)
3. Что показывает `bun run test:all-config`?
