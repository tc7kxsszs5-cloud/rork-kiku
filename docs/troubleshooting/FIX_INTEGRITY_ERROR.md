# 🔧 Исправление ошибки Integrity check failed

## Ошибка
```
error: Integrity check failed for tarball: @babel/generator
```

## Причина
Поврежденный кеш пакетов или неполная загрузка.

## ✅ Решение

### Шаг 1: Полная очистка кеша

```bash
cd /Users/mac/Desktop/rork-kiku

# Остановить все процессы
killall node

# Удалить node_modules
rm -rf node_modules

# Очистить ВСЕ кеши bun
bun pm cache rm
rm -rf ~/.bun/install/cache
rm -rf ~/.bun/install/global

# Очистить кеши npm (на всякий случай)
npm cache clean --force 2>/dev/null || true
```

### Шаг 2: Переустановка

```bash
# Переустановить с принудительной очисткой
bun install --force
```

### Шаг 3: Если все еще ошибка

**Удалить конкретный проблемный пакет:**

```bash
# Удалить @babel из кеша и node_modules
rm -rf ~/.bun/install/cache/@babel
rm -rf node_modules/@babel 2>/dev/null || true

# Переустановить
bun install --force
```

---

## 🔄 Альтернатива: Использовать npm

Если bun продолжает давать ошибки:

```bash
cd /Users/mac/Desktop/rork-kiku

# Удалить все
rm -rf node_modules bun.lockb

# Очистить кеш npm
npm cache clean --force

# Установить через npm
npm install
```

---

## ✅ После успешной установки

```bash
# Запустить приложение
bun run start:expo

# Или для симулятора:
bunx expo start --ios
```

---

## 💡 Советы

1. **Убедитесь в стабильном интернет-соединении** перед установкой
2. Если ошибка повторяется - попробуйте **другую сеть** (может быть проблема с прокси/файрволом)
3. Рассмотрите использование **VPN**, если есть проблемы с доступом к npm registry
