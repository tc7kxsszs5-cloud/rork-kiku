# 🔧 Нейтрализация ошибки ngrok tunnel timeout

## Проблема

```
CommandError: ngrok tunnel took too long to connect.
error: script "start:rork:tunnel" exited with code 1
```

## 🔍 Причины ошибки

1. **Медленное интернет-соединение** - ngrok не успевает подключиться
2. **Проблемы с ngrok сервисом** - временные сбои на стороне ngrok
3. **Брандмауэр/прокси** - блокируют подключение к ngrok
4. **Лимиты ngrok** - бесплатный план имеет ограничения
5. **Таймаут слишком короткий** - по умолчанию ~30 секунд

---

## ✅ Решения (от простого к сложному)

### 🎯 Решение 1: Использовать LAN вместо Tunnel (РЕКОМЕНДУЕТСЯ)

**Самый простой и надежный способ** - не использовать tunnel вообще.

```bash
# Вместо tunnel используйте LAN
bun run start              # Умный запуск (LAN по умолчанию)
bun run start:rork:lan     # Явный LAN режим
bun run start:simple       # Простой Expo запуск
```

**Преимущества:**
- ✅ Работает мгновенно
- ✅ Не зависит от интернета
- ✅ Более стабильно
- ✅ Быстрее для локальной разработки

**Требования:**
- Устройство и компьютер должны быть в одной Wi-Fi сети

---

### 🎯 Решение 2: Увеличить таймаут для tunnel

Создайте скрипт с увеличенным таймаутом:

```bash
# Создайте файл: scripts/start-tunnel-with-timeout.sh
#!/bin/bash
export EXPO_TUNNEL_TIMEOUT=60000  # 60 секунд вместо 30
bunx rork start -p d8v7u672uumlfpscvnbps --tunnel --scheme rork-app
```

**Или через переменную окружения:**

```bash
EXPO_TUNNEL_TIMEOUT=60000 bun run start:rork:tunnel
```

---

### 🎯 Решение 3: Использовать собственный ngrok аккаунт

Если нужен tunnel, используйте свой ngrok аккаунт:

1. **Зарегистрируйтесь на ngrok.com** (бесплатно)
2. **Получите authtoken:**
   ```bash
   ngrok config add-authtoken YOUR_TOKEN
   ```
3. **Используйте ngrok напрямую:**
   ```bash
   # В одном терминале
   ngrok http 8081
   
   # В другом терминале
   bunx expo start --lan
   ```

---

### 🎯 Решение 4: Автоматический fallback на LAN

Создайте скрипт, который автоматически переключается на LAN при ошибке tunnel:

```bash
#!/bin/bash
# scripts/start-with-fallback.sh

echo "🌐 Попытка запуска с tunnel..."
bunx rork start -p d8v7u672uumlfpscvnbps --tunnel --scheme rork-app 2>&1 | tee /tmp/tunnel.log &

TUNNEL_PID=$!
sleep 10

if ! kill -0 $TUNNEL_PID 2>/dev/null; then
    echo "❌ Tunnel не подключился, переключаюсь на LAN..."
    kill $TUNNEL_PID 2>/dev/null
    bunx rork start -p d8v7u672uumlfpscvnbps --lan --scheme rork-app
else
    wait $TUNNEL_PID
fi
```

---

### 🎯 Решение 5: Использовать Expo Dev Tools вместо Rork

Если Rork tunnel не работает, используйте стандартный Expo:

```bash
# Установите Expo CLI (если еще не установлен)
npm install -g expo-cli

# Запустите без tunnel
npx expo start --lan

# Или с tunnel (если нужен)
npx expo start --tunnel
```

---

### 🎯 Решение 6: Проверить и исправить сетевые проблемы

```bash
# 1. Проверить интернет-соединение
ping -c 3 8.8.8.8

# 2. Проверить доступность ngrok
curl -I https://ngrok.com

# 3. Проверить DNS
nslookup ngrok.com

# 4. Проверить прокси/брандмауэр
echo $HTTP_PROXY
echo $HTTPS_PROXY
```

**Если используется прокси:**
```bash
# Отключить прокси для ngrok
unset HTTP_PROXY
unset HTTPS_PROXY
bun run start:rork:tunnel
```

---

### 🎯 Решение 7: Очистить кэш и переустановить ngrok

```bash
# Очистить кэш ngrok
rm -rf ~/.ngrok
rm -rf node_modules/@expo/ngrok*

# Переустановить зависимости
bun install

# Попробовать снова
bun run start:rork:tunnel
```

---

### 🎯 Решение 8: Использовать альтернативные туннели

Если ngrok не работает, можно использовать другие сервисы:

#### Cloudflare Tunnel (бесплатно)
```bash
# Установить cloudflared
brew install cloudflared

# Запустить туннель
cloudflared tunnel --url http://localhost:8081
```

#### LocalTunnel (npm пакет)
```bash
# Установить
npm install -g localtunnel

# Запустить
lt --port 8081
```

---

## 🛠️ Реализация в проекте

### Вариант A: Обновить package.json

```json
{
  "scripts": {
    "start": "./scripts/smart-start.sh",
    "start:tunnel": "EXPO_TUNNEL_TIMEOUT=60000 bunx rork start -p d8v7u672uumlfpscvnbps --tunnel --scheme rork-app || bunx rork start -p d8v7u672uumlfpscvnbps --lan --scheme rork-app",
    "start:lan": "bunx rork start -p d8v7u672uumlfpscvnbps --lan --scheme rork-app"
  }
}
```

### Вариант B: Создать умный скрипт с retry

```bash
#!/bin/bash
# scripts/start-tunnel-smart.sh

MAX_RETRIES=3
RETRY_DELAY=5

for i in $(seq 1 $MAX_RETRIES); do
    echo "Попытка $i из $MAX_RETRIES..."
    
    if timeout 45 bunx rork start -p d8v7u672uumlfpscvnbps --tunnel --scheme rork-app; then
        exit 0
    fi
    
    if [ $i -lt $MAX_RETRIES ]; then
        echo "Ожидание $RETRY_DELAY секунд перед повтором..."
        sleep $RETRY_DELAY
    fi
done

echo "❌ Tunnel не подключился после $MAX_RETRIES попыток"
echo "🔄 Переключаюсь на LAN режим..."
bunx rork start -p d8v7u672uumlfpscvnbps --lan --scheme rork-app
```

---

## 📊 Сравнение решений

| Решение | Сложность | Надежность | Скорость | Рекомендация |
|---------|-----------|------------|----------|--------------|
| LAN вместо tunnel | ⭐ Легко | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ✅ **Лучший выбор** |
| Увеличить таймаут | ⭐⭐ Средне | ⭐⭐⭐ | ⭐⭐⭐ | ✅ Если нужен tunnel |
| Свой ngrok аккаунт | ⭐⭐⭐ Сложно | ⭐⭐⭐⭐ | ⭐⭐⭐ | ✅ Для production |
| Автоматический fallback | ⭐⭐ Средне | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ✅ Универсальный |
| Expo вместо Rork | ⭐ Легко | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ✅ Альтернатива |
| Исправить сеть | ⭐⭐⭐ Сложно | ⭐⭐⭐ | ⭐⭐⭐ | ⚠️ Если проблема в сети |
| Очистить кэш | ⭐ Легко | ⭐⭐ | ⭐⭐⭐ | ✅ Первая попытка |
| Альтернативные туннели | ⭐⭐⭐ Сложно | ⭐⭐⭐ | ⭐⭐⭐ | ⚠️ Если ngrok не работает |

---

## 🎯 Рекомендуемый подход

**Для разработки:**
```bash
# Всегда используйте LAN
bun run start
```

**Если tunnel действительно нужен:**
```bash
# 1. Попробуйте с увеличенным таймаутом
EXPO_TUNNEL_TIMEOUT=60000 bun run start:rork:tunnel

# 2. Если не работает - используйте свой ngrok аккаунт
ngrok http 8081

# 3. Если и это не работает - используйте альтернативу
cloudflared tunnel --url http://localhost:8081
```

---

## 🔍 Диагностика проблемы

Если нужно понять, почему tunnel не работает:

```bash
# 1. Проверить логи ngrok
DEBUG=* bunx rork start -p d8v7u672uumlfpscvnbps --tunnel --scheme rork-app

# 2. Проверить сетевые подключения
netstat -an | grep 8081

# 3. Проверить процессы ngrok
ps aux | grep ngrok

# 4. Проверить доступность портов
lsof -i :8081
```

---

## ✅ Чеклист решения проблемы

- [ ] Попробовал LAN режим (`bun run start`)
- [ ] Увеличил таймаут для tunnel
- [ ] Проверил интернет-соединение
- [ ] Проверил прокси/брандмауэр
- [ ] Очистил кэш ngrok
- [ ] Попробовал свой ngrok аккаунт
- [ ] Использовал альтернативный туннель
- [ ] Проверил логи для диагностики

---

**Главный вывод:** Для локальной разработки **всегда используйте LAN режим** - он быстрее, надежнее и не зависит от внешних сервисов! 🚀
