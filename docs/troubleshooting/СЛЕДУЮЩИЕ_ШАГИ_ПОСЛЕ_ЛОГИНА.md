# ✅ Следующие шаги после входа в Vercel

## 🎉 После успешной аутентификации:

### 1. Проверьте вход:

```bash
bunx vercel whoami
```

Должно показать ваш email.

---

### 2. Запустите автоматический деплой:

```bash
./auto-deploy.sh
```

Этот скрипт:
- ✅ Проверит Vercel CLI
- ✅ Выполнит деплой на Vercel
- ✅ Покажет URL вашего backend

---

### 3. Настройте Environment Variables:

```bash
./auto-setup.sh
```

Этот скрипт:
- ✅ Запросит Connection String из Supabase
- ✅ Добавит DATABASE_URL в Vercel
- ✅ Добавит NODE_ENV в Vercel

---

### 4. Получите Connection String из Supabase:

1. Идите на https://supabase.com/dashboard
2. Выберите ваш проект
3. Settings → Database
4. Найдите "Connection string" (любой режим: Session, Transaction или Pooling)
5. Скопируйте строку (она начинается с `postgresql://`)

---

### 5. Production деплой:

После настройки переменных:

```bash
bunx vercel --prod
```

---

## ✅ Готово!

После этих шагов ваш backend будет работать на Vercel с подключением к Supabase!
