# ✅ Проверка после передеплоя

## 🎉 Деплой завершен!

Ваш backend передеплоен с новыми переменными окружения.

---

## 📋 Проверьте работу API

### 1. Проверьте корневой endpoint:

Откройте в браузере:
https://backend-three-mauve-67.vercel.app/

**Должно показать:**
```json
{"status": "ok", "message": "API is running"}
```

---

### 2. Если все еще ошибка 500:

Проверьте Function Logs в Vercel Dashboard:

1. Откройте https://vercel.com/dashboard
2. Выберите проект **backend**
3. **Deployments** → выберите последний деплой
4. Нажмите **"Function Logs"** или **"View Function Logs"**
5. Найдите строку с ошибкой
6. Скопируйте текст ошибки

---

## 🔍 Возможные причины ошибки 500

1. **Проблемы с подключением к базе данных**
   - Проверьте Connection String
   - Убедитесь, что используется правильный пароль
   - Попробуйте Connection Pooling (порт 6543)

2. **Ошибки в коде при инициализации**
   - Проверьте логи функции
   - Убедитесь, что все импорты правильные

3. **Отсутствие зависимостей**
   - Проверьте, что все зависимости в `package.json`

---

## ✅ Если API работает

После успешной проверки:

1. ✅ Обновите URL в `lib/trpc.ts`:
   ```typescript
   const PROJECT_BASE_URL = 'https://backend-three-mauve-67.vercel.app';
   ```

2. ✅ Протестируйте приложение локально

3. ✅ Закоммитьте изменения

---

## ⚠️ Предупреждение

Предупреждение о `builds` в конфигурации - это нормально. Это означает, что настройки из `vercel.json` имеют приоритет над настройками в Dashboard. Это правильно для нашего проекта.

---

## ✅ Готово!

Проверьте работу API и сообщите результат!
