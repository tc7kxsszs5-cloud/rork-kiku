# 🔧 Исправление ошибки симулятора

## Ошибка
```
Unable to boot device because we cannot determine the runtime bundle.
xcrun simctl boot B0B350CC-0582-45DA-B08A-CF7B01901A00 exited with non-zero code: 2
```

## Причина
Симулятор "iPhone 14 Fixed" поврежден или имеет проблемы с runtime bundle.

## ✅ Решение

### Вариант 1: Удалить проблемный симулятор и использовать другой

```bash
# Удалить проблемный симулятор
xcrun simctl delete B0B350CC-0582-45DA-B08A-CF7B01901A00

# Запустить с другим симулятором (например, обычный iPhone 14)
cd /Users/mac/Desktop/rork-kiku
npx expo start --ios --simulator="iPhone 14"
```

### Вариант 2: Выбрать другой симулятор вручную

```bash
# Посмотреть доступные симуляторы
xcrun simctl list devices available

# Запустить с конкретным симулятором (выберите любой рабочий)
cd /Users/mac/Desktop/rork-kiku
npx expo start --ios --simulator="iPhone 14 Pro"
```

### Вариант 3: Открыть симулятор вручную и запустить

```bash
# Открыть симулятор вручную
open -a Simulator

# В симуляторе: File -> Open Simulator -> выберите iPhone 14 (не Fixed)

# Затем запустить Expo
cd /Users/mac/Desktop/rork-kiku
npx expo start
# Нажмите 'i' в терминале
```

### Вариант 4: Пересоздать симулятор (если нужен именно этот)

```bash
# Удалить проблемный симулятор
xcrun simctl delete B0B350CC-0582-45DA-B08A-CF7B01901A00

# Посмотреть доступные устройства и runtime
xcrun simctl list devicetypes | grep "iPhone 14"
xcrun simctl list runtimes

# Создать новый симулятор (замените runtime на доступный)
xcrun simctl create "iPhone 14 New" "iPhone 14" "iOS-16-0"  # или другой доступный runtime
```

---

## 🎯 Рекомендуемое решение

**Самый простой способ:**

```bash
# 1. Удалить проблемный симулятор
xcrun simctl delete B0B350CC-0582-45DA-B08A-CF7B01901A00

# 2. Запустить с обычным iPhone 14
cd /Users/mac/Desktop/rork-kiku
npx expo start --ios --simulator="iPhone 14"
```

---

## 📋 Проверить доступные симуляторы

```bash
# Посмотреть все доступные симуляторы
xcrun simctl list devices available

# Посмотреть все устройства (включая недоступные)
xcrun simctl list devices
```

---

## ✅ После исправления

После выбора рабочего симулятора, приложение должно запуститься нормально.
