# 🔍 Полная диагностика и исправление ошибок

## 🔧 Систематическая проверка всех проблем

---

## ✅ ШАГ 1: Проверка зависимостей

### Проблема: Может не хватать зависимостей

Проверьте `backend/package.json` - должны быть:
- ✅ `hono`
- ✅ `@hono/trpc-server`
- ✅ `@trpc/server`
- ✅ `superjson`
- ✅ `zod` (если используется)

---

## ✅ ШАГ 2: Проверка импортов

### Проблема: Абсолютные пути не работают в Vercel

**Исправлено:**
- ✅ `@/backend/trpc/create-context` → `../create-context`
- ✅ `@/utils/syncHelpers` → `../../../utils/syncHelpers`
- ✅ `@/constants/types` → типы определены локально

**Проверьте, что все исправлено:**
```bash
cd backend
grep -r "@/backend\|@/utils\|@/constants" trpc/ --include="*.ts"
```

Должно быть пусто (0 результатов).

---

## ✅ ШАГ 3: Проверка структуры файлов

### Должны существовать:

- ✅ `backend/index.ts`
- ✅ `backend/hono.ts`
- ✅ `backend/vercel.json`
- ✅ `backend/package.json`
- ✅ `backend/tsconfig.json`
- ✅ `backend/utils/syncHelpers.ts`
- ✅ `backend/trpc/create-context.ts`
- ✅ `backend/trpc/app-router.ts`

---

## ✅ ШАГ 4: Проверка конфигурации Vercel

### backend/vercel.json должен быть:

```json
{
  "version": 2
}
```

### backend/index.ts должен быть:

```typescript
import app from './hono';
export default app;
export const handler = app.fetch;
```

---

## ✅ ШАГ 5: Проверка Environment Variables

В Vercel должны быть:
- ✅ `DATABASE_URL` - connection string из Supabase
- ✅ `NODE_ENV` = `production`

---

## 🔧 Если все еще ошибки

### Проверьте логи в Vercel:

1. Vercel Dashboard → ваш проект
2. Deployments → последний deployment
3. **View Function Logs**
4. Скопируйте полный текст ошибки

**Это покажет точную причину!**

---

## 📋 Чеклист исправлений:

- [ ] Все абсолютные пути исправлены на относительные
- [ ] Создан `backend/utils/syncHelpers.ts`
- [ ] Типы определены локально в `store.ts`
- [ ] Все зависимости в `package.json`
- [ ] `vercel.json` правильный
- [ ] `index.ts` экспортирует handler
- [ ] Environment Variables настроены
- [ ] Все файлы закоммичены

---

## 🆘 Если не знаете, в чем проблема

**Напишите:**
1. Какую ошибку видите в Vercel?
2. Что показывают логи (View Function Logs)?
3. На каком шаге застряли?

И я помогу исправить!
