# ⚡ Быстрое решение: ngrok tunnel timeout

## 🎯 Самый простой способ

**Просто не используйте tunnel!**

```bash
# Вместо этого:
bun run start:rork:tunnel  # ❌ Может упасть с ошибкой

# Используйте это:
bun run start              # ✅ Работает всегда (LAN)
bun run start:rork:lan     # ✅ Явный LAN режим
```

---

## 🔧 Если tunnel действительно нужен

### Вариант 1: Умный запуск (автоматический fallback)

```bash
bun run start:rork:tunnel
```

**Что делает:**
- ✅ Пытается подключить tunnel
- ✅ Если не получается - автоматически переключается на LAN
- ✅ Работает всегда!

### Вариант 2: Tunnel с увеличенным таймаутом

```bash
bun run start:rork:tunnel:timeout
```

**Что делает:**
- ✅ Увеличивает таймаут до 60 секунд
- ✅ Дает больше времени для подключения

### Вариант 3: Прямой tunnel (без fallback)

```bash
bun run start:rork:tunnel:direct
```

**Что делает:**
- ⚠️ Прямой запуск tunnel без fallback
- ⚠️ Может упасть с ошибкой

---

## 📊 Сравнение команд

| Команда | Надежность | Скорость | Рекомендация |
|---------|------------|----------|--------------|
| `bun run start` | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ✅ **Лучший выбор** |
| `bun run start:rork:lan` | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ✅ Для явного LAN |
| `bun run start:rork:tunnel` | ⭐⭐⭐⭐ | ⭐⭐⭐ | ✅ Если нужен tunnel |
| `bun run start:rork:tunnel:timeout` | ⭐⭐⭐ | ⭐⭐ | ⚠️ Если медленный интернет |
| `bun run start:rork:tunnel:direct` | ⭐⭐ | ⭐⭐⭐ | ❌ Не рекомендуется |

---

## 💡 Рекомендация

**Для 99% случаев используйте:**
```bash
bun run start
```

Это автоматически использует LAN режим, который:
- ✅ Работает мгновенно
- ✅ Не зависит от интернета
- ✅ Более стабильно
- ✅ Быстрее для локальной разработки

**Tunnel нужен только если:**
- Вы разрабатываете на удаленном сервере
- Устройство не в той же сети
- Нужно протестировать с внешнего IP

---

## 🚨 Если ничего не помогает

1. **Проверьте интернет:**
   ```bash
   ping -c 3 8.8.8.8
   ```

2. **Очистите кэш:**
   ```bash
   bun run fix:startup
   ```

3. **Используйте стандартный Expo:**
   ```bash
   bun run start:expo
   ```

---

**Главное:** Для локальной разработки **всегда используйте LAN** - это самый надежный способ! 🚀
