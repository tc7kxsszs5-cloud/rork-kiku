# 🔧 Исправление проблемы "пригодные данные не найдены"

## 🔍 Проблема

Expo Go на телефоне не может подключиться к проекту, показывает "пригодные данные не найдены".

## ✅ Решения

### Решение 1: Остановить старые процессы и запустить заново

```bash
# 1. Остановить все процессы Expo
pkill -f "expo start"

# 2. Очистить порт 8081
lsof -ti:8081 | xargs kill -9

# 3. Запустить заново
bun run start:local
```

### Решение 2: Использовать другой порт

Если порт 8081 занят, Expo предложит использовать другой порт (например, 8083). Согласись на это.

### Решение 3: Проверить сеть

**Для LAN подключения:**
- Телефон и компьютер должны быть в **одной Wi-Fi сети**
- Проверь что Wi-Fi включен на обоих устройствах
- Убедись что нет VPN, который блокирует локальную сеть

**Для Tunnel подключения:**
```bash
bun run start
# Использует --tunnel, работает через интернет
# Может быть медленнее, но работает из любой сети
```

### Решение 4: Использовать веб-версию для тестирования

```bash
bun run start-web
# Откроется в браузере
# Можно тестировать без телефона
```

### Решение 5: Ввести URL вручную в Expo Go

1. Запусти `bun run start:local`
2. Дождись появления QR кода и URL в терминале
3. В Expo Go на телефоне:
   - Нажми "Enter URL manually" (или похожую кнопку)
   - Введи `exp://[IP_АДРЕС]:8081`
   - Например: `exp://192.168.1.100:8081`

---

## 📋 Пошаговая инструкция

### Шаг 1: Очистить старые процессы

```bash
# Остановить все Expo процессы
pkill -f "expo start"

# Подождать 2 секунды
sleep 2

# Проверить что процессы остановлены
ps aux | grep expo | grep -v grep
# Должно быть пусто
```

### Шаг 2: Запустить проект

```bash
# Для LAN (одна Wi-Fi сеть)
bun run start:local

# ИЛИ для Tunnel (через интернет)
bun run start
```

### Шаг 3: Подключиться через Expo Go

**Вариант A - QR код:**
1. Дождись появления QR кода в терминале
2. Открой Expo Go на телефоне
3. Нажми "Scan QR code"
4. Отсканируй QR код

**Вариант B - Вручную:**
1. Найди в терминале строку типа: `exp://192.168.1.100:8081`
2. Открой Expo Go
3. Нажми "Enter URL manually"
4. Введи этот адрес

---

## 🔍 Диагностика

### Проверить что Expo запущен:

```bash
# Должен показать процесс
ps aux | grep expo | grep -v grep

# Должен показать что порт занят
lsof -i:8081
```

### Проверить сеть:

```bash
# Узнать IP адрес компьютера
ifconfig | grep "inet " | grep -v 127.0.0.1

# Или на Mac:
ipconfig getifaddr en0
```

### Проверить Expo Go:

- Убедись что Expo Go установлен и обновлен
- Проверь что версия Expo Go совместима с версией Expo SDK (54)

---

## ⚠️ Частые проблемы

### Проблема 1: "Port 8081 is running this app in another window"

**Решение:**
```bash
# Остановить процесс на порту 8081
lsof -ti:8081 | xargs kill -9

# Или согласиться использовать другой порт
```

### Проблема 2: "пригодные данные не найдены" в Expo Go

**Причины:**
- Телефон и компьютер в разных сетях (для --lan)
- Брандмауэр блокирует подключение
- VPN блокирует локальную сеть
- Неправильный URL введен вручную

**Решение:**
- Использовать `--tunnel` вместо `--lan`
- Отключить VPN
- Проверить брандмауэр
- Использовать веб-версию для тестирования

### Проблема 3: QR код не сканируется

**Решение:**
- Использовать ручной ввод URL
- Убедиться что QR код четкий
- Попробовать другой способ сканирования

---

## 🚀 Рекомендуемый workflow

### Для разработки:

1. **Остановить старые процессы:**
   ```bash
   pkill -f "expo start"
   ```

2. **Запустить проект:**
   ```bash
   bun run start:local
   ```

3. **Подключиться через Expo Go:**
   - Отсканировать QR код
   - Или ввести URL вручную

### Для тестирования без телефона:

```bash
bun run start-web
# Откроется в браузере
```

---

## 💡 Советы

1. **Всегда используй одну Wi-Fi сеть** для LAN подключения
2. **Если LAN не работает** - используй `--tunnel`
3. **Для быстрого тестирования** - используй веб-версию
4. **Останавливай процессы** перед новым запуском
5. **Проверяй версию Expo Go** - должна быть совместима с SDK 54

---

## ✅ Быстрая команда для перезапуска

```bash
# Остановить и запустить заново
pkill -f "expo start" && sleep 2 && bun run start:local
```

---

**Если ничего не помогает:**
- Используй веб-версию: `bun run start-web`
- Или создай development build через EAS: `eas build --profile development --platform ios`
