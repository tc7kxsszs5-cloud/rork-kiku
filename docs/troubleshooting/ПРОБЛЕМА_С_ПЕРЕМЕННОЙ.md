# ❌ Проблема с переменной SUPABASE_ANON_KEY

## ❌ Проблема:

Переменная `SUPABASE_ANON_KEY` установлена в Vercel, но не применяется к деплою.

## ✅ Решение:

### Вариант 1: Переустановить переменную

```bash
cd /Users/mac/Desktop/rork-kiku/backend

# Удалить старую
bunx vercel env rm SUPABASE_ANON_KEY production
# y

# Добавить новую
bunx vercel env add SUPABASE_ANON_KEY production
# yes (sensitive)
# Вставьте anon public key из Supabase
```

### Вариант 2: Проверить значение переменной

Возможно, переменная установлена с неправильным значением или пустая.

---

## 📋 Получение anon key из Supabase:

1. https://supabase.com/dashboard
2. Settings → API
3. Найдите **"anon public"** key
4. Скопируйте его (длинная строка, начинается с `eyJ...`)

---

## 🚀 После установки:

```bash
bunx vercel --prod
curl "https://backend-three-mauve-67.vercel.app/api/trpc/test.dbCheck"
```

---

**Переустановите SUPABASE_ANON_KEY и задеплойте!**
