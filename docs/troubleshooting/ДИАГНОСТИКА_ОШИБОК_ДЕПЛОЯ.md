# 🔍 Диагностика ошибок деплоя

## ❓ Какая ошибка возникла?

Пожалуйста, скопируйте **полный текст ошибки**, чтобы я мог помочь.

---

## 🔧 Частые ошибки и решения

### 1️⃣ "Cannot find module" или "Module not found"

**Ошибка:**
```
Error: Cannot find module '@hono/trpc-server'
```

**Решение:**
```bash
cd backend
bun install
# или
npm install
```

Проверьте, что `node_modules` существует:
```bash
ls -la backend/node_modules
```

---

### 2️⃣ "TypeError: Cannot read property" или проблемы с импортами

**Ошибка:**
```
TypeError: Cannot read property 'fetch' of undefined
```

**Решение:**
Проверьте `backend/index.ts` - должен экспортировать app:
```typescript
import app from './hono';
export default app;
```

---

### 3️⃣ "Build failed" или "Function failed"

**Ошибка:**
```
Error: Build failed
```

**Решение:**
1. Проверьте `backend/package.json`:
   - Должен быть `"type": "module"`
   - Все зависимости указаны

2. Проверьте `backend/vercel.json`:
   ```json
   {
     "version": 2
   }
   ```

---

### 4️⃣ "Database connection failed"

**Ошибка:**
```
Error: connect ECONNREFUSED
```

**Решение:**
1. Проверьте Connection String в Vercel Environment Variables
2. Убедитесь, что используется **Session mode** connection string
3. Пароль в connection string должен быть правильным (без `[YOUR-PASSWORD]`)

---

### 5️⃣ "Invalid function runtime"

**Ошибка:**
```
Error: Invalid function runtime
```

**Решение:**
Vercel автоматически определит runtime для Hono. Убедитесь, что `vercel.json` минимальный:
```json
{
  "version": 2
}
```

---

### 6️⃣ "Missing dependencies"

**Ошибка:**
```
Error: Missing dependencies
```

**Решение:**
Добавьте все зависимости в `backend/package.json`:
```json
{
  "dependencies": {
    "hono": "^4.10.6",
    "@hono/trpc-server": "^0.4.0",
    "@trpc/server": "^11.7.2",
    "superjson": "^2.2.1"
  }
}
```

---

### 7️⃣ "Cannot use import statement"

**Ошибка:**
```
SyntaxError: Cannot use import statement
```

**Решение:**
Убедитесь, что в `backend/package.json` есть:
```json
{
  "type": "module"
}
```

---

### 8️⃣ Ошибки в Vercel Dashboard

**Где посмотреть логи:**
1. Идите в Vercel Dashboard
2. Выберите проект
3. Перейдите на вкладку **"Deployments"**
4. Выберите последний deployment
5. Нажмите **"View Function Logs"** или **"View Build Logs"**

---

## 📋 Что проверить:

1. ✅ Все файлы на месте (`index.ts`, `vercel.json`, `package.json`)
2. ✅ Зависимости установлены (`bun install` или `npm install` в `backend/`)
3. ✅ `package.json` имеет `"type": "module"`
4. ✅ Connection String правильно настроен в Vercel (Session mode)
5. ✅ Environment Variables добавлены в Vercel

---

## 🔍 Быстрая проверка

Выполните эти команды:

```bash
cd backend

# Проверка файлов
ls -la index.ts vercel.json package.json

# Проверка зависимостей
ls -la node_modules 2>/dev/null || echo "Нужно: bun install"

# Проверка package.json
cat package.json | grep -E "type|dependencies"
```

---

## 📝 Скопируйте ошибку

Пожалуйста, скопируйте **полный текст ошибки** из:
- Терминала (если использовали CLI)
- Vercel Dashboard → Deployments → View Logs

И я помогу исправить!
