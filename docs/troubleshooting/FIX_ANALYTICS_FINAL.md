# 🔧 Финальное решение ошибки AnalyticsProvider

## Проблема
```
SyntaxError: Identifier 'AnalyticsProvider' has already been declared.
```

## Решение (пошагово)

### Шаг 1: Полная очистка
```bash
cd ~/Desktop/rork-kiku

# Остановить все процессы
killall node
killall -9 node 2>/dev/null

# Удалить все кеши
rm -rf node_modules/.cache
rm -rf .expo
rm -rf .metro
rm -rf .rork
rm -rf .expo-shared
rm -rf ~/.expo
rm -rf ~/.metro
rm -rf ~/Library/Caches/metro-*
rm -rf ~/Library/Developer/Xcode/DerivedData/*

# Очистить npm/yarn/bun кеш (опционально)
# bun pm cache rm 2>/dev/null || true
```

### Шаг 2: Переустановка зависимостей
```bash
cd ~/Desktop/rork-kiku
rm -rf node_modules
bun install
```

### Шаг 3: Запуск с полной очисткой
```bash
# Для этого проекта используется Rork
bun run start

# Если нужна очистка кеша, сначала выполните:
rm -rf node_modules/.cache .expo .metro

# Затем запустите:
bun run start
```

### Шаг 4: Если ошибка продолжается - перезагрузите Mac
Это самый надежный способ очистить кеш Metro bundler в памяти.

## Запуск в симуляторе iOS 16

После успешного запуска Expo:

1. **Нажмите `i`** для запуска в iOS симуляторе
2. Или **нажмите `Shift + i`** для выбора конкретного симулятора iOS 16

### Альтернативный способ (прямой запуск):
```bash
# Открыть симулятор iOS 16
open -a Simulator

# В симуляторе: File → Open Simulator → iOS 16.0 → iPhone 14

# Запустить Rork (ваш проект использует Rork, не стандартный Expo)
bun run start

# Нажать 'i' в терминале для запуска в открытом симуляторе
```

## Проверка

Убедитесь, что:
- ✅ Нет файлов `*.backup*` в директории `app/`
- ✅ В `app/_layout.tsx` только один импорт `AnalyticsProvider`
- ✅ В `constants/AnalyticsContext.tsx` корректный экспорт
- ✅ Все процессы Node.js остановлены перед запуском

## Если ничего не помогает

1. **Перезагрузите Mac полностью**
2. После перезагрузки выполните Шаг 1-3
3. Запустите проект

Metro bundler может кешировать данные в памяти системы, и только перезагрузка может полностью очистить это.

