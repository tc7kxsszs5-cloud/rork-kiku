# 📱 Исправление "пригодные данные не найдены" в Expo Go

## 🔍 Проблема

Expo Go на телефоне показывает "пригодные данные не найдены" и не может подключиться к проекту.

---

## ✅ Решение (пошагово)

### Шаг 1: Остановить все процессы

```bash
pkill -f expo
pkill -f metro
sleep 2
```

### Шаг 2: Запустить Expo в LAN режиме

```bash
npx expo start --lan
```

**НЕ используй:**
- ❌ `--tunnel` (ngrok не работает)
- ❌ `bun run start` (использует tunnel)
- ❌ `bun run start-web` (только веб)

**Используй:**
- ✅ `npx expo start --lan` (локальная сеть)

---

### Шаг 3: Дождаться QR кода

В терминале появится:
```
› Metro waiting on exp://192.168.x.x:8081
› Scan the QR code above with Expo Go (Android) or the Camera app (iOS)
```

**Скопируй адрес:** `exp://192.168.x.x:8081`

---

### Шаг 4: Подключиться через Expo Go

#### Вариант A: Сканирование QR кода

1. Открой **Expo Go** на телефоне
2. Нажми **"Scan QR code"** (или кнопку камеры)
3. Наведи камеру на QR код в терминале
4. Дождись подключения

#### Вариант B: Ввод URL вручную

1. Открой **Expo Go** на телефоне
2. Найди кнопку **"Enter URL manually"** (может быть в меню или внизу)
3. Введи адрес из терминала: `exp://192.168.x.x:8081`
4. Нажми "Connect" или "Open"

---

## ⚠️ Важные требования

### 1. Одна Wi-Fi сеть

- ✅ Телефон и компьютер должны быть в **ОДНОЙ Wi-Fi сети**
- ❌ Разные сети не работают

**Проверка:**
- На телефоне: Настройки → Wi-Fi → посмотри название сети
- На компьютере: Системные настройки → Сеть → посмотри название сети
- Должны совпадать!

---

### 2. Отключить VPN

- ❌ VPN может блокировать локальную сеть
- ✅ Отключи VPN на телефоне и компьютере

---

### 3. Проверить брандмауэр

- На Mac: Системные настройки → Безопасность → Брандмауэр
- Убедись что порт 8081 не заблокирован

---

## 🔧 Альтернативные способы

### Способ 1: Использовать IP адрес напрямую

1. Узнай IP адрес компьютера:
   ```bash
   ifconfig | grep "inet " | grep -v 127.0.0.1
   ```
   Или на Mac:
   ```bash
   ipconfig getifaddr en0
   ```

2. В Expo Go введи:
   ```
   exp://[IP_АДРЕС]:8081
   ```
   Например: `exp://192.168.1.100:8081`

---

### Способ 2: Использовать другой порт

Если порт 8081 занят:

```bash
npx expo start --lan --port 8083
```

Затем в Expo Go используй новый порт: `exp://192.168.x.x:8083`

---

### Способ 3: Веб-версия (для тестирования)

Если Expo Go не работает вообще:

```bash
npx expo start --web
```

Откроется в браузере - можно тестировать без телефона.

---

## 🐛 Диагностика проблем

### Проблема: QR код не сканируется

**Решение:**
- Используй ручной ввод URL
- Убедись что QR код четкий
- Попробуй увеличить яркость экрана

---

### Проблема: "пригодные данные не найдены"

**Причины:**
1. Телефон и компьютер в разных сетях
2. VPN блокирует подключение
3. Брандмауэр блокирует порт
4. Неправильный URL введен

**Решение:**
1. Проверь что оба устройства в одной Wi-Fi сети
2. Отключи VPN
3. Проверь брандмауэр
4. Перепроверь URL (должен быть `exp://192.168.x.x:8081`)

---

### Проблема: "Connection timeout"

**Решение:**
1. Проверь что Expo запущен (`npx expo start --lan`)
2. Проверь что порт не занят другим процессом
3. Попробуй перезапустить Expo

---

## ✅ Чеклист подключения

- [ ] Остановлены все процессы Expo (`pkill -f expo`)
- [ ] Запущен Expo в LAN режиме (`npx expo start --lan`)
- [ ] Виден QR код в терминале
- [ ] Скопирован адрес `exp://192.168.x.x:8081`
- [ ] Телефон и компьютер в одной Wi-Fi сети
- [ ] VPN отключен
- [ ] Expo Go установлен на телефоне
- [ ] Попробовал сканировать QR код
- [ ] Попробовал ввести URL вручную

---

## 🚀 Быстрая команда для перезапуска

```bash
pkill -f expo && pkill -f metro && sleep 2 && npx expo start --lan
```

Это остановит все процессы и запустит Expo заново в LAN режиме.

---

## 💡 Советы

1. **Всегда используй `--lan`** вместо `--tunnel` (ngrok не работает)
2. **Проверяй Wi-Fi сеть** перед подключением
3. **Используй ручной ввод URL** если QR код не работает
4. **Веб-версия** для быстрого тестирования UI

---

**Главное: используй `npx expo start --lan` и убедись что телефон и компьютер в одной Wi-Fi сети!** 📱
