# 🔧 Исправление ошибки 404 NOT_FOUND

## ❌ Проблема

Получаете ошибку:
```
404: NOT_FOUND
Code: NOT_FOUND
```

## ✅ Решение

Я обновил конфигурацию. Нужно передеплоить.

---

## 🔧 Что исправлено:

1. ✅ Обновлен `backend/vercel.json` - добавлена правильная конфигурация маршрутов
2. ✅ Обновлен `backend/index.ts` - добавлен handler для Vercel

---

## 🚀 Что делать дальше:

### Вариант 1: Если деплоили через CLI

```bash
cd backend
vercel --prod
```

### Вариант 2: Если деплоили через GitHub

1. Закоммитьте изменения:
   ```bash
   git add backend/vercel.json backend/index.ts
   git commit -m "Fix 404 error - update Vercel config"
   git push
   ```

2. Vercel автоматически передеплоит

### Вариант 3: Через Vercel Dashboard

1. Идите в Vercel Dashboard
2. Выберите проект
3. Нажмите **"Redeploy"** → **"Redeploy"**

---

## ✅ Проверка после передеплоя:

1. Откройте ваш API URL:
   ```
   https://your-backend.vercel.app/
   ```

2. Должно вернуться:
   ```json
   {
     "status": "ok",
     "message": "API is running"
   }
   ```

3. Проверьте tRPC endpoint:
   ```
   https://your-backend.vercel.app/api/trpc/example.hi
   ```

---

## 📋 Если ошибка осталась:

1. Проверьте логи в Vercel Dashboard:
   - Deployments → выберите последний → View Function Logs

2. Убедитесь, что:
   - ✅ Environment Variables настроены
   - ✅ DATABASE_URL добавлен (если используется БД)

3. Попробуйте полную очистку и передеплой:
   ```bash
   cd backend
   rm -rf .vercel
   vercel --prod
   ```

---

## 🎯 Готово!

После передеплоя ошибка 404 должна исчезнуть!
