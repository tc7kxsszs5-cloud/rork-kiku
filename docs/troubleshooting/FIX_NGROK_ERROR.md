# 🔧 Исправление ошибки ngrok tunnel

## Ошибка
```
CommandError: ngrok tunnel took too long to connect.
error: script "start" exited with code 1
```

## ✅ Решение

Проблема с ngrok tunnel. Используйте команду **без туннеля**:

### Вариант 1: Через стандартный Expo (РЕКОМЕНДУЕТСЯ)

```bash
cd /Users/mac/Desktop/rork-kiku
bun run start:expo
```

Или напрямую:
```bash
bunx expo start
```

### Вариант 2: Через локальную сеть

```bash
cd /Users/mac/Desktop/rork-kiku
bun run start:local
```

Или:
```bash
bunx expo start --lan
```

---

## 📱 Для Expo Go

После запуска `bunx expo start`:

1. В терминале появится QR-код
2. Откройте Expo Go на телефоне
3. Отсканируйте QR-код
4. **Важно:** Убедитесь, что телефон и Mac в одной Wi-Fi сети!

---

## 🖥️ Для iOS симулятора

```bash
cd /Users/mac/Desktop/rork-kiku
bunx expo start --ios
```

Или:
```bash
bun run start:expo
# Затем нажмите 'i' в терминале
```

---

## 🔍 Проверка новых команд

Новые команды добавлены в `package.json`:
- `bun run start:expo` - запуск через стандартный Expo (без туннеля)
- `bun run start:local` - запуск через локальную сеть

---

**Используйте `bun run start:expo` вместо `bun run start`!**
