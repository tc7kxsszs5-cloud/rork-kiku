# ✅ Исправление: Используйте порт 6543

## ❌ Текущая проблема:

Используется порт `5432`, но для Connection Pooling нужен порт `6543`!

## ✅ Правильный Connection String:

```
postgres://postgres.apbkobhfnmcqqzqeeqss:gerkom-tYbpek-2cochi@aws-0-us-east-1.pooler.supabase.com:6543/postgres
```

**Изменение:** Порт `5432` → `6543`

---

## 📋 Команды:

```bash
cd /Users/mac/Desktop/rork-kiku/backend
bunx vercel env rm DATABASE_URL production
```

**Ответьте:** `y`

```bash
bunx vercel env add DATABASE_URL production
```

**Ответьте:**
- `Mark as sensitive?` → **yes**
- `What's the value of DATABASE_URL?` → Вставьте:
  ```
  postgres://postgres.apbkobhfnmcqqzqeeqss:gerkom-tYbpek-2cochi@aws-0-us-east-1.pooler.supabase.com:6543/postgres
  ```

## 🚀 После установки:

```bash
bunx vercel --prod
curl "https://backend-three-mauve-67.vercel.app/api/trpc/test.dbCheck"
```

---

## 💡 Почему порт 6543?

- Порт `5432` - для прямого подключения
- Порт `6543` - для Connection Pooling (SHARED POOLER)

---

**Используйте порт 6543!**
