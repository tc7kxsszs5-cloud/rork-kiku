# 🔧 Исправление проблемы с Vercel CLI

## ❌ Проблема

```
env: node: Permission denied
```

Это происходит потому, что глобальная установка Vercel CLI через Bun пытается использовать системный Node.js, к которому нет доступа.

---

## ✅ Решение: Используйте `bunx vercel`

Вместо глобальной установки используйте `bunx vercel` - это Bun's способ запуска пакетов без глобальной установки.

---

## 🚀 Использование

### Вход в Vercel:

```bash
bunx vercel login
```

### Деплой:

```bash
bunx vercel --prod
```

### Или используйте обновленные скрипты:

```bash
cd backend
./auto-deploy.sh
./auto-setup.sh
```

Скрипты теперь автоматически используют `bunx vercel`!

---

## 📋 Альтернатива: Исправление прав доступа

Если хотите использовать глобальную установку:

```bash
# Проверьте права доступа к Node.js
which node
ls -la $(which node)

# Если нужно, исправьте права
sudo chmod +x $(which node)
```

Но проще использовать `bunx vercel` - это работает без проблем!

---

## ✅ Готово!

Используйте `bunx vercel` вместо глобальной установки.
