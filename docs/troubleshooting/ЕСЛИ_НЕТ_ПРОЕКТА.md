# 🆘 Если нет проекта в Vercel Dashboard

## ❌ Проблема

Не видите разделы Settings → Environment Variables? Возможно, проект еще не создан!

---

## ✅ Решение 1: Создайте проект через CLI

### Шаг 1: Запустите деплой

```bash
cd backend
bunx vercel
```

### Шаг 2: Ответьте на вопросы:

1. **Set up and deploy?** → `Y`
2. **Which scope?** → выберите ваш аккаунт
3. **Link to existing project?** → `N` (нет)
4. **What's your project's name?** → `kiku-backend` (или любое имя)
5. **In which directory is your code located?** → `./` (точка слэш)
6. **Want to override the settings?** → `N` (нет)

После этого проект будет создан и задеплоен!

---

## ✅ Решение 2: Создайте проект через Dashboard

### Шаг 1: Откройте Vercel Dashboard

https://vercel.com/dashboard

### Шаг 2: Создайте новый проект

1. Нажмите кнопку **"Add New..."** (обычно справа вверху)
2. Выберите **"Project"**
3. Подключите GitHub репозиторий (если нужно)
4. Или выберите **"Import Git Repository"**

### Шаг 3: Настройте проект

1. **Root Directory:** `backend`
2. **Framework Preset:** Other (или оставьте по умолчанию)
3. **Build Command:** оставьте пустым
4. **Output Directory:** оставьте пустым
5. Нажмите **"Deploy"**

---

## ✅ Решение 3: Используйте CLI для добавления переменных

Если проект уже есть, но вы не видите его в Dashboard:

```bash
cd backend

# Добавьте DATABASE_URL с правильным паролем
echo "postgresql://postgres:ВАШ_ПАРОЛЬ@db.eznumgsmwvavyunqhxfc.supabase.co:5432/postgres" | bunx vercel env add DATABASE_URL production

# Добавьте NODE_ENV
echo "production" | bunx vercel env add NODE_ENV production
```

**Замените `ВАШ_ПАРОЛЬ` на ваш реальный пароль!**

---

## 🔍 Проверка проекта

Проверьте, какие проекты у вас есть:

```bash
bunx vercel ls
```

Если проект есть - он покажется в списке.

---

## 📋 После создания проекта

1. **Добавьте Environment Variables:**
   - Через Dashboard: Settings → Environment Variables
   - Или через CLI (см. выше)

2. **Передеплойте:**
   ```bash
   bunx vercel --prod
   ```

---

## ✅ Готово!

После создания проекта и добавления переменных все будет работать!
