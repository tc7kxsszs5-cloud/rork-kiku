# Что делать, если не работает

## 🚨 Быстрое исправление

```bash
# Автоматическое исправление
bun run fix

# Или вручную
./scripts/quick-fix.sh
```

Этот скрипт:
- ✅ Остановит процессы на портах 8081 и 8082
- ✅ Очистит кэш
- ✅ Проверит зависимости
- ✅ Проверит конфигурацию

## 🔍 Пошаговая диагностика

### Шаг 1: Проверьте консоль браузера

1. Откройте приложение в браузере
2. Нажмите F12 (открыть DevTools)
3. Перейдите на вкладку "Console"
4. Найдите ошибки (красным цветом)

**Что искать:**
- `Cannot read property 'theme' of undefined`
- `Provider is missing`
- `Module not found`
- `Context is not defined`

### Шаг 2: Проверьте тесты

```bash
bun run test:all-config
```

**Если тесты не проходят:**
- Исправьте ошибки, которые показывает тест
- Перезапустите тесты

### Шаг 3: Очистите и перезапустите

```bash
# Остановите приложение (Ctrl+C)
# Очистите кэш
bun run start:clear

# Или используйте скрипт
bun run fix
```

### Шаг 4: Проверьте порты

```bash
# Проверьте, не заняты ли порты
lsof -ti:8081
lsof -ti:8082

# Если заняты - освободите
lsof -ti:8081 | xargs kill -9
lsof -ti:8082 | xargs kill -9
```

## 🐛 Частые проблемы

### 1. "Cannot read property 'theme' of undefined"

**Исправлено:** Добавлена обработка ошибок во всех экранах

**Если все еще возникает:**
- Проверьте, что ThemeProvider обернут в _layout.tsx
- Проверьте консоль на другие ошибки

### 2. Белый экран

**Причина:** Ошибка в компоненте

**Решение:**
1. Откройте консоль (F12)
2. Найдите ошибку
3. Проверьте ErrorBoundary - он должен показать сообщение

### 3. Вкладки не переключаются

**Проверьте:**
- Все файлы вкладок существуют
- Нет ошибок в консоли
- Роутинг правильно настроен

### 4. Приложение не запускается

**Проверьте:**
```bash
# 1. Зависимости
bun install

# 2. Тесты
bun run test:all-config

# 3. Порты
lsof -ti:8081 | xargs kill -9

# 4. Запуск
bun run start:web
```

## 📋 Чеклист

Перед тем как сказать "не работает", проверьте:

- [ ] Тесты проходят: `bun run test:all-config`
- [ ] Нет ошибок в консоли браузера (F12)
- [ ] Порты свободны (8081, 8082)
- [ ] Зависимости установлены: `bun install`
- [ ] Кэш очищен: `bun run start:clear`
- [ ] Приложение запущено: `bun run start:web`

## 🆘 Если ничего не помогает

1. **Соберите информацию:**
   ```bash
   # Вывод тестов
   bun run test:all-config > test-output.txt
   
   # Вывод запуска
   bun run start:web > startup-output.txt 2>&1
   ```

2. **Проверьте версии:**
   ```bash
   bun --version
   node --version
   ```

3. **Переустановите зависимости:**
   ```bash
   rm -rf node_modules
   bun install
   ```

4. **Очистите все:**
   ```bash
   rm -rf .expo node_modules/.cache
   bun run start:clear
   ```

## 📝 Что уже исправлено

✅ Обработка ошибок в новых экранах (achievements, lessons)
✅ Обработка ошибок в TabLayout
✅ Обработка ошибок в настройках
✅ Обработка ошибок в чатах
✅ Все файлы вкладок созданы
✅ Все тесты проходят

## 🎯 Следующие шаги

1. Запустите `bun run fix`
2. Запустите `bun run start:web`
3. Откройте браузер на `http://localhost:8082`
4. Проверьте консоль (F12)
5. Если есть ошибки - скопируйте их и сообщите
