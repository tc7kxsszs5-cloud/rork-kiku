# 🔧 Решение проблемы с Xcode

## Проблема
Xcode показывает ошибку: **"The sandbox is not in sync with the Podfile.lock"**

## Причина
- Отсутствует файл `Podfile.lock`
- Ruby 2.6 (системный) не поддерживает gem `ffi`, который нужен для CocoaPods
- Нельзя запустить `pod install` для создания `Podfile.lock`

## ✅ Решение 1: Использовать Expo CLI (РЕКОМЕНДУЕТСЯ)

**Не нужен Xcode для разработки!**

```bash
# Запуск на iOS симуляторе
bun run ios

# Или через Expo
bunx expo start --ios
```

**Преимущества:**
- ✅ Работает сразу, без проблем
- ✅ Не требует настройки CocoaPods
- ✅ Быстрее для разработки
- ✅ Автоматически обновляет код

**Xcode нужен только для:**
- Настройки подписи приложения (Signing & Capabilities)
- Отладки нативных модулей
- Создания production билда

## ✅ Решение 2: Установить Ruby 3.0+ (если нужен Xcode)

Если вам обязательно нужен Xcode для работы:

```bash
# Установка Ruby через Homebrew (займет 10-30 минут)
brew install ruby

# После установки обновите PATH
echo 'export PATH="/opt/homebrew/opt/ruby/bin:$PATH"' >> ~/.zshrc
source ~/.zshrc

# Установите CocoaPods заново
gem install cocoapods

# Установите Pods
cd ios
pod install
```

## ✅ Решение 3: Создать Podfile.lock вручную (временное)

Если Pods уже установлены, можно создать минимальный `Podfile.lock`:

```bash
cd ios
cat > Podfile.lock << 'EOF'
PODS:
  - ExpoModulesCore (0.0.1)

DEPENDENCIES:
  - ExpoModulesCore

SPEC REPOS:
  trunk:
    - ExpoModulesCore

SPEC CHECKSUMS:
  ExpoModulesCore: placeholder

PODFILE CHECKSUM: placeholder

COCOAPODS: 1.10.2
EOF
```

**⚠️ Внимание:** Это временное решение. Лучше использовать Решение 1.

## 🎯 Рекомендация

**Для разработки:** Используйте `bun run ios` (Решение 1)

**Для production:** Установите Ruby 3.0+ (Решение 2) когда будете готовы к сборке

---

**Вывод:** Не нужно ходить по кругу! Используйте Expo CLI - это стандартный способ разработки Expo приложений.
