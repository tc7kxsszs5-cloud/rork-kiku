# 🔧 Исправление пути импорта

## ❌ Ошибка

```
Error [ERR_MODULE_NOT_FOUND]: Cannot find module '/var/task/trpc/routes/example/create-context.js' imported from /var/task/trpc/routes/example/hi/route.js
```

## ✅ Причина

В файле `backend/trpc/routes/example/hi/route.ts` неправильный путь импорта.

**Было:**
```typescript
import { publicProcedure } from "../create-context.js";
```

Это ищет файл в `backend/trpc/routes/create-context.js`, но файл находится в `backend/trpc/create-context.ts`.

---

## ✅ Решение

Исправлен путь импорта:

**Стало:**
```typescript
import { publicProcedure } from "../../create-context.js";
```

Теперь путь правильный: из `routes/example/` нужно подняться на два уровня вверх (`../../`) чтобы дойти до `trpc/create-context.ts`.

---

## 📋 Передеплойте

```bash
cd backend
bunx vercel --prod
```

---

## ✅ После передеплоя

Проверьте работу API:

https://backend-three-mauve-67.vercel.app/

Должно показать:
```json
{"status": "ok", "message": "API is running"}
```

---

## ✅ Готово!

После передеплоя ошибка должна исчезнуть!
