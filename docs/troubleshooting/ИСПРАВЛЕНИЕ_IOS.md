# 🔧 Исправление проблемы с iOS

## ❌ Проблема

Приложение не запускается на iOS через Expo Go.

## 🔍 Причина

В `app.json` было установлено:
```json
"newArchEnabled": true
```

**Expo Go не поддерживает новую архитектуру React Native**, поэтому приложение не может запуститься.

## ✅ Решение

Изменил `newArchEnabled` на `false` в `app.json`:
```json
"newArchEnabled": false
```

---

## 🚀 Как запустить на iOS

### Вариант 1: Через Expo Go (рекомендуется)

1. **Установите Expo Go на iPhone:**
   - Откройте App Store
   - Найдите "Expo Go"
   - Установите приложение

2. **Запустите dev сервер:**
   ```bash
   bun run start:simple
   ```

3. **Подключите iPhone:**
   - Убедитесь, что iPhone и Mac в одной Wi-Fi сети
   - Откройте Expo Go на iPhone
   - Отсканируйте QR код из терминала
   - Или введите URL вручную

### Вариант 2: Через iOS Simulator

1. **Убедитесь, что Xcode установлен:**
   ```bash
   xcode-select --version
   ```

2. **Запустите dev сервер:**
   ```bash
   bun run start:simple
   ```

3. **Нажмите 'i' в терминале** для запуска в iOS Simulator

---

## ⚠️ Важные замечания

### Для разработки (Expo Go):
- ✅ `newArchEnabled: false` - **обязательно**
- ✅ Работает на реальных устройствах
- ✅ Быстрое обновление кода

### Для продакшена (EAS Build):
- ✅ Можно использовать `newArchEnabled: true`
- ✅ Нужен собственный build через EAS
- ✅ Не работает в Expo Go

---

## 🔄 Если нужно вернуть новую архитектуру

Для продакшена можно создать отдельную конфигурацию:

```json
{
  "expo": {
    "newArchEnabled": false,  // Для Expo Go
    "build": {
      "production": {
        "newArchEnabled": true  // Для production build
      }
    }
  }
}
```

---

## ✅ Проверка

После исправления:

1. Перезапустите dev сервер:
   ```bash
   bun run start:simple
   ```

2. Нажмите 'i' для iOS Simulator
   ИЛИ
   Откройте Expo Go на iPhone и отсканируйте QR код

3. Приложение должно запуститься! 🎉

---

## 📝 Дополнительная информация

- **Expo Go** - это приложение для быстрой разработки
- **Новая архитектура** - это оптимизированная версия React Native
- **Совместимость**: Expo Go пока не поддерживает новую архитектуру
- **Решение**: Отключить для разработки, включить для продакшена

---

**Теперь iOS должен работать! 🚀**
