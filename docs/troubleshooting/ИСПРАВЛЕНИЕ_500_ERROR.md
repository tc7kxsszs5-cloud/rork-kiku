# 🔧 Исправление ошибки 500 INTERNAL_SERVER_ERROR

## ❌ Ошибка

```
500: INTERNAL_SERVER_ERROR
Code: FUNCTION_INVOCATION_FAILED
```

Это означает, что функция запускается, но падает при выполнении.

---

## 🔍 Возможные причины:

1. **Проблема с импортами модулей**
2. **Проблема с подключением к базе данных**
3. **Ошибка в коде createContext**
4. **Отсутствующие зависимости**

---

## ✅ Решение:

### Шаг 1: Проверьте логи в Vercel

1. Vercel Dashboard → ваш проект
2. Deployments → последний deployment
3. **View Function Logs**
4. Скопируйте полный текст ошибки

**Это самое важное!** Логи покажут точную причину ошибки.

---

### Шаг 2: Возможные исправления

#### Исправление 1: Проблема с createContext

Если ошибка связана с `createContext`, проверьте `backend/trpc/create-context.ts`.

#### Исправление 2: Проблема с базой данных

Если ошибка связана с подключением к БД:
1. Проверьте `DATABASE_URL` в Environment Variables
2. Убедитесь, что пароль заменен
3. Проверьте формат connection string

#### Исправление 3: Проблема с зависимостями

Если ошибка "Cannot find module":
1. Проверьте `package.json`
2. Убедитесь, что все зависимости указаны
3. Передеплойте проект

---

## 🆘 Что нужно сделать:

**Скопируйте полный текст ошибки из логов Vercel (View Function Logs)** и отправьте мне - я помогу исправить!

---

## 📋 Быстрая проверка:

1. ✅ Проверьте логи в Vercel (View Function Logs)
2. ✅ Проверьте Environment Variables (DATABASE_URL)
3. ✅ Проверьте, что все файлы закоммичены

---

## 🎯 Главное:

**Логи в Vercel покажут точную причину ошибки!**

Откройте View Function Logs и скопируйте ошибку.
