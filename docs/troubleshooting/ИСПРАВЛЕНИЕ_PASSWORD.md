# 🔧 Исправление Connection String - замена пароля

## ❌ Проблема

В Connection String есть `[YOUR-PASSWORD]` - это плейсхолдер, который нужно заменить на реальный пароль!

---

## ✅ Решение: Замените пароль

### Шаг 1: Найдите ваш пароль в Supabase

1. Идите в **Supabase Dashboard**
2. **Settings → Database**
3. Найдите раздел **"Database password"**
4. Если забыли - можно сбросить пароль (Reset database password)

---

### Шаг 2: Постройте правильный Connection String

**Формат:**
```
postgresql://postgres:ВАШ_РЕАЛЬНЫЙ_ПАРОЛЬ@db.eznumgsmwvavyunqhxfc.supabase.co:5432/postgres
```

**Пример:**
Если ваш пароль `mypassword123`, то:
```
postgresql://postgres:mypassword123@db.eznumgsmwvavyunqhxfc.supabase.co:5432/postgres
```

---

### Шаг 3: Обновите переменную в Vercel

Переменные уже существуют, но с неправильным значением. Нужно обновить!

#### Способ 1: Через Vercel Dashboard (рекомендуется)

1. Идите на https://vercel.com/dashboard
2. Выберите ваш проект
3. **Settings → Environment Variables**
4. Найдите `DATABASE_URL`
5. Нажмите **"Edit"** или **"..." → Edit**
6. Вставьте правильный Connection String (с реальным паролем)
7. Нажмите **Save**

#### Способ 2: Через CLI

```bash
cd backend

# Удалите старую переменную
bunx vercel env rm DATABASE_URL production

# Добавьте новую с правильным паролем
echo "postgresql://postgres:ВАШ_РЕАЛЬНЫЙ_ПАРОЛЬ@db.eznumgsmwvavyunqhxfc.supabase.co:5432/postgres" | bunx vercel env add DATABASE_URL production
```

**Замените `ВАШ_РЕАЛЬНЫЙ_ПАРОЛЬ` на ваш реальный пароль!**

---

## 📋 Альтернатива: Используйте Connection String из Supabase напрямую

Если в Supabase есть готовый Connection String (без `[YOUR-PASSWORD]`):

1. Скопируйте его полностью из Supabase
2. Обновите в Vercel Dashboard → Settings → Environment Variables → DATABASE_URL

---

## ✅ После обновления

### Передеплойте проект:

```bash
bunx vercel --prod
```

Или в Vercel Dashboard:
- **Deployments** → выберите последний → **Redeploy**

---

## 🔍 Проверка

После обновления проверьте:

1. Vercel Dashboard → Settings → Environment Variables
2. `DATABASE_URL` должен содержать реальный пароль (не `[YOUR-PASSWORD]`)
3. Передеплойте проект
4. Проверьте Function Logs - не должно быть ошибок подключения к базе

---

## ✅ Готово!

После замены пароля и передеплоя ваш backend будет подключен к Supabase!
