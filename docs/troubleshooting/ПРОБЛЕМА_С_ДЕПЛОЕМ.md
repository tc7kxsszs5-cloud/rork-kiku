# ❌ Проблема: Endpoint возвращает NOT_FOUND

## ❌ Проблема:

Endpoint `/api/trpc/test.dbCheck` возвращает `NOT_FOUND`.

## ✅ Решение:

Нужно сделать новый деплой после последних изменений.

---

## 🚀 Деплой:

```bash
cd /Users/mac/Desktop/rork-kiku/backend
bunx vercel --prod
```

---

## ✅ После деплоя:

Проверьте:
```bash
curl "https://backend-three-mauve-67.vercel.app/api/trpc/test.dbCheck"
```

---

**Выполните деплой и проверьте снова!**
