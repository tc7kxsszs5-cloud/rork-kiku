# 🔧 Исправление ошибки "requested path is invalid"

## ❌ Ошибка

```
{"error":"requested path is invalid"}
```

## ✅ Решение

Я обновил конфигурацию Vercel для правильной работы с Hono.

---

## 🔧 Что исправлено:

1. ✅ Упрощен `vercel.json` - теперь использует zero-config Hono
2. ✅ Обновлен `index.ts` - правильный экспорт для Vercel

---

## 🚀 Что делать дальше:

### Вариант 1: Передеплой через GitHub

```bash
git add backend/vercel.json backend/index.ts
git commit -m "Fix invalid path error"
git push
```

Vercel автоматически передеплоит.

### Вариант 2: Передеплой через Vercel Dashboard

1. Идите в Vercel Dashboard
2. Выберите проект
3. Deployments → последний deployment
4. Нажмите три точки (⋮) → **"Redeploy"**

---

## ✅ Проверка после передеплоя:

### 1. Главная страница:

Откройте:
```
https://your-backend.vercel.app/
```

**Должно вернуться:**
```json
{
  "status": "ok",
  "message": "API is running"
}
```

### 2. tRPC endpoint:

Откройте:
```
https://your-backend.vercel.app/api/trpc/example.hi
```

**Должен вернуться ответ от tRPC.**

---

## 🔍 Если ошибка осталась:

### Проверьте:

1. **Правильный ли URL:**
   - Должен быть: `https://your-backend.vercel.app/`
   - НЕ: `https://your-backend.vercel.app/index.ts`

2. **Проверьте логи в Vercel:**
   - Deployments → выберите deployment → View Function Logs
   - Посмотрите, какие ошибки там

3. **Проверьте Environment Variables:**
   - Settings → Environment Variables
   - Убедитесь, что `DATABASE_URL` и `NODE_ENV` добавлены

---

## 📋 Доступные endpoints:

После исправления должны работать:

- `GET /` - главная страница
- `GET /api/trpc/*` - tRPC endpoints
- `POST /api/trpc/*` - tRPC endpoints

---

## 🎯 Готово!

После передеплоя ошибка должна исчезнуть!

Передеплойте проект и проверьте снова.
