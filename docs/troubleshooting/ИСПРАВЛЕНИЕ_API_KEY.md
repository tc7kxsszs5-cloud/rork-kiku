# ✅ Исправление ошибки "Invalid API key"

## ✅ Хорошие новости:

Подключение к Supabase работает! Переменные окружения применяются.

## ❌ Проблема:

Ошибка "Invalid API key" - неправильный или неверный anon key.

## ✅ Решение:

### Вариант 1: Проверьте anon key в Supabase

1. Откройте https://supabase.com/dashboard
2. Выберите проект
3. **Settings** → **API**
4. Найдите **"anon public"** key
5. Убедитесь, что скопировали его полностью (длинная строка)

### Вариант 2: Используйте service_role key (для backend)

Для backend на Vercel можно использовать **service_role** key вместо anon key:

1. **Settings** → **API**
2. Найдите **"service_role"** key (секретный ключ)
3. Используйте его вместо anon key

**Важно:** service_role key имеет полный доступ к базе данных, используйте его только для backend!

---

## 📋 Команды для обновления:

```bash
cd /Users/mac/Desktop/rork-kiku/backend

# Удалить старый anon key
bunx vercel env rm SUPABASE_ANON_KEY production
# y

# Добавить service_role key (или правильный anon key)
bunx vercel env add SUPABASE_ANON_KEY production
# yes
# Вставьте service_role key (или правильный anon key)

# Деплой
bunx vercel --prod
```

---

## 💡 Рекомендация:

**Используйте service_role key для backend** - он имеет полный доступ и лучше подходит для serverless функций.

---

**Проверьте и обновите API key!**
