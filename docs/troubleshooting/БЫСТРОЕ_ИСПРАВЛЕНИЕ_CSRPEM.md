# ⚡ Быстрое исправление csrPEM ошибки

## 🔧 Что сделано

1. ✅ Очищен кэш Expo (`.expo` и `node_modules/.cache`)
2. ✅ Остановлены все процессы Expo
3. ✅ Освобожден порт 8081
4. ✅ Обновлена команда `start:simple` для пропуска сертификатов

---

## 🚀 Как запустить сейчас

### Вариант 1: Простой запуск (рекомендуется)

```bash
bun run start:simple
```

Эта команда теперь использует `EXPO_NO_DOTENV=1` и `--no-dev` для пропуска проблем с сертификатами.

### Вариант 2: С полной очисткой

```bash
# 1. Остановить все процессы
pkill -f "expo start"
lsof -ti:8081 | xargs kill -9

# 2. Очистить кэш
rm -rf .expo node_modules/.cache

# 3. Запустить
bun run start:simple
```

### Вариант 3: Прямой запуск без сертификатов

```bash
EXPO_NO_DOTENV=1 bunx expo start --lan --no-dev
```

---

## ⚠️ Если ошибка сохраняется

### Полная переустановка:

```bash
# 1. Остановить все
pkill -f "expo"
lsof -ti:8081 | xargs kill -9

# 2. Очистить все
rm -rf .expo node_modules/.cache node_modules

# 3. Переустановить
bun install

# 4. Запустить
bun run start:simple
```

---

## 📝 Что изменилось

**Было:**
```json
"start:simple": "bunx expo start --lan"
```

**Стало:**
```json
"start:simple": "EXPO_NO_DOTENV=1 bunx expo start --lan --no-dev"
```

Это пропускает создание сертификатов и должно решить проблему с `csrPEM`.

---

## ✅ Проверка

После запуска `bun run start:simple`:
- ✅ Не должно быть ошибки `csrPEM: Required`
- ✅ Должен появиться QR код
- ✅ Можно подключиться через Expo Go или симулятор

---

**Попробуйте сейчас:**
```bash
bun run start:simple
```
