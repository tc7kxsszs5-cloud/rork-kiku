# ✅ Готовый SQL скрипт для копирования

## 📋 Что делать:

1. Откройте файл `backend/schema.sql` в редакторе
2. Скопируйте **весь** SQL код
3. Откройте Supabase Dashboard → SQL Editor → New query
4. Вставьте скопированный SQL
5. Нажмите **Run** или **Execute**

---

## 📋 Или скопируйте отсюда:

Откройте файл `backend/schema.sql` - там готовый SQL скрипт для копирования!

---

## ✅ После выполнения:

Все таблицы будут созданы и готовы к использованию!

---

**Скопируйте SQL из файла `backend/schema.sql` и выполните в Supabase!**
