# RegressProof Session Note

**Date:** 2026-04-15
**Focus:** tighten RegressProof session automation so it tracks only project-relevant files
**Status:** partial
**Branch:** `main`
**Head:** `ed106119e`

## Completed

- resumed from project memory and prior session notes
- created the session note for today
- narrowed session automation so changed-file capture follows the RegressProof project boundary instead of the entire workspace
- improved RegressProof report and PR comment rendering to be more review-friendly
- added and validated a new `lint-js` fixture that produces a high-confidence `confirmed_agent_fault` verdict
- refined parser evidence grouping and file extraction so multi-line failure blocks are handled more robustly without breaking existing fixtures
- added and validated a first Python fixture, proving that RegressProof can classify a non-JS regression scenario with the expected high-confidence verdict
- added and validated a parser-targeted JS fixture with realistic multi-line stderr, confirming that grouped evidence now stays attached to one failure record
- added and validated a Swift fixture, including a local module-cache workaround so the scenario tests product behavior instead of sandbox noise
- upgraded the real-repo validation config from a placeholder echo check to a self-hosted RegressProof smoke check against fixture execution inside the real workspace
- ran a fresh real-repo validation pass and confirmed a stable smoke-level `successful_change` result with baseline fallback to `skip`
- added separate committed and workspace real-repo validation runners so RegressProof can validate both committed history and the current worktree boundary
- validated the workspace runner and confirmed that it now surfaces non-empty changed-file evidence for the current RegressProof worktree
- added a standalone packaging layer inside `regressproof/`, including a local `.gitignore`, subproject `AGENTS.md`, and an export helper that writes a near-standalone repo bundle
- verified the standalone export helper end-to-end and fixed it so fixture-level `.git` directories are excluded from the exported bundle
- upgraded committed attribution so RegressProof can diff `baselineRef..compareRef` explicitly and run the compared side from a snapshot instead of the live checkout
- validated the new commit-vs-commit path on the `lint-js` fixture, confirming non-empty changed files, `currentMode = snapshot`, and a correct high-confidence `confirmed_agent_fault` verdict
- verified the committed runner on the main repository and fixed it so missing historical RegressProof paths degrade to `baselineMode = skip` and `currentMode = worktree_fallback` instead of crashing
- added a committed-range readiness helper so RegressProof can report whether the main repository actually contains a meaningful historical range for committed attribution
- refined the readiness helper so it works for both real-repo boundary configs and fixture configs; main repo currently reports `not_ready`, while the `lint-js` fixture reports `ready`
- added a first `fixture materializer` script with dual support:
  - current embedded fixture repos can be cloned into temp repos
  - future tracked scenario packs can materialize into temp git history
- validated the materializer on `lint-js`, then ran RegressProof successfully against the materialized temp repo with the same high-confidence `confirmed_agent_fault` result
- added a fixture suite runner that materializes and validates all fixtures sequentially without stopping on the first failure
- added a generic fixture scenario-pack exporter and used it to create the first tracked scenario-pack prototype for `lint-js`
- validated `lint-js` again through the tracked scenario-pack path, confirming that the materialized temp repo still produces the same high-confidence `confirmed_agent_fault` verdict
- added a bulk fixture-pack exporter, converted the remaining fixtures, and verified that the full suite now passes in tracked scenario-pack mode (`10/10`)
- synchronized the docs and helper scripts so tracked scenario packs are now described as the canonical validation model rather than a migration experiment
- hardened the fixture pack exporter so it strips accidental duplicate files like `package 2.json` and `regressproof.config 2.json` from tracked packs; reran export plus suite and kept the result at `10/10`
- moved the old embedded fixture `.git` directories into `/tmp/regressproof-fixture-embedded-git-backups/`, staged the fixtures as ordinary repository files, and confirmed the suite still passes `10/10` in that post-import state
- upgraded the real-repo smoke path into a trust-check path by making workspace/real-repo validation run a meaningful tracked-fixture subset (`lint-js` + `preexisting-js`) instead of only a file-existence self-check
- tightened committed trust validation again so the real-repo configs rely on the existing `run-all-fixtures.js` subset path, which already exists on both sides of `HEAD~1..HEAD` and therefore compares more honestly across snapshot history
- changed committed baseline selection to prefer the nearest parent range before merge-base fallback; committed readiness is now `ready` on `HEAD~1..HEAD`, and committed trust validation now runs with `baselineMode = path_snapshot` and both sides passing
- restored the real-repo configs to a single `real-repo-trust-check.js` entrypoint after the parent-first range fix, and strengthened that trust-check to assert `tracked_scenario_pack` materialization in addition to expected verdicts
- added a dedicated `real:scenario` helper so the current committed trust scenario can be verified in one shot instead of manually chaining readiness plus committed report inspection
- added `proofSignals` to the report model so committed runs now expose why a trust verdict is believable, not only the final classification
- expanded the trust-check model with `shallow` and `deep` profiles and added a dedicated deep committed scenario helper so broader nested validation can run through the same committed path

## Changed Files

- `.github/workflows/regressproof.yml`
- `AGENTS.md`
- `docs/REGRESSPROOF_DECISION_LOG.md`
- `docs/REGRESSPROOF_DEMO_RELEASE.md`
- `docs/REGRESSPROOF_IMPLEMENTATION_PLAN.md`
- `docs/REGRESSPROOF_INDEX.md`
- `docs/REGRESSPROOF_MVP_TASK_BREAKDOWN.md`
- `docs/REGRESSPROOF_PRODUCT_BRIEF.md`
- `docs/REGRESSPROOF_PROTOTYPE_WALKTHROUGH.md`
- `docs/REGRESSPROOF_SESSION_TEMPLATE.md`
- `docs/REGRESSPROOF_SPEC.md`
- `docs/REGRESSPROOF_VALIDATION_PLAN.md`
- `docs/REGRESSPROOF_WORKFLOW_MEMORY.md`
- `docs/sessions/`
- `docs/sessions/2026-04-15-session.md`
- `regressproof/fixtures/lint-js/`
- `regressproof/fixtures/parser-js/`
- `regressproof/fixtures/python-js/`
- `regressproof/fixtures/swift-js/`
- `regressproof/.gitignore`
- `regressproof/AGENTS.md`
- `regressproof/regressproof.real-repo.config.json`
- `regressproof/regressproof.workspace-repo.config.json`
- `regressproof/scripts/export-standalone.js`
- `regressproof/scripts/materialize-fixture.js`
- `regressproof/scripts/check-committed-range-readiness.js`
- `regressproof/scripts/run-committed-real-repo-validation.js`
- `regressproof/scripts/run-workspace-real-repo-validation.js`
- `regressproof/src/config.js`
- `regressproof/src/git.js`
- `regressproof/src/report.js`
- `regressproof/src/verify.js`
- `regressproof/`
- `scripts/regressproof-session.js`

## Decisions

- treat RegressProof as the primary project boundary inside this repository
- session automation should report only RegressProof-relevant paths
- treat `regressproof/` as the packaging boundary for future standalone extraction
- committed attribution should compare explicit refs and not assume the working checkout is the compared current state

## Open Questions

- whether the next highest-value step after packaging is a cleaner standalone export polish pass or deeper committed real-repo attribution validation

## Next Best Step

- apply the new committed compare-ref path to a meaningful historical range inside the main repository
