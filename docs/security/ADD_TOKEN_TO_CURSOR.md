# 🔑 Как добавить GitHub токен в Cursor IDE

## 📋 Способы добавления токена

### Способ 1: Автоматический запрос (самый простой)

Cursor обычно **сам запрашивает** токен при первом использовании GitHub функций:

1. ✅ Вы создали токен на GitHub (скопировали его)
2. ✅ Cursor показывает всплывающее окно с запросом токена
3. ✅ Вставьте токен в поле
4. ✅ Нажмите **Save** или **Authenticate**
5. ✅ Готово!

---

### Способ 2: Через настройки Cursor

Если Cursor не запросил токен автоматически:

#### Вариант A: Через Command Palette

1. Нажмите `Cmd + Shift + P` (Mac) или `Ctrl + Shift + P` (Windows/Linux)
2. Введите: `GitHub: Sign in` или `GitHub: Set Personal Access Token`
3. Выберите команду из списка
4. Вставьте ваш токен
5. Нажмите **Enter**

#### Вариант B: Через Settings

1. Нажмите `Cmd + ,` (Mac) или `Ctrl + ,` (Windows/Linux) — откроются настройки
2. В поиске настроек введите: `github token` или `github authentication`
3. Найдите поле для GitHub Personal Access Token
4. Вставьте ваш токен
5. Настройки сохранятся автоматически

---

### Способ 3: Через Git Credential Helper (автоматически)

Cursor может использовать токен через git:

1. ✅ Токен уже добавлен в git credential helper (macOS Keychain)
2. ✅ Cursor автоматически использует его
3. ✅ Дополнительная настройка не требуется

**Как проверить:**
```bash
git config --global credential.helper
```

Если выводит `osxkeychain` — все настроено! ✅

---

### Способ 4: Через терминал (если нужно)

Если нужно добавить токен для git напрямую:

```bash
# При следующем git push/pull вас спросят логин и пароль
# Вместо пароля вставьте ваш токен
# Git сохранит его в Keychain автоматически
```

---

## 🔍 Как проверить, что токен работает

### Тест 1: Через терминал

```bash
cd ~/Desktop/rork-kiku
git fetch origin
```

Если команда выполняется **без ошибок аутентификации** — токен работает! ✅

### Тест 2: Через Cursor

1. Попробуйте сделать коммит через Cursor:
   - Откройте Source Control (иконка ветки слева)
   - Сделайте изменения в файле
   - Напишите commit message
   - Нажмите Commit

2. Если коммит проходит — токен работает! ✅

### Тест 3: Проверить на GitHub

1. GitHub.com → Settings → Developer settings → Personal access tokens
2. Найдите ваш токен
3. Проверьте **Last used** — должно быть время недавнего использования

---

## ⚠️ Если токен не работает

### Проблема 1: "Authentication failed"

**Решение:**
1. Проверьте, что токен скопирован полностью (начинается с `ghp_`)
2. Убедитесь, что токен не истек
3. Проверьте, что токен имеет права `repo`
4. Создайте новый токен и попробуйте снова

### Проблема 2: Cursor не запрашивает токен

**Решение:**
1. Попробуйте способ 2 (через настройки)
2. Перезапустите Cursor
3. Попробуйте сделать git операцию (commit/push) — Cursor должен запросить

### Проблема 3: Токен работает в терминале, но не в Cursor

**Решение:**
1. Добавьте токен через настройки Cursor (способ 2)
2. Перезапустите Cursor
3. Проверьте, что используется правильный токен

---

## 📝 Пошаговая инструкция (для начинающих)

### Шаг 1: Создайте токен на GitHub

1. GitHub.com → Settings → Developer settings → Personal access tokens
2. Generate new token (classic)
3. Настройте токен (см. [MANAGE_GITHUB_TOKENS.md](MANAGE_GITHUB_TOKENS.md))
4. **Скопируйте токен** (показывается один раз!)

### Шаг 2: Добавьте токен в Cursor

**Вариант A (рекомендуется):**
- Подождите, пока Cursor запросит токен автоматически
- Вставьте токен в появившееся окно

**Вариант B (если не запросил):**
- `Cmd + Shift + P` → `GitHub: Sign in`
- Вставьте токен

### Шаг 3: Проверьте работу

```bash
cd ~/Desktop/rork-kiku
git fetch origin
```

Если без ошибок — готово! ✅

---

## 🎯 Краткая памятка

```
1. Создать токен на GitHub → Скопировать
2. Cursor запросит токен → Вставить
   (Или: Cmd+Shift+P → "GitHub: Sign in" → Вставить)
3. Проверить: git fetch origin (должно работать)
4. Готово! ✅
```

---

## 💡 Полезные команды

### Проверить сохраненные credentials:

```bash
# macOS Keychain
security find-generic-password -s github.com 2>&1 | head -5

# Git config
git config --global credential.helper
```

### Очистить старые credentials (если нужно):

```bash
# macOS Keychain
git credential-osxkeychain erase
host=github.com
protocol=https
# Нажмите Enter дважды
```

---

**Нужна помощь?** Следуйте инструкциям выше. Самый простой способ — дождаться, пока Cursor сам запросит токен при первом использовании GitHub функций! 😊



