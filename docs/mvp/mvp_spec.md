# MVP Спецификация Rork-Kiku

## Обзор MVP

**Цель MVP**: Запустить iOS приложение в TestFlight для пилотного тестирования с ограниченной группой пользователей (50-100 семей) для валидации ключевых гипотез продукта.

**Временные рамки**: Q1-Q2 2026  
**Платформа**: iOS (TestFlight)  
**Целевая аудитория пилота**: Родители детей 6-12 лет в США

## Критические функции для MVP

### 1. Верификация родителя (Parent Verification)

**Обязательно для MVP**:
- ✅ Регистрация через Apple Sign In
- ✅ Базовая информация: имя, email, номер телефона
- ✅ Parental consent flow (согласие с ToS и Privacy Policy)
- ✅ Email верификация

**Опционально для MVP** (можно добавить в пилоте):
- 🔄 Верификация через платежную карту (малый платеж ~$1)
- 🔄 Документальная верификация (загрузка ID)
- 🔄 School/NGO verification codes

**User Flow**:
1. Родитель скачивает приложение из TestFlight
2. Нажимает "Sign in with Apple"
3. Подтверждает email адрес (6-значный код)
4. Принимает ToS и Privacy Policy
5. Создает профиль (имя, фото профиля опционально)
6. Переходит к созданию профиля ребёнка

### 2. Профиль ребёнка (Child Profile)

**Обязательно для MVP**:
- ✅ Создание профиля ребёнка с ограничениями по возрасту (6-12 лет)
- ✅ Имя ребёнка, дата рождения, аватар (опционально)
- ✅ Связь родитель-ребёнок (один родитель может иметь несколько детей)
- ✅ Настройки приватности профиля (кто может видеть профиль)
- ✅ Parental controls: уровни модерации (строгая/средняя/мягкая)

**User Flow**:
1. Родитель нажимает "Добавить ребёнка"
2. Вводит имя, дату рождения (возраст валидируется: 6-12 лет)
3. Выбирает уровень модерации (по умолчанию: строгая)
4. Опционально загружает аватар
5. Профиль создан → родитель видит главный экран приложения

### 3. Кураторный контент (Curated Content)

**Обязательно для MVP**:
- ✅ Библиотека образовательного контента (статьи, видео, игры)
- ✅ Категории: наука, искусство, спорт, книги, музыка
- ✅ Возрастные рекомендации (6-8, 9-12)
- ✅ Родительские рекомендации (почему этот контент полезен)
- ✅ Избранное (родитель/ребёнок может сохранять)

**Контент для MVP** (минимальный набор):
- 20-30 статей
- 10-15 видео (YouTube embeds или собственный hosting)
- 5-10 интерактивных активностей

**User Flow**:
1. Ребёнок/родитель открывает "Исследовать" tab
2. Видит категории контента
3. Выбирает категорию → список контента
4. Открывает контент → просмотр
5. Может добавить в избранное

### 4. Отправка и приём медиа (Media Sharing)

**Обязательно для MVP**:
- ✅ Отправка текстовых сообщений (родитель ↔ ребёнок)
- ✅ Отправка фото (родитель ↔ ребёнок)
- ✅ Галерея медиа (все отправленные/полученные фото)
- ✅ Уведомления о новых сообщениях (push notifications)

**Опционально для MVP**:
- 🔄 Видео (можно добавить позже, если модерация работает хорошо)
- 🔄 Голосовые сообщения
- 🔄 Стикеры/эмодзи реакции

**Ограничения для MVP**:
- Максимум 10 MB на фото
- Только JPEG/PNG форматы
- Нет групповых чатов (только 1:1 родитель-ребёнок)

**User Flow**:
1. Ребёнок открывает чат с родителем
2. Печатает текст или выбирает фото из галереи
3. Нажимает "Отправить"
4. Контент отправляется на модерацию
5. После модерации → доставляется родителю
6. Родитель получает push notification

### 5. Базовая фильтрация текста и медиа (Content Moderation)

**Текстовая модерация**:
- ✅ Blacklist ненормативной лексики (русский + английский)
- ✅ Sentiment analysis (детектирование негатива, угроз)
- ✅ PII detection (номера телефонов, адреса, email)

**Модерация изображений**:
- ✅ NSFW detection (Google Vision API или custom model)
- ✅ Violence/gore detection
- ✅ Face detection (проверка наличия лиц)

**Логика модерации для MVP**:
1. **Автоматическое одобрение** (score > 90%): контент доставляется мгновенно
2. **Ручная модерация** (score 50-90%): контент отправляется модератору
3. **Автоблокировка** (score < 50%): контент блокируется, уведомление родителю

**Latency для MVP**:
- Текст: < 2 секунды
- Изображения: < 10 секунд
- Ручная модерация: < 2 часа (в рабочее время)

**User Flow**:
1. Пользователь отправляет контент
2. Контент попадает в очередь модерации
3. ML-модель анализирует контент
4. Если безопасно → доставляется получателю
5. Если требуется ручная модерация → ждет модератора
6. Пользователь видит статус "Проверяется..."

### 6. Родительская панель (Parent Dashboard)

**Обязательно для MVP**:
- ✅ Просмотр всех сообщений ребёнка (отправленные/полученные)
- ✅ История активности ребёнка (время в приложении, контент)
- ✅ Настройки модерации (изменение уровня)
- ✅ Управление профилем ребёнка (редактирование, удаление)
- ✅ Блокировка/разблокировка контента
- ✅ Отчеты о модерации (что было заблокировано и почему)

**Опционально**:
- 🔄 Экспорт данных (GDPR)
- 🔄 Аналитика использования (графики, charts)
- 🔄 Рекомендации контента на основе интересов ребёнка

**User Flow**:
1. Родитель открывает "Родительская панель" tab
2. Видит список детей
3. Выбирает ребёнка → видит его активность
4. Может изменить настройки модерации
5. Может просмотреть отчеты о заблокированном контенте

### 7. Аналитика и логирование (Analytics & Logging)

**Обязательно для MVP**:
- ✅ Логирование всех событий (регистрация, отправка медиа, модерация)
- ✅ Crash reporting (Sentry или Firebase Crashlytics)
- ✅ Basic analytics (Firebase Analytics или Mixpanel)
- ✅ Retention tracking (DAU, WAU, MAU)

**Ключевые метрики для отслеживания в MVP**:
- User registrations (родители)
- Child profiles created
- Messages sent (text + images)
- Moderation actions (auto-approved, manual review, blocked)
- Time in app (parent vs child)
- Content views (curated content)
- Retention (day 1, day 7, day 30)

## User Flows для ключевых сценариев

### Flow 1: Onboarding нового родителя

```
1. Скачать приложение из TestFlight
   ↓
2. Открыть приложение → экран приветствия
   ↓
3. "Войти через Apple" → Apple Sign In flow
   ↓
4. Подтверждение email (6-значный код)
   ↓
5. Принятие ToS и Privacy Policy
   ↓
6. Создание профиля родителя (имя, фото)
   ↓
7. "Добавить ребёнка" → создание профиля ребёнка
   ↓
8. Welcome tour (3-5 экранов с объяснением функций)
   ↓
9. Главный экран приложения
```

### Flow 2: Ребёнок отправляет фото родителю

```
1. Ребёнок открывает чат с родителем
   ↓
2. Нажимает кнопку "+" → выбирает "Фото"
   ↓
3. Выбирает фото из галереи или делает новое
   ↓
4. Опционально добавляет текст
   ↓
5. Нажимает "Отправить"
   ↓
6. Фото загружается на сервер
   ↓
7. ML-модель анализирует фото (< 10 сек)
   ↓
8. Если безопасно → доставляется родителю + push notification
   ↓
9. Родитель видит фото в чате
```

### Flow 3: Родитель проверяет активность ребёнка

```
1. Родитель открывает "Родительская панель" tab
   ↓
2. Видит список детей
   ↓
3. Выбирает ребёнка
   ↓
4. Видит:
   - Время в приложении сегодня
   - Последние сообщения
   - Просмотренный контент
   - Заблокированный контент (если есть)
   ↓
5. Может изменить настройки модерации
   ↓
6. Может просмотреть детали каждого сообщения
```

## API Contract (черновой)

### Authentication

**POST /api/auth/register**
```json
Request:
{
  "appleIdToken": "string",
  "email": "string",
  "name": "string"
}

Response:
{
  "accessToken": "string",
  "refreshToken": "string",
  "userId": "string"
}
```

**POST /api/auth/verify-email**
```json
Request:
{
  "userId": "string",
  "code": "string"
}

Response:
{
  "verified": true
}
```

### User Profile

**POST /api/users/children**
```json
Request:
{
  "name": "string",
  "birthDate": "2015-05-10",
  "avatar": "base64 or URL",
  "moderationLevel": "strict|medium|soft"
}

Response:
{
  "childId": "string",
  "name": "string",
  "age": 11,
  "parentId": "string"
}
```

**GET /api/users/me**
```json
Response:
{
  "userId": "string",
  "email": "string",
  "name": "string",
  "children": [
    {
      "childId": "string",
      "name": "string",
      "age": 11
    }
  ]
}
```

### Content & Messaging

**POST /api/messages**
```json
Request:
{
  "fromUserId": "string",
  "toUserId": "string",
  "type": "text|image",
  "content": "string or presigned URL",
  "metadata": {}
}

Response:
{
  "messageId": "string",
  "status": "pending_moderation|approved|blocked",
  "moderationScore": 0.95
}
```

**GET /api/messages?userId=childId**
```json
Response:
{
  "messages": [
    {
      "messageId": "string",
      "from": "userId",
      "to": "userId",
      "type": "text",
      "content": "Hello!",
      "timestamp": "2026-01-02T12:00:00Z",
      "status": "approved"
    }
  ],
  "pagination": {
    "page": 1,
    "total": 100
  }
}
```

### Curated Content

**GET /api/content/curated?category=science&ageGroup=9-12**
```json
Response:
{
  "content": [
    {
      "contentId": "string",
      "title": "string",
      "description": "string",
      "type": "article|video|activity",
      "category": "science",
      "ageGroup": "9-12",
      "thumbnailUrl": "string",
      "contentUrl": "string"
    }
  ]
}
```

### Moderation

**POST /api/moderation/review**
```json
Request:
{
  "messageId": "string",
  "action": "approve|reject",
  "reason": "string",
  "moderatorId": "string"
}

Response:
{
  "success": true,
  "messageId": "string",
  "newStatus": "approved|blocked"
}
```

**GET /api/moderation/queue?status=pending**
```json
Response:
{
  "queue": [
    {
      "messageId": "string",
      "type": "text|image",
      "content": "...",
      "fromUserId": "string",
      "toUserId": "string",
      "moderationScore": 0.75,
      "timestamp": "2026-01-02T12:00:00Z"
    }
  ]
}
```

### Parent Dashboard

**GET /api/parent/dashboard?childId=childId**
```json
Response:
{
  "child": {
    "childId": "string",
    "name": "string",
    "age": 11
  },
  "activity": {
    "timeInAppToday": 45, // minutes
    "messagesSentToday": 12,
    "contentViewedToday": 5
  },
  "recentMessages": [...],
  "blockedContent": [
    {
      "messageId": "string",
      "reason": "NSFW content detected",
      "timestamp": "2026-01-02T10:00:00Z"
    }
  ]
}
```

## Backend требования для MVP

### Технологический стек

**Backend Framework**:
- Node.js + TypeScript + tRPC (рекомендуется)
- Альтернатива: Go + gRPC

**Database**:
- PostgreSQL (AWS RDS или GCP Cloud SQL)
- Redis (кэш и сессии)

**Object Storage**:
- AWS S3 или GCP Cloud Storage (медиа файлы)

**Hosting**:
- AWS EKS или GCP GKE (Kubernetes)
- Альтернатива: AWS Fargate, GCP Cloud Run (serverless)

**Authentication**:
- Apple Sign In SDK
- JWT tokens (access + refresh)

**Notifications**:
- APNs (Apple Push Notification service)
- Firebase Cloud Messaging (для Android позже)

### Microservices для MVP

**Минимальный набор сервисов**:
1. **Auth Service**: регистрация, аутентификация, токены
2. **User Service**: профили родителей и детей
3. **Message Service**: отправка/получение сообщений, хранение истории
4. **Moderation Service**: очередь модерации, интеграция с ML
5. **Content Service**: кураторный контент, рекомендации
6. **Notification Service**: push notifications

**Можно начать с monolith** и разделить на микросервисы позже, если команда маленькая.

### API Design

- **REST API** или **tRPC** (рекомендуется для TypeScript)
- **Rate Limiting**: 100 requests/minute per user
- **Authentication**: Bearer token (JWT) в headers
- **Versioning**: /api/v1/...
- **Error Handling**: стандартизированные коды ошибок

### Database Schema (упрощенная)

**users table**:
- user_id (PK)
- email
- name
- apple_id
- role (parent|child|moderator|admin)
- created_at
- updated_at

**children table**:
- child_id (PK)
- parent_id (FK → users)
- name
- birth_date
- avatar_url
- moderation_level
- created_at

**messages table**:
- message_id (PK)
- from_user_id (FK → users)
- to_user_id (FK → users)
- type (text|image|video)
- content (text or S3 URL)
- status (pending|approved|blocked)
- moderation_score
- created_at
- delivered_at

**curated_content table**:
- content_id (PK)
- title
- description
- type (article|video|activity)
- category
- age_group
- content_url
- thumbnail_url
- created_at

**moderation_logs table**:
- log_id (PK)
- message_id (FK → messages)
- action (auto_approve|manual_approve|block)
- reason
- moderator_id (FK → users, nullable)
- timestamp

## ML Inference требования для MVP

### Текстовая модерация

**Модели**:
- **Perspective API** (Google) для toxicity detection
- **Custom Blacklist**: список запрещенных слов (русский + английский)
- **Sentiment Analysis**: базовая модель (Hugging Face transformers)

**Latency**: < 2 секунды

**Accuracy target**: 90%+ для критических категорий

### Модерация изображений

**Модели**:
- **Google Vision API**: SafeSearch detection (adult, violence, racy)
- **AWS Rekognition**: альтернатива
- **Custom Model**: опционально (если нужна более точная модерация)

**Latency**: < 10 секунд

**Accuracy target**: 95%+ для NSFW content

### ML Infrastructure

**Hosting**:
- **Serverless**: AWS Lambda + SageMaker endpoint (для начала)
- **Dedicated GPU**: позже, если latency критична

**Model Storage**:
- S3 / GCS для хранения моделей

**Monitoring**:
- MLflow для tracking inference latency и accuracy
- CloudWatch / Cloud Monitoring для алертов

### Fallback стратегия

Если ML inference недоступна (downtime, высокая latency):
1. Все сообщения отправляются на ручную модерацию
2. Уведомление команды о проблеме
3. Пользователи видят "Задержка в проверке, извините"

## Non-functional требования для MVP

### Производительность
- API latency: p95 < 500ms
- Image upload: < 5 секунд для 5 MB файла
- ML inference: < 10 секунд для изображений

### Доступность
- Uptime: 99% (допускается 7 часов downtime в месяц для MVP)

### Безопасность
- HTTPS для всех API
- Шифрование медиа файлов at rest (S3 encryption)
- JWT токены с коротким сроком жизни (30 минут)
- Parental consent для всех действий детей

### Масштабируемость
- Поддержка 100-200 пользователей для MVP
- 1000-2000 сообщений в день

### Мониторинг
- Error tracking: Sentry
- Analytics: Firebase Analytics или Mixpanel
- Logs: CloudWatch или Cloud Logging

## Критерии успеха MVP

### Технические
- ✅ Приложение успешно запущено в TestFlight
- ✅ 0 critical bugs в течение первой недели
- ✅ API uptime > 99%
- ✅ ML moderation accuracy > 90%

### Продуктовые
- ✅ 50+ родителей зарегистрированы
- ✅ 70+ детских профилей создано
- ✅ 1000+ сообщений отправлено
- ✅ Day 7 retention > 40%
- ✅ NPS > 50

### Бизнесовые
- ✅ Валидация гипотез: родители хотят безопасную платформу для детей
- ✅ Положительные отзывы от пилотных пользователей
- ✅ Готовность платить (через опросы)
- ✅ Партнерства с 1-2 школами/НКО

## Out of Scope для MVP

**Не включать в MVP** (можно добавить позже):
- ❌ Android приложение
- ❌ Web версия (кроме admin dashboard)
- ❌ Видео сообщения
- ❌ Групповые чаты
- ❌ Геолокация
- ❌ In-app покупки/подписки
- ❌ Социальные функции (друзья, комментарии)
- ❌ Gamification (баллы, достижения)
- ❌ AI помощник/чат-бот

## Roadmap после MVP

### Phase 1: MVP Pilot (Q1-Q2 2026)
- TestFlight с 50-100 семьями
- Сбор feedback
- Итерация на основе данных

### Phase 2: Extended Pilot (Q3 2026)
- Добавление видео сообщений
- Улучшение ML модерации
- Расширение до 500-1000 пользователей

### Phase 3: Production Beta (Q4 2026)
- Публичный запуск в App Store (iOS)
- Добавление подписочной модели
- Android версия (начало разработки)

### Phase 4: Global Rollout (2027)
- Международная экспансия
- Новые языки
- Партнерства со школами и НКО

---

**Последнее обновление**: 2026-01-02  
**Версия документа**: 1.0 (ЧЕРНОВИК)  
**Статус**: ТРЕБУЕТ ПРОДУКТОВОГО И ТЕХНИЧЕСКОГО РЕВЬЮ
