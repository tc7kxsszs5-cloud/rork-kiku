# 🚀 Деплой после успешного входа

## ✅ Вы успешно авторизовались!

Теперь можно деплоить backend на Vercel.

---

## 📋 Шаг 1: Автоматический деплой

Запустите скрипт:

```bash
cd backend
./auto-deploy.sh
```

Скрипт:
- ✅ Проверит Vercel CLI
- ✅ Выполнит деплой на Vercel
- ✅ Покажет URL вашего backend

---

## 📋 Шаг 2: Настройка Environment Variables

После деплоя настройте переменные окружения:

```bash
./auto-setup.sh
```

Скрипт запросит:
- **DATABASE_URL** - Connection String из Supabase
- **NODE_ENV** - автоматически установится в `production`

---

## 📋 Шаг 3: Получите Connection String из Supabase

1. Идите на https://supabase.com/dashboard
2. Выберите ваш проект (`eznumgsmwvavyunqhxfc`)
3. Settings → Database
4. Найдите "Connection string"
5. Скопируйте строку (начинается с `postgresql://`)

**Важно:** Используйте любой доступный режим:
- Session mode (порт 6543) - предпочтительно
- Transaction mode (порт 6543)
- Pooling mode (порт 6543)
- Или обычный URI (порт 5432)

---

## 📋 Шаг 4: Production деплой

После настройки переменных:

```bash
bunx vercel --prod
```

---

## ✅ Готово!

После этих шагов ваш backend будет работать на Vercel!

---

## 🔍 Проверка работы

После деплоя откройте URL в браузере:
- Должно показать: `{"status": "ok", "message": "API is running"}`

---

## 📋 Если возникнут ошибки

1. Проверьте Function Logs в Vercel Dashboard
2. Убедитесь, что DATABASE_URL правильно настроен
3. Проверьте, что все переменные добавлены для Production
