# 🚀 Как делать деплои KIKU

## 📋 Быстрая справка

### Использование скрипта деплоя:

```bash
# Деплой всего проекта
./scripts/deploy.sh all

# Только backend
./scripts/deploy.sh backend

# Только mobile app
./scripts/deploy.sh mobile

# Через Docker
./scripts/deploy.sh docker
```

---

## 🎯 Три основных способа деплоя

### 1. ⚡ Локальное тестирование (5-10 минут)

**Для быстрого тестирования на вашем компьютере:**

```bash
# Терминал 1: Запустить backend
cd backend
bun install
bun run dev
# Backend будет доступен на http://localhost:3000

# Терминал 2: Запустить mobile app
cd ..
bun install
bun run start
# Отсканировать QR код в Expo Go на телефоне
```

**Когда использовать:**
- ✅ Тестирование новых функций
- ✅ Отладка
- ✅ Разработка

---

### 2. 📱 Деплой Mobile App через EAS (10-15 минут)

**Для сборки приложения и установки на реальные устройства:**

```bash
# 1. Установить EAS CLI (если еще не установлен)
npm install -g eas-cli

# 2. Войти в Expo аккаунт
eas login

# 3. Собрать preview версию (для тестирования)
eas build --profile preview --platform android
# или для iOS:
eas build --profile preview --platform ios

# 4. Собрать production версию (для публикации)
eas build --profile production --platform all
```

**После сборки:**
- Скачайте APK/IPA файл
- Установите на устройство
- Или отправьте в магазины: `eas submit --platform ios` / `eas submit --platform android`

**Требования:**
- Expo аккаунт (бесплатно) - https://expo.dev
- Для iOS: Apple Developer аккаунт ($99/год)
- Для Android: Google Play Developer аккаунт ($25 единоразово)

---

### 3. ☁️ Деплой Backend на Vercel (10 минут)

**Для развертывания backend в облаке:**

```bash
# 1. Установить Vercel CLI (если еще не установлен)
npm i -g vercel

# 2. Перейти в директорию backend
cd backend

# 3. Развернуть на production
vercel --prod

# 4. Настроить environment variables в Vercel Dashboard:
# Перейдите на https://vercel.com/dashboard
# Выберите проект → Settings → Environment Variables
# Добавьте:
# - DATABASE_URL
# - REDIS_URL (если используете)
# - JWT_SECRET
# - и другие переменные из .env
```

**Требования:**
- Vercel аккаунт (бесплатно) - https://vercel.com

---

## 🐳 Деплой через Docker (15 минут)

**Для запуска всей инфраструктуры локально:**

```bash
# Запустить все сервисы одной командой
docker-compose up -d

# Проверить статус
docker-compose ps

# Остановить
docker-compose down
```

**Что запускается:**
- Backend: http://localhost:3000
- PostgreSQL: localhost:5432
- Redis: localhost:6379

**Требования:**
- Docker и Docker Compose установлены

---

## 🔄 Автоматический деплой (CI/CD)

**GitHub Actions автоматически деплоят при push в `main`:**

### Backend:
- Автоматически деплоится на Vercel при изменении файлов в `backend/`

### Mobile App:
- Автоматически собирается preview build
- Автоматически публикуется OTA update

**Настройка (один раз):**

1. Перейдите в GitHub → Settings → Secrets and variables → Actions
2. Добавьте secrets:
   - `VERCEL_TOKEN` - токен из Vercel
   - `VERCEL_ORG_ID` - ID организации Vercel
   - `VERCEL_PROJECT_ID` - ID проекта Vercel
   - `EXPO_TOKEN` - токен из Expo (получить: `eas token:create`)

**Файлы CI/CD:**
- `.github/workflows/deploy-backend.yml`
- `.github/workflows/deploy-mobile.yml`

---

## 📱 Публикация в App Store / Google Play

### iOS (App Store):

```bash
# 1. Собрать production build
eas build --profile production --platform ios

# 2. Отправить в App Store
eas submit --platform ios
```

**Требования:**
- Apple Developer аккаунт ($99/год)
- Настроить `eas.json` с вашими Apple ID данными

### Android (Google Play):

```bash
# 1. Собрать production build
eas build --profile production --platform android

# 2. Отправить в Google Play
eas submit --platform android
```

**Требования:**
- Google Play Developer аккаунт ($25 единоразово)
- Service Account Key (JSON файл) в `eas.json`

---

## 🗄️ Настройка базы данных

### Вариант 1: Supabase (бесплатно)

1. Создайте аккаунт на https://supabase.com
2. Создайте новый проект
3. Скопируйте `DATABASE_URL` из настроек проекта
4. Добавьте в `.env` или в Vercel Environment Variables

### Вариант 2: Railway ($5/мес)

1. Создайте аккаунт на https://railway.app
2. Создайте PostgreSQL базу данных
3. Скопируйте `DATABASE_URL`
4. Добавьте в переменные окружения

### Вариант 3: Neon (бесплатно)

1. Создайте аккаунт на https://neon.tech
2. Создайте проект
3. Скопируйте connection string
4. Добавьте в переменные окружения

---

## 📋 Чеклист для production деплоя

### Перед деплоем:

- [ ] Все тесты проходят: `bun test`
- [ ] TypeScript проверка: `bunx tsc --noEmit`
- [ ] Линтинг: `bun run lint`
- [ ] Environment variables настроены
- [ ] База данных создана и доступна
- [ ] Backend URL обновлен в mobile app

### После деплоя:

- [ ] Проверить работу backend API
- [ ] Проверить подключение mobile app к backend
- [ ] Проверить работу основных функций
- [ ] Настроить мониторинг (Sentry)
- [ ] Настроить аналитику

---

## 🆘 Решение проблем

### Backend не деплоится:

```bash
# Проверить логи Vercel
vercel logs

# Проверить локально
cd backend
bun run dev
```

### Mobile app не собирается:

```bash
# Проверить конфигурацию
eas build:configure

# Проверить статус сборки
eas build:list

# Посмотреть логи
eas build:view [build-id]
```

### Docker не запускается:

```bash
# Проверить статус
docker-compose ps

# Посмотреть логи
docker-compose logs

# Пересобрать
docker-compose up -d --build
```

---

## 📚 Дополнительная документация

- **Полное руководство:** `docs/deployment/DEPLOYMENT_GUIDE.md`
- **Быстрый старт:** `docs/deployment/DEPLOY_NOW.md`
- **Резюме:** `docs/deployment/DEPLOYMENT_SUMMARY.md`

---

## 🎯 Рекомендуемый путь для начала

1. **Сегодня (10 минут):**
   - Локальное тестирование
   - Проверка работы всех функций

2. **На этой неделе (1 час):**
   - Деплой backend на Vercel
   - Настройка базы данных на Supabase
   - Сборка mobile app через EAS Preview

3. **Для production (2-3 дня):**
   - Production build mobile app
   - Отправка в App Store / Google Play
   - Настройка мониторинга и аналитики

---

**Готовы начать? Выберите вариант и следуйте инструкциям! 🚀**
