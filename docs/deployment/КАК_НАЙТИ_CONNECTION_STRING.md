# 🔍 Как найти Connection String в Supabase

## 📋 Где искать Connection String

### Вариант 1: Settings → Database → Connection string

1. Идите в **Settings** (шестеренка ⚙️ слева)
2. Выберите **Database**
3. Прокрутите вниз до секции **"Connection string"**
4. Там могут быть вкладки:
   - **URI** - прямой connection
   - **Session mode** - через pooler (если есть)
   - **Transaction mode** - через pooler (если есть)
   - **Pooling mode** - через pooler (если есть)

### Если нет вкладки "Session mode":

Используйте **"Transaction mode"** или **"Pooling mode"** - они тоже работают!

---

## 📋 Вариант 2: Connection Pooling

1. Settings → Database
2. Найдите секцию **"Connection Pooling"**
3. Там будет connection string с портом **6543** или **5432**

---

## 📋 Вариант 3: Прямой URI (если pooler не работает)

1. Settings → Database
2. Connection string → вкладка **"URI"**
3. Скопируйте connection string
4. Выглядит так:
   ```
   postgresql://postgres:[YOUR-PASSWORD]@db.xxxxx.supabase.co:5432/postgres
   ```
5. Замените `[YOUR-PASSWORD]` на ваш пароль

**⚠️ Внимание:** Этот вариант может не работать на Vercel без IPv4. Если получите ошибку - используйте Pooling.

---

## 📋 Вариант 4: Через Connection Pooling (рекомендуется)

1. Settings → Database
2. Найдите секцию **"Connection Pooling"**
3. Или прокрутите до **"Connection string"**
4. Найдите connection string с:
   - Порт: **6543** (это pooling)
   - Или порт: **5432** (это прямой)

**Используйте тот, где порт 6543** - это pooling, работает везде!

---

## 🔍 Как выглядит правильный Connection String

### Session/Transaction/Pooling mode (порт 6543):
```
postgresql://postgres.xxxxx:[YOUR-PASSWORD]@aws-0-us-west-1.pooler.supabase.com:6543/postgres
```

### Прямой URI (порт 5432):
```
postgresql://postgres:[YOUR-PASSWORD]@db.xxxxx.supabase.co:5432/postgres
```

---

## ✅ Что использовать

**Если есть вкладка с портом 6543:**
- Используйте её (Session/Transaction/Pooling mode)

**Если есть только URI (порт 5432):**
- Попробуйте сначала его
- Если не работает на Vercel - используйте Connection Pooling

---

## 🆘 Если не можете найти

1. **Проверьте, что вы в правильном месте:**
   - Settings → Database (не Settings → API)
   - Прокрутите страницу вниз

2. **Попробуйте поиск на странице:**
   - Нажмите Ctrl+F (или Cmd+F на Mac)
   - Введите "connection"
   - Найдите секцию с connection string

3. **Альтернатива:**
   - Используйте прямой URI (порт 5432)
   - Если не работает - напишите мне, помогу настроить

---

## 📋 Быстрая проверка

Connection string должен содержать:
- `postgresql://`
- Ваш пароль (или `[YOUR-PASSWORD]` для замены)
- Порт: `5432` или `6543`
- Хост: `db.xxxxx.supabase.co` или `pooler.supabase.com`

---

## 🎯 Готово!

Найдите любой connection string из этих вариантов и используйте его!
