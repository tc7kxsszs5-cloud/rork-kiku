# 🤝 Гайд для контрибьюторов

Добро пожаловать! Этот документ поможет вам начать работу над проектом KIKU.

## 📋 Содержание

- [Начало работы](#начало-работы)
- [Структура проекта](#структура-проекта)
- [Code Style](#code-style)
- [Структура тестов](#структура-тестов)
- [Процесс разработки](#процесс-разработки)
- [Коммиты](#коммиты)

## 🚀 Начало работы

### Предварительные требования

- **Node.js:** >= 18.0.0
- **Bun:** Последняя версия (рекомендуется)
- **Git:** Для работы с репозиторием

### Установка

```bash
# Клонирование репозитория
git clone <repository-url>
cd ogg

# Установка зависимостей
bun install

# Запуск приложения
bun run start
```

### Первые шаги

1. Создайте ветку для вашей работы:
   ```bash
   git checkout -b feature/your-feature-name
   ```

2. Внесите изменения

3. Запустите тесты:
   ```bash
   bun run test
   ```

4. Проверьте код:
   ```bash
   bun run lint
   bun run typecheck
   ```

## 📁 Структура проекта

```
.
├── app/                    # Expo Router приложение
│   ├── (tabs)/            # Табы навигации
│   └── _layout.tsx        # Корневой layout
├── components/            # React компоненты
│   ├── settings/         # Компоненты настроек
│   └── ...
├── contexts/              # React контексты
│   ├── AuthContext.tsx
│   └── ...
├── utils/                 # Утилиты
│   ├── aiService.ts
│   └── ...
├── constants/            # Константы
├── backend/              # Backend (tRPC)
│   ├── trpc/            # tRPC роуты
│   └── utils/          # Backend утилиты
├── __tests__/           # Тесты
│   ├── unit/           # Unit тесты
│   ├── integration/    # Integration тесты
│   └── e2e/           # E2E тесты
└── docs/               # Документация
```

## 📝 Code Style

### TypeScript

- Используйте **TypeScript** для всех новых файлов
- Избегайте `any`, используйте конкретные типы
- Используйте интерфейсы для объектов

```typescript
// ✅ Хорошо
interface User {
  id: string;
  name: string;
}

function getUser(id: string): User {
  // ...
}

// ❌ Плохо
function getUser(id: any): any {
  // ...
}
```

### Именование

- **Компоненты:** PascalCase (`UserProfile.tsx`)
- **Функции/переменные:** camelCase (`getUserData`)
- **Константы:** UPPER_SNAKE_CASE (`API_BASE_URL`)
- **Типы/Интерфейсы:** PascalCase (`UserData`)

### Импорты

Используйте алиасы `@/` для абсолютных путей:

```typescript
// ✅ Хорошо
import { User } from '@/types';
import { getUser } from '@/utils/api';

// ❌ Плохо
import { User } from '../../../types';
```

Группируйте импорты:

```typescript
// 1. Внешние библиотеки
import React from 'react';
import { View, Text } from 'react-native';

// 2. Внутренние модули
import { User } from '@/types';
import { getUser } from '@/utils/api';

// 3. Относительные импорты
import { styles } from './styles';
```

### Форматирование

- Используйте **2 пробела** для отступов
- Используйте одинарные кавычки для строк
- Добавляйте точку с запятой в конце строк
- Максимальная длина строки: 100 символов

```typescript
// ✅ Хорошо
const user = {
  id: '123',
  name: 'John',
};

// ❌ Плохо
const user={id:"123",name:"John"}
```

### Компоненты

```typescript
// ✅ Хорошо
interface Props {
  userId: string;
  onPress: () => void;
}

export function UserProfile({ userId, onPress }: Props) {
  return (
    <View>
      <Text>User: {userId}</Text>
    </View>
  );
}

// ❌ Плохо
export function UserProfile(props: any) {
  return <View><Text>User: {props.userId}</Text></View>;
}
```

## 🧪 Структура тестов

### Организация тестов

Тесты должны находиться рядом с тестируемым кодом или в `__tests__/`:

```
utils/
├── aiService.ts
└── __tests__/
    └── aiService.test.ts

# ИЛИ

__tests__/
└── unit/
    └── utils/
        └── aiService.test.ts
```

### Структура тестового файла

```typescript
// 1. Импорты
import { render } from '@testing-library/react-native';
import { Component } from '@/components/Component';

// 2. Моки (если нужны)
jest.mock('expo-constants', () => ({
  default: { expoConfig: { extra: {} } },
}));

// 3. Основной describe блок
describe('Component', () => {
  // 4. beforeEach/afterEach (если нужны)
  beforeEach(() => {
    jest.clearAllMocks();
  });

  // 5. Группы тестов
  describe('метод или функциональность', () => {
    it('должен делать что-то конкретное', () => {
      // Arrange
      const input = 'test';
      
      // Act
      const result = functionToTest(input);
      
      // Assert
      expect(result).toBe('expected');
    });
  });
});
```

### Типы тестов

1. **Unit тесты** - тестируют отдельные функции/компоненты
   ```typescript
   describe('calculateTotal', () => {
     it('должен суммировать числа', () => {
       expect(calculateTotal([1, 2, 3])).toBe(6);
     });
   });
   ```

2. **Integration тесты** - тестируют взаимодействие компонентов
   ```typescript
   describe('AuthFlow', () => {
     it('должен пройти полный цикл регистрации', async () => {
       // тест взаимодействия компонентов
     });
   });
   ```

3. **E2E тесты** - тестируют полные пользовательские сценарии
   ```typescript
   describe('Chat Flow', () => {
     it('должен отправить сообщение и получить ответ', async () => {
       // полный E2E сценарий
     });
   });
   ```

### Best practices для тестов

1. **Один тест = одна проверка**
   ```typescript
   // ✅ Хорошо
   it('должен возвращать true для валидного email', () => {
     expect(isValidEmail('test@example.com')).toBe(true);
   });

   // ❌ Плохо
   it('должен валидировать email', () => {
     expect(isValidEmail('test@example.com')).toBe(true);
     expect(isValidEmail('invalid')).toBe(false);
   });
   ```

2. **Используйте описательные имена**
   ```typescript
   // ✅ Хорошо
   it('должен возвращать ошибку при отсутствии API ключа', () => {
   
   // ❌ Плохо
   it('test error', () => {
   ```

3. **Группируйте связанные тесты**
   ```typescript
   describe('aiService', () => {
     describe('getAIConfig', () => {
       // тесты конфигурации
     });
     describe('analyzeMessage', () => {
       // тесты анализа
     });
   });
   ```

4. **Очищайте состояние между тестами**
   ```typescript
   beforeEach(() => {
     jest.clearAllMocks();
   });
   ```

## 🔄 Процесс разработки

### Workflow

1. **Создайте ветку**
   ```bash
   git checkout -b feature/your-feature-name
   ```

2. **Внесите изменения**
   - Пишите код следуя code style
   - Добавляйте тесты для новой функциональности
   - Обновляйте документацию при необходимости

3. **Проверьте код**
   ```bash
   # Линтинг
   bun run lint
   
   # Проверка типов
   bun run typecheck
   
   # Тесты
   bun run test
   ```

4. **Коммитьте изменения**
   ```bash
   git add .
   git commit -m "feat: добавить новую функциональность"
   ```

5. **Отправьте изменения**
   ```bash
   git push origin feature/your-feature-name
   ```

### Checklist перед коммитом

- [ ] Код следует code style
- [ ] Все тесты проходят (`bun run test`)
- [ ] Нет ошибок линтера (`bun run lint`)
- [ ] Нет ошибок TypeScript (`bun run typecheck`)
- [ ] Добавлены тесты для новой функциональности
- [ ] Обновлена документация (если нужно)

## 📝 Коммиты

### Формат коммитов

Используйте conventional commits:

```
<type>(<scope>): <subject>

<body>

<footer>
```

### Типы коммитов

- `feat:` - новая функциональность
- `fix:` - исправление бага
- `docs:` - изменения в документации
- `style:` - форматирование кода
- `refactor:` - рефакторинг
- `test:` - добавление/изменение тестов
- `chore:` - обновление зависимостей, конфигурации

### Примеры

```bash
# Новая функциональность
git commit -m "feat(auth): добавить биометрическую аутентификацию"

# Исправление бага
git commit -m "fix(sync): исправить синхронизацию при отсутствии сети"

# Тесты
git commit -m "test(aiService): добавить тесты для анализа сообщений"

# Документация
git commit -m "docs: обновить гайд по тестированию"
```

## 🐛 Отчеты об ошибках

При создании issue или PR:

1. **Опишите проблему** четко и подробно
2. **Добавьте шаги для воспроизведения**
3. **Укажите ожидаемое и фактическое поведение**
4. **Добавьте скриншоты** (если применимо)
5. **Укажите версию** Node.js, Bun, и т.д.

## ❓ Вопросы?

Если у вас есть вопросы:

1. Проверьте документацию в `docs/`
2. Посмотрите существующий код
3. Создайте issue с вопросом

## 🙏 Спасибо!

Спасибо за ваш вклад в проект KIKU! 🎉
