# 🚀 Быстрый запуск приложения

## ✅ Вариант 1: Expo Go (РЕКОМЕНДУЕТСЯ!)

**Это самый простой способ - работает сразу, обходит проблему с кешем!**

### Шаги:

1. **Установите Expo Go на iPhone/iPad:**
   - Откройте App Store на телефоне
   - Найдите и установите **"Expo Go"**
   - [Прямая ссылка для iOS](https://apps.apple.com/app/expo-go/id982107779)

2. **Запустите dev сервер на Mac:**

   **Если ошибка с ngrok tunnel, используйте:**
   ```bash
   cd /Users/mac/Desktop/rork-kiku
   bun run start:expo
   ```

   Или альтернативно:
   ```bash
   bunx expo start
   ```

3. **Отсканируйте QR-код:**
   - В терминале появится QR-код
   - Откройте приложение **Expo Go** на телефоне
   - Нажмите **"Scan QR code"**
   - Наведите камеру на QR-код в терминале
   - Приложение загрузится автоматически!

**Готово!** ✅ Приложение работает на вашем телефоне.

---

## ✅ Вариант 2: iOS Симулятор через Xcode 15.2

**Требует исправления ошибки AnalyticsProvider сначала!**

### Шаг 1: Очистка кеша (важно!)

```bash
cd /Users/mac/Desktop/rork-kiku

# Остановить все процессы
killall node

# Очистить кеш
rm -rf node_modules/.cache .expo .metro .rork .expo-shared ~/.expo ~/.metro

# Перезагрузить Mac (ОБЯЗАТЕЛЬНО!)
```

### Шаг 2: После перезагрузки Mac

```bash
cd /Users/mac/Desktop/rork-kiku

# Запустить с очисткой кеша
bunx expo start --clear --ios
```

Или:
```bash
bun run start
# Нажмите 'i' в терминале для запуска в симуляторе
```

---

## ✅ Вариант 3: Запуск через Xcode напрямую (Advanced)

**Только после исправления ошибки AnalyticsProvider!**

### Шаг 1: Создать нативный iOS проект

```bash
cd /Users/mac/Desktop/rork-kiku

# Создать iOS проект (первый раз)
npx expo prebuild --platform ios --clean
```

### Шаг 2: Открыть в Xcode

```bash
open ios/rork-kiku.xcworkspace
# Или если нет workspace:
open ios/rork-kiku.xcodeproj
```

### Шаг 3: Запустить в Xcode

1. В Xcode выберите симулятор в верхней панели (например, iPhone 14)
2. Нажмите кнопку **"Play"** (▶️) или `Cmd + R`
3. Дождитесь сборки и запуска

---

## 🎯 Какой вариант выбрать?

| Вариант | Сложность | Работает сейчас? | Рекомендация |
|---------|-----------|------------------|--------------|
| **Expo Go** | ⭐ Легко | ✅ **ДА!** | **✅ ИСПОЛЬЗУЙТЕ ЭТОТ!** |
| Симулятор (Expo) | ⭐⭐ Средне | ❌ Нужна очистка кеша | После перезагрузки Mac |
| Xcode напрямую | ⭐⭐⭐ Сложно | ❌ Нужна сборка | Для продвинутых |

---

## 💡 Рекомендация

**Используйте Expo Go (Вариант 1)** - это:
- ✅ Работает сразу, без проблем
- ✅ Не требует исправления ошибок
- ✅ Тестирование на реальном устройстве
- ✅ Все функции доступны

---

## 📱 Ссылки для скачивания Expo Go

- **iOS:** [App Store - Expo Go](https://apps.apple.com/app/expo-go/id982107779)
- **Android:** [Google Play - Expo Go](https://play.google.com/store/apps/details?id=host.exp.exponent)
