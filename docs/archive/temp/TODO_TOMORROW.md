# Задачи на завтра: Завершение новой модели профиля (Родитель + Дети)

## 🎯 Важная информация:
- **Возраст детей: 5-14/15 лет**
- Интерфейс должен быть адаптивным для этого диапазона
- Для младших (5-8 лет): очень простой, крупные элементы
- Для старших (9-15 лет): более функциональный, но все еще дружелюбный

## ✅ Что уже сделано:

1. **UserContext обновлен:**
   - Новая структура User (всегда родитель, массив children)
   - Добавлен интерфейс ChildProfile
   - Методы: addChild, removeChild, setActiveChild
   - Миграция старых данных
   - activeChild для текущего выбранного ребенка

## 📋 Что нужно сделать:

### 1. Обновить ProfileScreen (`app/(tabs)/profile.tsx`)
   - Убрать использование `isChild`, `isParent`, `role`
   - Показывать родительский профиль + список детей
   - Добавить интерфейс для добавления ребенка
   - При выборе ребенка (setActiveChild) показывать детский интерфейс
   - Кнопка переключения между просмотром родителя и ребенка

### 2. Обновить role-selection.tsx (`app/role-selection.tsx`)
   - Упростить: теперь всегда создаем родителя
   - Убрать выбор роли
   - Или сделать так, чтобы сразу после создания родителя можно было добавить ребенка

### 3. Обновить другие файлы, использующие `isParent`/`isChild`:
   - Проверить security-settings.tsx
   - Проверить другие компоненты

### 4. Создать интерфейс добавления ребенка:
   - Модальное окно или секция в профиле
   - Поля: имя, возраст (опционально)
   - Сохранение через addChild

### 5. Детский интерфейс профиля:
   - Показывать когда activeChildId установлен
   - Красивый детский дизайн (уже создан, нужно адаптировать)

## 🎯 Логика работы:

1. Пользователь всегда родитель
2. Родитель создает профиль (имя, email)
3. Родитель добавляет детей через интерфейс
4. В профиле показывается:
   - Информация о родителе
   - Список детей
   - Кнопка "Добавить ребенка"
   - Переключение между просмотром профиля родителя и ребенка
5. Когда выбран ребенок (activeChildId) - показывать детский интерфейс
6. Когда не выбран ребенок - показывать родительский интерфейс

## 📝 Файлы для обновления:

- `app/(tabs)/profile.tsx` - основной экран профиля
- `app/role-selection.tsx` - выбор роли (упростить)
- `constants/UserContext.tsx` - ✅ уже обновлен
- Проверить другие файлы на использование старой модели

## 🔄 Обратная совместимость:

- Миграция старых данных уже добавлена
- Старые профили с role будут автоматически конвертированы
