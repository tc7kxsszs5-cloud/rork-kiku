# 📋 План разработки: Система Родитель-Ребенок

## 🎯 Цель проекта

Создать безопасную платформу для общения детей (4-15 лет) с полным родительским контролем, мониторингом и соответствием законам США (COPPA, CIPA, законы Техаса).

---

## 📊 Версия 1.0 - MVP (Minimum Viable Product)

### Фаза 1: Аутентификация и верификация (2-3 недели)

#### 1.1 Регистрация родителя
- ✅ Email верификация (обязательно)
- ✅ Создание PIN-кода для безопасности
- ✅ Подтверждение возраста (18+)
- ✅ Согласие с COPPA и политикой конфиденциальности
- ✅ Сохранение в БД: `parents` таблица

#### 1.2 Регистрация ребенка
- ✅ Связь с родителем (parent_id)
- ✅ Email опционально (если есть)
- ✅ Возраст: 4-15 лет (валидация)
- ✅ Создание device_id для устройства ребенка
- ✅ Автоматическая связь с родителем
- ✅ Сохранение в БД: `children` таблица

#### 1.3 Система опекунов (Guardians)
- ✅ Опекун может зарегистрировать ребенка
- ✅ Опекун подтверждает принадлежность (документы/верификация)
- ✅ Опекун имеет те же права что и родитель
- ✅ Один ребенок может иметь несколько опекунов
- ✅ Сохранение в БД: `guardians` таблица

---

### Фаза 2: Мониторинг и безопасность (3-4 недели)

#### 2.1 100% мониторинг общения
- ✅ Все сообщения сохраняются в БД
- ✅ Родитель видит ВСЕ чаты ребенка
- ✅ Ребенок НЕ может скрыть чаты от родителя
- ✅ История сообщений доступна родителю
- ✅ Real-time синхронизация между устройствами

#### 2.2 AI мониторинг контента
- ✅ Анализ всех текстовых сообщений
- ✅ Анализ всех изображений
- ✅ Проверка всех ссылок (URL scanning)
- ✅ Обнаружение опасного контента
- ✅ Блокировка небезопасных материалов
- ✅ Уведомления родителю при обнаружении рисков

#### 2.3 Рекомендуемые AI сервисы:
1. **OpenAI Moderation API** - базовая модерация
2. **Google Cloud Vision API** - анализ изображений
3. **AWS Rekognition** - детекция контента
4. **URLVoid / VirusTotal API** - проверка ссылок
5. **Perspective API (Google)** - токсичность текста

**Безопасные для детей:**
- ✅ Не сохраняют данные детей
- ✅ Соответствуют COPPA
- ✅ Работают через API без прямого доступа к данным

---

### Фаза 3: Интерфейс и UX (2-3 недели)

#### 3.1 Возрастные версии интерфейса

**Версия для 4-7 лет:**
- ✅ Большие иконки
- ✅ Простые цвета
- ✅ Голосовые сообщения (приоритет)
- ✅ Эмодзи вместо текста
- ✅ Минимум текста

**Версия для 8-11 лет:**
- ✅ Средние иконки
- ✅ Простой текст
- ✅ Комбинация голоса и текста
- ✅ Игровые элементы

**Версия для 12-15 лет:**
- ✅ Стандартный интерфейс
- ✅ Больше функций
- ✅ Групповые чаты
- ✅ Медиа обмен

#### 3.2 Индикатор активности родителя
- ✅ Зеленый индикатор "Родитель онлайн"
- ✅ Показывается в чате ребенка
- ✅ Real-time обновление статуса
- ✅ Уведомление ребенку: "Мама/Папа рядом"

---

### Фаза 4: Групповые чаты и учебные программы (2-3 недели)

#### 4.1 Групповые чаты
- ✅ Школьные группы
- ✅ Учебные программы
- ✅ Родитель может участвовать
- ✅ Родитель видит все сообщения
- ✅ Модерация группы учителем/родителем

#### 4.2 Интеграция с учебными заведениями
- ✅ Регистрация школы/программы
- ✅ Связь ребенка с программой
- ✅ Родитель видит прогресс обучения
- ✅ Рекомендации программ для ребенка
- ✅ Отслеживание участия в программах

---

## 🗄️ Структура базы данных

### Новые таблицы:

```sql
-- Родители
CREATE TABLE parents (
  id TEXT PRIMARY KEY,
  email TEXT UNIQUE NOT NULL,
  email_verified BOOLEAN DEFAULT false,
  name TEXT NOT NULL,
  phone TEXT,
  pin_hash TEXT,
  created_at BIGINT NOT NULL,
  country TEXT DEFAULT 'US',
  state TEXT, -- Для соответствия законам штата
  coppa_consent BOOLEAN DEFAULT false,
  coppa_consent_date BIGINT
);

-- Дети
CREATE TABLE children (
  id TEXT PRIMARY KEY,
  parent_id TEXT REFERENCES parents(id),
  name TEXT NOT NULL,
  age INTEGER NOT NULL CHECK (age >= 4 AND age <= 15),
  email TEXT, -- Опционально
  email_verified BOOLEAN DEFAULT false,
  device_id TEXT UNIQUE NOT NULL,
  ui_version TEXT DEFAULT 'standard', -- 'young', 'middle', 'standard'
  created_at BIGINT NOT NULL,
  is_active BOOLEAN DEFAULT true
);

-- Опекуны
CREATE TABLE guardians (
  id TEXT PRIMARY KEY,
  email TEXT UNIQUE NOT NULL,
  email_verified BOOLEAN DEFAULT false,
  name TEXT NOT NULL,
  phone TEXT,
  verification_status TEXT DEFAULT 'pending', -- 'pending', 'verified', 'rejected'
  verification_documents JSONB,
  created_at BIGINT NOT NULL
);

-- Связь опекунов с детьми
CREATE TABLE guardian_children (
  guardian_id TEXT REFERENCES guardians(id),
  child_id TEXT REFERENCES children(id),
  relationship TEXT, -- 'legal_guardian', 'temporary', etc.
  verified BOOLEAN DEFAULT false,
  verified_at BIGINT,
  PRIMARY KEY (guardian_id, child_id)
);

-- Активность родителей (для индикатора)
CREATE TABLE parent_activity (
  parent_id TEXT REFERENCES parents(id),
  child_id TEXT REFERENCES children(id),
  is_online BOOLEAN DEFAULT false,
  last_seen BIGINT NOT NULL,
  device_info JSONB,
  PRIMARY KEY (parent_id, child_id)
);

-- Учебные программы
CREATE TABLE educational_programs (
  id TEXT PRIMARY KEY,
  name TEXT NOT NULL,
  institution_id TEXT,
  description TEXT,
  age_range_min INTEGER,
  age_range_max INTEGER,
  created_at BIGINT NOT NULL
);

-- Связь детей с программами
CREATE TABLE child_programs (
  child_id TEXT REFERENCES children(id),
  program_id TEXT REFERENCES educational_programs(id),
  enrolled_at BIGINT NOT NULL,
  progress JSONB,
  parent_approved BOOLEAN DEFAULT false,
  PRIMARY KEY (child_id, program_id)
);

-- Мониторинг ссылок и материалов
CREATE TABLE content_monitoring (
  id TEXT PRIMARY KEY,
  child_id TEXT REFERENCES children(id),
  chat_id TEXT,
  message_id TEXT,
  content_type TEXT, -- 'url', 'image', 'text', 'file'
  content_url TEXT,
  ai_analysis JSONB,
  risk_level TEXT,
  blocked BOOLEAN DEFAULT false,
  parent_notified BOOLEAN DEFAULT false,
  created_at BIGINT NOT NULL
);
```

---

## 🔐 Соответствие законам США/Техаса

### COPPA (Children's Online Privacy Protection Act)
- ✅ Родительское согласие перед регистрацией
- ✅ Минимальный сбор данных
- ✅ Родитель может удалить данные ребенка
- ✅ Прозрачная политика конфиденциальности
- ✅ Безопасное хранение данных

### CIPA (Children's Internet Protection Act)
- ✅ Фильтрация небезопасного контента
- ✅ Мониторинг онлайн активности
- ✅ Блокировка опасных материалов

### Законы Техаса
- ✅ Защита данных детей
- ✅ Ограничение доступа к нежелательному контенту
- ✅ Родительский контроль обязателен
- ✅ Прозрачность для родителей

---

## 📱 API Endpoints (новые)

### Аутентификация
```
POST /api/trpc/auth.registerParent
POST /api/trpc/auth.verifyParentEmail
POST /api/trpc/auth.registerChild
POST /api/trpc/auth.registerGuardian
POST /api/trpc/auth.verifyGuardian
```

### Мониторинг
```
GET /api/trpc/monitoring.getChildChats
GET /api/trpc/monitoring.getChildMessages
GET /api/trpc/monitoring.getContentAnalysis
POST /api/trpc/monitoring.reportContent
```

### Активность
```
POST /api/trpc/activity.updateParentStatus
GET /api/trpc/activity.getParentStatus
```

### Учебные программы
```
GET /api/trpc/education.getPrograms
POST /api/trpc/education.enrollChild
GET /api/trpc/education.getChildProgress
```

---

## 🎨 UI/UX Концепция

### Экран ребенка:
```
┌─────────────────────────────┐
│  [👤 Ребенок]  [⚙️ Настройки]│
├─────────────────────────────┤
│                             │
│  💬 Чаты                    │
│  ┌─────────────────────┐   │
│  │ 👨‍👩‍👧 Мама (🟢 онлайн) │   │
│  │ Последнее: Привет!   │   │
│  └─────────────────────┘   │
│                             │
│  ┌─────────────────────┐   │
│  │ 🏫 Школа            │   │
│  │ 5 новых сообщений   │   │
│  └─────────────────────┘   │
│                             │
│  [🟢 Мама рядом]            │
│                             │
└─────────────────────────────┘
```

### Экран родителя:
```
┌─────────────────────────────┐
│  [👤 Родитель]  [📊 Мониторинг]│
├─────────────────────────────┤
│                             │
│  👶 Дети                    │
│  ┌─────────────────────┐   │
│  │ Иван (8 лет)        │   │
│  │ 🟢 Активен          │   │
│  │ 3 новых сообщения   │   │
│  └─────────────────────┘   │
│                             │
│  ⚠️ Алерты                  │
│  ┌─────────────────────┐   │
│  │ Обнаружена ссылка   │   │
│  │ Средний риск        │   │
│  └─────────────────────┘   │
│                             │
└─────────────────────────────┘
```

---

## 🚀 План разработки по этапам

### Этап 1: Базовая структура (Неделя 1-2)
- [ ] Создать таблицы в БД
- [ ] Реализовать регистрацию родителя
- [ ] Реализовать регистрацию ребенка
- [ ] Email верификация

### Этап 2: Мониторинг (Неделя 3-4)
- [ ] Интеграция AI модерации
- [ ] Проверка ссылок
- [ ] Анализ изображений
- [ ] Уведомления родителю

### Этап 3: Интерфейс (Неделя 5-6)
- [ ] Возрастные версии UI
- [ ] Индикатор активности родителя
- [ ] Адаптация под возраст

### Этап 4: Группы и программы (Неделя 7-8)
- [ ] Групповые чаты
- [ ] Интеграция с учебными программами
- [ ] Отслеживание прогресса

### Этап 5: Тестирование и соответствие (Неделя 9-10)
- [ ] Тестирование безопасности
- [ ] Проверка соответствия COPPA/CIPA
- [ ] Оптимизация производительности

---

## 📈 Метрики успеха

- ✅ 100% сообщений мониторится
- ✅ 0% чатов скрыто от родителя
- ✅ <1 секунда задержка уведомлений
- ✅ 99.9% точность AI модерации
- ✅ Соответствие всем законам США

---

## 🔄 Следующие шаги

1. Создать SQL схему для новых таблиц
2. Реализовать API endpoints
3. Интегрировать AI сервисы
4. Создать UI компоненты
5. Тестирование и оптимизация
