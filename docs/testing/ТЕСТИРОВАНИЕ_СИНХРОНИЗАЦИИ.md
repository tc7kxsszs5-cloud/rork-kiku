# 🧪 Тестирование синхронизации данных

## 📋 Команды для тестирования:

### 1. Тест синхронизации чатов

**Отправка чатов на сервер:**
```bash
curl -X POST "https://backend-three-mauve-67.vercel.app/api/trpc/sync.chats.sync" \
  -H "Content-Type: application/json" \
  -d '{
    "deviceId": "test-device-123",
    "chats": [
      {
        "id": "chat-1",
        "contactName": "Иван",
        "contactPhone": "+79001234567",
        "lastMessage": "Привет!",
        "lastMessageTimestamp": 1706123456789,
        "riskLevel": "safe",
        "unreadCount": 0,
        "createdAt": 1706123456789,
        "updatedAt": 1706123456789,
        "messages": [
          {
            "id": "msg-1",
            "sender": "Иван",
            "content": "Привет!",
            "timestamp": 1706123456789,
            "messageType": "text"
          }
        ]
      }
    ]
  }'
```

**Получение чатов с сервера:**
```bash
curl "https://backend-three-mauve-67.vercel.app/api/trpc/sync.chats.get?input=%7B%22deviceId%22%3A%22test-device-123%22%7D"
```

---

### 2. Тест синхронизации алертов

**Отправка алертов на сервер:**
```bash
curl -X POST "https://backend-three-mauve-67.vercel.app/api/trpc/sync.alerts.sync" \
  -H "Content-Type: application/json" \
  -d '{
    "deviceId": "test-device-123",
    "alerts": [
      {
        "id": "alert-1",
        "alertType": "risk_message",
        "severity": "high",
        "title": "Обнаружено рискованное сообщение",
        "description": "Сообщение содержит небезопасный контент",
        "riskLevel": "high",
        "status": "active",
        "createdAt": 1706123456789
      }
    ]
  }'
```

**Получение алертов с сервера:**
```bash
curl "https://backend-three-mauve-67.vercel.app/api/trpc/sync.alerts.get?input=%7B%22deviceId%22%3A%22test-device-123%22%7D"
```

---

### 3. Проверка данных в Supabase Dashboard

1. Откройте https://supabase.com/dashboard
2. Выберите проект
3. **Table Editor**
4. Проверьте таблицы:
   - `chats` - должны быть сохраненные чаты
   - `messages` - должны быть сохраненные сообщения
   - `alerts` - должны быть сохраненные алерты
   - `sync_status` - должен быть статус синхронизации

---

## ✅ Ожидаемые результаты:

- ✅ Данные успешно сохраняются в базу данных
- ✅ Данные успешно загружаются из базы данных
- ✅ Синхронизация работает корректно
- ✅ Данные видны в Supabase Dashboard

---

**Используйте эти команды для тестирования завтра!**
