# Глубокие тесты и развертывание

## 🧪 Доступные тесты

### 1. Базовый тест (`test:startup`)
Проверяет наличие критических файлов и зависимостей:
```bash
bun run test:startup
```

**Проверяет:**
- ✅ Наличие всех критических файлов
- ✅ Наличие всех контекстов
- ✅ Наличие компонентов
- ✅ Конфигурационные файлы
- ✅ Зависимости

### 2. Глубокий тест (`test:deep`)
Проверяет структуру, экспорты, провайдеры и интеграции:
```bash
bun run test:deep
```

**Проверяет:**
- ✅ Структуру провайдеров в `_layout.tsx`
- ✅ Экспорты всех контекстов
- ✅ Порядок провайдеров
- ✅ Зависимости между провайдерами
- ✅ Роутинг и экраны
- ✅ Импорты в критических файлах
- ✅ Структуру файлов

### 3. Полный тест (`test:all-config`)
Запускает все тесты последовательно:
```bash
bun run test:all-config
```

Или:
```bash
./scripts/test-all-config.sh
```

## 📋 Структура проверок

### Проверка провайдеров
Проверяет наличие и порядок всех провайдеров:
1. QueryClientProvider
2. trpc.Provider
3. AppErrorBoundary
4. ThemeProvider
5. AgeComplianceProvider
6. UserProvider
7. AuthProvider
8. AnalyticsProvider
9. PremiumProvider
10. ABTestingProvider
11. PersonalizedAIProvider
12. MonitoringProvider
13. ChatBackgroundsProvider
14. ParentalControlsProvider
15. GamificationProvider
16. PredictiveAnalyticsProvider
17. AIParentingAssistantProvider
18. ReferralProgramProvider
19. SyncSettingsProvider
20. SecuritySettingsProvider
21. NotificationsProvider

### Проверка контекстов
Проверяет экспорты для каждого контекста:
- `AuthContext.tsx` → `AuthProvider`, `useAuth`
- `ThemeContext.tsx` → `ThemeProvider`, `useThemeMode`
- `UserContext.tsx` → `UserProvider`, `useUser`
- `MonitoringContext.tsx` → `MonitoringProvider`, `useMonitoring`
- `SyncSettingsContext.tsx` → `SyncSettingsProvider`, `useSyncSettings`
- `SecuritySettingsContext.tsx` → `SecuritySettingsProvider`, `useSecuritySettings`
- И другие...

### Проверка роутинга
Проверяет:
- Наличие основных экранов в `(tabs)/_layout.tsx`
- Правильность структуры файлов
- Наличие критических маршрутов

## 🔧 Исправление проблем

### Если тест показывает ошибки:

1. **Отсутствуют файлы:**
   ```bash
   # Проверьте структуру проекта
   ls -la app/
   ls -la constants/
   ```

2. **Отсутствуют экспорты:**
   - Проверьте, что контексты используют `createContextHook`
   - Проверьте, что экспорты правильные: `export const [Provider, useHook] = ...`

3. **Неправильный порядок провайдеров:**
   - `AuthProvider` должен быть внутри `UserProvider`
   - `MonitoringProvider` должен быть после `UserProvider` и `AuthProvider`

4. **Отсутствуют зависимости:**
   ```bash
   bun install
   ```

## 🚀 Развертывание

### Перед развертыванием:

1. **Запустите все тесты:**
   ```bash
   bun run test:all-config
   ```

2. **Проверьте, что все тесты пройдены:**
   - ✅ Базовый тест: пройден
   - ✅ Глубокий тест: пройден

3. **Запустите приложение:**
   ```bash
   bun run start:web
   ```

4. **Проверьте в браузере:**
   - Приложение открывается
   - Все вкладки работают
   - Настройки открываются
   - Чаты открываются

## 📊 Интерпретация результатов

### ✅ Все тесты пройдены
- Приложение готово к развертыванию
- Все компоненты на месте
- Все зависимости установлены

### ❌ Есть ошибки
- Исправьте критические ошибки
- Перезапустите тесты
- Проверьте логи

### ⚠️ Есть предупреждения
- Предупреждения не критичны
- Можно развертывать, но лучше исправить

## 🎯 Быстрая проверка

```bash
# Быстрая проверка перед запуском
bun run test:config

# Если все ОК - запускаем
bun run start:web
```

## 📝 Логи тестов

Все тесты выводят подробную информацию:
- ✅ Успешные проверки
- ❌ Ошибки
- ⚠️ Предупреждения

Используйте эту информацию для исправления проблем.
