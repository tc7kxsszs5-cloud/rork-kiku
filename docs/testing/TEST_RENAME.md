# 🧪 Тест переименования

Я временно переименовал `AnalyticsProvider` в `AnalyticsProviderTemp` в файле `app/_layout.tsx`.

## Цель теста:

Проверить, действительно ли проблема в имени `AnalyticsProvider`, или Metro bundler видит дубликат по какой-то другой причине.

## Что было изменено:

1. `import { AnalyticsProvider }` → `import { AnalyticsProvider as AnalyticsProviderTemp }`
2. `<AnalyticsProvider>` → `<AnalyticsProviderTemp>`
3. `</AnalyticsProvider>` → `</AnalyticsProviderTemp>`

## Что делать:

1. Запустите: `bun run start -- --clear`
2. Проверьте результат:
   - ✅ Если ошибка исчезла → проблема была в имени `AnalyticsProvider` (возможно, конфликт с чем-то)
   - ❌ Если ошибка осталась (но с другим именем) → проблема не в имени
   - ❌ Если ошибка осталась (с AnalyticsProviderTemp) → Metro bundler все еще видит дубликат

## Откат изменений:

Если нужно вернуть обратно:
```bash
cp app/_layout.tsx.backup3 app/_layout.tsx
```

---

**Запустите проект и сообщите результат! 🚀**






