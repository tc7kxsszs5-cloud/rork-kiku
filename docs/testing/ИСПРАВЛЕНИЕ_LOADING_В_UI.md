# 🔧 Исправление "Loading..." в Playwright UI

## ❌ Проблема

В Playwright UI только пишет **"Loading..."** и ничего не загружается.

## 🔍 Причины

1. **Playwright не может найти тесты** - проблема с конфигурацией
2. **Веб-сервер не готов** - приложение еще запускается
3. **Ошибка в тестах** - синтаксическая ошибка блокирует загрузку
4. **Кэш Playwright** - старые данные мешают

---

## ✅ Решения

### Решение 1: Перезапустить Playwright UI

**Закройте текущий процесс:**
```bash
# Найти процесс Playwright
ps aux | grep "playwright test --ui"

# Остановить его (нажмите Ctrl+C в терминале где запущен)
# Или:
pkill -f "playwright test --ui"
```

**Запустите заново:**
```bash
bun run test:playwright:ui
```

---

### Решение 2: Проверить, что приложение запущено

**Убедитесь, что веб-сервер работает:**
```bash
# Проверить, отвечает ли сервер
curl http://localhost:8082

# Если не отвечает, запустите:
bun run start:web
```

**Дождитесь сообщения:**
```
Web is waiting on http://localhost:8082
```

---

### Решение 3: Проверить тесты на ошибки

**Запустите тесты без UI, чтобы увидеть ошибки:**
```bash
bun run test:playwright
```

Если есть ошибки - исправьте их.

---

### Решение 4: Очистить кэш Playwright

```bash
# Очистить кэш
rm -rf node_modules/.cache/playwright
rm -rf test-results/
rm -rf playwright-report/

# Перезапустить
bun run test:playwright:ui
```

---

### Решение 5: Проверить конфигурацию

**Убедитесь, что в `playwright.config.ts` правильно указан путь:**
```typescript
testDir: './__tests__/playwright',  // ← Должен быть правильный путь
```

**Проверьте, что тесты в правильной папке:**
```bash
ls -la __tests__/playwright/
# Должны быть файлы .spec.ts
```

---

### Решение 6: Запустить в режиме отладки

**Посмотрите подробные логи:**
```bash
# Запустить с выводом всех логов
DEBUG=pw:* bun run test:playwright:ui
```

Это покажет, что именно не так.

---

## 🎯 Пошаговое исправление

### Шаг 1: Остановить все процессы

```bash
# Остановить Playwright
pkill -f "playwright"

# Остановить Expo (если нужно)
pkill -f "expo start"
```

### Шаг 2: Запустить приложение вручную

```bash
# Терминал 1: Запустить веб-сервер
bun run start:web
```

**Дождитесь:**
```
Web is waiting on http://localhost:8082
```

### Шаг 3: Запустить Playwright UI

```bash
# Терминал 2: Запустить Playwright
bun run test:playwright:ui
```

### Шаг 4: Проверить в UI

1. **Откройте окно Playwright UI**
2. **Проверьте статус вверху:**
   - ✅ `Connected` = хорошо
   - ❌ `Disconnected` = проблема
3. **Нажмите "Run all tests" ▶️**

---

## 🔍 Диагностика

### Проверить, что тесты найдены

```bash
# Запустить без UI - увидите список тестов
bun run test:playwright --list
```

**Должно показать:**
```
example.spec.ts
  KIKU Web App
    должна загрузиться главная страница
    должна отображаться навигация
    ...
```

Если ничего не показывает - проблема с конфигурацией или тестами.

---

### Проверить синтаксис тестов

```bash
# Проверить TypeScript
bunx tsc --noEmit __tests__/playwright/*.ts
```

Если есть ошибки - исправьте их.

---

## 💡 Быстрое исправление

### Самый простой способ:

```bash
# 1. Остановить всё
pkill -f "playwright"
pkill -f "expo"

# 2. Освободить порт
bun run kill-ports

# 3. Запустить приложение
bun run start:web
# (Дождитесь запуска в другом терминале)

# 4. Запустить Playwright
bun run test:playwright:ui
```

---

## 🚨 Если всё еще "Loading..."

### Вариант 1: Проверить консоль браузера

1. Откройте DevTools в окне Playwright UI (F12)
2. Посмотрите вкладку **Console**
3. Найдите ошибки (красные сообщения)
4. Исправьте их

### Вариант 2: Запустить без UI

```bash
# Запустить тесты в обычном режиме
bun run test:playwright
```

Это покажет ошибки, которые мешают UI загрузиться.

### Вариант 3: Проверить версию Playwright

```bash
bunx playwright --version
```

Должна быть установлена версия. Если нет:
```bash
bun install
bunx playwright install
```

---

## ✅ Чеклист исправления

- [ ] Остановлены все процессы Playwright
- [ ] Веб-сервер запущен и отвечает на `http://localhost:8082`
- [ ] Тесты найдены: `bun run test:playwright --list` показывает тесты
- [ ] Нет синтаксических ошибок в тестах
- [ ] Конфигурация правильная (`testDir` указывает на правильную папку)
- [ ] Playwright UI перезапущен

---

## 🎯 Итог

**"Loading..." обычно означает:**
1. Веб-сервер еще не готов → подождите или запустите вручную
2. Тесты не найдены → проверьте `testDir` в конфигурации
3. Ошибка в тестах → запустите `bun run test:playwright` чтобы увидеть ошибки

**Самое простое решение:**
1. Запустите приложение вручную: `bun run start:web`
2. Дождитесь запуска
3. Перезапустите Playwright UI: `bun run test:playwright:ui`
