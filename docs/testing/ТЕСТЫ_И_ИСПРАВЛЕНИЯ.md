# Тесты и исправления для запуска приложения

## 🧪 Тесты для проверки работоспособности

### 1. Запуск тестового скрипта

```bash
# Запустить тест проверки файлов и зависимостей
bun run scripts/test-app-startup.ts
```

Этот скрипт проверит:
- ✅ Наличие всех критических файлов
- ✅ Наличие всех контекстов
- ✅ Наличие компонентов
- ✅ Конфигурационные файлы
- ✅ Зависимости

### 2. Ручное тестирование

#### Тест 1: Запуск приложения
```bash
# Очистить кэш и запустить
bun run start:clear

# Или для веб
bun run start:web
```

**Ожидаемый результат:**
- Приложение запускается без ошибок
- Отображается экран выбора роли или главный экран

#### Тест 2: Проверка навигации
1. Откройте приложение
2. Перейдите на вкладку "Чаты" - должна открываться
3. Перейдите на вкладку "Настройки" - должна открываться
4. Перейдите на вкладку "Профиль" - должна открываться

**Ожидаемый результат:**
- Все вкладки открываются без ошибок
- Нет вылетов при переключении

#### Тест 3: Проверка контекстов
Откройте консоль браузера (F12) и проверьте:
- Нет ошибок типа "Cannot read property of undefined"
- Нет ошибок типа "Context is not defined"
- Нет ошибок типа "Provider is missing"

## 🔧 Исправления

### 1. Обработка ошибок в `_layout.tsx`

**Добавлено:**
- ✅ Try-catch для применения курсоров
- ✅ Try-catch для инициализации провайдеров
- ✅ Безопасная обработка useAuth, useRouter, useSegments
- ✅ Fallback UI при критических ошибках

### 2. Безопасная инициализация

**Изменения:**
```typescript
// Было:
const { isAuthenticated } = useAuth();

// Стало:
let isAuthenticated = false;
try {
  const auth = useAuth();
  isAuthenticated = auth.isAuthenticated;
} catch (error) {
  console.error('Auth error:', error);
}
```

### 3. Обработка ошибок в провайдерах

**Добавлено:**
- ✅ AppErrorBoundary оборачивает все провайдеры
- ✅ Try-catch в AppProviders для критических ошибок
- ✅ Fallback UI при ошибке инициализации

## 📋 Чеклист для проверки

### Перед запуском:
- [ ] Запустить `bun run scripts/test-app-startup.ts`
- [ ] Проверить, что все файлы на месте
- [ ] Проверить, что все зависимости установлены (`bun install`)

### При запуске:
- [ ] Проверить консоль на ошибки
- [ ] Проверить, что приложение открывается
- [ ] Проверить навигацию между вкладками
- [ ] Проверить открытие настроек
- [ ] Проверить открытие чатов

### Если есть ошибки:
1. Откройте консоль браузера (F12)
2. Найдите ошибку в консоли
3. Проверьте, какой компонент вызывает ошибку
4. Используйте ErrorBoundary для изоляции ошибки

## 🐛 Известные проблемы и решения

### Проблема: Приложение не открывается

**Решение:**
1. Очистить кэш: `bun run start:clear`
2. Проверить консоль на ошибки
3. Проверить, что все провайдеры правильно обернуты
4. Проверить, что все контексты экспортированы

### Проблема: Вылетает при открытии настроек

**Решение:**
- Уже исправлено: добавлена обработка ошибок в SyncSettings и BiometricAuthSettings
- Проверьте, что контексты SyncSettingsContext и SecuritySettingsContext правильно инициализированы

### Проблема: Вылетает при открытии чатов

**Решение:**
- Уже исправлено: добавлена обработка ошибок в MonitoringScreen
- Проверьте, что MonitoringContext правильно инициализирован

## 🚀 Демонстрационная версия

Для демонстрации приложения:

1. **Запустить веб-версию:**
   ```bash
   bun run start:web
   ```

2. **Открыть в браузере:**
   - Обычно доступно на `http://localhost:8081`
   - Или смотрите URL в консоли

3. **Проверить функционал:**
   - Регистрация родителя/ребенка
   - Переключение между вкладками
   - Открытие чатов
   - Открытие настроек
   - Открытие профиля

## 📝 Логирование

Все ошибки логируются в консоль с префиксами:
- `[RootLayout]` - ошибки инициализации
- `[AppProviders]` - ошибки провайдеров
- `[RootLayoutNav]` - ошибки навигации
- `[MonitoringScreen]` - ошибки экрана чатов
- `[MessengerSettingsScreen]` - ошибки настроек

## ✅ Результат

После всех исправлений:
- ✅ Приложение должно запускаться без ошибок
- ✅ Все вкладки должны открываться
- ✅ Настройки должны работать
- ✅ Чаты должны работать
- ✅ Демонстрационная версия должна быть стабильной
