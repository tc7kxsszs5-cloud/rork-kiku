# Покрытие Unit-тестами - Что требуется

## Принципы отбора

Функции для unit-тестирования должны быть:
- ✅ **Чистыми** (детерминированными) - одинаковый вход = одинаковый выход
- ✅ **Сложной логикой** - где вероятны ошибки
- ✅ **Без побочных эффектов** - не изменяют глобальное состояние
- ✅ **Без сложных моков** - только простые входные данные

---

## ✅ Критично - Требуется покрытие

### 1. **evaluateMessageRisk** (constants/MonitoringContext.tsx:84)
**Приоритет: 🔴 КРИТИЧЕСКИЙ**

**Почему важно:**
- Критическая функция безопасности - определяет уровень риска сообщений
- Сложная логика с регулярными выражениями
- Множественные правила и уровни риска
- Вычисление confidence score
- Обработка edge cases (пустые сообщения, множественные совпадения)

**Что тестировать:**
- Все уровни риска (safe, low, medium, high, critical)
- Сообщения без ключевых слов (safe level)
- Сообщения с множественными ключевыми словами
- Правильный выбор highest risk level
- Confidence score calculation
- Categories и reasons extraction
- Обработка emphasis (восклицательные знаки)
- Обработка CAPS и числовых паттернов

**Статус:** ❌ Не покрыто  
**Действие:** Вынести в `utils/riskEvaluation.ts` и протестировать

---

### 2. **evaluateImageRisk** (constants/MonitoringContext.tsx:115)
**Приоритет: 🟡 ВЫСОКИЙ**

**Почему важно:**
- Функция безопасности для блокировки изображений
- Простая, но критичная логика

**Что тестировать:**
- Блокировка по ключевым словам
- Нормализация URI (case-insensitive)
- Возврат правильных reasons
- Обработка безопасных изображений

**Статус:** ❌ Не покрыто  
**Действие:** Вынести в `utils/riskEvaluation.ts` и протестировать

---

### 3. **getDeltaAlerts** (backend/trpc/routes/sync/alerts.ts:26)
**Приоритет: 🟡 ВЫСОКИЙ**

**Почему важно:**
- Логика синхронизации данных
- Важна для корректной работы sync
- Обработка edge cases (нет timestamp, некорректные данные)

**Что тестировать:**
- Фильтрация по timestamp
- Обработка отсутствующих timestamp (fallback на 0)
- Пустые массивы
- Граничные значения (равный timestamp)
- Сортировка (если требуется)

**Статус:** ❌ Не покрыто  
**Действие:** Вынести в `utils/syncHelpers.ts` и протестировать

---

### 4. **getDeltaChats** (backend/trpc/routes/sync/chats.ts:77)
**Приоритет: 🟡 ВЫСОКИЙ**

**Почему важно:**
- Логика синхронизации чатов
- Аналогична getDeltaAlerts, но с дополнительным полем (updatedAt)

**Что тестировать:**
- Фильтрация по updatedAt или timestamp
- Fallback на timestamp если нет updatedAt
- Обработка отсутствующих полей
- Граничные значения

**Статус:** ❌ Не покрыто  
**Действие:** Вынести в `utils/syncHelpers.ts` и протестировать

---

### 5. **isUuid** (constants/NotificationsContext.tsx:27)
**Приоритет: 🟢 СРЕДНИЙ**

**Почему важно:**
- Валидация UUID - критична для безопасности и корректности данных
- Регулярное выражение может иметь edge cases

**Что тестировать:**
- Валидные UUID (все варианты)
- Невалидные UUID (неправильный формат)
- Граничные случаи (пустая строка, null, undefined, числа)
- Верхний/нижний регистр

**Статус:** ❌ Не покрыто  
**Действие:** Вынести в `utils/validation.ts` и протестировать

---

## 🟢 Желательно - Можно покрыть

### 6. **isLocalHost** (lib/trpc.ts:20)
**Приоритет: 🟢 НИЗКИЙ**

**Почему важно:**
- Определение локального хоста для разработки
- Множественные паттерны проверки

**Что тестировать:**
- localhost (разные варианты)
- 127.x.x.x
- 10.x.x.x
- 192.168.x.x
- .local домены
- Нелокальные хосты
- Edge cases (null, undefined, пустая строка)

**Статус:** ❌ Не покрыто  
**Действие:** Вынести в `utils/network.ts` и протестировать (опционально)

---

### 7. **formatChatName** (app/ai-recommendations.tsx:91)
**Приоритет: 🟢 НИЗКИЙ**

**Почему важно:**
- Форматирование имен чатов для UI
- Обработка групповых чатов и приватных

**Что тестировать:**
- Групповые чаты (groupName)
- Приватные чаты с 2+ участниками
- Чаты с 1 участником
- Чаты без участников (fallback)
- Edge cases (пустые массивы)

**Статус:** ❌ Не покрыто  
**Действие:** Вынести в `utils/formatting.ts` и протестировать (опционально)

---

## ❌ НЕ требуется покрытие

### Функции, которые НЕ должны быть в unit-тестах:

1. **isTimeRestricted** (ParentalControlsContext.tsx:293)
   - ❌ Зависит от состояния (settings, timeRestrictions)
   - ❌ Использует `new Date()` - недетерминированная
   - ❌ Это useCallback внутри Context - интеграционная логика

2. **mergeAlerts** / **mergeMessages** (backend/trpc/routes/sync/*)
   - ⚠️ Можно покрыть, но простая логика
   - Низкий приоритет

3. **formatTimestamp** (app/notifications-diagnostics.tsx:47)
   - ❌ Зависит от локали и времени - недетерминированная
   - ❌ Форматирование для UI - не критичная логика

4. **Все функции в Context providers**
   - ❌ Асинхронные
   - ❌ Зависят от состояния
   - ❌ Требуют моков React/AsyncStorage

5. **Все React компоненты**
   - ❌ Интеграционные тесты
   - ❌ Требуют React Testing Library

---

## 📋 План действий

### Фаза 1: Критичные функции (🔴 КРИТИЧЕСКИЙ приоритет)

1. ✅ Вынести `evaluateMessageRisk` в `utils/riskEvaluation.ts`
2. ✅ Создать `__tests__/unit/utils/riskEvaluation.test.ts`
3. ✅ Покрыть все уровни риска и edge cases

### Фаза 2: Высокий приоритет (🟡 ВЫСОКИЙ)

4. ✅ Вынести `evaluateImageRisk` в `utils/riskEvaluation.ts`
5. ✅ Добавить тесты для `evaluateImageRisk`
6. ✅ Вынести `getDeltaAlerts` и `getDeltaChats` в `utils/syncHelpers.ts`
7. ✅ Создать `__tests__/unit/utils/syncHelpers.test.ts`

### Фаза 3: Средний/Низкий приоритет (🟢)

8. ✅ Вынести `isUuid` в `utils/validation.ts`
9. ✅ Создать `__tests__/unit/utils/validation.test.ts`
10. ⏳ Опционально: `isLocalHost`, `formatChatName`

---

## ✅ Уже покрыто

- ✅ `utils/soundNotifications.ts` - все функции
- ✅ `utils/cursorStyles.ts` - константы и функции

---

## 📊 Статистика покрытия

**Всего критичных функций:** 5  
**Покрыто:** 0  
**Требуется покрытие:** 5

**Всего функций (включая желательные):** 7  
**Покрыто:** 0  
**Требуется покрытие:** 7

---

**Последнее обновление:** 2025-01-13
