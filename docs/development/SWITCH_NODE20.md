# 🔄 Переключение на Node.js 20

## Текущая ситуация
- Сейчас используется: Node.js v24.11.1 ❌ (слишком новая, проблемы совместимости)
- Нужно: Node.js 20.x ✅ (рекомендуется для Expo)

## ✅ Команды для переключения

Выполните по порядку:

```bash
# 1. Установить Node.js 20 (если не установлена)
nvm install 20

# 2. Переключиться на Node.js 20
nvm use 20

# 3. Проверить версию (должно показать v20.x.x)
node --version

# 4. Перейти в проект
cd /Users/mac/Desktop/rork-kiku

# 5. Переустановить зависимости (важно!)
rm -rf node_modules package-lock.json
npm install

# 6. Запустить приложение
npx expo start --ios
```

---

## 🔧 Установить Node.js 20 как версию по умолчанию (опционально)

Чтобы каждый раз не переключаться:

```bash
nvm alias default 20
nvm use default
```

---

## ✅ После переключения

Проверьте версию:
```bash
node --version
# Должно показать: v20.x.x (не v24!)
```

Затем переустановите зависимости и запускайте!
