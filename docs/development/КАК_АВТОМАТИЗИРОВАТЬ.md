# 🤖 Как автоматизировать деплой Vercel + Supabase

## 🚀 Автоматические скрипты

Я создал скрипты для автоматизации!

---

## 📋 Скрипт 1: Автоматический деплой

### `backend/auto-deploy.sh`

**Что делает:**
- ✅ Проверяет/устанавливает Vercel CLI
- ✅ Проверяет вход в Vercel
- ✅ Выполняет деплой

**Использование:**
```bash
cd backend
./auto-deploy.sh
```

---

## 📋 Скрипт 2: Настройка Environment Variables

### `backend/auto-setup.sh`

**Что делает:**
- ✅ Запрашивает Connection String
- ✅ Добавляет DATABASE_URL в Vercel
- ✅ Добавляет NODE_ENV в Vercel

**Использование:**
```bash
cd backend
./auto-setup.sh
```

---

## 🚀 Полная автоматизация:

```bash
cd backend

# 1. Деплой
./auto-deploy.sh

# 2. Настройка переменных
./auto-setup.sh

# 3. Production деплой (если нужно)
vercel --prod
```

---

## 📋 Альтернатива: GitHub Actions

Можно настроить автоматический деплой при каждом push!

### Создайте `.github/workflows/deploy.yml`:

```yaml
name: Deploy to Vercel

on:
  push:
    branches: [main]
    paths:
      - 'backend/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: amondnet/vercel-action@v25
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
          working-directory: ./backend
```

---

## ✅ Преимущества:

1. ✅ **Быстрее** - один скрипт вместо многих команд
2. ✅ **Надежнее** - меньше ошибок
3. ✅ **Повторяемо** - можно запускать много раз

---

## 🎯 Готово!

Используйте скрипты для автоматизации!
