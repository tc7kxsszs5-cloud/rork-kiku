# ⚡ Быстрая команда после Reset пароля

## 🎯 Если вы сделали Reset пароля

Используйте **НОВЫЙ пароль** для обновления DATABASE_URL.

## 📋 Команды (замените [НОВЫЙ_ПАРОЛЬ] на реальный новый пароль!)

```bash
cd /Users/mac/Desktop/rork-kiku/backend
bunx vercel env rm DATABASE_URL production
bunx vercel env add DATABASE_URL production
```

**Когда появится запрос, вставьте:**
```
postgresql://postgres:[НОВЫЙ_ПАРОЛЬ]@db.eznumgsmwvavyunqhxfc.supabase.co:5432/postgres
```

**Замените `[НОВЫЙ_ПАРОЛЬ]` на реальный новый пароль из Supabase!**

Затем:
```bash
bunx vercel --prod
sleep 25
curl "https://backend-three-mauve-67.vercel.app/api/trpc/test.dbCheck"
```

## 🔑 Где взять новый пароль

Если вы сделали Reset, но не скопировали пароль:
1. Supabase Dashboard → Settings → Database
2. Database password → **Reset database password**
3. Скопируйте новый пароль (показывается только один раз!)

---

**Важно:** Используйте НОВЫЙ пароль, не старый!
