# ⚡ Команда одной строкой

## 📋 Скопируйте и выполните (замените [ПАРОЛЬ] на реальный!)

```bash
cd /Users/mac/Desktop/rork-kiku/backend && bunx vercel env rm DATABASE_URL production && bunx vercel env add DATABASE_URL production
```

Когда появится запрос, вставьте:
```
postgresql://postgres:[ПАРОЛЬ]@db.eznumgsmwvavyunqhxfc.supabase.co:5432/postgres
```

**Замените `[ПАРОЛЬ]` на реальный пароль из Supabase!**

Затем:
```bash
bunx vercel --prod && sleep 25 && curl "https://backend-three-mauve-67.vercel.app/api/trpc/test.dbCheck"
```

---

## 🔑 Где взять пароль:

Supabase Dashboard → Settings → Database → Database password
