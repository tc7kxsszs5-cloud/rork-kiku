# ⚡ Команды для замены DATABASE_URL

## 🔄 Выполните эти команды по порядку:

```bash
cd /Users/mac/Desktop/rork-kiku/backend

# 1. Удалите старую переменную
bunx vercel env rm DATABASE_URL production

# 2. Добавьте новую
bunx vercel env add DATABASE_URL production
```

**Когда появится запрос, вставьте:**

```
postgresql://postgres:[ВАШ_ПАРОЛЬ]@db.eznumgsmwvavyunqhxfc.supabase.co:5432/postgres
```

**Замените `[ВАШ_ПАРОЛЬ]` на реальный пароль из Supabase!**

```bash
# 3. Перезапустите деплой
bunx vercel --prod

# 4. Проверьте (подождите 20-25 секунд)
curl "https://backend-three-mauve-67.vercel.app/api/trpc/test.dbCheck"
```

## 💡 Пример

Если пароль `mypass123`, вставьте:
```
postgresql://postgres:mypass123@db.eznumgsmwvavyunqhxfc.supabase.co:5432/postgres
```

## ⚠️ Если пароль содержит @, :, /, # и т.д.

Закодируйте их (например, `@` → `%40`)

---

**Готово! Выполните команды выше.**
