# ✅ Обновление переменных окружения

## ✅ Хорошие новости:

Переменные `SUPABASE_URL` и `SUPABASE_ANON_KEY` уже существуют!

## 📋 Что сделать:

### Вариант 1: Проверить значения (если они правильные)

Если переменные уже установлены с правильными значениями, можно сразу деплоить:

```bash
cd /Users/mac/Desktop/rork-kiku/backend
bunx vercel --prod
curl "https://backend-three-mauve-67.vercel.app/api/trpc/test.dbCheck"
```

### Вариант 2: Обновить значения (если нужно)

Если нужно обновить значения:

```bash
# Удалить старые
bunx vercel env rm SUPABASE_URL production
bunx vercel env rm SUPABASE_ANON_KEY production

# Добавить новые
bunx vercel env add SUPABASE_URL production
# Вставьте Project URL из Supabase
# yes (sensitive)

bunx vercel env add SUPABASE_ANON_KEY production
# Вставьте anon public key из Supabase
# yes (sensitive)
```

---

## 💡 Рекомендация:

**Попробуйте сначала Вариант 1** - если переменные уже установлены, возможно они правильные и можно сразу деплоить!

---

**Проверьте значения переменных и деплойте!**
