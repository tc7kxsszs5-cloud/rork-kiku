# 🔄 Рабочий процесс (Workflow) для команды

Стандартизированный процесс работы над проектом без ошибок.

## 📋 Ежедневный workflow

### Утро: Начало работы

```bash
# 1. Обновите код
git pull origin main

# 2. Установите зависимости (если были изменения)
bun install

# 3. Проверьте что все работает
bun run typecheck
bun run lint

# 4. Запустите проект
bun run start:simple
```

### Во время работы

1. **Создайте ветку для задачи**
   ```bash
   git checkout -b feature/your-feature-name
   ```

2. **Работайте над задачей**
   - Пишите код
   - Исправляйте ошибки сразу
   - Пишите тесты

3. **Регулярно проверяйте**
   ```bash
   # Каждые 30 минут или после значительных изменений
   bun run typecheck
   bun run lint
   ```

### Перед коммитом

```bash
# 1. Проверка TypeScript
bun run typecheck

# 2. Проверка линтера
bun run lint

# 3. Автоисправление (если возможно)
bun run lint:fix

# 4. Запуск тестов
bun run test

# 5. Проверка что приложение работает
# Убедитесь что нет runtime ошибок в консоли
```

### Коммит

```bash
# 1. Добавить изменения
git add .

# 2. Проверить что добавляется
git status

# 3. Создать коммит
git commit -m "feat: описание изменений"

# 4. Отправить на сервер
git push
```

## 🔴 Что делать при ошибках

### Красные крестики в IDE

1. **Не паникуйте** - это нормально
2. **Прочитайте сообщение об ошибке**
3. **Исправьте ошибку**
4. **Проверьте что ошибка исчезла**

### Процесс исправления

```bash
# Шаг 1: Определите тип ошибки
bun run typecheck    # TypeScript ошибки
bun run lint         # Линтер ошибки

# Шаг 2: Исправьте
# - Исправьте код вручную
# - Или используйте автоисправление: bun run lint:fix

# Шаг 3: Проверьте снова
bun run typecheck
bun run lint

# Шаг 4: Если были runtime ошибки - перезапустите
# В терминале Expo нажмите: r
```

### Когда перезапускать сервер

**Обязательно:**
- После изменения `package.json` или `app.json`
- После изменения нативного кода (ios/android)
- После критических ошибок импорта
- После изменения конфигурации

**Не обязательно:**
- После изменения компонентов (Hot Reload)
- После изменения стилей
- После исправления TypeScript типов (если код компилируется)

## ✅ Чеклист перед коммитом

- [ ] Нет TypeScript ошибок (`bun run typecheck`)
- [ ] Линтер проходит (`bun run lint`)
- [ ] Тесты проходят (`bun run test`)
- [ ] Приложение запускается без ошибок
- [ ] Код соответствует стилю проекта
- [ ] Добавлены тесты для новой функциональности
- [ ] Обновлена документация (если нужно)

## 🚫 Что НЕ делать

1. **Не коммитьте с ошибками**
   - Исправьте все ошибки перед коммитом

2. **Не игнорируйте предупреждения**
   - Они могут стать ошибками позже

3. **Не используйте `any` без необходимости**
   - Всегда старайтесь использовать правильные типы

4. **Не коммитьте закомментированный код**
   - Удаляйте или объясняйте зачем он нужен

5. **Не коммитьте файлы окружения**
   - `.env` должен быть в `.gitignore`

## 📚 Полезные команды

```bash
# Быстрая проверка всего
bun run check

# Автоисправление
bun run check:fix

# Очистка и перезапуск
bun run start:clear

# Полная проверка перед коммитом
bun run typecheck && bun run lint && bun run test
```

## 🎯 Идеальный день разработчика

1. ✅ Утро: `git pull` → `bun install` → `bun run start:simple`
2. ✅ Работа: Создал ветку → Работаю → Регулярно проверяю
3. ✅ Перед коммитом: Все проверки пройдены
4. ✅ Коммит: Чистый код, тесты, документация
5. ✅ Конец дня: `git push` → Все готово

---

**Следуйте этому workflow и ошибок будет минимум! 🚀**
