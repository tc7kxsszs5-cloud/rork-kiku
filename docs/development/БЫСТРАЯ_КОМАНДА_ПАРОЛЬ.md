# ⚡ Быстрая команда: Куда вставить пароль

## 🎯 Очень просто:

1. **Скопируйте** connection string из Supabase (Session mode)
2. **Замените** `[YOUR-PASSWORD]` на реальный пароль
3. **Вставьте** в Vercel

## 📋 Точные шаги:

### В Supabase Dashboard:

1. **Settings** → **Database**
2. Найдите **Connection string** → выберите **Session mode**
3. Скопируйте строку (она с `[YOUR-PASSWORD]`)
4. Найдите **Database password** → скопируйте пароль
5. Замените `[YOUR-PASSWORD]` на пароль в connection string

### В терминале:

```bash
cd /Users/mac/Desktop/rork-kiku/backend

# Удалите старую
bunx vercel env rm DATABASE_URL production

# Добавьте новую
bunx vercel env add DATABASE_URL production
# ← ВСТАВЬТЕ ЗДЕСЬ connection string С РЕАЛЬНЫМ ПАРОЛЕМ

# Перезапустите
bunx vercel --prod
```

## 💡 Пример:

**Скопировали из Supabase:**
```
postgresql://postgres:[YOUR-PASSWORD]@db.eznumgsmwvavyunqhxfc.supabase.co:5432/postgres
```

**Пароль:** `abc123`

**Заменили:**
```
postgresql://postgres:abc123@db.eznumgsmwvavyunqhxfc.supabase.co:5432/postgres
```

**Вставили в Vercel** (через команду выше)

---

**Всё!** Просто скопируйте, замените `[YOUR-PASSWORD]`, вставьте в Vercel.
