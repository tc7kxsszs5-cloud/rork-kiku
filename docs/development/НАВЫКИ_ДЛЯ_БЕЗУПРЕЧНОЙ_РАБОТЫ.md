# Навыки для безупречной работы с проектом

## 🎯 Критические навыки

### 1. Понимание архитектуры React Native / Expo

**Что нужно знать:**
- ✅ Структура Expo Router (file-based routing)
- ✅ React Context API и провайдеры
- ✅ Правильный порядок провайдеров (зависимости)
- ✅ Жизненный цикл компонентов
- ✅ Правила использования хуков (Rules of Hooks)

**Почему важно:**
- Неправильный порядок провайдеров → ошибки контекста
- Нарушение правил хуков → краш приложения
- Неправильная инициализация → белый экран

### 2. TypeScript и типизация

**Что нужно знать:**
- ✅ Строгая типизация (strict mode)
- ✅ Интерфейсы и типы
- ✅ Generic типы
- ✅ Обработка undefined/null
- ✅ Type guards

**Почему важно:**
- Ошибки типов → проблемы компиляции
- Неправильная типизация → runtime ошибки
- Отсутствие проверок → краши при undefined

### 3. Обработка ошибок

**Что нужно знать:**
- ✅ Try-catch блоки
- ✅ Error Boundaries
- ✅ Fallback значения
- ✅ Graceful degradation
- ✅ Логирование ошибок

**Почему важно:**
- Без обработки ошибок → краш приложения
- Неправильная обработка → белый экран
- Отсутствие fallback → плохой UX

### 4. Асинхронность и состояние

**Что нужно знать:**
- ✅ async/await
- ✅ Promise handling
- ✅ Состояние загрузки (loading states)
- ✅ isMounted паттерн
- ✅ Cleanup в useEffect

**Почему важно:**
- Утечки памяти без cleanup
- Race conditions без isMounted
- Бесконечные загрузки без обработки ошибок

### 5. Диагностика и отладка

**Что нужно знать:**
- ✅ Чтение логов и ошибок
- ✅ Использование DevTools
- ✅ Понимание стектрейсов
- ✅ Проверка зависимостей
- ✅ Тестирование компонентов

**Почему важно:**
- Без диагностики → невозможно найти проблему
- Неправильная диагностика → потеря времени
- Отсутствие тестов → регрессии

## 🔧 Специфичные навыки для этого проекта

### 1. Понимание структуры провайдеров

**Правильный порядок:**
```
QueryClientProvider
  → trpc.Provider
    → AppErrorBoundary
      → ThemeProvider
        → AgeComplianceProvider
          → UserProvider
            → AuthProvider
              → ... другие провайдеры
```

**Почему важен порядок:**
- `AuthProvider` использует `UserContext` → должен быть внутри `UserProvider`
- `MonitoringProvider` использует `useUser()` → должен быть после `UserProvider`
- Нарушение порядка → `Context is not defined` ошибки

### 2. Работа с контекстами

**Паттерн:**
```typescript
export const [Provider, useHook] = createContextHook(() => {
  // логика
  return { data, functions };
});
```

**Важно:**
- Всегда экспортировать и Provider, и hook
- Использовать try-catch при использовании хуков
- Проверять на undefined перед использованием

### 3. Обработка платформо-специфичного кода

**Паттерн:**
```typescript
if (Platform.OS === 'web') {
  // веб-специфичный код
} else {
  // нативный код
}
```

**Важно:**
- Всегда проверять платформу
- Не использовать нативные API на веб
- Предоставлять fallback для веб

### 4. Инициализация приложения

**Критические точки:**
1. Импорты (могут вызвать ошибки)
2. Инициализация контекстов
3. Асинхронная инициализация (эмодзи, стили)
4. Состояние готовности (isReady)
5. Обработка ошибок на каждом этапе

**Паттерн:**
```typescript
useEffect(() => {
  let isMounted = true;
  
  const init = async () => {
    try {
      await initializeSomething();
      if (isMounted) setIsReady(true);
    } catch (error) {
      console.error('Init error:', error);
      // Fallback
    }
  };
  
  init();
  return () => { isMounted = false; };
}, []);
```

## 📚 Что нужно изучить

### Обязательно:
1. **React Native / Expo документация**
   - Expo Router
   - Context API
   - Platform API

2. **TypeScript**
   - Strict mode
   - Type guards
   - Error handling types

3. **React Patterns**
   - Error Boundaries
   - Custom hooks
   - Provider patterns

### Рекомендуется:
1. **Testing**
   - Unit tests
   - Integration tests
   - E2E tests

2. **Debugging**
   - React DevTools
   - Chrome DevTools
   - Logging strategies

3. **Performance**
   - Memoization
   - Lazy loading
   - Code splitting

## 🎓 Практические навыки

### 1. Чтение ошибок

**Умение:**
- Понимать стектрейсы
- Находить источник ошибки
- Различать синтаксические и runtime ошибки

**Пример:**
```
Error: Cannot read property 'theme' of undefined
  at TabLayout (app/(tabs)/_layout.tsx:9:25)
```

**Решение:**
- Проверить, что ThemeProvider обернут
- Добавить проверку на undefined
- Использовать fallback значение

### 2. Диагностика проблем

**Процесс:**
1. Воспроизвести проблему
2. Проверить консоль (браузер/терминал)
3. Запустить тесты
4. Проверить логи
5. Изолировать проблему
6. Исправить

### 3. Предотвращение проблем

**Практики:**
- ✅ Всегда обрабатывать ошибки
- ✅ Проверять на undefined
- ✅ Использовать TypeScript strict mode
- ✅ Писать тесты
- ✅ Документировать код

## 🛠️ Инструменты

### Обязательные:
- **Bun** - runtime и package manager
- **Expo CLI** - разработка
- **TypeScript** - типизация
- **React DevTools** - отладка

### Полезные:
- **ESLint** - линтинг
- **Jest** - тестирование
- **Chrome DevTools** - отладка веб-версии

## ✅ Чеклист перед изменением кода

1. [ ] Понимаю, что делает код
2. [ ] Знаю зависимости компонента
3. [ ] Добавил обработку ошибок
4. [ ] Проверил типы
5. [ ] Протестировал изменения
6. [ ] Проверил на разных платформах

## 🎯 Итог

Для безупречной работы нужно:
1. **Понимание архитектуры** - как работает проект
2. **Обработка ошибок** - защита от крашей
3. **Диагностика** - умение находить проблемы
4. **Тестирование** - проверка работоспособности
5. **Документация** - понимание изменений

**Главное правило:** Всегда предполагай, что что-то может пойти не так, и защищай от этого.
