# 🎨 Как создать кастомные эмодзи в KIKU

## ✅ Что реализовано

### 1. Компонент создания (`CustomEmojiCreator.tsx`)
- Загрузка изображений из галереи
- Создание эмодзи из изображений
- Добавление названия, категории, тегов
- Поддержка SVG и Unicode

### 2. Компонент отображения (`CustomEmoji.tsx`)
- Отображение кастомных эмодзи
- Поддержка изображений, SVG, Unicode
- Адаптивный размер

### 3. Компонент рендеринга (`EmojiRenderer.tsx`)
- Отображение кастомных эмодзи в тексте сообщений
- Парсинг специального формата `[custom:id]`
- Автоматическая замена в сообщениях

### 4. Утилиты (`customEmojis.ts`)
- Сохранение/загрузка кастомных эмодзи
- Поиск по названию, тегам, категориям
- Управление категориями

---

## 🚀 Как создать кастомный эмодзи

### Способ 1: Через интерфейс приложения

1. **Откройте чат**
2. **Нажмите кнопку 😊** (эмодзи)
3. **Нажмите кнопку "Создать"** в категориях
4. **Заполните форму:**
   - Название (обязательно)
   - Категория (опционально)
   - Теги (опционально, через запятую)
   - Выберите изображение
5. **Нажмите "Создать эмодзи"**

### Способ 2: Программно (в коде)

```typescript
import { addCustomEmoji } from '@/utils/customEmojis';
import { CustomEmojiData } from '@/components/CustomEmoji';

// Создать эмодзи из изображения
const newEmoji: CustomEmojiData = {
  id: 'kiku_logo',
  name: 'KIKU Logo',
  type: 'image',
  source: 'https://example.com/kiku-logo.png', // URL изображения
  category: 'brand',
  tags: ['logo', 'kiku', 'brand'],
};

await addCustomEmoji(newEmoji);
```

### Способ 3: Из локального файла

```typescript
import * as FileSystem from 'expo-file-system';
import { addCustomEmoji } from '@/utils/customEmojis';

// Сохранить изображение локально
const imageUri = await FileSystem.documentDirectory + 'kiku-logo.png';
// Скопировать файл в локальное хранилище

const newEmoji: CustomEmojiData = {
  id: 'kiku_logo_local',
  name: 'KIKU Logo Local',
  type: 'image',
  source: imageUri, // Локальный путь
  category: 'brand',
};

await addCustomEmoji(newEmoji);
```

### Способ 4: SVG эмодзи

```typescript
const svgEmoji: CustomEmojiData = {
  id: 'shield_emoji',
  name: 'Shield',
  type: 'svg',
  source: `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="#FFD700">
    <path d="M12 2L4 5v6c0 5.55 3.84 10.74 8 12 4.16-1.26 8-6.45 8-12V5l-8-3z"/>
  </svg>`,
  category: 'safety',
  tags: ['shield', 'protection'],
};

await addCustomEmoji(svgEmoji);
```

---

## 📋 Требования к изображениям

### Рекомендации:
- **Формат:** PNG, JPG, WebP
- **Размер:** 64x64px - 256x256px (квадратные)
- **Прозрачность:** PNG с альфа-каналом (для лучшего качества)
- **Вес:** До 100KB (для быстрой загрузки)

### Примеры хороших эмодзи:
- ✅ Логотипы (квадратные)
- ✅ Иконки (простые, узнаваемые)
- ✅ Смайлики (стилизованные)
- ✅ Символы (простые формы)

---

## 🎨 Создание эмодзи из дизайна

### Шаг 1: Подготовка изображения

1. Создайте квадратное изображение (например, 128x128px)
2. Экспортируйте в PNG с прозрачностью
3. Оптимизируйте размер (используйте TinyPNG или аналоги)

### Шаг 2: Добавление в проект

**Вариант A: Локальные файлы**
```typescript
// Поместите файл в assets/images/emojis/
import kikuLogo from '@/assets/images/emojis/kiku-logo.png';

const emoji: CustomEmojiData = {
  id: 'kiku_logo',
  name: 'KIKU Logo',
  type: 'image',
  source: Image.resolveAssetSource(kikuLogo).uri,
  category: 'brand',
};
```

**Вариант B: Удалённый URL**
```typescript
const emoji: CustomEmojiData = {
  id: 'kiku_logo',
  name: 'KIKU Logo',
  type: 'image',
  source: 'https://your-cdn.com/emojis/kiku-logo.png',
  category: 'brand',
};
```

---

## 🔧 Интеграция кастомных эмодзи в EmojiPicker

Кастомные эмодзи автоматически:
- ✅ Появляются в категории "Кастомные"
- ✅ Доступны через поиск
- ✅ Отображаются в чате
- ✅ Сохраняются локально

---

## 📊 Примеры использования

### Пример 1: Логотип KIKU

```typescript
const kikuLogo: CustomEmojiData = {
  id: 'kiku_logo',
  name: 'KIKU',
  type: 'image',
  source: 'https://kiku.app/logo.png',
  category: 'brand',
  tags: ['logo', 'kiku', 'brand'],
};

await addCustomEmoji(kikuLogo);
```

### Пример 2: Эмодзи безопасности

```typescript
const shieldEmoji: CustomEmojiData = {
  id: 'shield',
  name: 'Shield',
  type: 'svg',
  source: `<svg>...</svg>`,
  category: 'safety',
  tags: ['shield', 'protection', 'safety'],
};

await addCustomEmoji(shieldEmoji);
```

### Пример 3: Эмодзи для детей

```typescript
const kidEmoji: CustomEmojiData = {
  id: 'happy_kid',
  name: 'Happy Kid',
  type: 'image',
  source: 'https://example.com/happy-kid.png',
  category: 'kids',
  tags: ['kid', 'happy', 'child'],
};

await addCustomEmoji(kidEmoji);
```

---

## 🎯 Использование в сообщениях

### Как это работает:

1. **Пользователь выбирает кастомный эмодзи** из пикера
2. **Эмодзи добавляется в текст** как `[custom:id]`
3. **При отображении** `EmojiRenderer` заменяет `[custom:id]` на изображение
4. **В базе данных** сохраняется текст с `[custom:id]`

### Пример:

```typescript
// Пользователь выбрал кастомный эмодзи с id 'kiku_logo'
// В поле ввода: "Привет [custom:kiku_logo]"
// При отправке сохраняется как есть
// При отображении EmojiRenderer показывает изображение
```

---

## 🔍 Поиск кастомных эмодзи

```typescript
import { searchCustomEmojis } from '@/utils/customEmojis';

// Поиск по названию, тегам или категории
const results = await searchCustomEmojis('kiku');
// Найдёт все эмодзи с "kiku" в названии, тегах или категории
```

---

## 📁 Структура файлов

```
components/
├── CustomEmoji.tsx          # Компонент отображения
├── CustomEmojiCreator.tsx   # Компонент создания
└── EmojiRenderer.tsx        # Рендеринг в тексте

utils/
├── customEmojis.ts          # Утилиты для работы
└── emojiUtils.ts            # Общие утилиты
```

---

## ✅ Итог

**Что можно сделать:**
- ✅ Создавать эмодзи из изображений
- ✅ Создавать эмодзи из SVG
- ✅ Добавлять категории и теги
- ✅ Использовать в сообщениях
- ✅ Искать по названию/тегам

**Как создать:**
1. Через интерфейс (кнопка "Создать" в пикере)
2. Программно (через `addCustomEmoji`)
3. Из локальных файлов
4. Из удалённых URL

**Где использовать:**
- В сообщениях чата
- В статусах пользователей
- В комментариях
- В любом тексте через `EmojiRenderer`

---

**Кастомные эмодзи готовы к использованию!** 🎨✨
