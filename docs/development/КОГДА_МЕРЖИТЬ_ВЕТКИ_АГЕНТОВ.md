# 🔀 Когда мержить ветки агентов

**Дата:** 2026-01-27  
**Статус:** Руководство по мерджу веток

---

## ⏰ КОГДА МЕРЖИТЬ ВЕТКИ

### ✅ МЕРЖИТЬ СЕЙЧАС (рекомендуется)

**Причины:**
1. ✅ Работа агентов до 14:30 **завершена**
2. ✅ Все ветки имеют коммиты и готовы к мерджу
3. ✅ Перед началом новой работы (завтра) нужно иметь чистую основу
4. ✅ Незакоммиченные изменения в текущей ветке нужно сначала закоммитить

**Текущие ветки для мерджа:**
- `agent-1-frontend-ui-14-30` ✅ Готово
- `agent-2-contexts-14-30` ✅ Готово
- `agent-3-backend-14-30` ✅ Готово
- `agent-4-testing-14-30` ✅ Готово
- `agent-5-optimization-14-30` ✅ Готово (текущая ветка)
- `agent-6-infrastructure-14-30` ✅ Готово

---

## 📋 ПРОЦЕСС МЕРДЖА

### Шаг 1: Подготовка (СЕЙЧАС)

```bash
# 1. Проверить текущий статус
cd /Users/mac/Desktop/rork-kiku
git status

# 2. Если есть незакоммиченные изменения - закоммитить
git add .
git commit -m "chore: finalize agent-5 work before merge"

# 3. Переключиться на develop (или main)
git checkout develop
# или
git checkout main

# 4. Обновить develop/main
git pull origin develop
# или
git pull origin main
```

---

### Шаг 2: Проверка конфликтов

```bash
# Проверить, какие файлы изменены в каждой ветке
for branch in agent-1-frontend-ui-14-30 agent-2-contexts-14-30 agent-3-backend-14-30 agent-4-testing-14-30 agent-5-optimization-14-30 agent-6-infrastructure-14-30; do
  echo "=== $branch ==="
  git diff develop...$branch --name-only
done
```

**Если есть пересекающиеся файлы:**
- Проверить конфликты вручную
- Решить конфликты перед мерджем

---

### Шаг 3: Мердж веток (последовательно)

**Рекомендуемый порядок мерджа:**

```bash
# 1. Агент 6 (Infrastructure) - обычно не конфликтует
git merge agent-6-infrastructure-14-30 --no-ff -m "merge: agent-6 infrastructure work"

# 2. Агент 4 (Testing) - тесты обычно не конфликтуют
git merge agent-4-testing-14-30 --no-ff -m "merge: agent-4 testing work"

# 3. Агент 3 (Backend) - backend обычно изолирован
git merge agent-3-backend-14-30 --no-ff -m "merge: agent-3 backend work"

# 4. Агент 2 (Contexts) - может конфликтовать с layout
git merge agent-2-contexts-14-30 --no-ff -m "merge: agent-2 contexts work"

# 5. Агент 5 (Optimization) - может конфликтовать с layout и компонентами
git merge agent-5-optimization-14-30 --no-ff -m "merge: agent-5 optimization work"

# 6. Агент 1 (Frontend UI) - может конфликтовать с компонентами
git merge agent-1-frontend-ui-14-30 --no-ff -m "merge: agent-1 frontend UI work"
```

**Если возникли конфликты:**
```bash
# Разрешить конфликты
git status  # посмотреть конфликтующие файлы
# Отредактировать файлы вручную
git add <resolved-files>
git commit -m "merge: resolve conflicts in <files>"
```

---

### Шаг 4: Тестирование после мерджа

```bash
# 1. Проверить линтер
bun run lint

# 2. Проверить TypeScript
bunx tsc --noEmit

# 3. Запустить тесты (если возможно)
bunx jest --testPathPattern=unit --maxWorkers=2

# 4. Проверить, что приложение запускается
bunx expo start --web
```

---

## ⚠️ КОГДА НЕ МЕРЖИТЬ

### ❌ НЕ мержить, если:

1. **Агент еще работает:**
   - Ветка имеет статус "working" в lock файле
   - Последний коммит был менее 1 часа назад
   - Агент не завершил задачу

2. **Есть незакоммиченные изменения:**
   - Сначала закоммитить все изменения
   - Затем мержить

3. **Есть критические конфликты:**
   - Разрешить конфликты перед мерджем
   - Или отложить мердж до разрешения

4. **Перед важным релизом:**
   - Мержить после стабилизации
   - Или в отдельную ветку для тестирования

---

## 🎯 РЕКОМЕНДУЕМЫЙ ГРАФИК МЕРДЖА

### Вариант 1: Мердж после завершения работы (рекомендуется)

**Когда:** После завершения всех задач агента

**Процесс:**
1. Агент завершает работу
2. Агент коммитит все изменения
3. Агент создает Pull Request
4. Координатор проверяет и мержит

**Преимущества:**
- ✅ Чистая история коммитов
- ✅ Возможность review перед мерджем
- ✅ Легче откатить изменения

---

### Вариант 2: Мердж в конце дня

**Когда:** В конце рабочего дня, если все агенты завершили работу

**Процесс:**
1. Все агенты завершают работу
2. Координатор проверяет статус
3. Координатор мержит все ветки последовательно
4. Тестирование после мерджа

**Преимущества:**
- ✅ Все изменения в одном месте
- ✅ Удобно для тестирования
- ✅ Меньше конфликтов (если агенты не работали параллельно)

---

### Вариант 3: Мердж перед новой работой

**Когда:** Перед началом новой работы (например, завтра)

**Процесс:**
1. Завершить текущую работу
2. Закоммитить все изменения
3. Мержить все ветки в develop
4. Начать новую работу с чистой основы

**Преимущества:**
- ✅ Чистая основа для новой работы
- ✅ Все предыдущие изменения интегрированы
- ✅ Меньше конфликтов в будущем

---

## 📊 ТЕКУЩАЯ СИТУАЦИЯ

### Статус веток:

| Ветка | Статус | Последний коммит | Готово к мерджу |
|-------|--------|------------------|-----------------|
| `agent-1-frontend-ui-14-30` | Завершено | Есть коммиты | ✅ Да |
| `agent-2-contexts-14-30` | Завершено | Есть коммиты | ✅ Да |
| `agent-3-backend-14-30` | Завершено | Есть коммиты | ✅ Да |
| `agent-4-testing-14-30` | Завершено | Есть коммиты | ✅ Да |
| `agent-5-optimization-14-30` | Текущая | Есть коммиты | ⚠️ Проверить статус |
| `agent-6-infrastructure-14-30` | Завершено | Есть коммиты | ✅ Да |

### Рекомендация: **МЕРЖИТЬ СЕЙЧАС**

**Причины:**
1. ✅ Работа до 14:30 завершена
2. ✅ Все ветки имеют коммиты
3. ✅ Завтра начинается новая работа (важные задачи)
4. ✅ Нужна чистая основа для новых агентов

---

## 🚀 БЫСТРЫЙ СКРИПТ ДЛЯ МЕРДЖА

```bash
#!/bin/bash
# Скрипт для мерджа всех веток агентов

cd /Users/mac/Desktop/rork-kiku

# 1. Проверить текущий статус
echo "Проверка статуса..."
git status

# 2. Переключиться на develop
echo "Переключение на develop..."
git checkout develop
git pull origin develop

# 3. Мержить ветки последовательно
branches=(
  "agent-6-infrastructure-14-30"
  "agent-4-testing-14-30"
  "agent-3-backend-14-30"
  "agent-2-contexts-14-30"
  "agent-5-optimization-14-30"
  "agent-1-frontend-ui-14-30"
)

for branch in "${branches[@]}"; do
  echo "Мердж $branch..."
  git merge "$branch" --no-ff -m "merge: $branch work"
  
  if [ $? -ne 0 ]; then
    echo "❌ Конфликт в $branch! Разрешите конфликты и продолжите."
    exit 1
  fi
done

# 4. Проверка
echo "Проверка линтера..."
bun run lint

echo "✅ Мердж завершен!"
```

---

## ✅ ЧЕКЛИСТ ПЕРЕД МЕРДЖЕМ

- [ ] Все изменения закоммичены
- [ ] Проверены конфликты между ветками
- [ ] Переключились на develop/main
- [ ] Обновили develop/main (`git pull`)
- [ ] Готовы разрешить конфликты (если возникнут)
- [ ] Есть время на тестирование после мерджа

---

## 📝 ПОСЛЕ МЕРДЖА

1. **Тестирование:**
   - Запустить линтер
   - Проверить TypeScript
   - Запустить тесты
   - Проверить запуск приложения

2. **Push:**
   ```bash
   git push origin develop
   ```

3. **Удаление веток (опционально):**
   ```bash
   git branch -d agent-1-frontend-ui-14-30
   git branch -d agent-2-contexts-14-30
   # ... и так далее
   ```

---

**Последнее обновление:** 2026-01-27  
**Рекомендация:** Мержить ветки **СЕЙЧАС**, перед началом новой работы завтра
