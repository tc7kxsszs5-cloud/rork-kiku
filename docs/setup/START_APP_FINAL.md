# 🚀 Финальные способы запуска приложения

## ✅ Гарантированно работающие варианты

### Вариант 1: Веб-версия (САМЫЙ НАДЕЖНЫЙ)

```bash
npx expo start --web
```

**Что происходит:**
- Запускает Metro Bundler
- Открывает браузер автоматически
- Приложение работает в браузере
- Не нужен телефон или симулятор

**Если не открывается автоматически:**
- Открой браузер вручную
- Перейди по адресу: `http://localhost:8081`

---

### Вариант 2: Интерактивный режим

```bash
npx expo start
```

**Что происходит:**
- Запускает Metro Bundler
- Показывает меню в терминале
- Нажми `w` для веб
- Нажми `i` для iOS симулятора
- Нажми `a` для Android
- Или отсканируй QR код для Expo Go

---

### Вариант 3: С очисткой кэша

```bash
npx expo start --clear --web
```

**Что делает:**
- Очищает кэш Metro Bundler
- Запускает веб-версию
- Используй если есть проблемы с кэшем

---

## 🔧 Если ничего не работает

### Шаг 1: Проверить зависимости

```bash
bun install
```

### Шаг 2: Переустановить зависимости

```bash
rm -rf node_modules
bun install
```

### Шаг 3: Проверить версию Node

```bash
node --version
# Должна быть 18 или выше
```

### Шаг 4: Проверить ошибки в коде

```bash
bunx tsc --noEmit
```

### Шаг 5: Очистить все кэши

```bash
rm -rf .expo
rm -rf node_modules/.cache
rm -rf .metro
npx expo start --clear --web
```

---

## 🎯 Самый надежный способ

### Для тестирования прямо сейчас:

```bash
# 1. Остановить все процессы
pkill -f expo && pkill -f metro

# 2. Очистить кэши
rm -rf .expo node_modules/.cache .metro

# 3. Запустить веб-версию
npx expo start --web
```

**Это гарантированно работает** - откроется в браузере.

---

## 📱 Для тестирования на телефоне

### Если веб работает, но нужен телефон:

1. **Запусти базовый Expo:**
   ```bash
   npx expo start
   ```

2. **Дождись QR кода в терминале**

3. **В Expo Go на телефоне:**
   - Открой Expo Go
   - Нажми "Scan QR code"
   - Отсканируй QR код
   - Или найди в терминале `exp://192.168.x.x:8081` и введи вручную

**Важно:** Телефон и компьютер должны быть в одной Wi-Fi сети.

---

## 💡 Альтернатива: Development Build

Если Expo Go не работает вообще:

```bash
# Создать development build
eas build --profile development --platform ios

# Установить на телефон через TestFlight или прямо
```

Это создаст нативное приложение для тестирования.

---

## ✅ Чеклист

- [ ] Попробовал `npx expo start --web`
- [ ] Попробовал `npx expo start` (интерактивный)
- [ ] Попробовал `npx expo start --clear --web`
- [ ] Проверил зависимости: `bun install`
- [ ] Проверил версию Node: `node --version`
- [ ] Проверил ошибки: `bunx tsc --noEmit`

---

## 🚀 Команда для быстрого запуска

```bash
pkill -f expo && pkill -f metro && rm -rf .expo node_modules/.cache .metro && npx expo start --web
```

**Это очистит всё и запустит веб-версию.**

---

**Начни с: `npx expo start --web`** - это самый надежный способ! 🎯
