# 💡 Простой вариант: Создайте простой пароль в Supabase

## ✅ Если пароль от Mac слишком сложный

Можно создать простой пароль прямо в Supabase:

## 📋 Инструкция

1. Откройте https://supabase.com/dashboard
2. Выберите проект
3. **Settings** → **Database**
4. Найдите **Database password**
5. Нажмите **Reset database password**
6. **Создайте простой пароль:**
   - Используйте только буквы (a-z, A-Z) и цифры (0-9)
   - Примеры: `MySecure123`, `Database456`, `Pass789`
   - **НЕ используйте:** `@`, `:`, `/`, `#`, `%`, `&`, `?`, `=`, пробелы
7. Скопируйте пароль

## 📋 Использование простого пароля

**Если пароль:** `MySecure123`

**Connection string:**
```
postgresql://postgres:MySecure123@aws-0-us-east-1.pooler.supabase.com:6543/postgres
```

**Команды:**
```bash
cd /Users/mac/Desktop/rork-kiku/backend
bunx vercel env rm DATABASE_URL production
bunx vercel env add DATABASE_URL production
# yes
# Вставьте connection string с простым паролем
bunx vercel --prod
```

## ✅ Преимущества простого пароля

- ✅ Не нужно URL-кодировать
- ✅ Меньше ошибок
- ✅ Проще отладить
- ✅ Работает сразу

---

**Рекомендация: Создайте простой пароль в Supabase (только буквы и цифры)!**
