# 📦 Пошаговая установка через npm

## Шаги (выполняйте по порядку!)

### 1. Перейдите в проект
```bash
cd /Users/mac/Desktop/rork-kiku
```

### 2. Остановите все процессы
```bash
killall node
```

### 3. Удалите node_modules и кеши
```bash
rm -rf node_modules
rm -f bun.lockb package-lock.json
npm cache clean --force
```

### 4. Установите зависимости через npm
```bash
npm install
```

**Подождите завершения установки** (может занять 2-5 минут)

### 5. После успешной установки - запустите

**Для Expo Go:**
```bash
npx expo start
```

**Для iOS симулятора:**
```bash
npx expo start --ios
```

**Для веб:**
```bash
npx expo start --web
```

---

## ⚠️ Если npm install тоже дает ошибки

### Попробуйте:
```bash
# Использовать более старую версию npm
npm install --legacy-peer-deps
```

### Или обновить npm:
```bash
npm install -g npm@latest
npm install
```

---

## ✅ После успешной установки

Используйте `npx expo start` вместо `bun run start` - это будет работать стабильнее!
