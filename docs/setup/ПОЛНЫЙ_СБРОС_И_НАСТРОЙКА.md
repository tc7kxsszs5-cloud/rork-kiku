# 🔄 Полный сброс и настройка DATABASE_URL

## 🎯 Пошаговая инструкция с нуля

### Шаг 1: Получите пароль из Supabase

1. Откройте https://supabase.com/dashboard
2. Выберите проект
3. **Settings** → **Database**
4. Найдите **Database password**
5. Если не виден - нажмите **Reset database password**
6. **Скопируйте пароль** (показывается только один раз!)
7. **Сохраните пароль** в безопасном месте

### Шаг 2: Удалите старую переменную

```bash
cd /Users/mac/Desktop/rork-kiku/backend
bunx vercel env rm DATABASE_URL production
```

### Шаг 3: Добавьте новую переменную с Connection Pooling

```bash
bunx vercel env add DATABASE_URL production
```

**Когда появится запрос:**
```
? Your value will be encrypted. Mark as sensitive? (y/N)
```
**Ответ:** `yes` или `y`

**Когда появится запрос:**
```
? What's the value of DATABASE_URL?
```

**Вставьте (замените `[ПАРОЛЬ]` на реальный пароль из шага 1):**

```
postgresql://postgres.eznumgsmwvavyunqhxfc:[ПАРОЛЬ]@aws-0-us-east-1.pooler.supabase.com:6543/postgres
```

**Нажмите Enter**

### Шаг 4: Перезапустите деплой

```bash
bunx vercel --prod
```

### Шаг 5: Проверьте подключение

```bash
# Подождите 20-25 секунд после деплоя
curl "https://backend-three-mauve-67.vercel.app/api/trpc/test.dbCheck"
```

## ✅ Успешный ответ

Если все правильно, вы увидите:
```json
{
  "result": {
    "data": {
      "json": {
        "success": true,
        "version": "PostgreSQL 15.x",
        "tables": [],
        "extensions": ["plpgsql"]
      }
    }
  }
}
```

## ❌ Если ошибка сохраняется

Попробуйте другие регионы:

**Вариант 2 (eu-west-1):**
```bash
bunx vercel env rm DATABASE_URL production
bunx vercel env add DATABASE_URL production
# yes
# Вставьте:
postgresql://postgres.eznumgsmwvavyunqhxfc:[ПАРОЛЬ]@aws-0-eu-west-1.pooler.supabase.com:6543/postgres
```

**Вариант 3 (ap-southeast-1):**
```bash
bunx vercel env rm DATABASE_URL production
bunx vercel env add DATABASE_URL production
# yes
# Вставьте:
postgresql://postgres.eznumgsmwvavyunqhxfc:[ПАРОЛЬ]@aws-0-ap-southeast-1.pooler.supabase.com:6543/postgres
```

## 📋 Формат Connection String

**Правильный формат:**
```
postgresql://postgres.[PROJECT-REF]:[ПАРОЛЬ]@aws-0-[REGION].pooler.supabase.com:6543/postgres
```

**Ваш случай:**
```
postgresql://postgres.eznumgsmwvavyunqhxfc:[ПАРОЛЬ]@aws-0-us-east-1.pooler.supabase.com:6543/postgres
```

**Ключевые моменты:**
- ✅ Пользователь: `postgres.eznumgsmwvavyunqhxfc` (с точкой)
- ✅ Хост: `aws-0-us-east-1.pooler.supabase.com` (pooler, не db)
- ✅ Порт: `6543` (не 5432)
- ✅ База: `/postgres`

---

**Выполните все шаги по порядку!**
