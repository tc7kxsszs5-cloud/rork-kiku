# ✅ Инструкция: Как скопировать SQL

## ❌ Ошибка:

Вы скопировали путь к файлу (`backend/schema.sql`) вместо содержимого файла.

## ✅ Правильный способ:

### Шаг 1: Откройте файл

Откройте файл `СКОПИРОВАТЬ_ЭТОТ_SQL.txt` в редакторе.

### Шаг 2: Скопируйте ВЕСЬ текст

Выделите **весь** текст в файле (Ctrl+A или Cmd+A) и скопируйте (Ctrl+C или Cmd+C).

### Шаг 3: Вставьте в Supabase

1. Откройте https://supabase.com/dashboard
2. Выберите проект
3. **SQL Editor** → **New query**
4. Вставьте скопированный SQL (Ctrl+V или Cmd+V)
5. Нажмите **Run** или **Execute**

---

## 💡 Важно:

- ❌ НЕ копируйте путь к файлу (`backend/schema.sql`)
- ✅ Копируйте СОДЕРЖИМОЕ файла (SQL код)

---

**Откройте файл `СКОПИРОВАТЬ_ЭТОТ_SQL.txt` и скопируйте весь SQL код оттуда!**
