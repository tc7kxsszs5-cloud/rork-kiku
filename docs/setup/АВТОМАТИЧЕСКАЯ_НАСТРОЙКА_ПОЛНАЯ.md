# 🚀 Полная автоматическая настройка

## ✅ Что я могу сделать автоматически

Я подготовил скрипты и файлы, которые упростят настройку!

---

## 📋 Вариант 1: Через Vercel CLI (рекомендуется)

### Шаг 1: Установите Vercel CLI

```bash
npm install -g vercel
```

### Шаг 2: Войдите в Vercel

```bash
vercel login
```

(Откроется браузер для входа)

### Шаг 3: Деплой

```bash
cd backend
vercel
```

Ответьте на вопросы (или нажмите Enter для значений по умолчанию).

### Шаг 4: Настройка Environment Variables

**Вариант A: Через скрипт (автоматически)**

```bash
cd backend
chmod +x setup-env.sh
./setup-env.sh
```

Скрипт попросит ввести Connection String из Supabase.

**Вариант B: Вручную через команды**

```bash
cd backend

# Добавить DATABASE_URL
vercel env add DATABASE_URL production
# (Вставьте ваш connection string из Supabase)

# Добавить NODE_ENV
vercel env add NODE_ENV production
# (Введите: production)
```

**Вариант C: Через Vercel Dashboard (самый простой)**

1. Идите на [vercel.com/dashboard](https://vercel.com/dashboard)
2. Выберите проект `kiku-backend`
3. Settings → Environment Variables
4. Добавьте:
   - `DATABASE_URL` = ваш connection string
   - `NODE_ENV` = `production`

### Шаг 5: Production деплой

```bash
vercel --prod
```

---

## 📋 Вариант 2: Через GitHub + Vercel (без CLI)

### Шаг 1: Закоммитьте изменения

```bash
git add backend/
git commit -m "Add backend for Vercel deployment"
git push
```

### Шаг 2: Подключите GitHub к Vercel

1. Идите на [vercel.com](https://vercel.com)
2. Нажмите **"Add New..."** → **"Project"**
3. Выберите **"Import Git Repository"**
4. Выберите ваш репозиторий `rork-kiku`
5. Настройте проект:
   - **Framework Preset:** Other
   - **Root Directory:** `backend`
   - **Build Command:** (оставьте пустым)
   - **Output Directory:** (оставьте пустым)
   - **Install Command:** `cd backend && bun install`
6. Нажмите **"Deploy"**

### Шаг 3: Настройте Environment Variables

1. После первого деплоя идите в **Settings** → **Environment Variables**
2. Добавьте:
   - `DATABASE_URL` = ваш connection string из Supabase
   - `NODE_ENV` = `production`
3. Нажмите **"Save"**
4. Vercel автоматически передеплоит с новыми переменными

---

## 📋 Вариант 3: Через Vercel Dashboard (полностью вручную)

### Шаг 1: Подготовьте файлы

Все файлы уже готовы:
- ✅ `backend/index.ts`
- ✅ `backend/vercel.json`
- ✅ `backend/package.json`

### Шаг 2: Создайте проект в Vercel

1. [vercel.com/new](https://vercel.com/new)
2. Выберите **"Deploy"** → **"Upload"**
3. Создайте ZIP архив из папки `backend`:
   ```bash
   cd backend
   zip -r ../backend.zip .
   ```
4. Загрузите `backend.zip` в Vercel
5. Нажмите **"Deploy"**

### Шаг 3: Настройте Environment Variables

1. Settings → Environment Variables
2. Добавьте `DATABASE_URL` и `NODE_ENV`

---

## 🎯 Какой вариант выбрать?

- **Вариант 1** - если у вас установлен Node.js и вы готовы использовать CLI
- **Вариант 2** - если вы используете GitHub (рекомендуется для автоматических деплоев)
- **Вариант 3** - если хотите всё сделать через веб-интерфейс

---

## ✅ После настройки

1. Проверьте работу API:
   ```
   https://your-backend.vercel.app/
   ```

2. Обновите `lib/trpc.ts`:
   ```typescript
   const PROJECT_BASE_URL = 'https://your-backend.vercel.app';
   ```

---

## 🚀 Готово!

Выберите удобный вариант и следуйте инструкциям!
