# Решение проблемы запуска - ФИНАЛ

## ❌ Проблема: Порт занят

**Ошибка:**
```
Port 8081 is running this app in another window
Input is required, but 'npx expo' is in non-interactive mode.
```

## ✅ Решение

### Вариант 1: Автоматическое исправление (рекомендуется)

```bash
# Освободить порты и запустить
bun run fix
bun run start:web
```

### Вариант 2: Вручную

```bash
# 1. Освободить порты
bun run kill-ports

# Или вручную:
lsof -ti:8081 | xargs kill -9
lsof -ti:8082 | xargs kill -9

# 2. Запустить на порту 8082
bun run start:web
```

### Вариант 3: Запуск на другом порту

```bash
# Запустить на порту 8083
PORT=8083 bunx expo start --web
```

## 🔧 Что исправлено

1. ✅ Добавлен флаг `--port 8082` в `start:web`
2. ✅ Создан скрипт `kill-ports.sh` для освобождения портов
3. ✅ Улучшен скрипт `quick-fix.sh`

## 🚀 Правильный запуск

```bash
# 1. Освободить порты (если заняты)
bun run kill-ports

# 2. Запустить приложение
bun run start:web
```

Или одной командой:
```bash
bun run fix && bun run start:web
```

## 📱 После запуска

1. Откройте браузер: `http://localhost:8082`
2. Проверьте консоль (F12) на ошибки
3. Проверьте все вкладки

## ✅ Все готово!

Теперь приложение должно запускаться без проблем.
