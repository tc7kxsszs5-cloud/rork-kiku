# 🔑 Создание простого пароля для Supabase

## 💡 Решение проблемы

Если пароль содержит специальные символы, создайте простой пароль (только буквы и цифры).

## 📋 Инструкция

### Шаг 1: Создайте простой пароль в Supabase

1. Откройте https://supabase.com/dashboard
2. Выберите проект
3. **Settings** → **Database**
4. Найдите **Database password**
5. Нажмите **Reset database password**
6. Создайте простой пароль:
   - ✅ Используйте только буквы (a-z, A-Z) и цифры (0-9)
   - ❌ НЕ используйте: `@`, `:`, `/`, `#`, `%`, `&`, `?`, `=`, пробелы
   - **Примеры хороших паролей:** `MyPass123`, `SecurePass456`, `Database789`

### Шаг 2: Используйте простой пароль

**Формат:**
```
postgresql://postgres:ПРОСТОЙ_ПАРОЛЬ@db.eznumgsmwvavyunqhxfc.supabase.co:5432/postgres
```

**Пример:**
Если пароль `MyPass123`:
```
postgresql://postgres:MyPass123@db.eznumgsmwvavyunqhxfc.supabase.co:5432/postgres
```

### Шаг 3: Обновите в Vercel

```bash
cd /Users/mac/Desktop/rork-kiku/backend
bunx vercel env rm DATABASE_URL production
bunx vercel env add DATABASE_URL production
# Вставьте connection string с простым паролем
bunx vercel --prod
```

### Шаг 4: Проверьте

```bash
curl "https://backend-three-mauve-67.vercel.app/api/trpc/test.dbCheck"
```

## ✅ Преимущества простого пароля

- ✅ Не нужно URL-кодировать
- ✅ Меньше ошибок при копировании
- ✅ Проще отладить проблемы
- ✅ Работает сразу без дополнительной обработки

---

**Создайте простой пароль (только буквы и цифры) и используйте его!**
