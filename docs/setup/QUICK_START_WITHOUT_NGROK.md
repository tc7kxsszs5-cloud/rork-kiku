# 🚀 Быстрый запуск БЕЗ ngrok

## 🔍 Проблема

ngrok tunnel не подключается, все команды с `--tunnel` падают с ошибкой.

## ✅ Решение: Использовать Expo напрямую

### Команды для запуска (по порядку):

#### 1. Простой запуск (LAN)
```bash
bun run start:simple
```
Или напрямую:
```bash
bunx expo start --lan
```

**Что делать:**
- Дождись QR кода в терминале
- Открой Expo Go на телефоне
- Отсканируй QR код
- **Важно:** Телефон и компьютер в одной Wi-Fi сети

---

#### 2. С очисткой кэша (если первый не работает)
```bash
bun run start:clear
```
Или напрямую:
```bash
bunx expo start --lan --clear
```

Это очистит кэш Metro Bundler и перезапустит.

---

#### 3. Веб-версия (для тестирования в браузере)
```bash
bun run start:web
```
Или напрямую:
```bash
bunx expo start --web
```

Откроется в браузере - можно тестировать без телефона.

---

#### 4. С указанием порта (если порт занят)
```bash
bunx expo start --lan --port 8083
```

Expo предложит использовать другой порт, если 8081 занят.

---

## 📋 Пошаговая инструкция

### Шаг 1: Остановить все процессы
```bash
pkill -f "expo"
pkill -f "metro"
sleep 2
```

### Шаг 2: Запустить проект
```bash
bun run start:simple
```

### Шаг 3: Подключиться через Expo Go
1. Дождись QR кода в терминале
2. Открой Expo Go на телефоне
3. Отсканируй QR код
4. Или найди в терминале `exp://192.168.x.x:8081` и введи вручную

---

## ⚠️ Если не работает

### Проблема: "Port 8081 is running this app in another window"

**Решение:**
```bash
# Остановить процесс на порту
lsof -ti:8081 | xargs kill -9

# Или использовать другой порт
bunx expo start --lan --port 8083
```

### Проблема: "пригодные данные не найдены" в Expo Go

**Причины:**
- Телефон и компьютер в разных сетях
- VPN блокирует локальную сеть
- Брандмауэр блокирует подключение

**Решение:**
- Убедись что оба устройства в одной Wi-Fi сети
- Отключи VPN
- Проверь брандмауэр
- Используй веб-версию: `bun run start:web`

---

## 🎯 Рекомендуемый порядок действий

1. **Остановить процессы:**
   ```bash
   pkill -f "expo" && pkill -f "metro"
   ```

2. **Запустить простым способом:**
   ```bash
   bun run start:simple
   ```

3. **Если не работает - с очисткой:**
   ```bash
   bun run start:clear
   ```

4. **Если всё равно не работает - веб:**
   ```bash
   bun run start:web
   ```

---

## 💡 Советы

- **Всегда используй одну Wi-Fi сеть** для LAN подключения
- **Останавливай процессы** перед новым запуском
- **Используй веб-версию** для быстрого тестирования
- **Не используй команды с `--tunnel`** если ngrok не работает

---

## ✅ Быстрая команда для перезапуска

```bash
pkill -f "expo" && pkill -f "metro" && sleep 2 && bun run start:simple
```

---

**Эти команды работают БЕЗ ngrok и Rork платформы!** 🚀
