# 💡 Связь между задачами и коммитами

## 🎯 Как это работает

### Ваш процесс работы:

```
1. Вы даете мне ЗАДАЧУ
   ↓
   "Исправить ошибку TypeScript"
   "Добавить тесты"
   "Настроить CI/CD"

2. МЫ ВМЕСТЕ РЕАЛИЗУЕМ задачу
   ↓
   Я делаю изменения в коде
   Вы проверяете

3. СОЗДАЕМ КОММИТ с описанием
   ↓
   git commit -m "Исправил ошибку TypeScript"
   git commit -m "Добавил unit тесты"
   git commit -m "Настроил CI/CD workflows"
```

---

## ✅ ДА, ВСЕ ИСПРАВЛЕНИЯ НУЖНО ОТРАЖАТЬ В COMMIT MESSAGE

### Правильно:

**Ваша задача:** "Исправить ошибку TypeScript в profile.tsx"

**Мы делаем:** Исправляем код

**Commit message:**
```bash
git commit -m "Исправил ошибку TypeScript в profile.tsx"
```

✅ **ДА** - все исправления и задачи отражаются в commit message!

---

## 📋 Примеры из нашей работы

### Пример 1: Исправление ошибок

**Задача:** "Исправить ошибки TypeScript (удалить role из User)"

**Что мы сделали:**
- Удалили свойство `role` из типа `User`
- Обновили `profile.tsx`, `role-selection.tsx`, `security-settings.tsx`

**Commit message:**
```bash
git commit -m "Описание ваших изменений"
```
или лучше:
```bash
git commit -m "Исправил ошибки TypeScript: удалил role из User"
```

---

### Пример 2: Добавление тестов

**Задача:** "Добавить unit тесты для utilities"

**Что мы сделали:**
- Создали `cursorStyles.test.ts`
- Создали `soundNotifications.test.ts`
- Настроили Jest

**Commit message:**
```bash
git commit -m "Добавил unit тесты для utilities"
```

---

### Пример 3: Настройка CI/CD

**Задача:** "Настроить CI/CD workflows"

**Что мы сделали:**
- Обновили `.github/workflows/ci.yml`
- Настроили проверки

**Commit message:**
```bash
git commit -m "Настроил CI/CD workflows"
```

---

### Пример 4: Очистка проекта

**Задача:** "Удалить ненужные папки"

**Что мы сделали:**
- Удалили странные папки
- Обновили `.gitignore`

**Commit message:**
```bash
git commit -m "Удалил ненужные папки и обновил .gitignore"
```

---

## 💡 Правило

### Commit message = Описание того, что СДЕЛАНО

**НЕ задача, а РЕЗУЛЬТАТ:**

```
❌ Плохо: "Исправить ошибку" (это задача)
✅ Хорошо: "Исправил ошибку" (это результат)
```

---

## 📝 Как правильно формулировать

### Шаблон:

```
[Глагол в прошедшем времени] [что сделано]
```

Примеры:
- ✅ "Исправил ошибку TypeScript"
- ✅ "Добавил unit тесты"
- ✅ "Настроил CI/CD"
- ✅ "Обновил документацию"
- ✅ "Удалил ненужные файлы"

---

## 🎯 Практические примеры

### Если вы дали мне задачу:

**Задача:** "Добавить push notifications в backend"

**Мы сделали:**
- Создали `send-push.ts`
- Интегрировали в `app-router.ts`

**Commit message:**
```bash
git commit -m "Добавил push notifications в backend"
```

---

### Если мы исправили несколько вещей:

**Задачи:**
- "Исправить ошибки TypeScript"
- "Добавить тесты"
- "Настроить CI/CD"

**Мы сделали:** Все вместе

**Commit message:**
```bash
git commit -m "Обновления проекта: исправления TypeScript, тесты, CI/CD"
```

Или отдельными коммитами:
```bash
git commit -m "Исправил ошибки TypeScript"
git commit -m "Добавил unit тесты"
git commit -m "Настроил CI/CD workflows"
```

---

## ✅ Чеклист

Перед коммитом:

- [ ] Задача выполнена ✅
- [ ] Код изменен ✅
- [ ] Commit message описывает что СДЕЛАНО ✅
- [ ] Commit message начинается с глагола (прошедшее время) ✅

---

## 💡 Главное правило

**ВСЕ задачи и исправления, которые мы реализуем, ДОЛЖНЫ быть отражены в commit message!**

Commit message = краткое описание результата работы

---

## 🔗 Связь

```
Ваша задача
    ↓
Наша работа (исправления, добавления)
    ↓
Commit message (описание результата)
    ↓
GitHub (история изменений)
```

---

**Вывод:** Да, все исправления и задачи отражаются в commit message! Это описание того, что было сделано. ✅
