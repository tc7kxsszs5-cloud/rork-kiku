# 📝 План коммитов

## 🎯 Группировка изменений

### 1. Коммит: Унификация логирования
**Файлы:**
- `constants/MonitoringContext.tsx`
- `constants/AnalyticsContext.tsx`
- `constants/UserContext.tsx`
- `constants/ParentalControlsContext.tsx`
- `constants/AuthContext.tsx`
- `constants/ChatBackgroundsContext.tsx`
- `constants/ThemeContext.tsx`
- `constants/AIModerationService.ts`
- `app/_layout.tsx`
- `app/chat/[chatId].tsx`

**Сообщение:**
```
feat: unify logging - replace console.* with logger.*

- Replace all console.log/error/warn/info with structured logger
- Add context and action metadata to all log entries
- Improve debugging and monitoring capabilities
- ~60+ replacements across constants/ and app/ directories
```

### 2. Коммит: Централизованная обработка ошибок
**Файлы:**
- `utils/errorHandler.ts` (новый файл)
- `constants/MonitoringContext.tsx`
- `constants/ParentalControlsContext.tsx`
- `constants/UserContext.tsx`

**Сообщение:**
```
feat: add centralized error handling with i18n support

- Create utils/errorHandler.ts with user-friendly error messages
- Integrate errorHandler into MonitoringContext, ParentalControlsContext, UserContext
- Add i18n support for error messages (en/ru)
- Add showUserFriendlyError, handleErrorSilently, withErrorHandling functions
```

### 3. Коммит: Добавление переводов ошибок
**Файлы:**
- `constants/locales/en.ts`
- `constants/locales/ru.ts`

**Сообщение:**
```
feat: add error message translations to i18n

- Add common.errors section to English and Russian locales
- Support for network, sync, load, save, permission, storage, analysis, SOS, auth errors
- Default error message for fallback cases
```

### 4. Коммит: Документация улучшений
**Файлы:**
- `СТАТУС_УЛУЧШЕНИЙ.md`
- `ИТОГОВЫЙ_ОТЧЕТ_УЛУЧШЕНИЙ.md`
- `ФИНАЛЬНЫЙ_ОТЧЕТ_УЛУЧШЕНИЙ.md`
- `ОБНОВЛЕНИЕ_ERRORHANDLER_I18N.md`
- `ПЛАН_КОММИТОВ.md` (этот файл)

**Сообщение:**
```
docs: add code quality improvement reports

- Add status and final reports for logging unification
- Document errorHandler i18n integration
- Include improvement metrics and results
```

---

## 🚀 Команды для выполнения

```bash
# 1. Унификация логирования
git add constants/MonitoringContext.tsx constants/AnalyticsContext.tsx constants/UserContext.tsx constants/ParentalControlsContext.tsx constants/AuthContext.tsx constants/ChatBackgroundsContext.tsx constants/ThemeContext.tsx constants/AIModerationService.ts app/_layout.tsx app/chat/[chatId].tsx
git commit -m "feat: unify logging - replace console.* with logger.*

- Replace all console.log/error/warn/info with structured logger
- Add context and action metadata to all log entries
- Improve debugging and monitoring capabilities
- ~60+ replacements across constants/ and app/ directories"

# 2. Централизованная обработка ошибок
git add utils/errorHandler.ts
git add constants/MonitoringContext.tsx constants/ParentalControlsContext.tsx constants/UserContext.tsx
git commit -m "feat: add centralized error handling with i18n support

- Create utils/errorHandler.ts with user-friendly error messages
- Integrate errorHandler into MonitoringContext, ParentalControlsContext, UserContext
- Add i18n support for error messages (en/ru)
- Add showUserFriendlyError, handleErrorSilently, withErrorHandling functions"

# 3. Переводы ошибок
git add constants/locales/en.ts constants/locales/ru.ts
git commit -m "feat: add error message translations to i18n

- Add common.errors section to English and Russian locales
- Support for network, sync, load, save, permission, storage, analysis, SOS, auth errors
- Default error message for fallback cases"

# 4. Документация
git add СТАТУС_УЛУЧШЕНИЙ.md ИТОГОВЫЙ_ОТЧЕТ_УЛУЧШЕНИЙ.md ФИНАЛЬНЫЙ_ОТЧЕТ_УЛУЧШЕНИЙ.md ОБНОВЛЕНИЕ_ERRORHANDLER_I18N.md ПЛАН_КОММИТОВ.md
git commit -m "docs: add code quality improvement reports

- Add status and final reports for logging unification
- Document errorHandler i18n integration
- Include improvement metrics and results"
```

---

## ✅ Проверка перед коммитом

1. ✅ `bun run lint` - 0 errors
2. ✅ `bunx tsc --noEmit` - 0 errors
3. ✅ `bun test` - все тесты проходят
4. ✅ Все файлы добавлены в git

---

## 📊 Статистика изменений

- **Файлов изменено:** ~20
- **Строк добавлено:** ~500+
- **Строк удалено:** ~200+
- **Новых файлов:** 5 (errorHandler.ts + 4 документа)

---

**Готово к коммиту!** 🚀
