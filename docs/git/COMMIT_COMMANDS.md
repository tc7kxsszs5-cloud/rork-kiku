# 📋 Команды для коммита изменений

## ✅ Что будет закоммичено

1. **Переключение языков (ru/en)** - полностью реализовано
2. **Исправление ошибок TypeScript** - analytics.tsx, kpiExport.ts
3. **Новые команды запуска** - без ngrok
4. **Документация** - инструкции по запуску и веб-версии

---

## 🚀 Команды для выполнения

### Шаг 1: Добавить все изменения

```bash
# Основные файлы
git add app/(tabs)/profile.tsx
git add app/(tabs)/analytics.tsx
git add utils/kpiExport.ts
git add package.json

# Документация
git add EXPO_GO_CONNECTION_FIX.md
git add QUICK_START_WITHOUT_NGROK.md
git add ALL_START_OPTIONS.md
git add START_APP_FINAL.md
git add WEB_VERSION_EXPLANATION.md
git add COMMIT_COMMANDS.md

# Или добавить все изменения сразу
git add .
```

---

### Шаг 2: Закоммитить

```bash
git commit -m "feat: добавить переключение языков и исправить ошибки

- Реализовано переключение между русским и английским языком
- Исправлены ошибки TypeScript в analytics.tsx и kpiExport.ts
- Добавлены новые команды запуска без ngrok (start:simple, start:clear, start:web)
- Добавлена документация по запуску приложения и веб-версии
- Исправлена интеграция i18n для автоматического применения языка"
```

---

### Шаг 3: Запушить в GitHub

```bash
git push origin main
```

---

## 📋 Полная последовательность команд

```bash
# Остановить все процессы (если запущены)
pkill -f expo && pkill -f metro

# Добавить все изменения
git add .

# Закоммитить
git commit -m "feat: добавить переключение языков и исправить ошибки

- Реализовано переключение между русским и английским языком
- Исправлены ошибки TypeScript в analytics.tsx и kpiExport.ts
- Добавлены новые команды запуска без ngrok
- Добавлена документация по запуску и веб-версии"

# Запушить
git push origin main
```

---

## 🔍 Проверка перед коммитом

### Проверить что изменилось:

```bash
git status
```

### Посмотреть изменения:

```bash
git diff app/(tabs)/profile.tsx
git diff app/(tabs)/analytics.tsx
git diff utils/kpiExport.ts
```

### Проверить что нет ошибок:

```bash
bunx tsc --noEmit
```

---

## ✅ После коммита

После успешного коммита и push:

1. ✅ Все изменения будут в GitHub
2. ✅ Можно продолжать работу
3. ✅ Можно запускать приложение: `npx expo start --web`

---

**Выполни команды выше для коммита всех изменений!** 🚀
