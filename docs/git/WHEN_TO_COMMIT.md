# ⏰ Когда делать коммиты

## ✅ Краткий ответ

**НЕ обязательно после КАЖДОГО изменения, но коммитьте регулярно!**

Лучше коммитить **логически связанные изменения** вместе.

---

## 🎯 Когда делать коммиты

### ✅ Коммитьте, когда:

1. **Задача выполнена** (закончили работу над одной функцией)
   ```
   ✅ Хорошо: "Добавил push notifications"
   ✅ Все изменения связаны с одной задачей
   ```

2. **Исправление готово** (исправили одну проблему)
   ```
   ✅ Хорошо: "Исправил ошибку TypeScript в profile.tsx"
   ✅ Все изменения связаны с одним исправлением
   ```

3. **Логически связанные изменения** (несколько файлов для одной цели)
   ```
   ✅ Хорошо: "Настроил CI/CD workflows"
   ✅ Изменили несколько файлов, но все для одной цели
   ```

4. **В конце рабочего дня** (сохранить прогресс)
   ```
   ✅ Хорошо: "Добавил тесты и исправил ошибки"
   ✅ Сохранить всю работу за день
   ```

---

## ❌ Когда НЕ нужно коммитить

### ❌ Не коммитьте:

1. **После каждого сохранения файла**
   ```
   ❌ Плохо: 10 коммитов за час с названиями "сохранение", "еще сохранение"
   ✅ Хорошо: 1 коммит "Добавил функцию синхронизации" после завершения работы
   ```

2. **Незавершенную работу**
   ```
   ❌ Плохо: "Начал добавлять тесты" (работа не закончена)
   ✅ Хорошо: "Добавил unit тесты для utilities" (работа завершена)
   ```

3. **Разные несвязанные задачи в одном коммите**
   ```
   ❌ Плохо: "Исправил ошибки, добавил тесты, обновил документацию, удалил файлы"
   ✅ Хорошо: Отдельные коммиты для каждой задачи
   ```

---

## 💡 Практические примеры

### Пример 1: Одна задача - один коммит

**Задача:** "Добавить push notifications"

**Работа:**
- Создали `send-push.ts`
- Обновили `app-router.ts`
- Добавили типы

**Коммит:**
```bash
git commit -m "Добавил push notifications в backend"
```

✅ **ОДИН коммит** - все изменения для одной задачи!

---

### Пример 2: Несколько задач - несколько коммитов

**Задачи:**
1. "Исправить ошибки TypeScript"
2. "Добавить тесты"
3. "Обновить документацию"

**Работа:**
- Исправили ошибки
- Добавили тесты
- Обновили документацию

**Коммиты:**
```bash
git commit -m "Исправил ошибки TypeScript"
git commit -m "Добавил unit тесты"
git commit -m "Обновил документацию"
```

✅ **ТРИ коммита** - каждая задача отдельно!

---

### Пример 3: Большая задача - несколько коммитов

**Задача:** "Настроить синхронизацию данных"

**Работа:**
- Создали sync routes (день 1)
- Добавили тесты (день 2)
- Интегрировали в frontend (день 3)

**Коммиты:**
```bash
git commit -m "Добавил sync routes для alerts и chats"
git commit -m "Добавил тесты для sync routes"
git commit -m "Интегрировал sync в frontend"
```

✅ **НЕСКОЛЬКО коммитов** - логические этапы одной задачи!

---

## 📋 Рекомендации

### ✅ Хорошая практика:

1. **Коммитьте логически связанные изменения**
   ```
   ✅ "Добавил функцию X" - все файлы для функции X
   ✅ "Исправил ошибку Y" - все изменения для исправления Y
   ```

2. **Коммитьте когда задача завершена**
   ```
   ✅ Работа закончена → коммит
   ❌ Работа не закончена → не коммитьте
   ```

3. **Коммитьте регулярно** (не реже раза в день)
   ```
   ✅ В конце дня - коммит с всей работой
   ✅ После выполнения задачи - коммит
   ```

4. **Один коммит = одна логическая единица работы**
   ```
   ✅ Одна функция
   ✅ Одно исправление
   ✅ Одна настройка
   ```

---

## ⏰ Частота коммитов

### Рекомендуемая частота:

- **Минимум:** 1 коммит в день (в конце работы)
- **Оптимально:** 2-5 коммитов в день (после каждой задачи)
- **Максимум:** 10-15 коммитов в день (если много маленьких задач)

### НЕ рекомендуется:

- ❌ 50+ коммитов в день (слишком часто)
- ❌ 1 коммит в неделю (слишком редко)

---

## 🎯 Практические сценарии

### Сценарий 1: Работа над одной функцией (2 часа)

**Работа:**
- Создали файлы
- Написали код
- Протестировали

**Коммит:**
```bash
git commit -m "Добавил функцию синхронизации данных"
```

✅ **1 коммит** после завершения!

---

### Сценарий 2: Работа над несколькими задачами (день)

**Задачи:**
- Утро: Исправил ошибки
- День: Добавил тесты
- Вечер: Обновил документацию

**Коммиты:**
```bash
git commit -m "Исправил ошибки TypeScript"
git commit -m "Добавил unit тесты"
git commit -m "Обновил документацию"
```

✅ **3 коммита** - по одному на задачу!

---

### Сценарий 3: Долгая задача (несколько дней)

**Задача:** "Рефакторинг системы мониторинга"

**День 1:** Создал новую структуру
```bash
git commit -m "Рефакторинг: создал новую структуру мониторинга"
```

**День 2:** Мигрировал старый код
```bash
git commit -m "Рефакторинг: мигрировал старый код"
```

**День 3:** Добавил тесты
```bash
git commit -m "Рефакторинг: добавил тесты"
```

✅ **Несколько коммитов** - логические этапы!

---

## ✅ Чеклист перед коммитом

Перед коммитом проверьте:

- [ ] Задача/исправление завершено?
- [ ] Изменения логически связаны?
- [ ] Код работает (хотя бы компилируется)?
- [ ] Commit message понятный?
- [ ] Не смешиваете разные задачи?

---

## 💡 Золотые правила

1. **Коммитьте логически связанные изменения вместе**
2. **Коммитьте когда задача завершена**
3. **Коммитьте регулярно (1-5 раз в день)**
4. **Один коммит = одна логическая единица работы**
5. **Лучше несколько маленьких коммитов, чем один большой**

---

## 🔗 Связь с нашей работой

### Пример из нашей работы:

**Задача:** "Исправить ошибки TypeScript, добавить тесты, настроить CI/CD"

**Что мы сделали:**
- Исправили ошибки
- Добавили тесты
- Настроили CI/CD
- Удалили папки

**Как мы сделали:**
```bash
git commit -m "Описание ваших изменений"  # Все вместе
```

**Как можно было сделать лучше:**
```bash
git commit -m "Исправил ошибки TypeScript"
git commit -m "Добавил unit тесты"
git commit -m "Настроил CI/CD workflows"
git commit -m "Удалил ненужные папки"
```

Оба варианта правильные! Но второй более детальный.

---

**Вывод:** Коммитьте регулярно, но логически связанные изменения вместе! ✅
