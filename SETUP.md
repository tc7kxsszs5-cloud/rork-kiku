# 🚀 Руководство по настройке проекта KIKU

Это руководство поможет вам настроить проект для работы без ошибок.

## 📋 Требования

- **Node.js**: 18.x или выше
- **Bun**: 1.0+ (рекомендуется) или npm/yarn
- **Xcode**: 15.2+ (для iOS разработки)
- **Android Studio**: (для Android разработки)
- **Git**: последняя версия

## 🔧 Установка

### 1. Клонирование репозитория

```bash
git clone https://github.com/tc7kxsszs5-cloud/rork-kiku.git
cd rork-kiku
```

### 2. Установка зависимостей

```bash
# Используя Bun (рекомендуется)
bun install

# Или используя npm
npm install
```

### 3. Настройка переменных окружения

```bash
# Скопируйте пример файла окружения
cp .env.example .env

# Отредактируйте .env и заполните необходимые значения
# См. раздел "Переменные окружения" ниже
```

### 4. Настройка iOS (только для macOS)

```bash
cd ios
pod install
cd ..
```

**Примечание:** Если возникают проблемы с CocoaPods:
- Убедитесь, что установлен CocoaPods: `sudo gem install cocoapods`
- Или используйте: `bunx pod-install`

### 5. Проверка установки

```bash
# Проверка TypeScript
bun run typecheck

# Проверка линтера
bun run lint

# Запуск тестов
bun run test
```

## 🏃 Запуск проекта

### Разработка (Expo)

```bash
# Запуск Expo сервера
bun run start:simple

# Или с туннелем (для тестирования на устройстве)
bun run start
```

**Команды в Expo CLI:**
- `i` - открыть в iOS симуляторе
- `a` - открыть в Android эмуляторе
- `w` - открыть в веб-браузере
- `r` - перезагрузить приложение

### Нативная сборка

```bash
# iOS
bun run ios

# Android
bun run android
```

## 🔐 Переменные окружения

Создайте файл `.env` в корне проекта на основе `.env.example`:

```env
# Expo
EXPO_PUBLIC_API_URL=http://localhost:3000

# API Keys (если нужны)
# OPENAI_API_KEY=your_key_here
# ANALYTICS_KEY=your_key_here

# Development
NODE_ENV=development
```

**⚠️ Важно:** Никогда не коммитьте файл `.env` в репозиторий!

## 📁 Структура проекта

```
rork-kiku/
├── app/                    # Экранные компоненты (Expo Router)
├── components/             # Переиспользуемые компоненты
├── constants/              # Константы, контексты, типы
├── utils/                  # Утилиты и хелперы
├── hooks/                  # React хуки
├── __tests__/              # Тесты
├── ios/                    # iOS нативный код
├── android/                # Android нативный код
├── assets/                 # Статические ресурсы
└── backend/               # Backend код (Hono + tRPC)
```

## 🛠️ Полезные команды

```bash
# Разработка
bun run start:simple       # Запуск Expo (локально)
bun run start              # Запуск через Rork платформу
bun run start:web          # Запуск веб-версии

# Проверка кода
bun run lint               # Проверка линтером
bun run lint:fix           # Автоисправление ошибок
bun run typecheck          # Проверка TypeScript

# Тестирование
bun run test               # Запуск всех тестов
bun run test:watch        # Тесты в watch режиме
bun run test:coverage     # С покрытием кода

# Сборка
bun run ios                # Сборка для iOS
bun run android            # Сборка для Android
```

## 🐛 Решение проблем

### Проблемы с зависимостями

```bash
# Очистка и переустановка
rm -rf node_modules bun.lock
bun install
```

### Проблемы с iOS Pods

```bash
cd ios
rm -rf Pods Podfile.lock
pod install
cd ..
```

### Проблемы с кэшем

```bash
# Очистка кэша Expo
bun run start:clear

# Или вручную
rm -rf .expo node_modules/.cache
```

### Проблемы с TypeScript

```bash
# Пересборка типов
bunx tsc --noEmit
```

## 📚 Дополнительная документация

- [README.md](README.md) - Общая информация о проекте
- [CONTRIBUTING.md](CONTRIBUTING.md) - Руководство по вкладу в проект
- [DEVELOPER_GUIDE.md](DEVELOPER_GUIDE.md) - Детальное руководство для разработчиков
- [DESIGN_SYSTEM.md](DESIGN_SYSTEM.md) - Дизайн-система проекта

## ✅ Чеклист перед началом работы

- [ ] Установлены все зависимости (`bun install`)
- [ ] Создан файл `.env` с необходимыми переменными
- [ ] Установлены Pods для iOS (если работаете с iOS)
- [ ] Проект запускается (`bun run start:simple`)
- [ ] Тесты проходят (`bun run test`)
- [ ] Линтер не показывает ошибок (`bun run lint`)

## 💡 Советы

1. **Используйте Bun** - это быстрее чем npm/yarn
2. **Следуйте структуре проекта** - не создавайте файлы в неправильных местах
3. **Пишите тесты** - для новых функций обязательно добавляйте тесты
4. **Проверяйте линтер** - перед коммитом запускайте `bun run lint`
5. **Используйте TypeScript** - избегайте `any`, используйте типы

## 🆘 Нужна помощь?

- Создайте Issue в GitHub
- Проверьте [раздел проблем](https://github.com/tc7kxsszs5-cloud/rork-kiku/issues)
- Обратитесь к команде в чате разработчиков

---

**Удачной разработки! 🚀**
