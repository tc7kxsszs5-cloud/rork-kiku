# 📋 Классификация "v1" в проекте KIKU

## 🎯 Что такое "v1"?

**"v1"** = **"version 1"** (версия 1) — это обозначение первой версии чего-либо.

---

## 📊 Классификация "v1" в IT проектах

### 1️⃣ **Версия приложения** (Application Version)
**Что это:** Номер версии самого приложения

**В нашем проекте:**
- `package.json`: `"version": "1.0.0"`
- `app.json`: `"version": "1.0.0"`

**Формат:** `MAJOR.MINOR.PATCH` (1.0.0)
- **MAJOR** (1) — большие изменения, ломающие совместимость
- **MINOR** (0) — новые функции, обратная совместимость
- **PATCH** (0) — исправления багов

**Примеры:**
- `v1.0.0` — первая стабильная версия
- `v1.1.0` — добавлены новые функции
- `v1.1.1` — исправлены баги
- `v2.0.0` — большие изменения (новая архитектура)

---

### 2️⃣ **Версия API** (API Version)
**Что это:** Версия программного интерфейса (API)

**В нашем проекте:**
- Используем tRPC (не REST API)
- Но можем иметь версии эндпоинтов

**Формат:** `/api/v1/...` или `/v1/...`

**Примеры:**
```
/api/v1/users      - версия 1 API для пользователей
/api/v1/messages  - версия 1 API для сообщений
/api/v2/users      - версия 2 API (новая структура)
```

**Зачем:** Обратная совместимость при изменении API

---

### 3️⃣ **Версия данных/метрик** (Data Version)
**Что это:** Версия структуры данных или метрик

**В нашем проекте:**
- `KPIS_SCALABILITY_GUIDE.md`: `feature_usage_v1` vs `feature_usage_v2`

**Примеры:**
```typescript
// Старая версия метрики
interface FeatureUsageV1 {
  feature: string;
  count: number;
}

// Новая версия метрики
interface FeatureUsageV2 {
  feature: string;
  count: number;
  timestamp: number;  // Новое поле
  userId: string;      // Новое поле
}
```

**Зачем:** Сохранить старые данные при изменении структуры

---

### 4️⃣ **Версия UUID** (UUID Version)
**Что это:** Версия стандарта UUID (уникальный идентификатор)

**В нашем проекте:**
- `utils/validation.ts`: упоминается UUID v1-v5

**Типы UUID:**
- **v1** — временные UUID (на основе времени и MAC-адреса)
- **v2** — DCE Security UUID (редко используется)
- **v3** — на основе MD5 хеша имени и пространства имён
- **v4** — случайные UUID (самые популярные) ⭐
- **v5** — на основе SHA-1 хеша имени и пространства имён

**Формат:**
```
xxxxxxxx-xxxx-1xxx-xxxx-xxxxxxxxxxxx
        ^^^^
        версия (1-5)
```

**В нашем коде:**
```typescript
// UUID v1-v5 формат: xxxxxxxx-xxxx-1-5xxx-8-9abxxx-xxxxxxxxxxxx
return /^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i.test(value);
```

---

### 5️⃣ **Версия Git тега** (Git Tag Version)
**Что это:** Тег версии в Git репозитории

**В нашем проекте:**
- Пока нет тегов, но можем создать

**Формат:** `v1.0.0`, `v1.1.0`, `v2.0.0`

**Команды:**
```bash
# Создать тег
git tag v1.0.0

# Запушить тег
git push origin v1.0.0

# Посмотреть все теги
git tag
```

**Зачем:** Отметить важные версии в истории проекта

---

### 6️⃣ **Версия библиотеки/пакета** (Package Version)
**Что это:** Версия используемых библиотек

**В нашем проекте:**
- `package.json` содержит версии всех зависимостей

**Примеры:**
```json
{
  "dependencies": {
    "react": "19.1.0",           // версия 19.1.0
    "react-native": "0.81.5",    // версия 0.81.5
    "expo": "^54.0.20"           // версия 54.0.20 (^ = совместимые обновления)
  }
}
```

**Символы:**
- `^` — совместимые обновления (^1.0.0 = 1.0.0 - 1.x.x)
- `~` — патч обновления (~1.0.0 = 1.0.0 - 1.0.x)
- `*` — любые версии (не рекомендуется)

---

## 📊 Сводная таблица классификации

| Тип | Где используется | Пример в проекте | Формат |
|-----|------------------|------------------|--------|
| **Версия приложения** | package.json, app.json | `"version": "1.0.0"` | `MAJOR.MINOR.PATCH` |
| **Версия API** | Backend endpoints | `/api/v1/users` | `/v1/...` |
| **Версия данных** | Структуры данных, метрики | `feature_usage_v1` | `*_v1`, `*_v2` |
| **Версия UUID** | Уникальные идентификаторы | UUID v1-v5 | `xxxx-xxxx-1xxx-xxxx` |
| **Git тег** | Git репозиторий | `v1.0.0` | `vMAJOR.MINOR.PATCH` |
| **Версия пакета** | package.json dependencies | `"react": "19.1.0"` | `MAJOR.MINOR.PATCH` |

---

## 🎯 К какой классификации относится "v1" в нашем проекте?

### ✅ Есть в проекте:

1. **Версия приложения** ✅
   - `package.json`: `"version": "1.0.0"`
   - `app.json`: `"version": "1.0.0"`

2. **Версия UUID** ✅
   - `utils/validation.ts`: упоминается UUID v1-v5

3. **Версия данных/метрик** ✅
   - `KPIS_SCALABILITY_GUIDE.md`: `feature_usage_v1`

4. **Версия пакетов** ✅
   - `package.json`: все зависимости имеют версии

### ❌ Нет в проекте (но можем добавить):

5. **Версия API** ❌
   - Используем tRPC (не REST)
   - Но можем версионировать процедуры

6. **Git теги** ❌
   - Пока нет тегов
   - Можем создать: `git tag v1.0.0`

---

## 💡 Зачем нужны версии?

### 1. **Отслеживание изменений**
- Понять какая версия у пользователя
- Знать что изменилось между версиями

### 2. **Обратная совместимость**
- Старые версии API продолжают работать
- Новые версии добавляют функции

### 3. **Управление зависимостями**
- Контролировать версии библиотек
- Избегать конфликтов

### 4. **Релизы**
- Отмечать важные версии
- Создавать стабильные релизы

---

## 🚀 Рекомендации для проекта KIKU

### 1. Создать Git теги для релизов:
```bash
git tag v1.0.0
git push origin v1.0.0
```

### 2. Версионировать API (если нужно):
```typescript
// v1 процедуры
trpc.router({
  v1: {
    users: userRouter,
    messages: messageRouter,
  },
  v2: {
    users: userRouterV2,  // новая версия
  },
});
```

### 3. Версионировать метрики (уже есть):
```typescript
// Старая версия
feature_usage_v1: Record<string, number>

// Новая версия
feature_usage_v2: Record<string, { count: number; timestamp: number }>
```

---

## 📝 Итог

**"v1"** в нашем проекте относится к:
- ✅ **Версии приложения** (1.0.0)
- ✅ **Версии UUID** (v1-v5)
- ✅ **Версии метрик** (feature_usage_v1)

**Классификация:** Система версионирования для отслеживания изменений и обеспечения совместимости.

---

**Теперь понятно к какой классификации относится "v1"!** 🎯
