#!/bin/bash
# Освобождение портов для запуска приложения

echo "🔧 Освобождение портов 8081 и 8082..."

# Убиваем процессы на портах
lsof -ti:8081 | xargs kill -9 2>/dev/null && echo "✅ Порт 8081 освобожден" || echo "ℹ️  Порт 8081 уже свободен"
lsof -ti:8082 | xargs kill -9 2>/dev/null && echo "✅ Порт 8082 освобожден" || echo "ℹ️  Порт 8082 уже свободен"

echo ""
echo "✅ Готово! Теперь можно запускать: bun run start:web"
