# 📥 Установка Git LFS вручную (если brew не работает)

## 🎯 Проблема

Homebrew заблокирован или не работает. Нужно установить Git LFS вручную.

## ✅ Решение 1: Подождать и попробовать снова

```bash
# Подождите минуту, затем попробуйте:
brew install git-lfs
```

## ✅ Решение 2: Скачать вручную

1. Зайти на https://git-lfs.github.com/
2. Скачать установщик для macOS
3. Установить
4. Затем:
```bash
git lfs install
git lfs track "*.mov"
git add .gitattributes
git commit -m "Setup Git LFS"
git add docs/demo/demo-video.mov
git commit --amend
git push
```

## ✅ Решение 3: Использовать YouTube (самый простой)

Если Git LFS вызывает проблемы - используйте YouTube. Это проще и быстрее!

---

## 💡 Рекомендация

Если Git LFS не устанавливается легко - используйте YouTube. Это займет 5 минут и точно сработает!
