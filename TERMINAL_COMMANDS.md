# 💻 Команды терминала для добавления видео на GitHub

## 🎯 Быстрая инструкция (по порядку)

### Шаг 1: Перейти в папку проекта

```bash
cd /Users/mac/Desktop/rork-kiku
```

### Шаг 2: Проверить изменения

```bash
git status
```

Эта команда покажет, какие файлы изменены (должен быть README.md)

### Шаг 3: Добавить изменения

```bash
git add README.md
```

### Шаг 4: Создать коммит

```bash
git commit -m "Add demo video"
```

### Шаг 5: Отправить на GitHub

```bash
git push
```

---

## ✅ Все команды одной строкой (если хотите быстро)

```bash
cd /Users/mac/Desktop/rork-kiku && git add README.md && git commit -m "Add demo video" && git push
```

---

## 🔍 Дополнительные полезные команды

### Посмотреть текущие изменения в README:

```bash
git diff README.md
```

### Если нужно найти видео файл на Mac:

```bash
# Поиск видео файлов на Desktop
find ~/Desktop -name "*.mp4" -o -name "*.mov" 2>/dev/null

# Поиск в Downloads
find ~/Downloads -name "*.mp4" -o -name "*.mov" 2>/dev/null
```

### Проверить, что всё готово:

```bash
# Проверить статус git
git status

# Посмотреть последние коммиты
git log --oneline -5
```

---

## ❓ Если что-то пошло не так

### Если git push не работает:

```bash
# Проверить, настроен ли remote
git remote -v

# Если нужно настроить (обычно уже настроен)
# git remote add origin https://github.com/your-username/rork-kiku.git
```

### Если нужно отменить изменения:

```bash
# Отменить изменения в README.md (если ещё не закоммичено)
git checkout README.md

# Или если уже закоммитили, но ещё не запушили
git reset HEAD~1
```

---

## 📋 Пошаговая инструкция (для новичков)

1. **Открыть Terminal** (найти через Spotlight: Cmd+Space, ввести "Terminal")

2. **Вставить и выполнить:**
   ```bash
   cd /Users/mac/Desktop/rork-kiku
   ```

3. **Проверить статус:**
   ```bash
   git status
   ```

4. **Добавить README.md:**
   ```bash
   git add README.md
   ```

5. **Создать коммит:**
   ```bash
   git commit -m "Add demo video"
   ```

6. **Отправить на GitHub:**
   ```bash
   git push
   ```

**Готово!** ✅

---

## 💡 Советы

- Каждую команду вводите отдельно и нажимайте Enter
- Если git просит ввести логин/пароль - введите их
- Если есть ошибки - скопируйте текст ошибки и спросите

---

**Все команды нужно выполнять в Terminal (Терминал) на Mac!** 💻
