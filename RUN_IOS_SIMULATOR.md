# 📱 Запуск в iOS симуляторе (Xcode 15)

## ✅ Проверка Xcode

У вас установлен **Xcode 15.2** - готово к работе!

## 🚀 Команды для запуска

### Способ 1: Прямой запуск в симуляторе (РЕКОМЕНДУЕТСЯ!)

```bash
cd /Users/mac/Desktop/rork-kiku
bunx expo start --ios
```

Эта команда автоматически запустит приложение в iOS симуляторе. **Не требует Expo Go!**

### Способ 2: Через стандартный Expo с выбором

```bash
cd /Users/mac/Desktop/rork-kiku
bun run start:expo
# Затем нажмите 'i' в терминале
```

### Способ 3: Выбор конкретного симулятора

```bash
cd /Users/mac/Desktop/rork-kiku
bunx expo start --ios
```

Эта команда автоматически запустит приложение в iOS симуляторе.

### Способ 4: Выбор конкретного симулятора

```bash
# Сначала посмотреть доступные симуляторы
xcrun simctl list devices available

# Затем запустить с выбором устройства
cd /Users/mac/Desktop/rork-kiku
bunx expo start --ios --simulator="iPhone 14"
```

## 📋 Пошаговая инструкция (Рекомендуемый способ)

1. **Откройте терминал и перейдите в проект:**
   ```bash
   cd /Users/mac/Desktop/rork-kiku
   ```

2. **Запустите dev сервер:**
   ```bash
   bun run start
   ```

3. **Дождитесь загрузки** (появится QR-код и меню)

4. **Нажмите `i`** в терминале для запуска в iOS симуляторе

5. **Дождитесь открытия симулятора** и загрузки приложения

## 🔍 Проверка перед запуском

```bash
# Проверить версию Xcode
xcodebuild -version
# Должно показать: Xcode 15.2

# Проверить доступные симуляторы
xcrun simctl list devices available | grep iPhone

# Проверить, что Xcode выбран правильно
xcode-select -p
# Должно показать: /Applications/Xcode.app/Contents/Developer
```

## ⚠️ Если возникли проблемы

### Симулятор не открывается:
```bash
# Открыть симулятор вручную
open -a Simulator

# Затем запустить приложение
bun run start
# Нажать 'i'
```

### Ошибка с лицензией Xcode:
```bash
sudo xcodebuild -license accept
```

### Ошибка с Command Line Tools:
```bash
sudo xcode-select --switch /Applications/Xcode.app/Contents/Developer
xcode-select --install
```

### Очистка кеша перед запуском:
```bash
rm -rf node_modules/.cache .expo .metro
bun run start
```

## 📝 Дополнительные команды

### Запуск с очисткой кеша:
```bash
bunx expo start --clear --ios
```

### Запуск с логированием:
```bash
DEBUG=expo* bun run start
# Затем нажать 'i'
```

### Запуск конкретного симулятора iPhone:
```bash
bunx expo start --ios --simulator="iPhone 14 Pro"
```

---

**Готово!** После запуска приложение откроется в iOS симуляторе, и вы сможете протестировать все функции.

