# 🚨 Исправление ошибки AnalyticsProvider

## Ошибка
```
SyntaxError: Identifier 'AnalyticsProvider' has already been declared. (16:9)
```

## ✅ Решение

Эта ошибка вызвана кешем Metro bundler. В файле `app/_layout.tsx` только **один** импорт `AnalyticsProvider`, но Metro bundler использует закешированную версию.

### Способ 1: Полная очистка кеша (РЕКОМЕНДУЕТСЯ)

```bash
cd /Users/mac/Desktop/rork-kiku

# Остановить все процессы
killall node

# Очистить все кеши
rm -rf node_modules/.cache .expo .metro .rork .expo-shared
rm -rf ~/.expo ~/.metro
rm -rf ~/Library/Caches/metro-*
rm -rf ~/Library/Caches/com.apple.dt.Xcode/Downloads/*

# Перезагрузить Mac (ВАЖНО!)
# Metro bundler кеширует данные в памяти системы
```

**После перезагрузки:**
```bash
cd /Users/mac/Desktop/rork-kiku
bun run start
# Нажмите 'i' для iOS симулятора
```

### Способ 2: Запуск с --clear флагом

```bash
cd /Users/mac/Desktop/rork-kiku
bunx expo start --clear --ios
```

### Способ 3: Переустановка зависимостей

```bash
cd /Users/mac/Desktop/rork-kiku
killall node
rm -rf node_modules
bun install
bun run start
# Нажмите 'i' для iOS симулятора
```

### Способ 4: Проверка файла

Убедитесь, что в файле `app/_layout.tsx` только один импорт:
```typescript
import { AnalyticsProvider } from "@/constants/AnalyticsContext";
```

Это должно быть на строке 13, и больше нигде в файле не должно быть этого импорта.

---

## 🔍 Диагностика

Проверить количество импортов:
```bash
cd /Users/mac/Desktop/rork-kiku
grep -c "import.*AnalyticsProvider" app/_layout.tsx
# Должно показать: 1
```

---

## ⚠️ Важно

Metro bundler хранит кеш в памяти системы Mac. **Перезагрузка Mac** - самый надежный способ полностью очистить кеш.

Если проблема сохраняется после перезагрузки, проверьте, что файл `app/_layout.tsx` сохранен и нет несохраненных изменений.
