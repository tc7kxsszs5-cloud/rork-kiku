# Руководство по безопасным Git коммитам

## 🎯 Цель

Научиться делать безопасные и правильные коммиты для сохранения вашей работы.

---

## 📋 Базовые команды Git

### 1. Проверить статус изменений

```bash
# Посмотреть, какие файлы изменены
git status

# Краткий вид (одна строка на файл)
git status --short
```

**Что показывает:**
- `M` - Modified (изменен)
- `A` - Added (добавлен)
- `D` - Deleted (удален)
- `??` - Untracked (не отслеживается)

---

### 2. Посмотреть изменения в файлах

```bash
# Показать изменения во всех файлах
git diff

# Показать изменения в конкретном файле
git diff app/(tabs)/profile.tsx

# Показать только имена измененных файлов
git diff --name-only
```

---

### 3. Добавить файлы в staging (подготовка к коммиту)

```bash
# Добавить все изменения
git add .

# Добавить конкретный файл
git add app/(tabs)/profile.tsx

# Добавить все файлы определенного типа
git add '*.ts'

# Интерактивный режим (можно выбрать файлы)
git add -i
```

---

### 4. Создать коммит

```bash
# Простой коммит с сообщением
git commit -m "Описание изменений"

# Коммит с подробным описанием
git commit -m "Краткое описание" -m "Подробное описание изменений"

# Коммит с открытием редактора для длинного сообщения
git commit
```

---

### 5. Просмотр истории коммитов

```bash
# Показать последние коммиты
git log

# Краткий вид (одна строка на коммит)
git log --oneline

# Последние 10 коммитов
git log -10

# С графом веток
git log --oneline --graph
```

---

## ✅ Рекомендуемый процесс (безопасный)

### Шаг 1: Проверить изменения

```bash
git status
```

**Проверьте:**
- Какие файлы изменены?
- Все ли изменения нужны?
- Нет ли случайных файлов (node_modules, .DS_Store)?

---

### Шаг 2: Посмотреть, что именно изменилось

```bash
git diff
```

**Проверьте:**
- Правильны ли изменения?
- Нет ли отладочного кода (console.log)?
- Нет ли закомментированного кода?

---

### Шаг 3: Добавить только нужные файлы

```bash
# Вариант 1: Добавить все (если уверены)
git add .

# Вариант 2: Добавить конкретные файлы (безопаснее)
git add app/(tabs)/profile.tsx
git add constants/UserContext.tsx
git add __tests__/unit/utils/
```

---

### Шаг 4: Создать коммит с понятным сообщением

```bash
git commit -m "Исправил ошибки TypeScript в profile.tsx"
```

**Хорошие примеры сообщений:**
- ✅ `"Исправил ошибки TypeScript в profile.tsx"`
- ✅ `"Добавил детерминированные тесты для cursorStyles"`
- ✅ `"Обновил документацию по тестированию"`
- ✅ `"Удалил использование role из всех компонентов"`

**Плохие примеры:**
- ❌ `"fix"` (слишком кратко)
- ❌ `"изменения"` (не информативно)
- ❌ `"asdf"` (бессмысленно)

---

### Шаг 5: Проверить результат

```bash
git log -1
```

Проверьте, что коммит создан правильно.

---

## 🛡️ Безопасность и лучшие практики

### 1. Не коммитить чувствительные данные

**НЕ коммитить:**
- Пароли
- API ключи
- Токены доступа
- Личные данные

**Проверьте `.gitignore`:**
```bash
cat .gitignore
```

Если нужно добавить файл в игнор:
```bash
echo "секретный_файл.txt" >> .gitignore
git add .gitignore
git commit -m "Добавил секретный файл в gitignore"
```

---

### 2. Делать маленькие, логические коммиты

**Хорошо:**
- Один коммит = одно изменение/задача
- Легко понять, что изменилось
- Легко откатить, если нужно

**Плохо:**
- Один большой коммит со всеми изменениями
- Смешанные задачи в одном коммите

---

### 3. Коммитить часто

**Рекомендация:**
- Коммитить после каждой выполненной задачи
- Не накапливать изменения
- Это защита от потери работы

---

### 4. Проверять перед коммитом

```bash
# Запустить тесты
bun run test:unit

# Проверить линтер
bun run lint

# Проверить типы
bun run typecheck

# Только потом коммитить
git add .
git commit -m "Описание"
```

---

## 🔄 Отмена изменений (если что-то пошло не так)

### Отменить изменения в файле (до git add)

```bash
# Отменить изменения в файле
git checkout -- app/(tabs)/profile.tsx

# Отменить все изменения
git checkout -- .
```

### Убрать файл из staging (после git add, до commit)

```bash
# Убрать файл из staging
git reset HEAD app/(tabs)/profile.tsx

# Убрать все из staging
git reset HEAD
```

### Отменить последний коммит (оставить изменения)

```bash
# Отменить коммит, но оставить изменения в файлах
git reset --soft HEAD~1

# Отменить коммит и убрать изменения из staging
git reset HEAD~1
```

⚠️ **Внимание:** `git reset` изменяет историю. Используйте осторожно!

---

## 📤 Отправка в удаленный репозиторий (GitHub)

### Первая отправка

```bash
# Добавить удаленный репозиторий (если еще не добавлен)
git remote add origin https://github.com/ваш-username/ваш-репозиторий.git

# Отправить изменения
git push -u origin main
```

### Последующие отправки

```bash
# Просто отправка
git push
```

---

## 📥 Получение изменений с GitHub

```bash
# Получить изменения
git pull

# Или отдельно
git fetch
git merge
```

---

## 🔍 Полезные команды для проверки

```bash
# Показать, что будет в коммите
git diff --staged

# Показать статистику изменений
git diff --stat

# Показать последний коммит
git show

# Показать изменения между коммитами
git diff HEAD~1 HEAD
```

---

## 📝 Шаблон безопасного коммита

```bash
# 1. Проверка
git status

# 2. Просмотр изменений
git diff

# 3. Запуск проверок (опционально)
bun run lint
bun run typecheck
bun run test:unit

# 4. Добавление файлов
git add .

# 5. Проверка того, что будет закоммичено
git diff --staged

# 6. Создание коммита
git commit -m "Краткое описание изменений"

# 7. Проверка результата
git log -1
```

---

## 💡 Советы

1. **Всегда проверяйте `git status` перед коммитом**
2. **Используйте понятные сообщения коммитов**
3. **Коммитьте часто, но логично**
4. **Не коммитьте временные файлы**
5. **Проверяйте изменения через `git diff`**

---

## 🆘 Если что-то пошло не так

**Не паникуйте!** Git сохраняет историю, и можно откатить изменения.

```bash
# Посмотреть последние действия
git reflog

# Вернуться к предыдущему состоянию
git reset --hard HEAD~1
```

---

**Помните:** Git - это инструмент для безопасности. Регулярные коммиты защищают вашу работу от потери!
