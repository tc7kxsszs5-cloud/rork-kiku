# ⚡ Быстрое исправление ошибки

## ❗ ВАЖНО: Вы должны быть в правильной директории!

Вы были в `/Users/mac` (домашняя директория), а нужно в `/Users/mac/Desktop/rork-kiku` (директория проекта).

## ✅ Правильные команды:

```bash
# 1. Перейдите в директорию проекта
cd ~/Desktop/rork-kiku

# 2. Проверьте, что вы в правильной директории
pwd
# Должно показать: /Users/mac/Desktop/rork-kiku

# 3. Проверьте наличие файлов проекта
ls -la package.json app.json
# Должны быть файлы package.json и app.json

# 4. Очистите кеш (в правильной директории!)
rm -rf node_modules/.cache .expo .metro .rork

# 5. Запустите с очисткой кеша
bun run start -- --clear
```

---

## 🔍 Как проверить, что вы в правильной директории:

```bash
# Команда pwd покажет текущую директорию
pwd

# Должно быть: /Users/mac/Desktop/rork-kiku
# НЕ: /Users/mac
```

---

## 📋 Полная последовательность команд:

Скопируйте и выполните все команды подряд:

```bash
cd ~/Desktop/rork-kiku
pwd
ls package.json app.json
rm -rf node_modules/.cache .expo .metro .rork
bun run start -- --clear
```

---

**Главное - убедитесь, что вы в директории `/Users/mac/Desktop/rork-kiku` перед выполнением команд! 🎯**

