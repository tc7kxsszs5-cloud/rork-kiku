# 📁 Как удалить странные ненужные папки

## Шаг 1: Добавить папки в .gitignore

Это предотвратит их добавление в Git в будущем.

### Команда:
```bash
# Открыть .gitignore в редакторе и добавить в конец файла:
```

**Добавьте эти строки в конец `.gitignore`:**
```
# Странные папки для удаления
-https-github.com-tc7kxsszs5-cloud-rork-kiku.git-/
e807b43d-2c4e-4a88-9ed1-1b6c686be9a6/
rork-kiku/
KIKU79/
kiku/
```

---

## Шаг 2: Проверить, отслеживаются ли папки в Git

Проверим, какие из них Git отслеживает:

```bash
git ls-files | grep -E "(kiku|KIKU79|rork-kiku|e807b43d|-https-github)" | head -20
```

Если команда выводит файлы - значит Git их отслеживает.

---

## Шаг 3: Удалить из Git индекса (если отслеживаются)

**ВАЖНО:** Это удалит папки из Git, но НЕ удалит их физически с диска.

```bash
# Удалить каждую папку из Git (если она отслеживается)
git rm -r --cached "-https-github.com-tc7kxsszs5-cloud-rork-kiku.git-" 2>/dev/null || echo "Папка не отслеживается"
git rm -r --cached "e807b43d-2c4e-4a88-9ed1-1b6c686be9a6" 2>/dev/null || echo "Папка не отслеживается"
git rm -r --cached "rork-kiku" 2>/dev/null || echo "Папка не отслеживается"
git rm -r --cached "KIKU79" 2>/dev/null || echo "Папка не отслеживается"
git rm -r --cached "kiku" 2>/dev/null || echo "Папка не отслеживается"
```

**Что делает `git rm -r --cached`:**
- `-r` = рекурсивно (для папок)
- `--cached` = только из Git индекса, не удаляет файлы с диска

---

## Шаг 4: Удалить папки физически

**ВНИМАНИЕ:** Это удалит папки с вашего диска навсегда!

```bash
# Удалить каждую папку
rm -rf "-https-github.com-tc7kxsszs5-cloud-rork-kiku.git-"
rm -rf "e807b43d-2c4e-4a88-9ed1-1b6c686be9a6"
rm -rf "rork-kiku"
rm -rf "KIKU79"
rm -rf "kiku"
```

**Что делает `rm -rf`:**
- `rm` = remove (удалить)
- `-r` = recursive (рекурсивно, для папок)
- `-f` = force (принудительно, без подтверждения)

---

## Шаг 5: Проверить результат

```bash
# Проверить статус Git
git status

# Проверить, что папки удалены
ls -la | grep -E "(kiku|KIKU79|rork-kiku|e807b43d|-https-github)"
```

---

## 🔄 Все команды сразу (для копирования)

Если хотите выполнить все сразу:

```bash
# 1. Добавить в .gitignore (вручную отредактируйте файл)

# 2. Удалить из Git
git rm -r --cached "-https-github.com-tc7kxsszs5-cloud-rork-kiku.git-" 2>/dev/null || true
git rm -r --cached "e807b43d-2c4e-4a88-9ed1-1b6c686be9a6" 2>/dev/null || true
git rm -r --cached "rork-kiku" 2>/dev/null || true
git rm -r --cached "KIKU79" 2>/dev/null || true
git rm -r --cached "kiku" 2>/dev/null || true

# 3. Удалить физически
rm -rf "-https-github.com-tc7kxsszs5-cloud-rork-kiku.git-"
rm -rf "e807b43d-2c4e-4a88-9ed1-1b6c686be9a6"
rm -rf "rork-kiku"
rm -rf "KIKU79"
rm -rf "kiku"

# 4. Проверить
git status
ls -la | grep -E "(kiku|KIKU79|rork-kiku|e807b43d|-https-github)" || echo "✅ Все папки удалены"
```

---

## ⚠️ Важные замечания

1. **Резервная копия не нужна** - эти папки не являются частью основного проекта
2. **Если ошибка "Permission denied"** - возможно, папка открыта в Finder или другом приложении
3. **Если папка не найдена** - значит она уже удалена, это нормально

---

## ✅ После удаления

После выполнения всех команд:
- Папки будут удалены с диска
- Они будут добавлены в .gitignore (чтобы Git их игнорировал)
- Git больше не будет их отслеживать
- При следующем коммите изменения будут зафиксированы
