# 📱 Команды для создания EAS Build

## 🚀 Быстрый старт

### 1. Создать iOS build для тестирования (Preview)

```bash
eas build --platform ios --profile preview
```

**Время:** ~15-20 минут  
**Результат:** .app файл для симулятора iOS

---

### 2. Создать iOS build для App Store (Production)

```bash
eas build --platform ios --profile production
```

**Время:** ~20-30 минут  
**Результат:** .ipa файл готовый для App Store  
**Требования:** 
- Apple Developer аккаунт ($99/год)
- Настроенный bundleIdentifier в app.json

---

### 3. Создать Android build (быстрее для тестирования)

```bash
eas build --platform android --profile production
```

**Время:** ~10-15 минут  
**Результат:** .apk файл

---

### 4. Создать build для обеих платформ

```bash
eas build --platform all --profile production
```

**Время:** ~30-40 минут  
**Результат:** iOS .ipa и Android .apk

---

## 📋 Полезные команды

### Проверить статус сборки

```bash
# Список всех сборок
eas build:list

# Детали конкретной сборки
eas build:view [BUILD_ID]

# Скачать build
eas build:download [BUILD_ID]
```

### Отправить в App Store / Google Play

```bash
# iOS App Store
eas submit --platform ios

# Google Play
eas submit --platform android

# Обе платформы
eas submit --platform all
```

### Отменить сборку

```bash
eas build:cancel [BUILD_ID]
```

---

## ⚙️ Настройка перед сборкой

### 1. Проверить app.json

Убедитесь, что в `app.json` правильно настроены:

```json
{
  "expo": {
    "ios": {
      "bundleIdentifier": "com.kiku.app"
    },
    "android": {
      "package": "com.kiku.app"
    }
  }
}
```

### 2. Проверить eas.json

Убедитесь, что профили настроены правильно (уже обновлено).

### 3. Логин в EAS

```bash
eas login
```

---

## 🐛 Решение проблем

### Ошибка: "No builds found"

**Решение:** Сначала нужно создать build:
```bash
eas build --platform ios --profile production
```

### Ошибка: "Invalid bundle identifier"

**Решение:** Проверьте `bundleIdentifier` в `app.json` и убедитесь, что он уникален.

### Ошибка: "Apple Developer account required"

**Решение:** 
1. Зарегистрируйтесь на https://developer.apple.com ($99/год)
2. Создайте App ID в Apple Developer Portal
3. Обновите `eas.json` с вашими credentials

### Build долго выполняется

**Нормально!** Первая сборка может занять 20-30 минут. Последующие сборки обычно быстрее благодаря кешированию.

---

## 💡 Рекомендации

1. **Начните с preview build** для тестирования
2. **Используйте Android build** для быстрого тестирования (быстрее чем iOS)
3. **Production build** создавайте только когда готовы к публикации
4. **Следите за квотами** EAS (бесплатный план имеет ограничения)

---

## 📊 Мониторинг сборки

После запуска `eas build` вы получите:
- Ссылку на страницу сборки в Expo Dashboard
- Build ID для отслеживания
- Уведомления о завершении

Можно отслеживать прогресс на: https://expo.dev


