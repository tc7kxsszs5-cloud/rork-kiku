# 🚀 Способы запуска приложения

## ✅ Вариант 1: Expo Go (САМЫЙ ПРОСТОЙ - Рекомендуется!)

**Преимущества:**
- ✅ Не требует сборки
- ✅ Работает сразу
- ✅ Обходит проблемы с Metro bundler кешем
- ✅ Полный доступ ко всем функциям

### Шаги:

1. **Установите Expo Go на ваш iPhone/iPad:**
   - Откройте App Store
   - Найдите "Expo Go"
   - Установите приложение

2. **Запустите dev сервер:**
   ```bash
   cd /Users/mac/Desktop/rork-kiku
   bun run start
   ```

3. **Отсканируйте QR-код:**
   - В терминале появится QR-код
   - Откройте Expo Go на телефоне
   - Нажмите "Scan QR code"
   - Отсканируйте QR-код из терминала

4. **Готово!** Приложение загрузится на ваш телефон

---

## ✅ Вариант 2: iOS Симулятор через Expo CLI

**После исправления ошибки AnalyticsProvider:**

```bash
cd /Users/mac/Desktop/rork-kiku

# Запустить с очисткой кеша
bunx expo start --clear --ios
```

Или:
```bash
bun run start
# Нажмите 'i' в терминале
```

---

## ✅ Вариант 3: Прямой запуск через Xcode (Advanced)

Для этого нужно сначала исправить ошибку AnalyticsProvider, затем:

1. **Создать iOS проект:**
   ```bash
   cd /Users/mac/Desktop/rork-kiku
   npx expo prebuild --platform ios
   ```

2. **Открыть проект в Xcode:**
   ```bash
   open ios/rork-kiku.xcworkspace
   # Или если нет workspace:
   open ios/rork-kiku.xcodeproj
   ```

3. **Выбрать симулятор в Xcode:**
   - В верхней панели Xcode выберите симулятор (например, iPhone 14)
   - Нажмите кнопку "Play" (▶️) для запуска

---

## 🎯 Рекомендация

**Используйте Expo Go (Вариант 1)** - это самый простой и быстрый способ:
- Работает сразу, без исправления ошибок
- Не требует сборки
- Все функции доступны
- Можно тестировать на реальном устройстве

---

## 🔧 Перед запуском (для симулятора)

Если хотите использовать симулятор, сначала нужно исправить ошибку AnalyticsProvider:

```bash
# 1. Остановить все процессы
killall node

# 2. Очистить кеш
rm -rf node_modules/.cache .expo .metro .rork .expo-shared ~/.expo ~/.metro

# 3. Перезагрузить Mac (важно!)

# 4. После перезагрузки:
cd /Users/mac/Desktop/rork-kiku
bun run start
# Нажмите 'i' для iOS симулятора
```

---

## 📱 Сравнение способов

| Способ | Сложность | Требует исправления ошибки | Скорость |
|--------|-----------|----------------------------|----------|
| **Expo Go** | ⭐ Легко | ❌ Нет | ⚡ Быстро |
| Симулятор (Expo) | ⭐⭐ Средне | ✅ Да | ⏱️ Средне |
| Xcode напрямую | ⭐⭐⭐ Сложно | ✅ Да | 🐌 Медленно |

**Рекомендация: Используйте Expo Go!**
