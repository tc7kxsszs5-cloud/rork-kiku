# 🚀 Полный план реализации критических функций KIDS by KIKU

## 📋 Что нужно для полной реализации

---

## ✅ 1. Analytics - ЗАВЕРШЕНО (100%)

**Статус:** ✅ Готово  
**Действия:** Ничего не требуется

---

## 🔔 2. Push Notifications - Требуется доработка (60% → 100%)

### Что уже есть:
- ✅ NotificationsContext создан
- ✅ Регистрация устройств работает
- ✅ Backend роуты для регистрации
- ✅ Локальные уведомления (scheduleNotificationAsync)

### Что нужно реализовать:

#### A. Backend endpoint для отправки push
**Файл:** `backend/trpc/routes/notifications/send-push.ts`

**Что нужно:**
1. Установить `expo-server-sdk` для отправки push через Expo Push Notification Service
2. Создать tRPC процедуру для отправки push
3. Интегрировать с существующим хранилищем устройств

**Зависимости:**
```bash
bun add expo-server-sdk
```

**Пример кода:**
```typescript
import { Expo } from 'expo-server-sdk';
const expo = new Expo();

// Процедура отправки push
export const sendPushProcedure = publicProcedure
  .input(z.object({
    deviceIds: z.array(z.string()),
    title: z.string(),
    body: z.string(),
    data: z.record(z.any()).optional(),
  }))
  .mutation(async ({ input }) => {
    // Получить push tokens для deviceIds
    // Отправить через Expo SDK
    // Вернуть результаты
  });
```

#### B. Интеграция в MonitoringContext
**Файл:** `constants/MonitoringContext.tsx`

**Что нужно:**
1. Использовать `useNotifications` для получения push tokens
2. При создании алерта отправлять push через backend
3. Обработать ошибки отправки

**Изменения:**
- Импортировать `useNotifications`
- Заменить `Notifications.scheduleNotificationAsync` на вызов backend API
- Добавить обработку ошибок

#### C. Интеграция в ParentalControlsContext
**Файл:** `constants/ParentalControlsContext.tsx`

**Что нужно:**
1. При triggerSOS отправлять push через backend
2. Отправлять push родителям/опекунам

**Время реализации:** 2-3 часа

---

## 🔄 3. Backend Sync - Требуется улучшение (40% → 100%)

### Что уже есть:
- ✅ Backend роуты (sync/chats, sync/alerts, sync/settings)
- ✅ tRPC интеграция
- ✅ In-memory хранилище

### Что нужно реализовать:

#### A. Улучшить синхронизацию чатов
**Файл:** `backend/trpc/routes/sync/chats.ts`

**Что нужно:**
1. **Merge логика** - правильное объединение конфликтов
2. **Incremental sync** - синхронизация только изменений (delta sync)
3. **Timestamp-based sync** - использовать временные метки для определения новых данных
4. **Обработка конфликтов** - стратегия разрешения конфликтов (last-write-wins или merge)

**Улучшения:**
```typescript
// Пример улучшенной синхронизации
const mergeChats = (serverChats, clientChats) => {
  // Объединить чаты, учитывая timestamp
  // Разрешить конфликты
  // Вернуть объединенный результат
};

const getDeltaSync = (lastSyncTimestamp, allChats) => {
  // Вернуть только изменения после lastSyncTimestamp
  return allChats.filter(chat => chat.updatedAt > lastSyncTimestamp);
};
```

#### B. Аналогично для алертов и настроек
**Файлы:** 
- `backend/trpc/routes/sync/alerts.ts`
- `backend/trpc/routes/sync/settings.ts`

#### C. (Опционально) Добавить PostgreSQL
**Для production:** Заменить in-memory хранилище на реальную БД

**Что нужно:**
1. Установить PostgreSQL драйвер
2. Создать схему БД
3. Мигрировать код с Map на SQL запросы

**Время реализации:** 3-4 часа (базовая версия), +2-3 часа (PostgreSQL)

---

## 📝 4. Дополнительные улучшения Analytics

### Что можно добавить:

#### A. Добавить trackEvent в ParentalControlsContext
**Файл:** `constants/ParentalControlsContext.tsx`

**Что нужно добавить:**
- `trackEvent('settings_changed')` в `updateSettings`
- `trackEvent('contact_removed')` в `removeContact`

**Время:** 15 минут

---

## 🎯 План реализации (пошагово)

### Этап 1: Завершить Analytics (15 мин)
1. ✅ Добавить trackEvent в updateSettings
2. ✅ Добавить trackEvent в removeContact
3. ✅ Проверить все события

### Этап 2: Push Notifications (2-3 часа)
1. Установить `expo-server-sdk`
2. Создать backend endpoint для отправки push
3. Интегрировать в MonitoringContext
4. Интегрировать в ParentalControlsContext
5. Протестировать отправку push

### Этап 3: Backend Sync (3-4 часа)
1. Улучшить merge логику для чатов
2. Добавить incremental sync
3. Улучшить синхронизацию алертов
4. Улучшить синхронизацию настроек
5. Протестировать синхронизацию

### Этап 4: (Опционально) PostgreSQL (2-3 часа)
1. Настроить PostgreSQL
2. Создать схему БД
3. Мигрировать код

---

## 💻 Что я могу сделать с вашей помощью:

### ✅ Могу сделать сразу:
1. Добавить недостающие trackEvent вызовы
2. Создать backend endpoint для push notifications
3. Улучшить логику синхронизации
4. Написать весь необходимый код

### ⚠️ Что нужно от вас:
1. **Решения по архитектуре:**
   - Использовать PostgreSQL или оставить in-memory?
   - Стратегия разрешения конфликтов при синхронизации?
   
2. **Тестирование:**
   - Проверить на реальных устройствах
   - Проверить работу push notifications
   - Проверить синхронизацию между устройствами

3. **Конфигурация (опционально):**
   - Expo Project ID (для push notifications)
   - PostgreSQL credentials (если используем БД)

---

## 🚀 Рекомендуемый порядок действий:

### Сейчас (быстро):
1. ✅ Завершить Analytics (15 мин) - я могу сделать
2. ✅ Улучшить синхронизацию (2-3 часа) - я могу сделать

### Следующий этап:
3. ✅ Push Notifications (2-3 часа) - я могу сделать

### Позже (опционально):
4. ⚠️ PostgreSQL (2-3 часа) - нужны ваши решения

---

## 📊 Итоговый чеклист:

### Analytics:
- [x] MonitoringContext - готово
- [x] ParentalControlsContext - почти готово (нужно добавить 2 события)
- [ ] Протестировать все события

### Push Notifications:
- [x] NotificationsContext - готово
- [ ] Backend endpoint для отправки - нужно сделать
- [ ] Интеграция в MonitoringContext - нужно сделать
- [ ] Интеграция в ParentalControlsContext - нужно сделать
- [ ] Тестирование - нужно проверить

### Backend Sync:
- [x] Базовая структура - готово
- [ ] Merge логика - нужно улучшить
- [ ] Incremental sync - нужно добавить
- [ ] Тестирование - нужно проверить
- [ ] PostgreSQL (опционально) - нужно решить

---

## 💬 Что нужно от вас:

**Вопросы для принятия решений:**

1. **Push Notifications:**
   - Используем Expo Push Notification Service? (рекомендую: Да)
   - Есть Expo Project ID? (нужен для отправки push)

2. **Backend Sync:**
   - Оставляем in-memory или переходим на PostgreSQL?
   - Стратегия конфликтов: last-write-wins или merge?

3. **Приоритеты:**
   - Что делать первым: Push Notifications или Backend Sync?
   - Нужен ли PostgreSQL сейчас или позже?

---

**Готов начать реализацию! Скажите, с чего начнем?** 🚀


