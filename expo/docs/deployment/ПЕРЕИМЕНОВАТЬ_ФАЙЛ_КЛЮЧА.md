# 📝 Как переименовать файл ключа в google-service-account.json

## ✅ Да, нужно переименовать!

Файл **обязательно** должен называться `google-service-account.json`, потому что в `eas.json` указан именно этот путь:

```json
{
  "submit": {
    "production": {
      "android": {
        "serviceAccountKeyPath": "./google-service-account.json"
      }
    }
  }
}
```

---

## 🔄 Способ 1: Через терминал (рекомендуется)

### Если файл в папке Downloads:

```bash
# Перейдите в папку проекта
cd /Users/mac/Desktop/rork-kiku

# Переименуйте файл (замените название на ваше)
mv ~/Downloads/ваш-проект-xxxxx-xxxxx.json ./google-service-account.json
```

**Пример:**
```bash
mv ~/Downloads/kiku-play-publisher-abc123-def456.json ./google-service-account.json
```

### Если файл уже в папке проекта:

```bash
cd /Users/mac/Desktop/rork-kiku

# Переименуйте файл
mv название-файла.json ./google-service-account.json
```

---

## 🔄 Способ 2: Через Finder (macOS)

1. Откройте Finder
2. Найдите скачанный JSON файл (обычно в папке "Загрузки")
3. Скопируйте файл в папку проекта: `/Users/mac/Desktop/rork-kiku/`
4. В папке проекта нажмите на файл правой кнопкой мыши
5. Выберите "Переименовать" (Rename)
6. Введите новое имя: `google-service-account.json`
7. Нажмите Enter

---

## ✅ Проверка

После переименования проверьте:

```bash
# Перейдите в папку проекта
cd /Users/mac/Desktop/rork-kiku

# Проверьте, что файл существует
ls -la google-service-account.json

# Должно показать что-то вроде:
# -rw-r--r--  1 user  staff  2345 Jan 25 12:00 google-service-account.json
```

Или просто проверьте в Finder, что файл `google-service-account.json` находится в папке `/Users/mac/Desktop/rork-kiku/`

---

## ⚠️ Важно

1. **Файл должен быть в корне проекта** (в папке `/Users/mac/Desktop/rork-kiku/`)
2. **Название должно быть точно** `google-service-account.json` (без пробелов, с дефисами)
3. **Файл уже добавлен в `.gitignore`** - не коммитьте его в Git!

---

## 🎯 Готово!

После переименования файл готов к использованию. EAS автоматически найдет его при публикации в Google Play Store.
