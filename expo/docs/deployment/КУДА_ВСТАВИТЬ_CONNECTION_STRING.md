# 📋 Куда вставить Connection String

## ✅ Способ 1: Через автоматический скрипт (рекомендуется)

### Шаг 1: Запустите скрипт

```bash
cd backend
./auto-setup.sh
```

### Шаг 2: Когда скрипт спросит:

```
📋 Введите Connection String из Supabase:
DATABASE_URL: 
```

**Вставьте скопированный Connection String и нажмите Enter**

Скрипт автоматически добавит его в Vercel!

---

## ✅ Способ 2: Вручную через Vercel Dashboard

### Шаг 1: Откройте Vercel Dashboard

1. Идите на https://vercel.com/dashboard
2. Выберите ваш проект (или создайте новый)

### Шаг 2: Откройте Settings

1. Нажмите на проект
2. Выберите **Settings** (вверху)
3. Выберите **Environment Variables** (слева)

### Шаг 3: Добавьте переменные

#### Переменная 1: DATABASE_URL

1. Нажмите **"Add New"**
2. **Name:** `DATABASE_URL`
3. **Value:** вставьте ваш Connection String
4. Выберите **Production** (и другие окружения, если нужно)
5. Нажмите **Save**

#### Переменная 2: NODE_ENV

1. Нажмите **"Add New"**
2. **Name:** `NODE_ENV`
3. **Value:** `production`
4. Выберите **Production**
5. Нажмите **Save**

---

## ✅ Способ 3: Через Vercel CLI

### Шаг 1: Добавьте DATABASE_URL

```bash
cd backend
echo "ВАШ_CONNECTION_STRING" | bunx vercel env add DATABASE_URL production
```

**Замените `ВАШ_CONNECTION_STRING` на скопированный Connection String!**

### Шаг 2: Добавьте NODE_ENV

```bash
echo "production" | bunx vercel env add NODE_ENV production
```

---

## 📋 Что делать после добавления

### 1. Передеплойте проект:

```bash
bunx vercel --prod
```

Или в Vercel Dashboard:
- Deployments → выберите последний → Redeploy

### 2. Проверьте работу:

Откройте URL вашего backend в браузере - должно работать!

---

## ✅ Готово!

После добавления Connection String и передеплоя ваш backend будет подключен к Supabase!

---

## 🔍 Проверка

После добавления переменных проверьте:

1. Vercel Dashboard → Settings → Environment Variables
2. Должны быть:
   - ✅ `DATABASE_URL` = ваш Connection String
   - ✅ `NODE_ENV` = `production`

---

## 📋 Пример

Если ваш Connection String:
```
postgresql://postgres:password@db.xxxxx.supabase.co:5432/postgres
```

Вставьте его полностью в:
- Скрипт `./auto-setup.sh` (когда спросит)
- Или в Vercel Dashboard → Settings → Environment Variables → DATABASE_URL
