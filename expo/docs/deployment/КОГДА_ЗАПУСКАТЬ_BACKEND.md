# ⏰ Когда нужно запускать Backend?

## 🎯 Короткий ответ: **НЕТ, не всегда!**

Backend нужно запускать только когда:
- ✅ Вы разрабатываете/тестируете локально
- ✅ Нужно протестировать изменения перед деплоем
- ✅ Работаете над backend кодом

---

## 📊 Разные сценарии

### 1. 🏠 Локальная разработка

**Когда запускать:**
- Работаете над backend кодом
- Тестируете API endpoints
- Отлаживаете проблемы

**Как запустить:**
```bash
cd backend
bun run dev
```

**Когда останавливать:**
- Закончили работу
- Переходите на другую задачу
- Закрываете компьютер

**Статус:** ⚠️ Запускается вручную, когда нужно

---

### 2. ☁️ Production на Vercel

**Когда запускается:**
- ✅ **Автоматически** - всегда работает (serverless)
- ✅ Запускается по требованию при запросах
- ✅ Не нужно запускать вручную

**Как работает:**
- Vercel автоматически запускает backend при каждом запросе
- Если нет запросов - "спит" (не потребляет ресурсы)
- При запросе - мгновенно "просыпается"

**Статус:** ✅ Работает автоматически, всегда доступен

---

### 3. 🔄 Автоматический деплой через GitHub Actions

**Когда запускается:**
- ✅ Автоматически при push в `main` ветку
- ✅ Не нужно запускать вручную

**Как работает:**
1. Вы делаете `git push`
2. GitHub Actions автоматически:
   - Собирает backend
   - Деплоит на Vercel
   - Backend становится доступным

**Статус:** ✅ Полностью автоматический

---

## 🎯 Рекомендации по использованию

### Для ежедневной разработки:

**Сценарий 1: Работаете только с frontend (mobile app)**
```bash
# Backend НЕ нужен локально
# Используйте production backend на Vercel
# Просто запустите mobile app:
bun run start
```

**Сценарий 2: Работаете с backend кодом**
```bash
# Запустите backend локально:
cd backend
bun run dev

# В другом терминале - mobile app:
cd ..
bun run start
```

**Сценарий 3: Тестируете изменения**
```bash
# 1. Запустите backend локально
cd backend && bun run dev

# 2. Протестируйте изменения

# 3. Закоммитьте и запушьте
git add .
git commit -m "Update backend"
git push

# 4. GitHub Actions автоматически задеплоит на Vercel
# 5. Можете остановить локальный backend
```

---

## ⚡ Быстрая справка

| Ситуация | Нужен ли локальный backend? |
|----------|------------------------------|
| Разработка frontend | ❌ Нет (используйте Vercel) |
| Разработка backend | ✅ Да (запустите локально) |
| Тестирование API | ✅ Да (запустите локально) |
| Production использование | ❌ Нет (Vercel работает автоматически) |
| Деплой изменений | ❌ Нет (GitHub Actions сделает это) |

---

## 🔧 Практические примеры

### Пример 1: Обычный день разработки

```bash
# Утро: Начинаете работу
# Backend НЕ запущен ✅ (это нормально)

# Работаете над UI компонентами
# Backend НЕ нужен ✅

# Обед: Закрыли терминал
# Backend НЕ запущен ✅ (это нормально)

# Вечер: Нужно протестировать новую функцию
cd backend
bun run dev  # Запустили только когда нужно ✅

# Протестировали, закончили
# Ctrl+C - остановили backend ✅
```

### Пример 2: Деплой изменений

```bash
# Сделали изменения в backend
cd backend
bun run dev  # Протестировали локально

# Всё работает, закоммитили
git add .
git commit -m "Add new feature"
git push

# GitHub Actions автоматически:
# 1. Соберет backend
# 2. Задеплоит на Vercel
# 3. Backend будет доступен на production

# Локальный backend можно остановить ✅
```

---

## 🎓 Важные моменты

### ✅ Это нормально:
- Backend не запущен локально большую часть времени
- Запускать только когда нужно тестировать/разрабатывать
- Использовать production backend на Vercel для frontend разработки

### ❌ Не нужно:
- Держать backend запущенным 24/7 локально
- Запускать backend "на всякий случай"
- Беспокоиться, что backend не запущен

### ✅ Автоматизация:
- Production backend на Vercel работает автоматически
- GitHub Actions деплоит автоматически
- Не нужно ничего запускать вручную для production

---

## 📱 Для Mobile App

**Mobile app может работать с:**
1. **Production backend на Vercel** (рекомендуется)
   - Всегда доступен
   - Не нужно запускать локально
   - Используется в `lib/trpc.ts`

2. **Локальный backend** (для тестирования)
   - Только когда разрабатываете backend
   - Нужно запустить: `cd backend && bun run dev`
   - Обновить URL в `lib/trpc.ts` на `http://localhost:3000`

---

## 🎯 Итог

**Backend НЕ должен быть всегда запущен локально!**

✅ **Запускайте локально только когда:**
- Разрабатываете backend код
- Тестируете API endpoints
- Отлаживаете проблемы

✅ **Используйте production backend на Vercel:**
- Для обычной разработки frontend
- Для тестирования mobile app
- Он работает автоматически, всегда доступен

✅ **Автоматический деплой:**
- GitHub Actions задеплоит изменения автоматически
- Не нужно запускать ничего вручную

---

**Вывод: Backend на Vercel работает автоматически. Локальный backend нужен только для разработки backend кода! 🚀**
