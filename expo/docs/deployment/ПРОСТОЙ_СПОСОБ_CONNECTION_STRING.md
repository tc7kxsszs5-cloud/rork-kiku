# 🔍 Простой способ получить Connection String

## ✅ Используйте просто URI

Если нет вкладок Session/Transaction/Pooling mode, используйте **просто URI** - он тоже работает!

---

## 📋 Шаг за шагом:

### 1. Откройте Supabase Dashboard

1. Идите на [supabase.com](https://supabase.com)
2. Войдите в аккаунт
3. Откройте ваш проект

### 2. Найдите Connection String

1. Слева нажмите **Settings** (шестеренка ⚙️)
2. Выберите **Database**
3. Прокрутите вниз
4. Найдите секцию **"Connection string"** или **"Connection info"**
5. Там будет поле с текстом типа:
   ```
   postgresql://postgres:[YOUR-PASSWORD]@db.xxxxx.supabase.co:5432/postgres
   ```

### 3. Скопируйте Connection String

1. Нажмите на кнопку **"Copy"** (или выделите и скопируйте)
2. Выглядит примерно так:
   ```
   postgresql://postgres:[YOUR-PASSWORD]@db.abcdefgh.supabase.co:5432/postgres
   ```

### 4. Замените пароль

В скопированном connection string найдите `[YOUR-PASSWORD]` и замените на ваш реальный пароль базы данных.

**Пример:**
- Было: `postgresql://postgres:[YOUR-PASSWORD]@db.xxxxx.supabase.co:5432/postgres`
- Стало: `postgresql://postgres:мойПароль123@db.xxxxx.supabase.co:5432/postgres`

---

## ✅ Готово!

Используйте этот connection string в Vercel:
- Settings → Environment Variables
- Key: `DATABASE_URL`
- Value: ваш connection string (с замененным паролем)

---

## 🆘 Если не можете найти Connection String

### Альтернатива 1: Через Connection Info

1. Settings → Database
2. Найдите секцию **"Connection Info"**
3. Там будут данные:
   - Host
   - Database name
   - Port
   - User
   - Password

4. Соберите connection string вручную:
   ```
   postgresql://postgres:ВАШ_ПАРОЛЬ@HOST:5432/postgres
   ```

### Альтернатива 2: Через API Settings

1. Settings → API
2. Там может быть connection string для разных языков
3. Найдите PostgreSQL connection string

---

## 📋 Что должно быть в Connection String

Правильный формат:
```
postgresql://postgres:ПАРОЛЬ@хост:5432/postgres
```

Где:
- `postgres` - пользователь
- `ПАРОЛЬ` - ваш пароль базы данных
- `хост` - что-то типа `db.xxxxx.supabase.co`
- `5432` - порт
- `postgres` - название базы данных

---

## 🎯 Главное

**Любой connection string из Supabase подойдет!**

Просто:
1. Найдите любое поле с connection string
2. Скопируйте
3. Замените `[YOUR-PASSWORD]` на ваш пароль
4. Используйте в Vercel

---

## ❓ Если все еще не можете найти

Напишите, что именно вы видите в разделе Settings → Database, и я помогу найти connection string!
