# 🔍 Где найти Connection String в секции Pooling

## ✅ Вы нашли правильную секцию!

Вы видите **"Connection pooling configuration"** - это хорошо!

---

## 📋 Где искать Connection String

### Вариант 1: Прокрутите ниже

В той же секции **"Connection pooling"** или **"Database"**:
1. Прокрутите страницу **вниз**
2. Ищите секцию:
   - **"Connection string"**
   - **"Connection URI"**
   - **"Connection info"**
   - **"Connect via"**

### Вариант 2: Ищите кнопку "Copy"

1. В секции Connection pooling может быть кнопка **"Copy connection string"**
2. Или ссылка **"Show connection string"**
3. Нажмите на неё

### Вариант 3: Вкладка "Connection string"

1. В секции Database может быть вкладка **"Connection string"**
2. Или отдельная секция ниже

---

## 📋 Что должно быть в Connection String

Выглядит так:
```
postgresql://postgres.xxxxx:[YOUR-PASSWORD]@aws-0-us-west-1.pooler.supabase.com:6543/postgres
```

Или так:
```
postgresql://postgres:[YOUR-PASSWORD]@db.xxxxx.supabase.co:5432/postgres
```

---

## 🔍 Где точно искать

### 1. Прокрутите вниз в секции Database

После "Connection pooling configuration" должна быть:
- **"Connection string"** секция
- Или **"Connection URI"**
- Или кнопка **"Show connection string"**

### 2. Проверьте вкладки

Могут быть вкладки:
- **URI**
- **Connection string**
- **Connect via**

### 3. Ищите поле с текстом

Ищите поле, где написано что-то типа:
- `postgresql://...`
- `postgres://...`
- Или кнопку **"Copy"** рядом с таким текстом

---

## 🆘 Если не можете найти

### Альтернатива: Соберите вручную

Если видите отдельные поля:
- **Host:** `db.xxxxx.supabase.co` или `aws-0-us-west-1.pooler.supabase.com`
- **Port:** `5432` или `6543`
- **Database:** `postgres`
- **User:** `postgres`
- **Password:** ваш пароль

Соберите connection string:
```
postgresql://postgres:ВАШ_ПАРОЛЬ@HOST:PORT/postgres
```

---

## ✅ Что делать сейчас

1. **Прокрутите страницу вниз** в секции Database
2. Ищите секцию **"Connection string"** или **"Connection URI"**
3. Или ищите кнопку **"Copy"** или **"Show connection string"**
4. Скопируйте connection string
5. Замените `[YOUR-PASSWORD]` на ваш пароль

---

## 🎯 Главное

Connection string **обязательно есть** в Settings → Database!

Просто прокрутите вниз или ищите кнопку "Copy" рядом с текстом `postgresql://...`

---

## ❓ Если все еще не можете найти

**Напишите, что еще вы видите на странице Settings → Database:**
- Какие еще секции есть?
- Есть ли кнопки "Copy" или "Show"?
- Есть ли вкладки?

Или сделайте скриншот - помогу найти!
