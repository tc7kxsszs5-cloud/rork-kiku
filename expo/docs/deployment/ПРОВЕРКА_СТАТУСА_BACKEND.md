# 🔍 Проверка статуса Backend

## Текущая ситуация

### ✅ Что настроено:
- Backend код готов в `backend/`
- Vercel проект настроен (projectId: `prj_BJTXERIYMuiYilYlTq7QZLBk1GmW`)
- GitHub Actions workflow для автоматического деплоя

### ❌ Что не работает:
- Backend НЕ запущен локально (порт 3000 свободен)
- Rork URL (`https://d8v7u672uumlfpscvnbps.rork.live`) не имеет backend API (404)
- Неизвестно, задеплоен ли backend на Vercel

---

## 🔍 Как проверить статус

### 1. Проверить локальный запуск:

```bash
# Проверить, запущен ли backend локально
lsof -i :3000

# Если пусто - backend не запущен
```

### 2. Проверить Vercel деплой:

**Вариант A: Через Vercel Dashboard**
1. Откройте https://vercel.com/dashboard
2. Найдите проект `backend`
3. Проверьте URL (например: `https://backend-xxx.vercel.app`)
4. Откройте URL в браузере - должно быть: `{"status":"ok","message":"API is running"}`

**Вариант B: Через Vercel CLI**
```bash
# Установить Vercel CLI (если еще не установлен)
npm install -g vercel

# Войти
vercel login

# Проверить проекты
cd backend
vercel ls

# Проверить конкретный проект
vercel inspect
```

### 3. Проверить Rork URL:

```bash
# Проверить backend API на Rork
curl https://d8v7u672uumlfpscvnbps.rork.live/api/trpc/example.hi

# Если 404 - backend не настроен на Rork
```

---

## 🚀 Варианты запуска

### Вариант 1: Локальный запуск (для разработки)

```bash
cd backend
bun install
bun run dev
```

Backend будет доступен на: `http://localhost:3000`

**Проверка:**
```bash
curl http://localhost:3000/
# Должно вернуть: {"status":"ok","message":"API is running"}
```

---

### Вариант 2: Деплой на Vercel (для production)

```bash
cd backend

# Первый раз (если еще не задеплоен)
vercel

# Production деплой
vercel --prod
```

После деплоя получите URL типа: `https://backend-xxx.vercel.app`

**Обновить в приложении:**
В `lib/trpc.ts` замените:
```typescript
const PROJECT_BASE_URL = 'https://ваш-backend.vercel.app';
```

---

### Вариант 3: Автоматический деплой через GitHub Actions

Если настроены secrets в GitHub:
- `VERCEL_TOKEN`
- `VERCEL_ORG_ID`
- `VERCEL_PROJECT_ID`

То при push в `main` backend автоматически задеплоится.

**Проверить:**
1. GitHub → Actions
2. Найдите последний workflow "Deploy Backend"
3. Проверьте статус

---

## 📋 Чеклист проверки

- [ ] Backend запущен локально? (`lsof -i :3000`)
- [ ] Backend задеплоен на Vercel? (проверить Dashboard)
- [ ] Vercel URL работает? (`curl https://your-backend.vercel.app/`)
- [ ] URL обновлен в `lib/trpc.ts`?
- [ ] Environment variables настроены в Vercel?
- [ ] База данных подключена?

---

## 🆘 Решение проблем

### Backend не запускается локально:

```bash
cd backend
bun install  # Установить зависимости
bun run dev  # Запустить
```

### Vercel деплой не работает:

```bash
cd backend
vercel login  # Войти
vercel --prod  # Задеплоить
```

### 404 на API endpoints:

1. Проверьте, что backend задеплоен
2. Проверьте URL в `lib/trpc.ts`
3. Проверьте CORS настройки в `backend/index.ts`

---

## 🎯 Рекомендация

**Для разработки:**
- Запустите локально: `cd backend && bun run dev`

**Для production:**
- Задеплойте на Vercel: `cd backend && vercel --prod`
- Обновите URL в `lib/trpc.ts`

---

**Текущий статус: Backend НЕ запущен. Нужно либо запустить локально, либо задеплоить на Vercel.**
