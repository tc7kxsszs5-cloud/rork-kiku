# 📱 Публикация в Google Play Store - Полное руководство

## ✅ Преимущества Google Play Store

- **Быстрая регистрация:** $25 единоразово (vs $99/год для Apple)
- **Быстрое одобрение:** Обычно 1-3 дня (vs 1-2 недели для Apple)
- **Проще процесс:** Меньше требований и ограничений
- **Больше пользователей:** Android имеет большую долю рынка

---

## 🚀 Шаг 1: Регистрация в Google Play Console

### 1.1 Создание аккаунта

1. Перейдите на https://play.google.com/console
2. Нажмите "Создать аккаунт разработчика"
3. Оплатите единоразовый взнос **$25** (принимается карта)
4. Заполните информацию о разработчике:
   - Имя разработчика
   - Email
   - Телефон
   - Адрес

### 1.2 Создание приложения

1. В Google Play Console нажмите "Создать приложение"
2. Выберите:
   - **Название приложения:** KIKU
   - **Язык по умолчанию:** Русский / English
   - **Тип приложения:** Приложение
   - **Бесплатное или платное:** Бесплатное

---

## 🔐 Шаг 2: Настройка Service Account для автоматической публикации

### 2.1 Создание Service Account в Google Cloud

1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект или выберите существующий
3. Перейдите в **IAM & Admin** → **Service Accounts**
4. Нажмите **Create Service Account**
5. Заполните:
   - **Name:** `kiku-play-publisher`
   - **Description:** `Service account for Google Play publishing`
6. Нажмите **Create and Continue**

### 2.2 Назначение роли

1. В разделе **Grant this service account access to project**:
   - Выберите роль: **Service Account User**
2. Нажмите **Continue** → **Done**

### 2.3 Создание ключа

1. Найдите созданный Service Account в списке
2. Нажмите на него → вкладка **Keys**
3. Нажмите **Add Key** → **Create new key**
4. Выберите **JSON**
5. Скачайте файл ключа (например, `kiku-play-service-account.json`)

### 2.4 Подключение к Google Play Console

1. В Google Play Console перейдите в **Setup** → **API access**
2. Найдите раздел **Service accounts**
3. Нажмите **Link service account**
4. Вставьте **Email** из скачанного JSON файла (поле `client_email`)
5. Нажмите **Grant access**
6. Выберите права:
   - ✅ **View app information and download bulk reports**
   - ✅ **Manage production releases**
   - ✅ **Manage testing track releases**
   - ✅ **Manage testing track releases and edit store listing**
7. Нажмите **Invite user**

### 2.5 Сохранение ключа в проект

1. Переместите скачанный JSON файл в корень проекта:
   ```bash
   # Переименуйте файл
   mv ~/Downloads/kiku-play-service-account.json ./google-service-account.json
   ```

2. **ВАЖНО:** Добавьте файл в `.gitignore`:
   ```bash
   echo "google-service-account.json" >> .gitignore
   ```

3. Проверьте, что файл добавлен в `.gitignore`:
   ```bash
   cat .gitignore | grep google-service-account
   ```

---

## ⚙️ Шаг 3: Настройка проекта

### 3.1 Проверка app.json

Ваш `app.json` уже настроен для Android! Проверьте:

```json
{
  "expo": {
    "android": {
      "package": "app.rork.greeting_project_58uufiz",
      "adaptiveIcon": {
        "foregroundImage": "./assets/images/adaptive-icon.png",
        "backgroundColor": "#ffffff"
      },
      "permissions": [
        // ... ваши разрешения
      ]
    }
  }
}
```

### 3.2 Обновление eas.json

Для Google Play Store нужно использовать **AAB** (Android App Bundle) вместо APK:

```json
{
  "build": {
    "production": {
      "android": {
        "buildType": "app-bundle"  // Изменено с "apk" на "app-bundle"
      }
    }
  }
}
```

**Примечание:** APK можно использовать для тестирования, но для публикации в Google Play требуется AAB.

---

## 🏗️ Шаг 4: Сборка приложения

### 4.1 Установка EAS CLI

```bash
# Если еще не установлен
npm install -g eas-cli
# или
bun add -g eas-cli
```

### 4.2 Вход в Expo

```bash
eas login
```

### 4.3 Сборка для production

```bash
# Сборка Android App Bundle для Google Play
eas build --platform android --profile production
```

**Время сборки:** ~10-15 минут

### 4.4 Проверка сборки

```bash
# Список всех сборок
eas build:list

# Детали конкретной сборки
eas build:view [BUILD_ID]

# Скачать build локально (опционально)
eas build:download [BUILD_ID]
```

---

## 📤 Шаг 5: Публикация в Google Play Store

### 5.1 Автоматическая публикация через EAS

```bash
# Автоматическая отправка в Google Play
eas submit --platform android --profile production
```

EAS автоматически:
- ✅ Найдет последнюю сборку
- ✅ Загрузит AAB в Google Play Console
- ✅ Опубликует в выбранный track (production/internal/testing)

### 5.2 Ручная публикация (альтернатива)

Если автоматическая публикация не работает:

1. Скачайте AAB файл:
   ```bash
   eas build:download [BUILD_ID]
   ```

2. В Google Play Console:
   - Перейдите в **Production** → **Releases**
   - Нажмите **Create new release**
   - Загрузите AAB файл
   - Заполните **Release notes**
   - Нажмите **Review release** → **Start rollout to Production**

---

## 📋 Шаг 6: Заполнение информации о приложении

Перед публикацией нужно заполнить:

### 6.1 Store Listing (Информация о приложении)

1. Перейдите в **Store presence** → **Main store listing**
2. Заполните:
   - **Название приложения:** KIKU
   - **Краткое описание:** (до 80 символов)
   - **Полное описание:** (до 4000 символов)
   - **Графические ресурсы:**
     - Иконка приложения (512x512 px)
     - Скриншоты (минимум 2, максимум 8)
     - Feature graphic (1024x500 px)
     - Промо-графика (180x120 px, опционально)

### 6.2 Контентный рейтинг

1. Перейдите в **Policy** → **App content**
2. Заполните анкету о контенте приложения
3. Получите рейтинг (обычно автоматически)

### 6.3 Целевая аудитория

1. Перейдите в **Policy** → **Target audience**
2. Укажите:
   - Возрастная категория
   - Наличие рекламы
   - Покупки в приложении

### 6.4 Privacy Policy

1. Создайте страницу Privacy Policy (можно на GitHub Pages или отдельный сайт)
2. В **Policy** → **Privacy policy** укажите URL

---

## 🧪 Шаг 7: Тестирование перед публикацией

### 7.1 Internal Testing Track

1. Создайте Internal Testing release:
   ```bash
   eas build --platform android --profile production
   eas submit --platform android --profile production --track internal
   ```

2. Добавьте тестеров в Google Play Console:
   - **Testing** → **Internal testing** → **Testers**
   - Добавьте email адреса тестеров

### 7.2 Closed Testing Track

Для более широкого тестирования:

```bash
eas submit --platform android --profile production --track alpha
# или
eas submit --platform android --profile production --track beta
```

---

## 🎯 Быстрый старт (TL;DR)

```bash
# 1. Установить EAS CLI
npm install -g eas-cli

# 2. Войти в Expo
eas login

# 3. Собрать приложение
eas build --platform android --profile production

# 4. Опубликовать
eas submit --platform android --profile production
```

**Время:** ~20-30 минут (включая сборку)

---

## ⚠️ Важные моменты

### Подпись приложения

EAS автоматически создает и управляет ключом подписи. Ключ хранится в облаке Expo и используется для всех сборок.

**Если нужно использовать свой ключ:**
1. Создайте keystore:
   ```bash
   keytool -genkeypair -v -storetype PKCS12 -keystore kiku-release-key.keystore -alias kiku-key -keyalg RSA -keysize 2048 -validity 10000
   ```

2. Добавьте в `eas.json`:
   ```json
   {
     "build": {
       "production": {
         "android": {
           "buildType": "app-bundle",
           "credentials": {
             "keystore": {
               "keystorePath": "./kiku-release-key.keystore",
               "keystorePassword": "YOUR_PASSWORD",
               "keyAlias": "kiku-key",
               "keyPassword": "YOUR_PASSWORD"
             }
           }
         }
       }
     }
   }
   ```

### Версионирование

Версия приложения берется из `app.json`:
```json
{
  "expo": {
    "version": "1.0.0",
    "android": {
      "versionCode": 1  // Автоматически увеличивается EAS
    }
  }
}
```

При каждой новой сборке `versionCode` автоматически увеличивается.

---

## 🔄 Обновление приложения

Для обновления приложения:

1. Обновите версию в `app.json`:
   ```json
   {
     "expo": {
       "version": "1.0.1"  // Увеличьте версию
     }
   }
   ```

2. Соберите новую версию:
   ```bash
   eas build --platform android --profile production
   ```

3. Опубликуйте:
   ```bash
   eas submit --platform android --profile production
   ```

---

## 🆘 Решение проблем

### Ошибка: "Service account not found"

- Проверьте, что файл `google-service-account.json` находится в корне проекта
- Проверьте, что email из JSON файла добавлен в Google Play Console

### Ошибка: "App not found in Google Play Console"

- Убедитесь, что приложение создано в Google Play Console
- Проверьте `package` в `app.json` совпадает с Package name в Google Play Console

### Ошибка: "Version code already exists"

- Увеличьте `version` в `app.json`
- EAS автоматически увеличит `versionCode`

### Ошибка при сборке

```bash
# Проверьте логи
eas build:view [BUILD_ID]

# Попробуйте очистить кеш
eas build --platform android --profile production --clear-cache
```

---

## 📚 Полезные ссылки

- [Google Play Console](https://play.google.com/console)
- [EAS Build документация](https://docs.expo.dev/build/introduction/)
- [EAS Submit документация](https://docs.expo.dev/submit/introduction/)
- [Android App Bundle](https://developer.android.com/guide/app-bundle)

---

## ✅ Чеклист перед публикацией

- [ ] Зарегистрирован аккаунт в Google Play Console ($25)
- [ ] Создан Service Account и добавлен ключ в проект
- [ ] Файл `google-service-account.json` добавлен в `.gitignore`
- [ ] Обновлен `eas.json` для использования `app-bundle`
- [ ] Заполнена информация о приложении в Google Play Console
- [ ] Добавлены скриншоты и графика
- [ ] Создана Privacy Policy
- [ ] Заполнен контентный рейтинг
- [ ] Протестировано на Internal Testing track
- [ ] Готово к публикации! 🚀

---

**Удачи с публикацией!** 🎉
