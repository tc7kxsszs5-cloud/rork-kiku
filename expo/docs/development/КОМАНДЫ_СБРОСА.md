# ⚡ Команды для полного сброса

## 🔄 Выполните по порядку:

```bash
cd /Users/mac/Desktop/rork-kiku/backend

# 1. Удалите старую переменную
bunx vercel env rm DATABASE_URL production

# 2. Добавьте новую
bunx vercel env add DATABASE_URL production
```

**Запрос 1:** `Mark as sensitive?` → **yes**

**Запрос 2:** `What's the value of DATABASE_URL?` → **вставьте:**

```
postgresql://postgres.eznumgsmwvavyunqhxfc:[ПАРОЛЬ]@aws-0-us-east-1.pooler.supabase.com:6543/postgres
```

**Замените `[ПАРОЛЬ]` на реальный пароль из Supabase!**

```bash
# 3. Перезапустите
bunx vercel --prod

# 4. Проверьте (подождите 20-25 секунд)
curl "https://backend-three-mauve-67.vercel.app/api/trpc/test.dbCheck"
```

---

**Готово! Выполните команды выше.**
