# 📝 Что такое TDD шаблон и для чего он нужен?

## 🎯 Что это?

Это **шаблон для TDD (Test-Driven Development)** - методологии разработки через тестирование.

**TDD** = сначала пишем тест, потом код

## 🔄 Как работает TDD (кратко):

### Классический цикл "Red-Green-Refactor":

1. **🔴 RED** - Пишем тест, который падает (тест для еще несуществующей функции)
2. **🟢 GREEN** - Пишем минимальный код, чтобы тест прошел
3. **🔵 REFACTOR** - Улучшаем код, сохраняя тесты зелеными

## 📋 Разбор шаблона:

### Структура шаблона:

```markdown
### Task N: [Component Name]

**Files:**
- Create: `exact/path/to/file.ts`      # Новый файл
- Modify: `exact/path/to/existing.ts:123-145`  # Изменения в существующем
- Test: `tests/exact/path/to/test.spec.ts`    # Файл с тестами
```

### Шаги:

#### **Step 1: Write the failing test** (🔴 RED)
```typescript
describe('specific behavior', () => {
  it('should return expected result', () => {
    const result = function(input);  // Функции еще нет!
    expect(result).toBe(expected);
  });
});
```
**Цель:** Написать тест для функции, которой еще нет

#### **Step 2: Run test to verify it fails** (🔴 RED)
```bash
npm run test -- test.spec.ts
Expected: FAIL with "function is not defined"
```
**Цель:** Убедиться, что тест действительно падает (проверка, что тест работает)

#### **Step 3: Write minimal implementation** (🟢 GREEN)
```typescript
export function function(input: InputType): ReturnType {
  return expected;  // Минимальная реализация - просто возвращает ожидаемое
}
```
**Цель:** Написать минимальный код, чтобы тест прошел

#### **Step 4: Run test to verify it passes** (🟢 GREEN)
```bash
npm run test -- test.spec.ts
Expected: PASS
```
**Цель:** Убедиться, что тест проходит

#### **Step 5: Commit** (💾 Сохранение)
```bash
git add tests/path/test.spec.ts src/path/file.ts
git commit -m "feat: add specific feature"
```
**Цель:** Сохранить изменения в git

## 🎯 Зачем это нужно?

### Преимущества TDD:

1. **✅ Гарантия покрытия тестами**
   - Код пишется только для прохождения тестов
   - 100% покрытие новой функциональности

2. **✅ Лучший дизайн кода**
   - Сначала думаем о том, КАК использовать функцию (тест)
   - Потом думаем о реализации
   - Код получается более удобным в использовании

3. **✅ Защита от регрессий**
   - Тесты защищают от случайного поломки функциональности
   - При рефакторинге сразу видно, что сломалось

4. **✅ Документация через тесты**
   - Тесты показывают, как использовать код
   - Примеры использования прямо в тестах

5. **✅ Уверенность в изменениях**
   - Можно менять код, зная, что тесты покажут ошибки
   - Меньше страха "сломать что-то"

## 📊 Пример для проекта KIKU:

### Задача: Добавить функцию проверки возраста ребенка

#### Step 1: Пишем тест (🔴 RED)
```typescript
// __tests__/unit/utils/childAge.test.ts
describe('validateChildAge', () => {
  it('should return true for valid age (5-17)', () => {
    expect(validateChildAge(10)).toBe(true);
  });
  
  it('should return false for age less than 5', () => {
    expect(validateChildAge(3)).toBe(false);
  });
  
  it('should return false for age more than 17', () => {
    expect(validateChildAge(20)).toBe(false);
  });
});
```

#### Step 2: Запускаем тест (🔴 RED)
```bash
$ bun test __tests__/unit/utils/childAge.test.ts
❌ FAIL: validateChildAge is not defined
```

#### Step 3: Пишем минимальную реализацию (🟢 GREEN)
```typescript
// utils/childAge.ts
export function validateChildAge(age: number): boolean {
  return age >= 5 && age <= 17;
}
```

#### Step 4: Запускаем тест (🟢 GREEN)
```bash
$ bun test __tests__/unit/utils/childAge.test.ts
✅ PASS: 3 tests passed
```

#### Step 5: Коммитим
```bash
git add __tests__/unit/utils/childAge.test.ts utils/childAge.ts
git commit -m "feat: add child age validation"
```

## ⚠️ Когда НЕ использовать TDD:

1. **Прототипирование** - когда нужно быстро проверить идею
2. **Исследовательская разработка** - когда не знаешь, что получится
3. **Очень простой код** - когда тест будет сложнее самого кода
4. **UI компоненты** - сложно тестировать визуальные элементы

## 🎯 Когда использовать TDD:

1. **Критичная бизнес-логика** - когда ошибка стоит дорого
2. **Сложные алгоритмы** - когда нужно много тестов для проверки
3. **API и утилиты** - когда важно правильное поведение
4. **Рефакторинг** - когда меняешь код, но поведение должно остаться

## 📚 Для проекта KIKU:

### Где TDD полезен:

✅ **Утилиты безопасности** (`riskEvaluation`, `validation`)
- Критично для безопасности детей
- Нужно много тестов для разных случаев

✅ **Бизнес-логика** (`kpiModeling`, `analyticsMetrics`)
- Сложные расчеты
- Нужна точность

✅ **Утилиты** (`timeRestrictions`, `syncHelpers`)
- Много edge cases
- Важна надежность

### Где TDD менее полезен:

⚠️ **UI компоненты** (`components/*.tsx`)
- Сложно тестировать визуальные элементы
- Лучше интеграционные тесты

⚠️ **Контексты** (`constants/*Context.tsx`)
- Много зависимостей
- Лучше интеграционные тесты

## 🎓 Вывод:

**TDD шаблон** - это структурированный способ разработки:
1. Сначала тест (определяем, что хотим)
2. Потом код (реализуем минимально)
3. Потом улучшаем (рефакторим)

**Для проекта KIKU:**
- ✅ Отлично подходит для утилит и бизнес-логики
- ✅ Уже используется (много unit тестов)
- ✅ Помогает поддерживать качество кода

---

**Итог:** Это не обязательный шаблон, а полезная методология для написания надежного кода с гарантированным покрытием тестами.
