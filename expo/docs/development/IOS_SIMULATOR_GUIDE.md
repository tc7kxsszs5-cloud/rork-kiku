# 📱 Запуск в симуляторе iOS 16

## Быстрый старт

1. **Очистите кеш:**
   ```bash
   cd ~/Desktop/rork-kiku
   rm -rf node_modules/.cache .expo .metro
   killall node
   ```

2. **Запустите проект (используется Rork):**
   ```bash
   bun run start
   ```

3. **Выберите симулятор:**
   - Нажмите `i` для запуска в iOS симуляторе
   - Или нажмите `Shift + i` для выбора конкретного симулятора

## Выбор iOS 16 симулятора

Если у вас установлен Xcode с iOS 16 SDK:

1. **Откройте симулятор вручную:**
   ```bash
   open -a Simulator
   ```

2. **Выберите устройство iOS 16:**
   - В меню Simulator: `File` → `Open Simulator` → `iOS 16.x` → выберите устройство (например, iPhone 14)

3. **Запустите проект (Rork) и выберите этот симулятор:**
   ```bash
   bun run start
   ```
   - Нажмите `Shift + i` для выбора устройства
   - Или нажмите `i` чтобы использовать текущий открытый симулятор

## Альтернативный способ (прямой запуск)

Для Rork используйте стандартную команду:
```bash
# Запустить проект
bun run start

# Затем нажать 'i' в терминале для запуска в симуляторе
```

## Проверка установленных симуляторов

```bash
# Показать все доступные симуляторы
xcrun simctl list devices available
```

## Устранение проблем

**Если симулятор iOS 16 не найден:**
1. Установите Xcode с поддержкой iOS 16 SDK
2. В Xcode: `Xcode` → `Settings` → `Platforms` → загрузите iOS 16.0

**Если проект не запускается:**
```bash
# Полная очистка и перезапуск
rm -rf node_modules/.cache .expo .metro .rork
killall node
bun install
bun run start
```

## Важно

⚠️ Сначала нужно решить проблему с `AnalyticsProvider` ошибкой перед запуском в симуляторе!

