# 📋 Команды для копирования (ТОЧНО)

## 1. Перейдите в директорию

```bash
cd /Users/mac/Desktop/rork-kiku/backend
```

## 2. Удалите старый DATABASE_URL

```bash
bunx vercel env rm DATABASE_URL production
```

## 3. Добавьте новый DATABASE_URL

```bash
bunx vercel env add DATABASE_URL production
```

**Когда спросит `Mark as sensitive?`** → введите: **yes**

**Когда спросит `What's the value of DATABASE_URL?`** → вставьте:

```
postgresql://postgres.eznumgsmwvavyunqhxfc:gerkom-tYbpek-2cochi@aws-0-us-east-1.pooler.supabase.com:6543/postgres
```

## 4. Деплой

```bash
bunx vercel --prod
```

## 5. Проверка

```bash
curl "https://backend-three-mauve-67.vercel.app/api/trpc/test.dbCheck"
```

---

## ✅ Проверка правильности:

После проверки выполните:

```bash
curl -s "https://backend-three-mauve-67.vercel.app/api/trpc/test.dbCheck" | grep -o '"username":"[^"]*"'
```

**Должно показать:**
```
"username":"postgres.eznumgsmwvavyunqhxfc"
```

**НЕ должно быть:**
```
"username":"postgres"
```
