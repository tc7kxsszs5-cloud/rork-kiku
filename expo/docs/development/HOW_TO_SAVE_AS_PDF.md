# 📄 Как сохранить резюме в PDF

Я создал HTML файл `PROJECT_RESUME.html`, который открыт в вашем браузере.

## 🚀 Быстрый способ (через браузер):

1. **HTML файл уже открыт в браузере**

2. **Нажмите `Cmd + P`** (или File → Print)

3. **В диалоге печати:**
   - В левом нижнем углу нажмите **"Save as PDF"** (или "Сохранить как PDF")
   - Выберите рабочее место (Desktop)
   - Назовите файл: **`KIDS_by_KIKU_Resume.pdf`**
   - Нажмите **"Save"** (Сохранить)

4. **Готово!** PDF файл будет на вашем рабочем столе.

---

## ⚙️ Автоматическая конвертация (опционально):

Если хотите автоматическую конвертацию в будущем, установите `wkhtmltopdf`:

```bash
brew install wkhtmltopdf
```

Затем запустите скрипт:
```bash
./create_pdf_resume.sh
```

---

## 📋 Альтернативные способы:

### Способ 1: Safari
1. Откройте `PROJECT_RESUME.html` в Safari
2. Cmd + P → Save as PDF

### Способ 2: Chrome
1. Откройте `PROJECT_RESUME.html` в Chrome
2. Cmd + P → Сохранить как PDF

### Способ 3: Preview (через Safari)
1. Откройте файл в Safari
2. Cmd + P
3. В левом нижнем углу → "PDF" → "Save as PDF"

---

**Файлы:**
- `PROJECT_RESUME.html` - HTML версия (уже открыта)
- `PROJECT_RESUME.md` - Markdown версия (исходник)
- `KIDS_by_KIKU_Resume.pdf` - PDF версия (будет создана на рабочем столе)


