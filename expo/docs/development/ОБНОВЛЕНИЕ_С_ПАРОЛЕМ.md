# ✅ Обновление Connection String с реальным паролем

## 📋 Шаг 1: Постройте правильный Connection String

Замените `ВАШ_ПАРОЛЬ` на пароль, который вы нашли в Supabase:

```
postgresql://postgres:ВАШ_ПАРОЛЬ@db.eznumgsmwvavyunqhxfc.supabase.co:5432/postgres
```

**Пример:**
Если ваш пароль `mypassword123`, то:
```
postgresql://postgres:mypassword123@db.eznumgsmwvavyunqhxfc.supabase.co:5432/postgres
```

---

## 📋 Шаг 2: Обновите в Vercel

### Способ 1: Через Vercel Dashboard (проще)

1. Откройте https://vercel.com/dashboard
2. Выберите ваш проект
3. **Settings** → **Environment Variables**
4. Найдите `DATABASE_URL`
5. Нажмите **"Edit"** или **"..." → Edit**
6. Вставьте правильный Connection String (с реальным паролем вместо `[YOUR-PASSWORD]`)
7. Нажмите **Save**

---

### Способ 2: Через CLI

```bash
cd backend

# Удалите старую переменную
bunx vercel env rm DATABASE_URL production

# Добавьте новую с правильным паролем
echo "postgresql://postgres:ВАШ_ПАРОЛЬ@db.eznumgsmwvavyunqhxfc.supabase.co:5432/postgres" | bunx vercel env add DATABASE_URL production
```

**Замените `ВАШ_ПАРОЛЬ` на ваш реальный пароль!**

---

## 📋 Шаг 3: Передеплойте проект

После обновления переменной нужно передеплоить:

```bash
bunx vercel --prod
```

Или в Vercel Dashboard:
- **Deployments** → выберите последний → **Redeploy**

---

## ✅ Готово!

После передеплоя ваш backend будет подключен к Supabase с правильным паролем!

---

## 🔍 Проверка

После передеплоя проверьте:

1. Откройте URL вашего backend в браузере
2. Должно показать: `{"status": "ok", "message": "API is running"}`
3. Проверьте Function Logs в Vercel Dashboard - не должно быть ошибок подключения к базе

---

## ⚠️ Важно

- Не делитесь паролем публично
- Пароль должен быть в Connection String без пробелов
- После обновления обязательно передеплойте проект
