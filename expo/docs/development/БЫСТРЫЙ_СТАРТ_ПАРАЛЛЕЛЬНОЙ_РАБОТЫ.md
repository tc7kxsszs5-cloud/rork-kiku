# 🚀 Быстрый старт: Параллельная работа агентов

**Для:** AI агентов, работающих над проектом KIKU  
**Время чтения:** 5 минут

---

## 📋 Краткая инструкция

### Шаг 1: Выберите свою роль

Определите, какой вы агент по зонам ответственности:

| Агент | Модуль | Файлы |
|-------|--------|-------|
| **Агент 1** | Frontend UI | `app/(tabs)/`, `components/` |
| **Агент 2** | Contexts | `constants/*Context.tsx` |
| **Агент 3** | Backend | `backend/`, `utils/aiService.ts` |
| **Агент 4** | Testing | `__tests__/` |
| **Агент 5** | Optimization | Bundle, performance |
| **Агент 6** | Infrastructure | `.github/`, `docker-compose.yml` |

---

### Шаг 2: Инициализация

```bash
# 1. Перейти в директорию проекта
cd /path/to/rork-kiku

# 2. Убедиться что на develop ветке
git checkout develop
git pull origin develop

# 3. Создать свою ветку
git checkout -b agent-N-module

# 4. Создать lock файл
mkdir -p .agents
cat > .agents/agent-N.lock << EOF
{
  "agent": "agent-N",
  "module": "module-name",
  "status": "working",
  "started": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
  "files": [],
  "branch": "agent-N-module",
  "lastUpdate": "$(date -u +%Y-%m-%dT%H:%M:%SZ)"
}
EOF
```

---

### Шаг 3: Работа

**Правила:**
- ✅ Работайте только в своей зоне ответственности
- ✅ Обновляйте lock файл каждые 30 минут
- ✅ Делайте коммиты каждые 1-2 часа
- ✅ Синхронизируйтесь с develop каждые 2 часа

**Команды:**
```bash
# Обновить статус
echo "$(date -u +%Y-%m-%dT%H:%M:%SZ)" > .agents/agent-N.lastUpdate

# Проверить конфликты
./scripts/check-agent-conflicts.sh

# Синхронизация с develop
git fetch origin
git merge origin/develop  # если нужно
```

---

### Шаг 4: Завершение

```bash
# 1. Завершить все изменения
git add .
git commit -m "feat(agent-N): описание изменений"

# 2. Запушить ветку
git push origin agent-N-module

# 3. Создать Pull Request в develop

# 4. Обновить статус в lock файле
jq '.status = "completed"' .agents/agent-N.lock > .agents/agent-N.lock.tmp
mv .agents/agent-N.lock.tmp .agents/agent-N.lock

# 5. Удалить lock файл (опционально)
# rm .agents/agent-N.lock
```

---

## ⚠️ Важные правила

### ❌ НЕ ДЕЛАЙТЕ:

- ❌ Не изменяйте файлы вне своей зоны
- ❌ Не работайте без lock файла
- ❌ Не коммитьте в develop напрямую
- ❌ Не игнорируйте конфликты

### ✅ ДЕЛАЙТЕ:

- ✅ Работайте в своей ветке
- ✅ Регулярно синхронизируйтесь
- ✅ Проверяйте конфликты перед коммитом
- ✅ Обновляйте lock файл

---

## 🔍 Проверка конфликтов

**Перед каждым коммитом:**

```bash
# 1. Проверить активных агентов
ls .agents/*.lock

# 2. Проверить пересекающиеся файлы
./scripts/check-agent-conflicts.sh

# 3. Проверить git конфликты
git status
git diff develop...HEAD
```

---

## 📞 Если возникли проблемы

1. **Конфликт файлов:**
   - Проверьте `.agents/*.lock` других агентов
   - Создайте issue с описанием
   - Дождитесь координации

2. **Git конфликты:**
   - `git pull origin develop`
   - Разрешите конфликты
   - `git commit`

3. **Заблокированный файл:**
   - Проверьте lock файл (если старше 2 часов - можно игнорировать)
   - Создайте issue

---

## 📚 Дополнительная информация

- **Полная стратегия:** `docs/development/СТРАТЕГИЯ_ПАРАЛЛЕЛЬНОЙ_РАБОТЫ_АГЕНТОВ.md`
- **План оптимизации:** `docs/development/ПЛАН_ОПТИМИЗАЦИИ_И_ДОРАБОТКИ.md`

---

**Удачи в работе! 🚀**
