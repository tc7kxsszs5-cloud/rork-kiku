# 🌐 Как открыть API в браузере

## 🚀 Вариант 1: Если деплой на Vercel (Production)

### Шаг 1: Получите URL

1. Идите на [vercel.com/dashboard](https://vercel.com/dashboard)
2. Найдите проект **kiku-backend**
3. Скопируйте URL (выглядит так):
   ```
   https://kiku-backend.vercel.app
   ```
   или
   ```
   https://kiku-backend-xxxxx.vercel.app
   ```

### Шаг 2: Откройте в браузере

Просто вставьте URL в адресную строку браузера и нажмите Enter.

**Должно вернуться:**
```json
{
  "status": "ok",
  "message": "API is running"
}
```

### Шаг 3: Проверьте tRPC endpoint

Откройте:
```
https://your-backend.vercel.app/api/trpc/example.hi
```

---

## 🔧 Вариант 2: Локально (для тестирования)

### Шаг 1: Запустите локальный сервер

```bash
cd backend
bun run dev
```

Или:
```bash
cd backend
bun run index.ts
```

### Шаг 2: Откройте в браузере

Откройте:
```
http://localhost:3000
```

Или (если использует другой порт):
```
http://localhost:8080
```

Проверьте в терминале, на каком порту запустился сервер.

---

## 📋 Быстрая проверка

### Если деплой на Vercel:

1. **Найдите URL в Vercel Dashboard:**
   - vercel.com → Projects → kiku-backend
   - Скопируйте URL из карточки проекта

2. **Откройте в браузере:**
   ```
   https://your-backend-url.vercel.app/
   ```

3. **Проверьте endpoints:**
   - Главная: `https://your-backend-url.vercel.app/`
   - tRPC: `https://your-backend-url.vercel.app/api/trpc/example.hi`

---

## 🔍 Если не знаете URL

### Через Vercel CLI:

```bash
cd backend
vercel ls
```

Покажет список деплоев и их URLs.

### Через Vercel Dashboard:

1. vercel.com/dashboard
2. Выберите проект
3. Вкладка **"Deployments"**
4. Нажмите на последний deployment
5. Скопируйте URL

---

## ✅ Что должно отображаться

### Главная страница (`/`):

```json
{
  "status": "ok",
  "message": "API is running"
}
```

### tRPC endpoint (`/api/trpc/example.hi`):

Должен вернуть ответ от tRPC (JSON с данными).

---

## 🎯 Готово!

Откройте URL в браузере и проверьте работу API!
