# ✅ Применение переменных окружения

## ✅ Хорошие новости:

Переменные `SUPABASE_URL` и `SUPABASE_ANON_KEY` установлены в Vercel!

## ❌ Проблема:

Переменные установлены, но не применены к текущему деплою. Нужно сделать новый деплой.

## 🚀 Решение:

```bash
cd /Users/mac/Desktop/rork-kiku/backend
bunx vercel --prod
```

Это создаст новый деплой с применением переменных окружения.

## ✅ Проверка:

```bash
curl "https://backend-three-mauve-67.vercel.app/api/trpc/test.dbCheck"
```

**Ожидаемый результат:**
```json
{
  "result": {
    "data": {
      "json": {
        "success": true,
        "message": "Подключение к Supabase работает!",
        "method": "Supabase SDK"
      }
    }
  }
}
```

---

**Выполните деплой для применения переменных окружения!**
