# 🤝 Руководство по участию в проекте KIKU

Спасибо за интерес к проекту KIKU! Мы рады любому вкладу в защиту детей в цифровом мире.

## 📋 Содержание

- [Кодекс поведения](#кодекс-поведения)
- [Как начать](#как-начать)
- [Процесс разработки](#процесс-разработки)
- [Стандарты кода](#стандарты-кода)
- [Создание Pull Request](#создание-pull-request)
- [Процесс Code Review](#процесс-code-review)
- [Сообщение об ошибках](#сообщение-об-ошибках)
- [Предложение новых функций](#предложение-новых-функций)

---

## 📜 Кодекс поведения

Этот проект следует нашему [Code of Conduct](CODE_OF_CONDUCT.md). Участвуя, вы обязуетесь поддерживать дружелюбное и уважительное сообщество.

---

## 🚀 Как начать

### Предварительные требования

- **Node.js:** 18+ (рекомендуется 20+)
- **Bun:** Последняя версия ([установка](https://bun.sh))
- **Git:** 2.30+
- **iOS:** Xcode 15+ (для iOS разработки)
- **Android:** Android Studio (для Android разработки)
- **Expo CLI:** Устанавливается автоматически через Bun

### Первоначальная настройка

1. **Форкните репозиторий**
   ```bash
   # Перейдите на GitHub и нажмите "Fork"
   ```

2. **Клонируйте ваш форк**
   ```bash
   git clone https://github.com/YOUR_USERNAME/rork-kiku.git
   cd rork-kiku
   ```

3. **Установите зависимости**
   ```bash
   bun install
   ```

4. **Настройте upstream**
   ```bash
   git remote add upstream https://github.com/tc7kxsszs5-cloud/rork-kiku.git
   ```

5. **Запустите проект**
   ```bash
   # Frontend (mobile app)
   bun run start
   
   # Backend (отдельный терминал)
   cd backend
   bun run dev
   ```

6. **Проверьте, что все работает**
   ```bash
   bun run check
   bun run test:unit
   ```

---

## 💻 Процесс разработки

### Структура проекта

```
rork-kiku/
├── app/                    # Экспо роуты (file-based routing)
├── components/             # React Native компоненты
├── constants/              # Контексты, типы, конфигурация
├── hooks/                  # Кастомные React хуки
├── utils/                  # Утилиты и вспомогательные функции
├── backend/                # Backend API (Hono + tRPC)
├── assets/                 # Изображения, шрифты, иконки
└── docs/                   # Документация
```

### Ветвление

1. **Создайте ветку от `main`**
   ```bash
   git checkout -b feature/your-feature-name
   # или
   git checkout -b fix/your-bug-fix
   # или
   git checkout -b docs/your-docs-improvement
   ```

2. **Соглашения по именованию веток:**
   - `feature/` - новые функции
   - `fix/` - исправления багов
   - `docs/` - документация
   - `refactor/` - рефакторинг
   - `test/` - тесты

3. **Делайте небольшие, фокусированные коммиты**
   ```bash
   git commit -m "feat: добавить функцию родительского контроля"
   ```

4. **Держите ветку в актуальном состоянии**
   ```bash
   git fetch upstream
   git rebase upstream/main
   ```

---

## 📝 Стандарты кода

### TypeScript

- ✅ Используйте строгую типизацию (`strict: true`)
- ✅ Избегайте `any` - используйте конкретные типы
- ✅ Используйте интерфейсы для объектов
- ✅ Используйте enum для фиксированных наборов значений

**Пример:**
```typescript
interface Message {
  id: string;
  text: string;
  riskLevel: RiskLevel;
  timestamp: Date;
}

enum RiskLevel {
  safe = 'safe',
  low = 'low',
  medium = 'medium',
  high = 'high',
  critical = 'critical',
}
```

### React Native / Expo

- ✅ Используйте функциональные компоненты с хуками
- ✅ Используйте `useCallback` и `useMemo` для оптимизации
- ✅ Используйте Context API для глобального состояния
- ✅ Следуйте паттернам проекта (см. `.cursorrules`)

**Пример:**
```typescript
export const MyComponent = () => {
  const { data } = useMyContext();
  const handlePress = useCallback(() => {
    // логика
  }, []);

  return <View>...</View>;
};
```

### Стили

- ✅ Используйте StyleSheet для стилей
- ✅ Избегайте inline стилей для сложных компонентов
- ✅ Используйте тему через `useTheme()` hook
- ✅ Следуйте дизайн-системе (см. `DESIGN_SYSTEM.md`)

### Именование

- **Компоненты:** PascalCase (`MonitoringScreen.tsx`)
- **Хуки:** camelCase с `use` префиксом (`useMonitoring.ts`)
- **Контексты:** PascalCase с `Context` суффиксом (`MonitoringContext.tsx`)
- **Утилиты:** camelCase (`formatDate.ts`)
- **Константы:** UPPER_SNAKE_CASE (`ANALYTICS_STORAGE_KEY`)

### Коммиты

Следуйте [Conventional Commits](https://www.conventionalcommits.org/):

```
feat: добавить новую функцию
fix: исправить баг
docs: обновить документацию
style: форматирование кода
refactor: рефакторинг
test: добавить тесты
chore: обновить зависимости
```

---

## 🔄 Создание Pull Request

### Перед созданием PR

1. **Убедитесь, что код проходит проверки:**
   ```bash
   bun run check
   bun run lint
   bun run typecheck
   bun run test
   ```

2. **Обновите документацию**, если необходимо

3. **Добавьте тесты** для новых функций

4. **Обновите CHANGELOG**, если применимо

### Процесс создания PR

1. **Отправьте вашу ветку**
   ```bash
   git push origin feature/your-feature-name
   ```

2. **Создайте Pull Request на GitHub**
   - Используйте шаблон PR (заполнится автоматически)
   - Опишите изменения подробно
   - Укажите связанные issues (если есть)
   - Добавьте скриншоты/видео для UI изменений

3. **Убедитесь, что CI проходит**
   - Все проверки должны быть зелеными
   - Если что-то не проходит, исправьте

4. **Ответьте на комментарии ревьюеров**
   - Будьте открыты к обратной связи
   - Вносите изменения по запросу
   - Обсуждайте спорные моменты конструктивно

### Шаблон PR

```markdown
## Описание
Краткое описание изменений

## Тип изменений
- [ ] Bug fix
- [ ] New feature
- [ ] Breaking change
- [ ] Documentation update

## Чеклист
- [ ] Код соответствует стандартам проекта
- [ ] Я проверил код сам
- [ ] Я прокомментировал сложные части кода
- [ ] Я обновил документацию
- [ ] Мои изменения не генерируют новых предупреждений
- [ ] Я добавил тесты для новых функций
- [ ] Все тесты проходят локально

## Скриншоты (если применимо)
Добавьте скриншоты для UI изменений
```

---

## 👀 Процесс Code Review

### Для авторов PR

- ✅ Будьте открыты к обратной связи
- ✅ Отвечайте на комментарии быстро
- ✅ Исправляйте замечания полностью
- ✅ Задавайте вопросы, если что-то непонятно

### Для ревьюеров

- ✅ Будьте конструктивными и уважительными
- ✅ Объясняйте, почему что-то нужно изменить
- ✅ Предлагайте альтернативные решения
- ✅ Хвалите хороший код

### Критерии одобрения

- ✅ Код соответствует стандартам проекта
- ✅ Тесты добавлены и проходят
- ✅ Документация обновлена
- ✅ Нет конфликтов с `main`
- ✅ CI проверки проходят

---

## 🐛 Сообщение об ошибках

Если вы нашли ошибку:

1. **Проверьте существующие issues** - возможно, проблема уже известна
2. **Создайте новый issue** с шаблоном bug report
3. **Включите:**
   - Четкое описание проблемы
   - Шаги для воспроизведения
   - Ожидаемое поведение
   - Фактическое поведение
   - Скриншоты/логи (если применимо)
   - Информацию об окружении

---

## 💡 Предложение новых функций

1. **Создайте issue** с шаблоном feature request
2. **Опишите:**
   - Проблему, которую решает функция
   - Предлагаемое решение
   - Альтернативные варианты
   - Дополнительный контекст

3. **Обсудите** с мейнтейнерами перед началом работы
4. **Дождитесь одобрения** перед созданием PR

---

## 🔒 Безопасность

**КРИТИЧЕСКИ ВАЖНО:** Это проект для защиты детей. Безопасность - наш приоритет.

- ✅ Никогда не коммитьте секреты, токены, API ключи
- ✅ Используйте `SecureStore` для чувствительных данных
- ✅ Валидируйте весь пользовательский ввод
- ✅ Следуйте best practices безопасности (см. `SECURITY.md`)

Если вы нашли уязвимость безопасности:
- **НЕ** создавайте публичный issue
- Напишите на: **g_mikheil@icloud.com**
- См. `SECURITY.md` для деталей

---

## 🧪 Тестирование

### Запуск тестов

```bash
# Все тесты
bun test

# Только unit тесты
bun run test:unit

# Только integration тесты
bun run test:integration

# С покрытием
bun run test:coverage

# Watch mode
bun run test:watch
```

### Написание тестов

- ✅ Пишите тесты для новых функций
- ✅ Стремитесь к покрытию >80%
- ✅ Используйте описательные имена тестов
- ✅ Тестируйте edge cases

**Пример:**
```typescript
describe('MonitoringContext', () => {
  it('should analyze message and detect risk', async () => {
    // Arrange
    const message = { text: 'test message', id: '1' };
    
    // Act
    const result = await analyzeMessage(message);
    
    // Assert
    expect(result.riskLevel).toBeDefined();
  });
});
```

---

## 📚 Дополнительные ресурсы

- [Дизайн-система](DESIGN_SYSTEM.md)
- [Архитектура проекта](PROJECT_SUMMARY.md)
- [Руководство по развертыванию](DEPLOYMENT_GUIDE.md)
- [Политика безопасности](SECURITY.md)
- [Code of Conduct](CODE_OF_CONDUCT.md)

---

## ❓ Вопросы?

Если у вас есть вопросы:

1. Проверьте документацию проекта
2. Поищите существующие issues
3. Создайте issue с вопросом
4. Свяжитесь с мейнтейнерами

---

**Спасибо за ваш вклад в защиту детей! 🙏**
