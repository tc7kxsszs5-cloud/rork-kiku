# 🤖 Инструкция для AI агентов

**Версия:** 1.0  
**Дата:** 2026-01-26

---

## 🎯 ВАША РОЛЬ

Вы - один из 6 AI агентов, работающих параллельно над проектом KIKU. Ваша задача - выполнить назначенные вам задачи без конфликтов с другими агентами.

---

## 📋 ПЕРЕД НАЧАЛОМ РАБОТЫ

### 1. Прочитать документацию:

- ✅ `СТРАТЕГИЯ_ПАРАЛЛЕЛЬНОЙ_РАБОТЫ_АГЕНТОВ.md` - как работать параллельно
- ✅ `ПЛАН_ДЛЯ_АГЕНТОВ_НА_ЗАВТРА.md` - ваши конкретные задачи
- ✅ `БЫСТРЫЙ_СТАРТ_ПАРАЛЛЕЛЬНОЙ_РАБОТЫ.md` - быстрая инструкция

### 2. Определить свою роль:

| Агент | Модуль | Ветка |
|-------|--------|-------|
| Агент 1 | Frontend UI | `agent-1-frontend-ui` |
| Агент 2 | Contexts | `agent-2-contexts` |
| Агент 3 | Backend | `agent-3-backend` |
| Агент 4 | Testing | `agent-4-testing` |
| Агент 5 | Optimization | `agent-5-optimization` |
| Агент 6 | Infrastructure | `agent-6-infrastructure` |

---

## 🚀 ПРОЦЕСС РАБОТЫ

### Шаг 1: Инициализация

```bash
# 1. Перейти в проект
cd /Users/mac/Desktop/rork-kiku

# 2. Обновить develop
git checkout develop
git pull origin develop

# 3. Создать свою ветку
git checkout -b agent-N-module

# 4. Создать lock файл
mkdir -p .agents
cat > .agents/agent-N.lock << EOF
{
  "agent": "agent-N",
  "module": "module-name",
  "status": "working",
  "started": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
  "files": [],
  "branch": "agent-N-module",
  "lastUpdate": "$(date -u +%Y-%m-%dT%H:%M:%SZ)"
}
EOF
```

### Шаг 2: Начать работу

1. **Открыть свой раздел в `ПЛАН_ДЛЯ_АГЕНТОВ_НА_ЗАВТРА.md`**
2. **Выбрать первую задачу**
3. **Добавить файлы в lock при начале работы:**

```bash
# Обновить lock файл
jq '.files += ["path/to/file.tsx"]' .agents/agent-N.lock > .agents/agent-N.lock.tmp
mv .agents/agent-N.lock.tmp .agents/agent-N.lock
jq ".lastUpdate = \"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"" .agents/agent-N.lock > .agents/agent-N.lock.tmp
mv .agents/agent-N.lock.tmp .agents/agent-N.lock
```

### Шаг 3: Работа над задачей

1. **Читать файлы перед редактированием**
2. **Следовать стилю кода из `.cursorrules`**
3. **Писать тесты для нового кода**
4. **Обновлять lock файл каждые 30-60 минут**

### Шаг 4: Перед коммитом

```bash
# 1. Проверить конфликты
./scripts/check-agent-conflicts.sh

# 2. Запустить тесты
bun test

# 3. Проверить линтер
bun run lint

# 4. Проверить типы
bun run typecheck
```

### Шаг 5: Коммит

```bash
# 1. Добавить изменения
git add .

# 2. Коммит с понятным сообщением
git commit -m "[Agent-N] Add tests for ComponentName"

# 3. Push в свою ветку
git push origin agent-N-module
```

### Шаг 6: Завершение работы

```bash
# 1. Обновить координацию (удалить из activeAgents)
# 2. Удалить lock файл
rm .agents/agent-N.lock

# 3. Создать PR в develop (если задача завершена)
```

---

## ⚠️ ПРАВИЛА БЕЗОПАСНОСТИ

### ❌ НИКОГДА НЕ ДЕЛАТЬ:

1. **Не работать над файлами других агентов** (см. зоны ответственности)
2. **Не коммитить lock файлы** (они в .gitignore)
3. **Не работать без lock файла** (другие агенты не будут знать о вашей работе)
4. **Не игнорировать конфликты** (всегда проверять перед коммитом)
5. **Не изменять общие файлы** без координации

### ✅ ВСЕГДА ДЕЛАТЬ:

1. **Обновлять lock файл** при изменении файлов
2. **Проверять конфликты** перед коммитом
3. **Запускать тесты** перед коммитом
4. **Следовать стилю кода** из `.cursorrules`
5. **Писать понятные коммиты** с префиксом `[Agent-N]`

---

## 🔍 ЗОНЫ ОТВЕТСТВЕННОСТИ

### Агент 1: Frontend UI

**Можно работать:**
- `app/(tabs)/*.tsx`
- `app/chat/[chatId].tsx`
- `app/register-*.tsx`
- `components/*.tsx` (кроме `components/settings/`)
- `__tests__/unit/components/*.test.tsx`
- `__tests__/unit/screens/*.test.tsx`

**Нельзя работать:**
- `constants/*.tsx` (контексты)
- `backend/**`
- `utils/*.ts` (кроме оптимизации)

---

### Агент 2: Contexts

**Можно работать:**
- `constants/*.tsx` (все контексты)
- `__tests__/unit/contexts/*.test.tsx`
- `app/_layout.tsx` (только порядок провайдеров)

**Нельзя работать:**
- `components/*.tsx`
- `app/**/*.tsx` (кроме _layout.tsx)
- `backend/**`

---

### Агент 3: Backend

**Можно работать:**
- `backend/**`
- `utils/aiService.ts`
- `utils/syncService.ts`
- `.env.example`
- `__tests__/unit/backend/*.test.ts`

**Нельзя работать:**
- `app/**`
- `components/**`
- `constants/**` (кроме использования)

---

### Агент 4: Testing

**Можно работать:**
- `__tests__/integration/**`
- `__tests__/playwright/**`
- `playwright.config.ts`
- `jest.config.js`
- `jest.setup.js`

**Нельзя работать:**
- Исходный код (только тесты)
- `app/**` (кроме тестов)
- `components/**` (кроме тестов)

---

### Агент 5: Optimization

**Можно работать:**
- `app/_layout.tsx` (оптимизация провайдеров)
- `metro.config.js`
- `babel.config.js`
- `package.json` (оптимизация зависимостей)
- Скрипты анализа bundle

**Нельзя работать:**
- Логика компонентов
- Логика контекстов
- Backend код

---

### Агент 6: Infrastructure

**Можно работать:**
- `.github/workflows/**`
- `eas.json`
- `scripts/**` (CI/CD скрипты)
- Docker файлы
- Конфигурация деплоя

**Нельзя работать:**
- Исходный код приложения
- Тесты (кроме CI конфигурации)

---

## 📞 ПОДДЕРЖКА

### При проблемах:

1. **Конфликты:** Проверить `./scripts/check-agent-conflicts.sh`
2. **Тесты не проходят:** Проверить что все зависимости установлены
3. **Не понятна задача:** Перечитать `ПЛАН_ДЛЯ_АГЕНТОВ_НА_ЗАВТРА.md`
4. **Координация:** Проверить `.agents/coordination.json`

---

## ✅ ЧЕКЛИСТ ПЕРЕД КОММИТОМ

- [ ] Lock файл обновлен
- [ ] Конфликты проверены
- [ ] Тесты проходят (`bun test`)
- [ ] Линтер проходит (`bun run lint`)
- [ ] Типы проверены (`bun run typecheck`)
- [ ] Коммит сообщение понятное с префиксом `[Agent-N]`
- [ ] Не коммитятся lock файлы

---

**Удачи в работе! 🚀**
