# 📱 Как использовать Expo Go

## ✅ Да, Expo Go можно использовать!

Ваш проект KIKU - это Expo приложение, поэтому его можно запускать через Expo Go.

---

## 🚀 Как запустить:

### 1. Запустите сервер разработки:

```bash
cd ~/Desktop/rork-kiku
bun run start -- --clear
```

### 2. После запуска вы увидите:

- **QR-код** в терминале
- Инструкции для подключения

### 3. На телефоне:

#### iOS (iPhone/iPad):
1. Откройте приложение **Expo Go** из App Store
2. Отсканируйте QR-код из терминала камерой (или через Expo Go)
3. Приложение начнет загружаться

#### Android:
1. Откройте приложение **Expo Go** из Google Play
2. Отсканируйте QR-код из терминала
3. Приложение начнет загружаться

---

## ⚠️ Важные моменты:

### Если используете Tunnel (--tunnel):
- Приложение работает через интернет
- Может быть медленнее, но работает из любой сети
- Убедитесь, что и телефон, и компьютер подключены к интернету

### Если используете LAN:
- Компьютер и телефон должны быть в одной Wi-Fi сети
- Быстрее, чем tunnel

---

## 🔍 Проверка подключения:

1. **Убедитесь, что сервер запущен** - в терминале должно быть сообщение "Metro waiting on..."
2. **Проверьте QR-код** - он должен быть виден в терминале
3. **На телефоне** - откройте Expo Go и отсканируйте QR-код

---

## 🐛 Если возникают проблемы:

### Ошибка "Unable to resolve module":
- Убедитесь, что вы в правильной директории: `cd ~/Desktop/rork-kiku`
- Очистите кеш: `rm -rf node_modules/.cache .expo .metro`

### Ошибка подключения:
- Убедитесь, что компьютер и телефон в одной сети (если не используете tunnel)
- Попробуйте использовать `--tunnel` флаг

### Ошибка "AnalyticsProvider has already been declared":
- Выполните полную очистку кеша (уже сделано)
- Перезапустите сервер

---

## 💡 Полезные команды:

```bash
# Запуск с очисткой кеша
bun run start -- --clear

# Запуск с tunnel (работает из любой сети)
bun run start  # уже включает --tunnel

# Остановка сервера
# Нажмите Ctrl+C в терминале
```

---

**Теперь вы можете тестировать приложение на реальном устройстве через Expo Go! 📱✨**


