# 🔄 Замена DATABASE_URL - Пошаговая инструкция

## 📋 Команды для замены

### Шаг 1: Удалите старую переменную

```bash
cd /Users/mac/Desktop/rork-kiku/backend
bunx vercel env rm DATABASE_URL production
```

### Шаг 2: Добавьте новую переменную

```bash
bunx vercel env add DATABASE_URL production
```

Когда появится запрос:
```
? What's the value of DATABASE_URL?
```

**Вставьте правильный connection string:**

```
postgresql://postgres:[ВАШ_ПАРОЛЬ]@db.eznumgsmwvavyunqhxfc.supabase.co:5432/postgres
```

**ВАЖНО:** Замените `[ВАШ_ПАРОЛЬ]` на реальный пароль из Supabase!

### Шаг 3: Перезапустите деплой

```bash
bunx vercel --prod
```

### Шаг 4: Проверьте

```bash
# Подождите 20-25 секунд после деплоя
curl "https://backend-three-mauve-67.vercel.app/api/trpc/test.dbCheck"
```

## ⚠️ Важно о пароле

### Если пароль простой (только буквы и цифры):

Просто замените `[ВАШ_ПАРОЛЬ]` на пароль:
```
postgresql://postgres:abc123@db.eznumgsmwvavyunqhxfc.supabase.co:5432/postgres
```

### Если пароль содержит специальные символы:

Закодируйте их:
- `@` → `%40`
- `:` → `%3A`
- `/` → `%2F`
- `#` → `%23`
- `%` → `%25`
- `&` → `%26`
- `?` → `%3F`
- `=` → `%3D`
- Пробел → `%20`

**Пример:**
Пароль `pass@123` → используйте `pass%40123`:
```
postgresql://postgres:pass%40123@db.eznumgsmwvavyunqhxfc.supabase.co:5432/postgres
```

## 🔑 Где взять пароль

1. Откройте https://supabase.com/dashboard
2. Выберите проект
3. **Settings** → **Database**
4. Найдите **Database password**
5. Если не виден - нажмите **Reset database password**
6. Скопируйте пароль

## ✅ Правильный формат

Connection string должен быть **точно в таком формате:**

```
postgresql://postgres:[ПАРОЛЬ]@db.eznumgsmwvavyunqhxfc.supabase.co:5432/postgres
```

**Проверьте:**
- ✅ Начинается с `postgresql://`
- ✅ Пользователь: `postgres`
- ✅ После `:` идет пароль (без пробелов)
- ✅ После пароля идет `@`
- ✅ Хост: `db.eznumgsmwvavyunqhxfc.supabase.co`
- ✅ Порт: `:5432`
- ✅ База: `/postgres`

---

**Выполните команды выше и замените `[ВАШ_ПАРОЛЬ]` на реальный пароль!**
