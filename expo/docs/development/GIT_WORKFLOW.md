# 📋 Правильный порядок работы с Git

## ✅ Правильная последовательность

### Шаг 1: ПРОВЕРКА (посмотреть что изменилось)

```bash
# Посмотреть статус - какие файлы изменены
git status

# Посмотреть изменения в конкретном файле (опционально)
git diff app/(tabs)/profile.tsx
```

**Что делать:**
- Проверь что все нужные файлы в списке
- Убедись что нет лишних файлов
- Если всё ок - переходи к шагу 2

---

### Шаг 2: ДОБАВЛЕНИЕ (добавить файлы в staging)

```bash
# Добавить все изменения
git add .

# ИЛИ добавить конкретные файлы
git add app/(tabs)/profile.tsx
git add app/(tabs)/analytics.tsx
git add utils/kpiExport.ts
git add package.json
git add *.md
```

**Что делать:**
- Добавь все файлы которые хочешь закоммитить
- Проверь статус снова: `git status` (должны быть зеленые файлы)

---

### Шаг 3: КОММИТ (сохранить изменения локально)

```bash
git commit -m "feat: добавить переключение языков и исправить ошибки

- Реализовано переключение между русским и английским языком
- Исправлены ошибки TypeScript в analytics.tsx и kpiExport.ts
- Добавлены новые команды запуска без ngrok
- Добавлена документация по запуску и веб-версии"
```

**Что делать:**
- Напиши понятное сообщение коммита
- Коммит сохраняет изменения **локально** (на твоем компьютере)
- После коммита изменения еще НЕ в GitHub

---

### Шаг 4: PUSH (отправить в GitHub)

```bash
git push origin main
```

**Что делать:**
- Отправляет коммиты в GitHub
- После push изменения будут видны в репозитории
- Это и есть "запуск коммитов" в GitHub

---

## 📋 Полная последовательность команд

```bash
# ШАГ 1: Проверка
git status

# ШАГ 2: Добавление
git add .

# ШАГ 3: Коммит
git commit -m "feat: добавить переключение языков и исправить ошибки

- Реализовано переключение между русским и английским языком
- Исправлены ошибки TypeScript в analytics.tsx и kpiExport.ts
- Добавлены новые команды запуска без ngrok
- Добавлена документация по запуску и веб-версии"

# ШАГ 4: Push в GitHub
git push origin main
```

---

## 🔍 Дополнительные проверки (опционально)

### Перед коммитом:

```bash
# Проверить что нет ошибок TypeScript
bunx tsc --noEmit

# Посмотреть что именно изменилось
git diff --stat
```

### После коммита (перед push):

```bash
# Посмотреть последний коммит
git log -1

# Проверить что всё правильно
git status
```

---

## 💡 Важно понимать

### Коммит vs Push:

- **`git commit`** - сохраняет изменения **локально** (на твоем компьютере)
- **`git push`** - отправляет коммиты **в GitHub** (в интернет)

### Порядок важен:

1. ✅ Сначала проверка (`git status`)
2. ✅ Потом добавление (`git add`)
3. ✅ Потом коммит (`git commit`)
4. ✅ Потом push (`git push`)

---

## 🚀 Быстрая команда (если уверен)

Если ты уверен что всё правильно, можно сделать всё сразу:

```bash
git add . && git commit -m "feat: добавить переключение языков и исправить ошибки" && git push origin main
```

Но лучше делать по шагам для контроля!

---

## ✅ Итог

**Правильный порядок:**
1. Проверка (`git status`)
2. Добавление (`git add`)
3. Коммит (`git commit`)
4. Push (`git push`) ← это "запуск коммитов" в GitHub

**Выполняй команды по порядку!** 🚀
