# 🤖 Автоматизация деплоя Vercel + Supabase

## 🚀 Автоматические скрипты

Я создал скрипты для автоматизации процесса!

---

## 📋 Скрипт 1: Автоматический деплой

### `backend/auto-deploy.sh`

**Что делает:**
- ✅ Проверяет Git репозиторий
- ✅ Коммитит изменения
- ✅ Пушит в GitHub
- ✅ Проверяет/устанавливает Vercel CLI
- ✅ Проверяет вход в Vercel
- ✅ Выполняет деплой

**Использование:**
```bash
cd backend
chmod +x auto-deploy.sh
./auto-deploy.sh
```

---

## 📋 Скрипт 2: Автоматическая настройка Environment Variables

### `backend/auto-setup.sh`

**Что делает:**
- ✅ Проверяет Vercel CLI
- ✅ Запрашивает Connection String
- ✅ Добавляет DATABASE_URL в Vercel
- ✅ Добавляет NODE_ENV в Vercel

**Использование:**
```bash
cd backend
chmod +x auto-setup.sh
./auto-setup.sh
```

---

## 🚀 Полная автоматизация (оба скрипта):

```bash
cd backend

# 1. Автоматический деплой
chmod +x auto-deploy.sh
./auto-deploy.sh

# 2. Настройка Environment Variables
chmod +x auto-setup.sh
./auto-setup.sh

# 3. Production деплой
vercel --prod
```

---

## 📋 Альтернатива: GitHub Actions

Можно настроить автоматический деплой через GitHub Actions при каждом push!

### Создайте `.github/workflows/deploy.yml`:

```yaml
name: Deploy to Vercel

on:
  push:
    branches: [main]
    paths:
      - 'backend/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: amondnet/vercel-action@v25
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
          working-directory: ./backend
```

---

## ✅ Преимущества автоматизации:

1. ✅ **Быстрее** - не нужно делать все вручную
2. ✅ **Надежнее** - меньше ошибок
3. ✅ **Повторяемо** - можно запускать много раз
4. ✅ **Удобно** - один скрипт вместо многих команд

---

## 🎯 Готово!

Используйте скрипты для автоматизации деплоя!
