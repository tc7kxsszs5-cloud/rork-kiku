# 📋 Команды для копирования

## 1. Удалить старый DATABASE_URL

```bash
cd /Users/mac/Desktop/rork-kiku/backend
bunx vercel env rm DATABASE_URL production
```

## 2. Добавить новый DATABASE_URL

```bash
bunx vercel env add DATABASE_URL production
```

**Ответьте:**
- `Mark as sensitive?` → **yes**
- `What's the value of DATABASE_URL?` → Вставьте connection string

## 3. Деплой

```bash
bunx vercel --prod
```

## 4. Проверка

```bash
curl "https://backend-three-mauve-67.vercel.app/api/trpc/test.dbCheck"
```

---

## 📝 Формат Connection String:

```
postgresql://postgres:[ВАШ_ПАРОЛЬ_ИЗ_SUPABASE]@aws-0-us-east-1.pooler.supabase.com:6543/postgres
```

**Где взять пароль:**
1. Supabase Dashboard → Settings → Database
2. Найдите Connection string
3. Скопируйте пароль из него (между `:` и `@`)
