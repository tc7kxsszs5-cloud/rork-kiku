# 🤖 Стратегия параллельной работы нескольких AI агентов

**Дата создания:** 2026-01-26  
**Версия:** 1.0  
**Статус:** Активный

---

## 📊 Обзор

Этот документ описывает стратегию работы нескольких AI агентов одновременно над проектом KIKU без конфликтов и дублирования работы.

**Цель:** Ускорить разработку в 3-5 раз за счет параллельной работы агентов над независимыми модулями.

---

## 🎯 Принципы разделения работы

### 1. Модульная изоляция
Каждый агент работает над **независимым модулем**, который не влияет на другие.

### 2. Четкие границы
Каждый агент имеет **четко определенную зону ответственности** с конкретными файлами.

### 3. Координация через файлы
Используем **файлы-маркеры** и **git branches** для координации.

### 4. Автоматическая проверка конфликтов
Перед коммитом проверяем конфликты через git.

---

## 🗂️ Разделение по модулям

### Агент 1: Frontend UI (Экраны и компоненты)

**Зона ответственности:**
- `app/(tabs)/*.tsx` - все экраны вкладок
- `app/chat/[chatId].tsx` - экран чата
- `app/register-*.tsx` - экраны регистрации
- `components/*.tsx` - все UI компоненты (кроме settings)

**Задачи из плана:**
- Unit тесты компонентов UI (20-25 часов)
- Оптимизация компонентов (code splitting)
- Оптимизация изображений в компонентах

**Файлы для работы:**
```
app/
├── (tabs)/
│   ├── index.tsx
│   ├── alerts.tsx
│   ├── analytics.tsx
│   ├── profile.tsx
│   └── ...
├── chat/
│   └── [chatId].tsx
├── register-parent.tsx
└── register-child.tsx

components/
├── ChatBackgroundPicker.tsx
├── CustomEmoji.tsx
├── EmojiPicker.tsx
└── ...
```

**Git branch:** `agent-1-frontend-ui`

**Маркерный файл:** `.agents/agent-1.lock` (создается при начале работы)

---

### Агент 2: Context и State Management

**Зона ответственности:**
- `constants/*Context.tsx` - все контексты
- `constants/types.ts` - типы для контекстов
- `app/_layout.tsx` - провайдеры (только оптимизация)

**Задачи из плана:**
- Тесты контекстов (10-12 часов)
- Оптимизация провайдеров (8-12 часов)
- Объединение связанных провайдеров

**Файлы для работы:**
```
constants/
├── AuthContext.tsx
├── UserContext.tsx
├── MonitoringContext.tsx
├── ParentalControlsContext.tsx
├── NotificationsContext.tsx
├── AnalyticsContext.tsx
├── ThemeContext.tsx
└── ... (все контексты)

app/
└── _layout.tsx (только оптимизация провайдеров)
```

**Git branch:** `agent-2-contexts`

**Маркерный файл:** `.agents/agent-2.lock`

---

### Агент 3: Backend и API

**Зона ответственности:**
- `backend/**/*.ts` - весь backend код
- `lib/trpc.ts` - tRPC клиент
- `utils/syncService.ts` - синхронизация
- `utils/aiService.ts` - AI интеграция

**Задачи из плана:**
- Реальная AI интеграция (15-20 часов)
- Миграция на PostgreSQL (8-10 часов)
- Backend тесты
- Rate limiting

**Файлы для работы:**
```
backend/
├── index.ts
├── trpc/
│   ├── app-router.ts
│   ├── middleware/
│   └── routes/
│       ├── auth/
│       ├── sync/
│       └── ...
└── utils/

lib/
└── trpc.ts

utils/
├── aiService.ts
├── syncService.ts
└── syncHelpers.ts
```

**Git branch:** `agent-3-backend`

**Маркерный файл:** `.agents/agent-3.lock`

---

### Агент 4: Тестирование

**Зона ответственности:**
- `__tests__/**/*.test.*` - все тесты
- `jest.config.js` - конфигурация тестов
- `jest.setup.js` - настройка тестов

**Задачи из плана:**
- Unit тесты (20-30 часов)
- Integration тесты (15-20 часов)
- E2E тесты (25-30 часов)
- Исправление проблем с ESM

**Файлы для работы:**
```
__tests__/
├── unit/
│   └── utils/
├── integration/
│   ├── app/
│   └── contexts/
└── e2e/
    ├── screens/
    └── processes/

jest.config.js
jest.setup.js
```

**Git branch:** `agent-4-testing`

**Маркерный файл:** `.agents/agent-4.lock`

---

### Агент 5: Оптимизация и Performance

**Зона ответственности:**
- Bundle анализ и оптимизация
- Code splitting стратегия
- Performance метрики
- Оптимизация зависимостей

**Задачи из плана:**
- Code splitting (10-15 часов)
- Bundle анализ (8-10 часов)
- Оптимизация зависимостей
- Performance тесты

**Файлы для работы:**
```
package.json (оптимизация зависимостей)
metro.config.js (оптимизация сборки)
babel.config.js (оптимизация транспиляции)

Создает новые файлы:
scripts/
├── analyze-bundle.ts
├── optimize-imports.ts
└── performance-metrics.ts
```

**Git branch:** `agent-5-optimization`

**Маркерный файл:** `.agents/agent-5.lock`

---

### Агент 6: Инфраструктура и DevOps

**Зона ответственности:**
- `.github/workflows/` - CI/CD
- `docker-compose.yml` - контейнеры
- `scripts/deploy.sh` - деплой
- Документация по инфраструктуре

**Задачи из плана:**
- CI/CD улучшения (8-12 часов)
- Production инфраструктура (15-20 часов)
- Масштабирование (10-15 часов)

**Файлы для работы:**
```
.github/
└── workflows/
    ├── ci.yml
    └── deploy.yml

docker-compose.yml
scripts/
├── deploy.sh
└── setup-env.sh

docs/deployment/
```

**Git branch:** `agent-6-infrastructure`

**Маркерный файл:** `.agents/agent-6.lock`

---

## 🔄 Механизм координации

### 1. Файлы-маркеры (Lock Files)

**Расположение:** `.agents/agent-N.lock`

**Формат:**
```json
{
  "agent": "agent-1-frontend-ui",
  "status": "working",
  "started": "2026-01-26T10:00:00Z",
  "files": [
    "app/(tabs)/index.tsx",
    "components/ChatItem.tsx"
  ],
  "branch": "agent-1-frontend-ui",
  "lastUpdate": "2026-01-26T10:30:00Z"
}
```

**Правила:**
- Агент создает lock файл перед началом работы
- Обновляет `lastUpdate` каждые 30 минут
- Удаляет lock файл при завершении работы
- Если lock файл старше 2 часов - считается заброшенным

### 2. Git Branches

**Стратегия:**
- Каждый агент работает в своей ветке: `agent-N-module`
- Перед началом работы: `git checkout -b agent-N-module`
- Регулярные коммиты (каждые 1-2 часа работы)
- Pull request в `develop` после завершения задачи

**Workflow:**
```bash
# Агент начинает работу
git checkout develop
git pull origin develop
git checkout -b agent-1-frontend-ui
# ... работа ...
git add .
git commit -m "feat(agent-1): add unit tests for ChatScreen"
git push origin agent-1-frontend-ui
```

### 3. Файл координации

**Расположение:** `.agents/coordination.json`

**Формат:**
```json
{
  "activeAgents": [
    {
      "id": "agent-1",
      "module": "frontend-ui",
      "status": "working",
      "branch": "agent-1-frontend-ui",
      "started": "2026-01-26T10:00:00Z",
      "estimatedFinish": "2026-01-26T14:00:00Z"
    }
  ],
  "completedTasks": [
    {
      "agent": "agent-1",
      "task": "Unit tests for ChatScreen",
      "completed": "2026-01-26T12:00:00Z",
      "branch": "agent-1-frontend-ui"
    }
  ],
  "conflicts": []
}
```

**Обновление:**
- Каждый агент обновляет свой статус
- Проверка конфликтов перед коммитом
- Автоматическое обновление при завершении задач

---

## 🚫 Правила предотвращения конфликтов

### Правило 1: Один файл - один агент

**Если файл в зоне ответственности агента:**
- Только этот агент может его изменять
- Другие агенты могут только читать

**Исключения:**
- `package.json` - только агент 5 (оптимизация) или координатор
- `app/_layout.tsx` - только агент 2 (контексты) для провайдеров
- Общие типы - координация через PR

### Правило 2: Зависимости между модулями

**Если агент A зависит от изменений агента B:**
1. Агент A ждет завершения задачи агента B
2. Или работает с моками/stubs
3. Или создает issue для координации

**Пример:**
- Агент 1 (UI) может работать с моками контекстов от Агента 2
- Агент 4 (тесты) может писать тесты для кода, который еще разрабатывается

### Правило 3: Общие файлы

**Файлы, требующие координации:**
- `package.json` - изменения только через координатора
- `tsconfig.json` - изменения только через координатора
- `app/_layout.tsx` - только агент 2 (контексты)
- Общие типы в `constants/types.ts` - через PR и review

**Процесс изменения общих файлов:**
1. Создать issue с описанием изменения
2. Дождаться одобрения координатора
3. Внести изменение
4. Уведомить других агентов

### Правило 4: Регулярная синхронизация

**Каждый агент должен:**
- Каждые 2 часа: `git pull origin develop`
- Перед коммитом: проверить конфликты
- После коммита: обновить lock файл

**Команды:**
```bash
# Перед началом работы
git checkout develop
git pull origin develop

# Во время работы (каждые 2 часа)
git fetch origin
git merge origin/develop  # если нужно

# Перед коммитом
git status
git diff develop...HEAD  # проверить изменения
```

---

## 📋 Чеклист для каждого агента

### Перед началом работы:

- [ ] Прочитать этот документ
- [ ] Проверить `.agents/coordination.json` на активных агентов
- [ ] Убедиться что нет конфликтов в зоне ответственности
- [ ] Создать git branch: `agent-N-module`
- [ ] Создать lock файл: `.agents/agent-N.lock`
- [ ] Обновить `.agents/coordination.json`

### Во время работы:

- [ ] Работать только в своей зоне ответственности
- [ ] Обновлять lock файл каждые 30 минут
- [ ] Делать коммиты каждые 1-2 часа
- [ ] Синхронизироваться с develop каждые 2 часа
- [ ] Проверять конфликты перед коммитом

### После завершения задачи:

- [ ] Завершить все изменения
- [ ] Запустить тесты (если применимо)
- [ ] Обновить документацию
- [ ] Создать Pull Request в `develop`
- [ ] Удалить lock файл
- [ ] Обновить `.agents/coordination.json` (статус: completed)

---

## 🔍 Проверка конфликтов

### Автоматическая проверка

**Скрипт:** `scripts/check-agent-conflicts.sh`

```bash
#!/bin/bash
# Проверка конфликтов между агентами

# Проверить активные lock файлы
ACTIVE_AGENTS=$(ls .agents/*.lock 2>/dev/null | wc -l)

if [ $ACTIVE_AGENTS -gt 1 ]; then
  echo "⚠️  Активных агентов: $ACTIVE_AGENTS"
  
  # Проверить пересекающиеся файлы
  for lock in .agents/*.lock; do
    agent=$(jq -r '.agent' $lock)
    files=$(jq -r '.files[]' $lock)
    echo "Агент $agent работает с:"
    echo "$files"
  done
fi

# Проверить git конфликты
git fetch origin
CONFLICTS=$(git diff --name-only develop...HEAD | wc -l)
if [ $CONFLICTS -gt 0 ]; then
  echo "⚠️  Есть изменения относительно develop"
fi
```

**Использование:**
```bash
bun run scripts/check-agent-conflicts.sh
```

### Ручная проверка

**Перед коммитом:**
```bash
# Проверить какие файлы изменены
git status

# Проверить конфликты с develop
git fetch origin
git merge-base HEAD origin/develop
git diff --name-only $(git merge-base HEAD origin/develop) HEAD

# Проверить lock файлы других агентов
cat .agents/*.lock | jq '.files[]' | sort | uniq -d
```

---

## 📊 Примеры параллельной работы

### Сценарий 1: Тестирование + Оптимизация

**Агент 4 (Тестирование):**
- Пишет тесты для `app/(tabs)/index.tsx`
- Не изменяет сам файл, только читает

**Агент 1 (Frontend UI):**
- Оптимизирует `app/(tabs)/index.tsx`
- Добавляет lazy loading

**Результат:** Оба агента работают параллельно, нет конфликтов

---

### Сценарий 2: Контексты + Backend

**Агент 2 (Contexts):**
- Оптимизирует провайдеры в `app/_layout.tsx`
- Объединяет связанные контексты

**Агент 3 (Backend):**
- Интегрирует реальный AI API
- Обновляет `utils/aiService.ts`

**Результат:** Нет пересечений файлов, работа параллельна

---

### Сценарий 3: Конфликт (требует координации)

**Агент 1 (Frontend UI):**
- Хочет изменить `app/_layout.tsx` для добавления нового экрана

**Агент 2 (Contexts):**
- Работает над оптимизацией провайдеров в `app/_layout.tsx`

**Решение:**
1. Агент 1 создает issue: "Need to add new screen route"
2. Координатор решает: Агент 2 сначала завершает оптимизацию
3. Агент 1 ждет завершения или работает в отдельной ветке
4. После завершения Агент 2 - Агент 1 делает merge и добавляет свой код

---

## 🛠️ Инструменты и скрипты

### 1. Скрипт инициализации агента

**Файл:** `scripts/init-agent.sh`

```bash
#!/bin/bash
# Инициализация работы агента

AGENT_ID=$1
MODULE=$2

if [ -z "$AGENT_ID" ] || [ -z "$MODULE" ]; then
  echo "Usage: ./scripts/init-agent.sh agent-N module-name"
  exit 1
fi

# Создать branch
git checkout develop
git pull origin develop
git checkout -b $AGENT_ID-$MODULE

# Создать lock файл
mkdir -p .agents
cat > .agents/$AGENT_ID.lock << EOF
{
  "agent": "$AGENT_ID",
  "module": "$MODULE",
  "status": "working",
  "started": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
  "files": [],
  "branch": "$AGENT_ID-$MODULE",
  "lastUpdate": "$(date -u +%Y-%m-%dT%H:%M:%SZ)"
}
EOF

echo "✅ Агент $AGENT_ID инициализирован"
echo "Branch: $AGENT_ID-$MODULE"
echo "Lock file: .agents/$AGENT_ID.lock"
```

**Использование:**
```bash
./scripts/init-agent.sh agent-1 frontend-ui
```

### 2. Скрипт обновления статуса

**Файл:** `scripts/update-agent-status.sh`

```bash
#!/bin/bash
# Обновление статуса агента

AGENT_ID=$1
FILES=$2  # Список файлов через запятую

if [ -z "$AGENT_ID" ]; then
  echo "Usage: ./scripts/update-agent-status.sh agent-N [files]"
  exit 1
fi

LOCK_FILE=".agents/$AGENT_ID.lock"

if [ ! -f "$LOCK_FILE" ]; then
  echo "❌ Lock файл не найден: $LOCK_FILE"
  exit 1
fi

# Обновить lastUpdate
jq ".lastUpdate = \"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"" $LOCK_FILE > $LOCK_FILE.tmp
mv $LOCK_FILE.tmp $LOCK_FILE

# Обновить файлы если указаны
if [ ! -z "$FILES" ]; then
  IFS=',' read -ra FILE_ARRAY <<< "$FILES"
  jq ".files = $(printf '%s\n' "${FILE_ARRAY[@]}" | jq -R . | jq -s .)" $LOCK_FILE > $LOCK_FILE.tmp
  mv $LOCK_FILE.tmp $LOCK_FILE
fi

echo "✅ Статус обновлен"
```

**Использование:**
```bash
./scripts/update-agent-status.sh agent-1 "app/(tabs)/index.tsx,components/ChatItem.tsx"
```

### 3. Скрипт завершения работы

**Файл:** `scripts/finish-agent-work.sh`

```bash
#!/bin/bash
# Завершение работы агента

AGENT_ID=$1

if [ -z "$AGENT_ID" ]; then
  echo "Usage: ./scripts/finish-agent-work.sh agent-N"
  exit 1
fi

LOCK_FILE=".agents/$AGENT_ID.lock"

if [ ! -f "$LOCK_FILE" ]; then
  echo "❌ Lock файл не найден"
  exit 1
fi

BRANCH=$(jq -r '.branch' $LOCK_FILE)

# Обновить статус
jq ".status = \"completed\"" $LOCK_FILE > $LOCK_FILE.tmp
mv $LOCK_FILE.tmp $LOCK_FILE

# Создать PR (если нужно)
echo "✅ Работа завершена"
echo "Branch: $BRANCH"
echo "Создайте Pull Request: git push origin $BRANCH"

# Удалить lock файл (опционально, можно оставить для истории)
# rm $LOCK_FILE
```

---

## 📈 Метрики эффективности

### Ожидаемое ускорение:

**Без параллелизации:**
- 1 агент: 145-185 часов = 4-5 месяцев

**С 6 агентами:**
- Параллельная работа: ~30-40 часов (самая длинная задача)
- Координация: +10-15 часов
- **Итого: 40-55 часов = 1-1.5 месяца**

**Ускорение: 3-4 раза**

### Факторы влияния:

1. **Зависимости между задачами** - могут замедлить
2. **Координация** - требует времени
3. **Конфликты** - требуют разрешения
4. **Code review** - нужно время на review PR

---

## 🎯 Рекомендации

### Для максимальной эффективности:

1. **Начать с независимых модулей:**
   - Агент 4 (Тесты) может работать параллельно со всеми
   - Агент 5 (Оптимизация) может работать параллельно
   - Агент 6 (Инфраструктура) независим

2. **Последовательность для зависимых:**
   - Сначала Агент 2 (Контексты) - оптимизация провайдеров
   - Потом Агент 1 (UI) - может использовать оптимизированные провайдеры
   - Агент 3 (Backend) - независим, может работать параллельно

3. **Регулярная синхронизация:**
   - Ежедневный merge в develop
   - Еженедельный review прогресса
   - Автоматические проверки конфликтов

---

## 📞 Контакты и поддержка

### При возникновении конфликтов:

1. Проверить `.agents/coordination.json`
2. Проверить lock файлы других агентов
3. Создать issue с описанием конфликта
4. Дождаться координации

### Координатор:

- Отслеживает активных агентов
- Разрешает конфликты
- Управляет общими файлами
- Проводит code review PR

---

**Последнее обновление:** 2026-01-26  
**Версия документа:** 1.0  
**Статус:** Активный
