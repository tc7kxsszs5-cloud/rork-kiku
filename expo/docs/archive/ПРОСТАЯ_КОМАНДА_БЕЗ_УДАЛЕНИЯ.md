# ⚡ Простая команда без удаления

## 🎯 Просто добавьте заново

Vercel сам спросит, перезаписать ли существующую переменную:

```bash
cd /Users/mac/Desktop/rork-kiku/backend
bunx vercel env add DATABASE_URL production
```

**Запросы:**

1. `A variable already exists. Overwrite?` → **yes**
2. `Mark as sensitive?` → **yes**
3. `What's the value?` → вставьте:

```
postgresql://postgres.eznumgsmwvavyunqhxfc:[ПАРОЛЬ]@aws-0-us-east-1.pooler.supabase.com:6543/postgres
```

**Замените `[ПАРОЛЬ]` на реальный пароль!**

Затем:
```bash
bunx vercel --prod
curl "https://backend-three-mauve-67.vercel.app/api/trpc/test.dbCheck"
```

---

**Готово! Не нужно удалять - просто добавьте заново!**
