# 📝 Пошаговые команды для коммитов

## ✅ Проверка перед коммитами

```bash
# 1. Проверить статус
git status

# 2. Проверить, что нет ошибок
bun run lint
bunx tsc --noEmit

# 3. Проверить тесты
bun test
```

---

## 🎯 Вариант 1: Автоматический (рекомендуется)

```bash
# Выполнить скрипт
./КОММИТЫ_ТОЧНЫЕ.sh
```

---

## 🎯 Вариант 2: Пошаговые команды вручную

### Шаг 1: Унификация логирования

```bash
# Добавить файлы
git add constants/MonitoringContext.tsx
git add constants/AnalyticsContext.tsx
git add constants/UserContext.tsx
git add constants/ParentalControlsContext.tsx
git add constants/AuthContext.tsx
git add constants/ChatBackgroundsContext.tsx
git add constants/ThemeContext.tsx
git add constants/AIModerationService.ts
git add app/_layout.tsx
git add app/chat/\[chatId\].tsx

# Проверить, что добавлено
git status

# Коммит
git commit -m "feat: unify logging - replace console.* with logger.*" \
  -m "- Replace all console.log/error/warn/info with structured logger" \
  -m "- Add context and action metadata to all log entries" \
  -m "- Improve debugging and monitoring capabilities" \
  -m "- ~60+ replacements across constants/ and app/ directories"
```

### Шаг 2: Централизованная обработка ошибок

```bash
# Добавить новый файл errorHandler.ts
git add utils/errorHandler.ts

# Добавить изменения в контекстах (если есть)
git add constants/MonitoringContext.tsx
git add constants/ParentalControlsContext.tsx
git add constants/UserContext.tsx

# Проверить
git status

# Коммит
git commit -m "feat: add centralized error handling with i18n support" \
  -m "- Create utils/errorHandler.ts with user-friendly error messages" \
  -m "- Integrate errorHandler into MonitoringContext, ParentalControlsContext, UserContext" \
  -m "- Add i18n support for error messages (en/ru)" \
  -m "- Add showUserFriendlyError, handleErrorSilently, withErrorHandling functions"
```

### Шаг 3: Переводы ошибок

```bash
# Добавить локализации
git add constants/locales/en.ts
git add constants/locales/ru.ts

# Проверить
git status

# Коммит
git commit -m "feat: add error message translations to i18n" \
  -m "- Add common.errors section to English and Russian locales" \
  -m "- Support for network, sync, load, save, permission, storage, analysis, SOS, auth errors" \
  -m "- Default error message for fallback cases"
```

### Шаг 4: Документация (опционально)

```bash
# Добавить файлы документации
git add СТАТУС_УЛУЧШЕНИЙ.md
git add ИТОГОВЫЙ_ОТЧЕТ_УЛУЧШЕНИЙ.md
git add ФИНАЛЬНЫЙ_ОТЧЕТ_УЛУЧШЕНИЙ.md
git add ОБНОВЛЕНИЕ_ERRORHANDLER_I18N.md
git add ПЛАН_КОММИТОВ.md
git add КОММИТЫ_ГОТОВЫ.md

# Проверить
git status

# Коммит
git commit -m "docs: add code quality improvement reports" \
  -m "- Add status and final reports for logging unification" \
  -m "- Document errorHandler i18n integration" \
  -m "- Include improvement metrics and results"
```

---

## ✅ Проверка после коммитов

```bash
# Посмотреть последние коммиты
git log --oneline -5

# Проверить статус
git status

# Посмотреть изменения в последнем коммите
git show HEAD
```

---

## 🚀 Отправка на сервер

```bash
# Отправить все коммиты
git push origin main

# Или если ветка называется по-другому
git push origin $(git branch --show-current)
```

---

## ⚠️ Если что-то пошло не так

### Отменить последний коммит (но сохранить изменения)
```bash
git reset --soft HEAD~1
```

### Отменить последний коммит (удалить изменения)
```bash
git reset --hard HEAD~1
```

### Изменить последний коммит
```bash
git commit --amend -m "новое сообщение"
```

---

**Готово!** 🎉
