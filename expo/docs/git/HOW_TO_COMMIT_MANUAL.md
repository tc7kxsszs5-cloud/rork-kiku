# 📝 Как делать коммиты вручную (лучший способ)

## ✅ Преимущества ручных коммитов

1. **Контроль** - ты видишь что именно коммитишь
2. **Лучшие сообщения** - можешь написать понятное описание
3. **Логическая группировка** - коммитишь связанные изменения вместе
4. **Опыт** - лучше понимаешь процесс Git

---

## 📋 Правильный процесс

### Шаг 1: Проверить что изменилось
```bash
git status
```

### Шаг 2: Посмотреть изменения (опционально)
```bash
# Посмотреть все изменения
git diff

# Посмотреть изменения в конкретном файле
git diff файл.tsx
```

### Шаг 3: Добавить файлы
```bash
# Добавить конкретные файлы (рекомендуется)
git add файл1.tsx файл2.md

# Или добавить все изменения
git add .
```

### Шаг 4: Проверить что будет закоммичено
```bash
git status
```

### Шаг 5: Закоммитить с понятным сообщением
```bash
git commit -m "feat: добавить новую функцию X"
```

### Шаг 6: Проверить коммит
```bash
git log --oneline -1
```

### Шаг 7: Запушить
```bash
git push origin main
```

---

## 📝 Правила хороших сообщений коммитов

### Формат: `тип: краткое описание`

### Типы коммитов:
- `feat:` - новая функция
- `fix:` - исправление бага
- `docs:` - документация
- `style:` - форматирование (не влияет на код)
- `refactor:` - рефакторинг кода
- `test:` - тесты
- `chore:` - обновление конфигов, зависимостей

### ✅ Примеры хороших сообщений:
```bash
git commit -m "feat: добавить отслеживание активации пользователей"
git commit -m "fix: исправить ошибку в AnalyticsContext"
git commit -m "docs: обновить README для инвесторов"
git commit -m "refactor: улучшить структуру PremiumContext"
```

### ❌ Примеры плохих сообщений (не делай так):
```bash
git commit -m "изменения"
git commit -m "fix"
git commit -m "update"
```

---

## 🎯 Рекомендуемый процесс для будущих изменений

### Пример работы над задачей:

1. **Работаешь над задачей**, делаешь изменения в коде

2. **Проверяешь что изменилось:**
   ```bash
   git status
   ```

3. **Смотришь изменения** (чтобы понять что коммитить):
   ```bash
   git diff
   ```

4. **Добавляешь файлы по логическим группам:**
   
   Например, сначала код:
   ```bash
   git add constants/AnalyticsContext.tsx components/ActivationTracker.tsx
   git commit -m "feat: добавить отслеживание активации"
   ```
   
   Потом документацию:
   ```bash
   git add README.md INVESTOR_PACKAGE_COMPLETE.md
   git commit -m "docs: обновить документацию для инвесторов"
   ```

5. **Проверяешь коммиты перед push:**
   ```bash
   git log --oneline -5
   ```

6. **Пушишь все коммиты разом:**
   ```bash
   git push origin main
   ```

---

## 💡 Советы

- **Коммить часто** - лучше много маленьких коммитов, чем один большой
- **Одно изменение = один коммит** - если исправил баг и добавил функцию, сделай 2 коммита
- **Проверяй перед коммитом** - `git status` и `git diff` твои друзья
- **Пиши понятные сообщения** - через месяц ты сам поймешь что было сделано

---

## 🚀 Быстрая команда (если уверен)

Если ты уверен в изменениях и хочешь все закоммитить сразу:

```bash
git add . && git commit -m "feat: описание изменений" && git push origin main
```

Но лучше делать по шагам для контроля!
