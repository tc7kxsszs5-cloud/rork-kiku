# 🚀 Быстрые команды для правильного workflow

## 📋 Правильная последовательность: ПРОВЕРКА → КОММИТ → PUSH

---

## 🎯 Вариант 1: Автоматический скрипт (рекомендуется)

### Использование:

```bash
# С сообщением коммита
./check-commit-push.sh "feat: добавить новую функцию"

# Или без параметра (скрипт спросит сообщение)
./check-commit-push.sh
```

**Что делает:**
1. ✅ Проверяет TypeScript
2. ✅ Проверяет Lint
3. ✅ Проверяет тесты
4. ✅ Показывает статус
5. ✅ Добавляет изменения
6. ✅ Создаёт коммит
7. ✅ Пушит на GitHub

---

## 🎯 Вариант 2: Ручные команды (пошагово)

### Шаг 1: ПРОВЕРКА

```bash
# Проверить всё
bunx tsc --noEmit && bun run lint && bun test --testPathPattern=unit --run && echo "✅ Всё готово к коммиту!"
```

**Или по отдельности:**

```bash
# TypeScript
bunx tsc --noEmit

# Lint
bun run lint

# Тесты
bun test --testPathPattern=unit --run
```

### Шаг 2: КОММИТ

```bash
# Добавить изменения
git add .

# Закоммитить
git commit -m "feat: добавить новую функцию"
```

### Шаг 3: PUSH

```bash
# Запушить
git push origin main
```

---

## 🎯 Вариант 3: Одна команда (всё сразу)

```bash
# Проверка → Коммит → Push (если проверка прошла)
bunx tsc --noEmit && bun run lint && git add . && git commit -m "feat: добавить новую функцию" && git push origin main
```

**⚠️ Внимание:** Эта команда выполнит коммит и push только если все проверки пройдут!

---

## 📋 Примеры использования

### Пример 1: Новая функция

```bash
./check-commit-push.sh "feat: добавить KPI Dashboard"
```

### Пример 2: Исправление бага

```bash
./check-commit-push.sh "fix: исправить ошибку в аналитике"
```

### Пример 3: Документация

```bash
./check-commit-push.sh "docs: обновить README"
```

### Пример 4: Рефакторинг

```bash
./check-commit-push.sh "refactor: улучшить структуру кода"
```

---

## ⚠️ Что делать если проверка не прошла?

### Если TypeScript ошибки:

1. Исправить ошибки в файлах
2. Проверить снова: `bunx tsc --noEmit`
3. Повторять пока нет ошибок

### Если Lint ошибки:

1. Исправить ошибки в файлах
2. Или автофикс: `bun run lint --fix`
3. Проверить снова: `bun run lint`

### Если тесты не проходят:

1. Исправить тесты или код
2. Проверить снова: `bun test --testPathPattern=unit --run`
3. Повторять пока все тесты проходят

---

## ✅ Чеклист перед коммитом

- [ ] `bunx tsc --noEmit` - нет ошибок
- [ ] `bun run lint` - нет критичных ошибок
- [ ] `bun test --testPathPattern=unit --run` - все тесты проходят
- [ ] Код работает локально
- [ ] Все изменения сохранены

**Только после этого → коммит и push!**

---

## 🎯 Быстрая команда (копировать-вставить)

```bash
# Замени "сообщение" на своё
./check-commit-push.sh "feat: добавить новую функцию"
```

---

## 📝 Формат сообщений коммита

- `feat:` - новая функция
- `fix:` - исправление бага
- `docs:` - документация
- `refactor:` - рефакторинг
- `test:` - тесты
- `chore:` - технические изменения

**Примеры:**
- `feat: добавить KPI Dashboard`
- `fix: исправить ошибку в аналитике`
- `docs: обновить README`
- `refactor: улучшить структуру кода`

---

## 🚀 Итог

**Рекомендуемый способ:**
```bash
./check-commit-push.sh "feat: добавить новую функцию"
```

**Это гарантирует:**
- ✅ Проверка перед коммитом
- ✅ Безошибочные коммиты
- ✅ Зелёные галочки на GitHub
- ✅ Правильный workflow

---

**Используй эту команду для каждого коммита!** 🚀
