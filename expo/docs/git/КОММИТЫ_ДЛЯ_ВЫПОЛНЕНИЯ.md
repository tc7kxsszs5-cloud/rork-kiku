# 📋 Коммиты для выполнения

## ✅ Коммит 1: Интеграция Supabase SDK и обновление routes

```bash
cd /Users/mac/Desktop/rork-kiku

# Добавить основные изменения backend
git add backend/package.json
git add backend/utils/supabase.ts
git add backend/trpc/routes/sync/chats.ts
git add backend/trpc/routes/sync/alerts.ts
git add backend/trpc/routes/test/
git add backend/index.ts
git add backend/trpc/app-router.ts
git add backend/vercel.json

# Коммит
git commit -m "feat: интеграция Supabase SDK и замена in-memory хранилища на PostgreSQL

- Добавлен @supabase/supabase-js для работы с базой данных
- Обновлены routes sync.chats и sync.alerts для использования Supabase
- Добавлен тестовый endpoint test.dbCheck для проверки подключения
- In-memory хранилище (Map) заменено на PostgreSQL через Supabase SDK"
```

---

## ✅ Коммит 2: SQL схема базы данных

```bash
# Добавить SQL схему
git add backend/schema.sql

# Коммит
git commit -m "feat: добавлена SQL схема базы данных

- Создана схема для таблиц: chats, messages, alerts, devices, sync_status
- Добавлены индексы для оптимизации запросов"
```

---

## ✅ Коммит 3: Обновление зависимостей и конфигурации

```bash
# Добавить обновления зависимостей
git add backend/bun.lock
git add package.json
git add app.json

# Коммит
git commit -m "chore: обновление зависимостей и конфигурации

- Обновлены зависимости для поддержки Supabase
- Обновлена конфигурация проекта"
```

---

## ✅ Коммит 4: Исправления импортов и путей

```bash
# Добавить исправления импортов
git add backend/trpc/routes/example/hi/route.ts
git add backend/trpc/routes/notifications/*.ts
git add backend/trpc/routes/sync/settings.ts

# Коммит
git commit -m "fix: исправление импортов и путей в routes

- Исправлены относительные пути импортов
- Обновлены routes для совместимости с новой структурой"
```

---

## ✅ Коммит 5: Удаление неиспользуемых файлов

```bash
# Удалить неиспользуемый файл
git add backend/hono.ts

# Коммит
git commit -m "refactor: удален неиспользуемый файл hono.ts

- Функциональность перенесена в index.ts"
```

---

## 📋 Опционально: Добавить временные файлы в .gitignore

Если не хотите коммитить временные файлы с инструкциями:

```bash
# Добавить в .gitignore
echo "*.md" >> .gitignore
echo "*.txt" >> .gitignore
echo "*.sh" >> .gitignore
echo "*.log" >> .gitignore

git add .gitignore
git commit -m "chore: добавлены временные файлы в .gitignore"
```

---

## 🚀 Push в репозиторий

После всех коммитов:

```bash
git push origin main
```

---

**Выполните коммиты по порядку!**
