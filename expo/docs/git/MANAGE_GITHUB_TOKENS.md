# 🔑 Управление GitHub токенами

## 📋 Как проверить существующие токены

### Шаг 1: Перейдите на GitHub

1. Откройте **GitHub.com** в браузере
2. Войдите в свой аккаунт
3. Нажмите на ваш **аватар** (правый верхний угол)
4. Выберите **Settings**

### Шаг 2: Перейдите к токенам

1. В левом меню найдите **Developer settings**
2. Нажмите на **Personal access tokens**
3. Выберите **Tokens (classic)** — здесь будут ваши токены

### Шаг 3: Проверьте список токенов

Вы увидите список всех ваших токенов:
- **Note** — название токена (например, "Cursor IDE")
- **Last used** — когда токен использовался последний раз
- **Expires** — срок действия токена
- **Scopes** — права доступа

### 📝 Что искать

Ищите токены с названиями:
- `Cursor IDE`
- `Cursor`
- `GitHub CLI`
- Или любые другие токены, которые вы создавали ранее

---

## 🗑️ Как отозвать старый токен

### Вариант 1: Отозвать один токен

1. В списке токенов найдите нужный токен
2. Нажмите на **три точки (⋯)** справа от токена
3. Выберите **Delete** или **Revoke**
4. Подтвердите удаление

### Вариант 2: Отозвать все неиспользуемые токены

1. В списке токенов проверьте каждый токен
2. Отзовите токены, которые:
   - ❌ Больше не используются
   - ❌ Созданы для старых проектов
   - ❌ Имеют подозрительную активность
   - ❌ Истекли, но все еще активны

### ⚠️ Важно

- После отзыва токен **перестанет работать немедленно**
- Cursor (или другие приложения) перестанут иметь доступ
- Вам нужно будет создать новый токен и обновить его в Cursor

---

## ✨ Как создать новый токен

### Шаг 1: Создайте новый токен

1. На странице **Personal access tokens → Tokens (classic)**
2. Нажмите **Generate new token** → **Generate new token (classic)**

### Шаг 2: Настройте токен

**Note (название):**
```
Cursor IDE - KIKU Project
```
(Любое описательное название, чтобы вы помнили, для чего токен)

**Expiration (срок действия):**
- `90 days` (рекомендуется для начала)
- `1 year` (если хотите реже обновлять)
- `No expiration` (только если уверены в безопасности)

**Select scopes (права доступа):**

✅ **Обязательно:**
- `repo` — Полный контроль над репозиториями
  - Доступ к коду, коммитам, веткам, issues, pull requests

✅ **Опционально (если используете GitHub Actions):**
- `workflow` — Обновление GitHub Actions workflows

❌ **НЕ давайте:**
- `admin:org` — Управление организацией
- `delete_repo` — Удаление репозиториев
- `admin:public_key` — Управление ключами

### Шаг 3: Создайте и скопируйте

1. Нажмите **Generate token** (внизу страницы)
2. **⚠️ ВАЖНО:** Скопируйте токен **сразу же**!
   - Токен показывается только один раз
   - Выглядит так: `ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`
3. Сохраните токен в безопасном месте (временно, пока не добавите в Cursor)

### Шаг 4: Добавьте токен в Cursor

1. Cursor запросит токен (или вы можете добавить его в настройках)
2. Вставьте скопированный токен
3. Cursor сохранит токен безопасно

---

## 🔄 Полная инструкция: Старый → Новый токен

### Если у вас ЕСТЬ старый токен:

1. ✅ **Проверьте список токенов** (см. выше)
2. ✅ **Создайте новый токен** (см. выше)
3. ✅ **Добавьте новый токен в Cursor**
4. ✅ **Протестируйте** — попробуйте сделать коммит через Cursor
5. ✅ **Отзовите старый токен** (после того, как новый работает)

### Если у вас НЕТ старого токена:

1. ✅ **Создайте новый токен** (см. выше)
2. ✅ **Добавьте токен в Cursor**
3. ✅ **Готово!**

---

## 🔍 Как проверить, работает ли токен

### Через терминал:

```bash
cd ~/Desktop/rork-kiku
git fetch origin
```

Если команда выполняется без ошибок аутентификации — токен работает! ✅

### Через Cursor:

1. Попробуйте сделать коммит через Cursor
2. Если коммит проходит — токен работает! ✅

---

## 📊 Рекомендации по безопасности

### ✅ Делайте:

- Используйте **минимальные права** (`repo` достаточно)
- Устанавливайте **срок действия** токена
- Используйте **описательные названия** для токенов
- **Регулярно проверяйте** список токенов
- **Отзывайте неиспользуемые** токены

### ❌ НЕ делайте:

- Не публикуйте токены в коде или коммитах
- Не делитесь токенами с другими
- Не используйте токены без срока действия (если не уверены)
- Не давайте лишние права (например, `admin:org`)

---

## 🎯 Краткая памятка

```
1. GitHub.com → Settings → Developer settings → Personal access tokens
2. Проверьте список токенов
3. Отзовите старые/неиспользуемые (если есть)
4. Generate new token (classic)
5. Название: "Cursor IDE - KIKU Project"
6. Права: только "repo" (достаточно)
7. Скопируйте токен СРАЗУ
8. Добавьте токен в Cursor
9. Готово! ✅
```

---

**Нужна помощь?** Следуйте инструкциям выше пошагово. Если что-то непонятно — спросите! 😊



