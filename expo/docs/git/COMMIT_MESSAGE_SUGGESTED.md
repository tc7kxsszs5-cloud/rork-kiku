# 💡 Рекомендуемое сообщение для коммита

## Вариант 1: Краткое (рекомендуется)

```bash
git commit -m "Обновления проекта: исправления TypeScript, тесты, CI/CD и очистка"
```

## Вариант 2: Подробное (с описанием)

```bash
git commit -m "Обновления проекта: исправления TypeScript, тесты, CI/CD и очистка" -m "- Исправил ошибки TypeScript (удален role из User)
- Добавил unit тесты для utilities
- Настроил CI/CD workflows
- Удалил ненужные папки и обновил .gitignore
- Исправил sync routes (дубликаты переменных)"
```

## Вариант 3: По категориям (если хотите несколько коммитов)

**Коммит 1:**
```bash
git add app/ backend/trpc/routes/sync/
git commit -m "Исправил ошибки TypeScript и sync routes"
```

**Коммит 2:**
```bash
git add __tests__/ jest.config.js jest.setup.js
git commit -m "Добавил unit тесты и настройки Jest"
```

**Коммит 3:**
```bash
git add .github/ .gitignore
git commit -m "Настроил CI/CD и очистил проект"
```

---

## 📝 Что означают части сообщения:

- **"Обновления проекта"** - общее описание
- **"исправления TypeScript"** - что было исправлено в коде
- **"тесты"** - добавлены тесты
- **"CI/CD"** - настройки автоматизации
- **"очистка"** - удаление ненужных файлов

---

## ✅ Рекомендация

Используйте **Вариант 1** - он короткий и понятный. Это хорошая практика для коммитов.
