# ✅ Правильный порядок: Проверка → Коммит → Пуш

## 🎯 Правильная последовательность

```
1. ПРОВЕРКА → 2. КОММИТ → 3. ПУШ
```

---

## 📋 Шаг 1: ПРОВЕРКА (перед коммитом)

### 1.1. Проверить статус изменений

```bash
# Посмотреть какие файлы изменены
git status

# Краткий статус
git status --short

# Посмотреть изменения в файлах
git diff
```

### 1.2. Проверить код (обязательно!)

```bash
# Проверить TypeScript (типы)
bunx tsc --noEmit

# Проверить линтер
bun run lint

# Запустить тесты (быстрые unit тесты)
bun test --testPathPattern=unit --run
```

### 1.3. Быстрая проверка всего сразу

```bash
# Проверить всё одной командой
bunx tsc --noEmit && bun run lint && bun test --testPathPattern=unit --run && echo "✅ Всё готово к коммиту!"
```

**Если команда выполнилась без ошибок → можно коммитить!**

---

## 📝 Шаг 2: КОММИТ (после проверки)

### 2.1. Выбрать файлы для коммита

**Вариант A: Коммитить только iOS симулятор (рекомендуется для начала)**

```bash
# Добавить только изменения iOS симулятора
git add scripts/ios-sim-auto.sh package.json
```

**Вариант B: Коммитить все изменения**

```bash
# Добавить все изменения
git add -A
# или
git add .
```

**Вариант C: Выборочное добавление**

```bash
# Добавить конкретные файлы
git add файл1.ts файл2.ts

# Или добавить все файлы в папке
git add scripts/
```

### 2.2. Проверить что будет в коммите

```bash
# Посмотреть что добавлено в staging
git status

# Посмотреть изменения которые будут закоммичены
git diff --staged
```

### 2.3. Создать коммит

```bash
# Коммит с сообщением
git commit -m "feat(ios): добавлен автоматический запуск приложения в симуляторе

- Создан скрипт ios-sim-auto.sh для автоматического запуска
- Обновлена команда ios:sim для использования нового скрипта
- Добавлена команда ios:sim:auto для явного использования"
```

**Формат сообщения коммита:**
- `feat:` - новая функция
- `fix:` - исправление ошибки
- `chore:` - обновление зависимостей, конфигурации
- `docs:` - документация
- `refactor:` - рефакторинг кода

---

## 🚀 Шаг 3: ПУШ (после коммита)

### 3.1. Проверить перед пушем

```bash
# Посмотреть последние коммиты
git log --oneline -5

# Проверить статус
git status

# Проверить что ветка впереди origin
git status
```

### 3.2. Отправить на GitHub

```bash
# Отправить в main ветку
git push origin main

# Или просто (если настроен upstream)
git push
```

---

## 🔄 Полная последовательность команд

### Для коммита iOS симулятора:

```bash
# 1. ПРОВЕРКА
cd /Users/mac/Desktop/rork-kiku
git status
bunx tsc --noEmit && bun run lint && echo "✅ Проверка пройдена"

# 2. КОММИТ
git add scripts/ios-sim-auto.sh package.json
git status
git commit -m "feat(ios): добавлен автоматический запуск приложения в симуляторе"

# 3. ПУШ
git log --oneline -1
git push origin main
```

### Для коммита всех изменений:

```bash
# 1. ПРОВЕРКА
cd /Users/mac/Desktop/rork-kiku
git status
bunx tsc --noEmit && bun run lint && echo "✅ Проверка пройдена"

# 2. КОММИТ
git add -A
git status
git commit -m "chore: обновление проекта и добавление автоматического запуска iOS симулятора"

# 3. ПУШ
git log --oneline -1
git push origin main
```

---

## ⚠️ Что делать если проверка не прошла

### Если TypeScript ошибки:

```bash
# Исправить ошибки в коде
# ... редактирование файлов ...

# Проверить снова
bunx tsc --noEmit
```

### Если Lint ошибки:

```bash
# Попробовать автоисправление
bun run lint:fix

# Проверить снова
bun run lint
```

### Если тесты не проходят:

```bash
# Запустить тесты с подробным выводом
bun test --testPathPattern=unit --run --verbose

# Исправить тесты
# ... редактирование ...

# Проверить снова
bun test --testPathPattern=unit --run
```

**Только после того как все проверки пройдут → делайте коммит!**

---

## ✅ Чеклист перед коммитом

- [ ] `git status` - проверил изменения
- [ ] `bunx tsc --noEmit` - нет ошибок TypeScript
- [ ] `bun run lint` - нет критичных ошибок линтера
- [ ] `bun test --testPathPattern=unit --run` - все тесты проходят (опционально)
- [ ] Код работает локально
- [ ] Выбрал правильные файлы для коммита (`git add`)
- [ ] Написал понятное сообщение коммита

**Только после этого → коммит и push!**

---

## 💡 Советы

1. **Всегда проверяй ДО коммита** - это экономит время
2. **Исправляй ошибки сразу** - не откладывай
3. **Используй быструю команду** - проверяй всё сразу
4. **Коммить только после проверки** - гарантия качества
5. **Делай маленькие коммиты** - легче откатывать изменения
6. **Пиши понятные сообщения** - легче найти изменения позже

---

## 🎯 Итог

**Правильный порядок:**
```
1. git status          → посмотреть изменения
2. bunx tsc --noEmit   → проверить типы
3. bun run lint        → проверить код
4. git add ...         → добавить файлы
5. git commit -m "..."  → закоммитить
6. git push            → отправить на GitHub
```

**Неправильный порядок:**
```
❌ git commit → git push → проверка (красные крестики!)
```
