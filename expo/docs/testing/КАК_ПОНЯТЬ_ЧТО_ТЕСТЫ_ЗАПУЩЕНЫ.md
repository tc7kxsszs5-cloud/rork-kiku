# 🎯 Как понять, что Playwright запустил тесты?

## 📊 Визуальные признаки запуска тестов

### 1. **В терминале вы увидите логи**

Когда вы запускаете `bun run test:playwright`, в терминале появляются сообщения:

```
Running 5 tests using 5 workers

  ✓ [chromium] › example.spec.ts:14:5 › KIKU Web App › должна загрузиться главная страница (2.3s)
  ✓ [chromium] › example.spec.ts:22:5 › KIKU Web App › должна отображаться навигация (1.8s)
  ✓ [firefox] › example.spec.ts:14:5 › KIKU Web App › должна загрузиться главная страница (2.5s)
  ...
```

---

## 🔍 Пошаговые признаки запуска

### Шаг 1: Запуск веб-сервера

**Вы увидите:**
```
Starting web server...
```

Или если сервер уже запущен:
```
Using existing web server at http://localhost:8082
```

**Что это значит:** Playwright пытается запустить ваше приложение.

---

### Шаг 2: Ожидание готовности сервера

**Вы увидите:**
```
Web server is ready!
```

Или в процессе ожидания:
```
Waiting for http://localhost:8082...
```

**Что это значит:** Playwright ждет, пока приложение запустится.

---

### Шаг 3: Начало тестов

**Вы увидите:**
```
Running 5 tests using 5 workers

  1) [chromium] › example.spec.ts:14:5 › KIKU Web App › должна загрузиться главная страница
```

**Что это значит:** Тесты начали выполняться!

---

### Шаг 4: Выполнение тестов

**Вы увидите прогресс:**
```
  ✓ [chromium] › example.spec.ts:14:5 › KIKU Web App › должна загрузиться главная страница (2.3s)
  → [firefox] › example.spec.ts:14:5 › KIKU Web App › должна загрузиться главная страница (в процессе...)
  ✓ [webkit] › example.spec.ts:14:5 › KIKU Web App › должна загрузиться главная страница (2.1s)
```

**Что это значит:** Тесты выполняются параллельно в разных браузерах.

---

### Шаг 5: Завершение

**Вы увидите итоги:**
```
  5 passed (15.2s)
  
  To open last HTML report run:
    bunx playwright show-report
```

**Что это значит:** Все тесты завершены!

---

## 🎨 Разные способы запуска и их признаки

### 1. Обычный запуск (`bun run test:playwright`)

**Что вы увидите:**
```
Running 5 tests using 5 workers

  ✓ [chromium] › example.spec.ts:14:5 › KIKU Web App › должна загрузиться главная страница (2.3s)
  ✓ [chromium] › example.spec.ts:22:5 › KIKU Web App › должна отображаться навигация (1.8s)
  ...

  5 passed (15.2s)
```

**Признаки:**
- ✅ Видны названия тестов
- ✅ Видны браузеры (chromium, firefox, webkit)
- ✅ Видны галочки (✓) или крестики (✗)
- ✅ Видно время выполнения

---

### 2. UI режим (`bun run test:playwright:ui`)

**Что вы увидите:**
```
Opening Playwright UI...
```

**Затем откроется окно браузера** с интерфейсом Playwright:

```
┌─────────────────────────────────────┐
│  Playwright Test UI                 │
├─────────────────────────────────────┤
│  📋 Tests                           │
│  ✓ example.spec.ts                  │
│    ✓ должна загрузиться...          │
│    ✓ должна отображаться...         │
│                                     │
│  🎬 Watch mode: ON                  │
│  ▶️ Run all tests                   │
└─────────────────────────────────────┘
```

**Признаки:**
- ✅ Открывается окно браузера
- ✅ Виден список тестов
- ✅ Можно видеть выполнение в реальном времени
- ✅ Можно видеть браузер во время теста

---

### 3. Debug режим (`bun run test:playwright:debug`)

**Что вы увидите:**
```
Opening Playwright Inspector...
```

**Затем откроется:**
- Окно браузера с вашим приложением
- Окно Playwright Inspector с отладкой

**Признаки:**
- ✅ Открывается браузер
- ✅ Видно окно отладки
- ✅ Можно пошагово выполнять тест
- ✅ Можно видеть каждый шаг

---

### 4. Headed режим (`bun run test:playwright:headed`)

**Что вы увидите:**
```
Running 5 tests using 5 workers
```

**И одновременно откроются окна браузеров:**
- Chrome окно открывается и закрывается
- Firefox окно открывается и закрывается
- Safari окно открывается и закрывается

**Признаки:**
- ✅ Видны окна браузеров
- ✅ Видно, как выполняются действия
- ✅ Можно наблюдать за тестами визуально

---

## 📋 Полный пример вывода

Вот как выглядит **полный запуск** тестов:

```bash
$ bun run test:playwright

Running 5 tests using 5 workers

Starting web server...
Web server is ready!

  1) [chromium] › example.spec.ts:14:5 › KIKU Web App › должна загрузиться главная страница
  2) [firefox] › example.spec.ts:14:5 › KIKU Web App › должна загрузиться главная страница
  3) [webkit] › example.spec.ts:14:5 › KIKU Web App › должна загрузиться главная страница
  4) [Mobile Chrome] › example.spec.ts:14:5 › KIKU Web App › должна загрузиться главная страница
  5) [Mobile Safari] › example.spec.ts:14:5 › KIKU Web App › должна загрузиться главная страница

  ✓ [chromium] › example.spec.ts:14:5 › KIKU Web App › должна загрузиться главная страница (2.3s)
  ✓ [chromium] › example.spec.ts:22:5 › KIKU Web App › должна отображаться навигация (1.8s)
  ✓ [chromium] › example.spec.ts:30:5 › KIKU Web App › должна работать темная/светлая тема (1.2s)
  ✓ [firefox] › example.spec.ts:14:5 › KIKU Web App › должна загрузиться главная страница (2.5s)
  ✓ [webkit] › example.spec.ts:14:5 › KIKU Web App › должна загрузиться главная страница (2.1s)
  ...

  5 passed (15.2s)

  To open last HTML report run:
    bunx playwright show-report
```

---

## ✅ Чеклист: Как понять, что тесты запущены?

### В терминале вы увидите:

- [ ] Сообщение "Starting web server..." или "Using existing web server..."
- [ ] Сообщение "Web server is ready!"
- [ ] Сообщение "Running X tests using Y workers"
- [ ] Список тестов с названиями файлов
- [ ] Прогресс выполнения (галочки ✓ или крестики ✗)
- [ ] Время выполнения каждого теста
- [ ] Итоговый результат (X passed/failed)

### Визуально (если используете UI или headed режим):

- [ ] Открывается окно браузера
- [ ] Видно выполнение тестов в реальном времени
- [ ] Видны действия в браузере (клики, ввод текста)

---

## 🚨 Если тесты НЕ запускаются

### Признаки проблемы:

```
Error: Timed out waiting for http://localhost:8082
```

**Что это значит:** Приложение не запустилось за 2 минуты.

**Решение:**
1. Проверьте, что порт 8082 свободен
2. Запустите приложение вручную: `bun run start:web`
3. Проверьте, что оно доступно на `http://localhost:8082`

---

```
Error: No tests found
```

**Что это значит:** Playwright не нашел тесты.

**Решение:**
1. Проверьте, что тесты в папке `__tests__/playwright/`
2. Проверьте расширение файлов (должно быть `.spec.ts`)
3. Проверьте конфигурацию `testDir` в `playwright.config.ts`

---

```
Error: Cannot find module '@playwright/test'
```

**Что это значит:** Playwright не установлен.

**Решение:**
```bash
bun install
bunx playwright install
```

---

## 🎯 Быстрая проверка

### Самый простой способ понять, что тесты запущены:

```bash
# Запустите с UI - вы сразу увидите интерфейс
bun run test:playwright:ui
```

**Вы увидите:**
- ✅ Окно браузера с интерфейсом Playwright
- ✅ Список всех тестов
- ✅ Кнопку "Run all tests"
- ✅ Выполнение тестов в реальном времени

---

## 📊 Итоговая таблица признаков

| Признак | Что означает | Где видно |
|---------|--------------|-----------|
| "Starting web server..." | Playwright запускает приложение | Терминал |
| "Web server is ready!" | Приложение готово к тестам | Терминал |
| "Running X tests..." | Тесты начали выполняться | Терминал |
| Галочки ✓ | Тест прошел успешно | Терминал |
| Крестики ✗ | Тест упал с ошибкой | Терминал |
| Открывается браузер | Тесты выполняются визуально | Окно браузера |
| "X passed (Y seconds)" | Все тесты завершены | Терминал |

---

## 💡 Совет

**Для первого запуска используйте UI режим:**

```bash
bun run test:playwright:ui
```

Это самый наглядный способ увидеть, что происходит! 🎯
