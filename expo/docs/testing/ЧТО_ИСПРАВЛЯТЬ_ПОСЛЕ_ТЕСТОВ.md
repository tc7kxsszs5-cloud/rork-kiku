# 🔧 Что исправлять после тестов Playwright?

## ❌ НЕТ! Playwright НЕ исправляет код

**Playwright только находит ошибки, но НЕ исправляет их автоматически.**

Исправлять нужно **ВРУЧНУЮ** в коде приложения или в тестах.

---

## 🎯 Два типа ошибок и что делать

### Тип 1: Ошибка в приложении (исправлять код приложения)

**Пример ошибки:**
```
Error: page.locator('form') → Timeout
Element not found
```

**Что это значит:**
- Тест ищет форму на странице `/register-parent`
- Формы нет или она не загружается
- **Проблема в приложении**, не в тесте

**Где исправлять:**
- В файле приложения: `app/register-parent.tsx`
- Добавить форму или исправить, чтобы она загружалась

**Как исправить:**
1. Откройте `app/register-parent.tsx`
2. Проверьте, есть ли там форма
3. Если нет - добавьте
4. Если есть, но не загружается - исправьте код

---

### Тип 2: Ошибка в тесте (исправлять тест)

**Пример ошибки:**
```
Error: page.locator('form') → Timeout
```

**Но форма ЕСТЬ на странице!**

**Что это значит:**
- Тест ищет форму неправильным способом
- Селектор неправильный
- **Проблема в тесте**, не в приложении

**Где исправлять:**
- В файле теста: `__tests__/playwright/example.spec.ts`
- Изменить селектор или способ поиска

**Как исправить:**
1. Откройте `__tests__/playwright/example.spec.ts`
2. Найдите неправильный селектор
3. Исправьте его на правильный

---

## 🔍 Как понять, что исправлять?

### Шаг 1: Проверить приложение вручную

```bash
# Запустите приложение
bun run start:web

# Откройте в браузере
# http://localhost:8082/register-parent
```

**Посмотрите:**
- Есть ли форма на странице?
- Загружается ли она?
- Работает ли страница?

---

### Шаг 2: Определить проблему

#### Если форма ЕСТЬ в браузере:
→ **Проблема в тесте** → Исправляйте тест

#### Если формы НЕТ в браузере:
→ **Проблема в приложении** → Исправляйте код приложения

---

## 📝 Примеры исправлений

### Пример 1: Исправление приложения

**Ошибка в тесте:**
```
Error: page.locator('form') → Timeout
```

**Проверка:**
- Открыли `http://localhost:8082/register-parent`
- Формы нет на странице

**Исправление:**
1. Откройте `app/register-parent.tsx`
2. Добавьте форму:
```tsx
export default function RegisterParent() {
  return (
    <View>
      <form data-testid="register-form">
        {/* форма */}
      </form>
    </View>
  );
}
```

3. Сохраните файл
4. Запустите тест снова

---

### Пример 2: Исправление теста

**Ошибка в тесте:**
```
Error: page.locator('form') → Timeout
```

**Проверка:**
- Открыли `http://localhost:8082/register-parent`
- Форма ЕСТЬ на странице
- Но тест ее не находит

**Исправление:**
1. Откройте `__tests__/playwright/example.spec.ts`
2. Найдите тест:
```typescript
test('должна отображаться форма регистрации', async ({ page }) => {
  await page.goto('/register-parent');
  const form = page.locator('form').first(); // ← Неправильный селектор
  await expect(form).toBeVisible();
});
```

3. Исправьте селектор:
```typescript
test('должна отображаться форма регистрации', async ({ page }) => {
  await page.goto('/register-parent');
  await page.waitForLoadState('networkidle'); // ← Добавить ожидание
  const form = page.getByTestId('register-form'); // ← Правильный селектор
  await expect(form).toBeVisible();
});
```

4. Сохраните файл
5. Запустите тест снова

---

## 🎯 Алгоритм исправления

```
1. Playwright находит ошибку
   ↓
2. Прочитайте ошибку в терминале
   ↓
3. Откройте приложение в браузере вручную
   ↓
4. Проверьте, есть ли проблема в реальности
   ↓
5. Если проблема ЕСТЬ → Исправляйте код приложения
   Если проблемы НЕТ → Исправляйте тест
   ↓
6. Запустите тест снова
   ↓
7. Проверьте, исправлено ли
```

---

## 📋 Чеклист: Что исправлять?

### Исправлять код приложения, если:
- [ ] Элемент отсутствует на странице
- [ ] Страница не загружается
- [ ] Функция не работает
- [ ] Ошибка в реальном приложении

### Исправлять тест, если:
- [ ] Элемент есть, но тест его не находит
- [ ] Селектор неправильный
- [ ] Тест проверяет не то, что нужно
- [ ] Недостаточно ожиданий (timeout)

---

## 💡 Где что исправлять?

### Код приложения:
```
app/
├── register-parent.tsx    ← Исправлять здесь
├── register-child.tsx     ← Исправлять здесь
├── (tabs)/
│   ├── index.tsx          ← Исправлять здесь
│   └── profile.tsx         ← Исправлять здесь
└── ...
```

### Тесты:
```
__tests__/playwright/
└── example.spec.ts        ← Исправлять здесь
```

---

## 🛠️ Инструменты для исправления

### Для исправления кода приложения:
- ✅ Ваш редактор (VS Code, Cursor)
- ✅ Файлы в папке `app/`
- ✅ Компоненты React Native

### Для исправления тестов:
- ✅ Ваш редактор (VS Code, Cursor)
- ✅ Файлы в папке `__tests__/playwright/`
- ✅ Playwright API документация

---

## ✅ Итог

**Playwright НЕ исправляет код автоматически!**

**Что делать:**
1. ✅ Playwright находит ошибки → показывает в терминале
2. ✅ Вы читаете ошибку → понимаете проблему
3. ✅ Проверяете приложение вручную → определяете, где проблема
4. ✅ Исправляете код или тест → вручную в редакторе
5. ✅ Запускаете тест снова → проверяете исправление

**Playwright = детектив (находит проблемы)**
**Вы = врач (исправляете проблемы)** 🚀

---

## 🎯 Главное правило

**Если проблема в приложении → исправляйте код приложения**
**Если проблема в тесте → исправляйте тест**

**Playwright только показывает, где проблема, но не исправляет её!**
