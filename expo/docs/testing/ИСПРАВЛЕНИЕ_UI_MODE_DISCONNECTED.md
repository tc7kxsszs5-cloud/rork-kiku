# 🔧 Исправление "UI Mode disconnected" в Playwright

## ❌ Проблема

Вы видите сообщение **"UI Mode disconnected"** в Playwright UI.

## 🔍 Причины

1. **Веб-сервер не запустился** - Playwright не может подключиться к приложению
2. **Порт 8082 занят** - другой процесс использует порт
3. **Веб-сервер упал** - сервер запустился, но затем остановился
4. **Таймаут** - сервер не успел запуститься за 2 минуты

---

## ✅ Решения

### Решение 1: Освободить порт и перезапустить

```bash
# 1. Освободить порт 8082
bun run kill-ports

# Или вручную:
lsof -ti:8082 | xargs kill -9

# 2. Запустить тесты снова
bun run test:playwright:ui
```

---

### Решение 2: Запустить приложение вручную

Если Playwright не может автоматически запустить сервер:

```bash
# Терминал 1: Запустите приложение вручную
bun run start:web

# Дождитесь сообщения:
# "Web is waiting on http://localhost:8082"

# Терминал 2: Запустите тесты
bun run test:playwright:ui
```

Playwright обнаружит уже запущенный сервер и использует его.

---

### Решение 3: Увеличить таймаут

Если приложение долго запускается, увеличьте таймаут в `playwright.config.ts`:

```typescript
webServer: {
  command: 'bun run start:web',
  url: 'http://localhost:8082',
  reuseExistingServer: !process.env.CI,
  timeout: 180 * 1000,  // ← Увеличено до 3 минут
  stdout: 'ignore',
  stderr: 'pipe',
}
```

---

### Решение 4: Проверить логи веб-сервера

Измените конфигурацию, чтобы видеть логи:

```typescript
webServer: {
  command: 'bun run start:web',
  url: 'http://localhost:8082',
  reuseExistingServer: !process.env.CI,
  timeout: 120 * 1000,
  stdout: 'pipe',  // ← Изменено с 'ignore' на 'pipe'
  stderr: 'pipe',
}
```

Теперь вы увидите логи Expo в терминале и поймете, почему сервер не запускается.

---

### Решение 5: Использовать другой порт

Если порт 8082 постоянно занят:

1. Измените порт в `package.json`:
```json
"start:web": "bunx expo start --web --port 8083"
```

2. Измените порт в `playwright.config.ts`:
```typescript
baseURL: 'http://localhost:8083',
webServer: {
  url: 'http://localhost:8083',
  // ...
}
```

---

## 🔍 Диагностика

### Шаг 1: Проверить, занят ли порт

```bash
lsof -i:8082
```

Если порт занят, вы увидите процессы. Освободите их:
```bash
lsof -ti:8082 | xargs kill -9
```

### Шаг 2: Проверить, запускается ли приложение

```bash
# Запустите вручную и посмотрите на ошибки
bun run start:web
```

Если есть ошибки - исправьте их перед запуском тестов.

### Шаг 3: Проверить конфигурацию

Убедитесь, что в `playwright.config.ts` правильно указан URL:

```typescript
baseURL: 'http://localhost:8082',
webServer: {
  url: 'http://localhost:8082',
  // ...
}
```

---

## 🎯 Быстрое исправление

### Самый быстрый способ:

```bash
# 1. Освободить все порты
bun run kill-ports

# 2. Запустить приложение вручную (в отдельном терминале)
bun run start:web

# 3. В другом терминале запустить тесты
bun run test:playwright:ui
```

---

## 📋 Чеклист исправления

- [ ] Освобожден порт 8082
- [ ] Приложение запускается вручную без ошибок
- [ ] URL в конфигурации правильный (`http://localhost:8082`)
- [ ] Команда `start:web` работает
- [ ] Таймаут достаточный (120 секунд)
- [ ] Нет конфликтов с другими процессами

---

## 💡 Предотвращение проблемы

### Вариант 1: Всегда запускать приложение вручную

```bash
# Терминал 1
bun run start:web

# Терминал 2
bun run test:playwright:ui
```

### Вариант 2: Использовать скрипт

Создайте скрипт `test-playwright.sh`:

```bash
#!/bin/bash
# Освободить порты
bun run kill-ports

# Запустить приложение в фоне
bun run start:web &
WEB_PID=$!

# Подождать запуска
sleep 10

# Запустить тесты
bun run test:playwright:ui

# Остановить приложение
kill $WEB_PID
```

---

## 🚨 Если ничего не помогает

1. **Перезапустите терминал**
2. **Очистите кэш:**
   ```bash
   bun run start:clear
   ```
3. **Переустановите зависимости:**
   ```bash
   bun install
   ```
4. **Проверьте версию Playwright:**
   ```bash
   bunx playwright --version
   ```

---

## ✅ Итог

**"UI Mode disconnected"** обычно означает, что веб-сервер не запустился или упал.

**Самое простое решение:**
1. Освободить порт: `bun run kill-ports`
2. Запустить приложение вручную: `bun run start:web`
3. Запустить тесты: `bun run test:playwright:ui`
