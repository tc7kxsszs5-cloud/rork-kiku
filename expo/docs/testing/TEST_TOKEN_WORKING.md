# ✅ Как убедиться, что токен GitHub работает?

## 🚀 Быстрая проверка (30 секунд)

### Способ 1: Через терминал (самый простой)

```bash
cd ~/Desktop/rork-kiku
git fetch origin
```

**Результат:**
- ✅ **Команда выполнилась БЕЗ ошибок** → Токен работает! 🎉
- ❌ **"Authentication failed"** → Токен не работает или неверный
- ❌ **"Permission denied"** → Токен не имеет нужных прав
- 🔐 **Запрос пароля** → Токен не настроен

---

## 📋 Полная проверка (3 способа)

### Тест 1: git fetch (базовая проверка)

```bash
cd ~/Desktop/rork-kiku
git fetch origin
```

**Ожидаемый результат:** Команда выполняется без ошибок

---

### Тест 2: git ls-remote (проверка доступа к репозиторию)

```bash
cd ~/Desktop/rork-kiku
git ls-remote --heads origin
```

**Ожидаемый результат:** Список веток репозитория

**Если выводит:**
- ✅ Список веток (например, `refs/heads/main`) → Токен работает! ✅
- ❌ Ошибка аутентификации → Токен не работает

---

### Тест 3: Через Cursor (проверка коммита)

1. Откройте любой файл в Cursor (например, `README.md`)
2. Сделайте небольшое изменение (добавьте пробел или комментарий)
3. Откройте **Source Control** (иконка ветки слева или `Ctrl + Shift + G`)
4. Напишите commit message (например, "test commit")
5. Нажмите **Commit** (галочка вверху)

**Результат:**
- ✅ Коммит проходит → Токен работает! ✅
- ❌ Ошибка аутентификации → Токен не работает
- 🔐 Запрос токена/пароля → Вставьте токен

---

## 🔍 Проверка на GitHub

### Способ 1: Проверить последнее использование токена

1. Откройте **GitHub.com**
2. Войдите в аккаунт
3. **Settings** → **Developer settings** → **Personal access tokens** → **Tokens (classic)**
4. Найдите ваш токен (например, "Cursor IDE - KIKU Project")
5. Проверьте колонку **Last used**

**Если показывает:**
- ✅ Недавнее время (сегодня/вчера) → Токен используется и работает! ✅
- ❌ "Never" → Токен не использовался (возможно, не настроен)

---

### Способ 2: Проверить через GitHub API (продвинутый способ)

```bash
# Замените YOUR_TOKEN на ваш токен (или используйте переменную окружения)
curl -H "Authorization: token YOUR_TOKEN" https://api.github.com/user
```

**Ожидаемый результат:** JSON с информацией о вашем аккаунте

**Если выводит:**
- ✅ JSON с вашими данными → Токен работает! ✅
- ❌ "Bad credentials" → Токен неверный или истек
- ❌ "Not found" → Токен не имеет нужных прав

---

## 🎯 Пошаговая инструкция проверки

### Шаг 1: Базовая проверка

```bash
cd ~/Desktop/rork-kiku
git fetch origin
```

**Если работает:**
- ✅ Токен настроен и работает!
- ✅ Переходите к Шагу 2 (опционально)

**Если НЕ работает:**
- ❌ Нужно добавить/обновить токен
- 📖 См. инструкции: [ADD_TOKEN_TO_CURSOR.md](ADD_TOKEN_TO_CURSOR.md)

---

### Шаг 2: Проверка через GitHub (опционально)

1. GitHub.com → Settings → Developer settings → Personal access tokens
2. Найдите ваш токен
3. Проверьте **Last used**

**Если показывает недавнее время:**
- ✅ Токен точно работает! ✅

---

### Шаг 3: Тест коммита (опционально)

1. Сделайте тестовый коммит через Cursor
2. Если коммит проходит → Токен работает! ✅

---

## ❌ Что делать, если токен НЕ работает?

### Проблема 1: "Authentication failed"

**Решение:**
1. Проверьте, что токен скопирован полностью (начинается с `ghp_`)
2. Убедитесь, что токен не истек
3. Проверьте, что токен имеет права `repo`
4. Создайте новый токен и обновите его

### Проблема 2: "Permission denied"

**Решение:**
1. Проверьте права токена (должен быть `repo`)
2. Убедитесь, что у вас есть доступ к репозиторию
3. Создайте новый токен с правильными правами

### Проблема 3: "Token expired"

**Решение:**
1. GitHub.com → Settings → Developer settings → Personal access tokens
2. Найдите истекший токен
3. Создайте новый токен
4. Обновите токен в Cursor

---

## 📊 Таблица проверки

| Тест | Команда/Действие | Ожидаемый результат | Статус |
|------|------------------|---------------------|--------|
| Базовая проверка | `git fetch origin` | Выполняется без ошибок | ✅ |
| Доступ к репозиторию | `git ls-remote origin` | Список веток | ✅ |
| Коммит через Cursor | Commit через UI | Коммит проходит | ✅ |
| GitHub Last used | Проверка на GitHub.com | Недавнее время | ✅ |

---

## ✅ Итоговая проверка

Выполните эту команду для быстрой проверки:

```bash
cd ~/Desktop/rork-kiku && \
echo "🔍 Проверка токена..." && \
git fetch origin 2>&1 && \
echo "✅ Токен работает!" || \
echo "❌ Токен не работает. Нужно настроить."
```

**Если выводит "✅ Токен работает!"** — всё отлично! 🎉

---

## 💡 Полезные команды

### Проверить сохраненные credentials:

```bash
# Git credential helper
git config --global credential.helper

# macOS Keychain (может показать наличие сохраненных credentials)
security find-generic-password -s github.com 2>&1 | head -3
```

### Очистить старые credentials (если нужно):

```bash
git credential-osxkeychain erase
host=github.com
protocol=https
# Нажмите Enter дважды
```

---

**Вывод:** Самый простой способ проверить — выполнить `git fetch origin`. Если команда выполняется без ошибок — токен работает! ✅



