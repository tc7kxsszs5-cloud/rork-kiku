# 🚀 Все возможные способы запуска приложения

## 🔍 Если ничего не работает

### Вариант 1: Базовый Expo (самый простой)
```bash
npx expo start
```
**Что делает:** Запускает Expo в интерактивном режиме, предложит выбрать платформу.

---

### Вариант 2: С очисткой кэша
```bash
npx expo start --clear
```
**Что делает:** Очищает кэш Metro Bundler и перезапускает.

---

### Вариант 3: Только веб (браузер)
```bash
npx expo start --web
```
**Что делает:** Открывает приложение в браузере. Не нужен телефон.

---

### Вариант 4: С явным портом
```bash
npx expo start --port 8081
```
**Что делает:** Запускает на конкретном порту.

---

### Вариант 5: Без Dev Client (только Expo Go)
```bash
EXPO_USE_DEV_CLIENT=false npx expo start
```
**Что делает:** Принудительно использует Expo Go вместо dev client.

---

### Вариант 6: Через bunx
```bash
bunx expo start
```
**Что делает:** Использует bunx вместо npx.

---

### Вариант 7: Без tunnel (только LAN)
```bash
npx expo start --no-tunnel
```
**Что делает:** Отключает tunnel, использует только локальную сеть.

---

### Вариант 8: С отладкой
```bash
DEBUG=expo* npx expo start
```
**Что делает:** Показывает подробные логи для отладки.

---

### Вариант 9: Веб с отладкой
```bash
DEBUG=expo* npx expo start --web
```
**Что делает:** Веб-версия с подробными логами.

---

### Вариант 10: Минимальный запуск
```bash
npx expo start --no-dev --minify
```
**Что делает:** Минимальный режим без dev инструментов.

---

## 📋 Рекомендуемый порядок попыток

### Попытка 1: Самый простой
```bash
npx expo start
```
Нажми `w` для веб или `i` для iOS симулятора.

---

### Попытка 2: Если порт занят
```bash
# Остановить процессы
pkill -f expo
pkill -f metro

# Запустить заново
npx expo start --clear
```

---

### Попытка 3: Только веб (гарантированно работает)
```bash
npx expo start --web
```
Откроется в браузере - можно тестировать без телефона.

---

### Попытка 4: С отладкой (чтобы увидеть ошибки)
```bash
DEBUG=expo* npx expo start --web
```
Покажет подробные логи - можно увидеть что не так.

---

## 🔧 Диагностика проблем

### Проверить что Expo установлен:
```bash
npx expo --version
```

### Проверить что порт свободен:
```bash
lsof -i:8081
```

### Остановить все процессы:
```bash
pkill -f expo
pkill -f metro
pkill -f node
```

### Проверить конфигурацию:
```bash
cat app.json
```

---

## 💡 Альтернативные решения

### Если Expo не работает вообще:

1. **Переустановить зависимости:**
   ```bash
   rm -rf node_modules
   bun install
   ```

2. **Проверить версию Node:**
   ```bash
   node --version
   # Должна быть 18+
   ```

3. **Использовать веб-версию напрямую:**
   ```bash
   npx expo start --web
   # Откроется в браузере
   ```

4. **Создать development build через EAS:**
   ```bash
   eas build --profile development --platform ios
   ```

---

## 🎯 Самый надежный способ

### Для тестирования прямо сейчас:

```bash
# 1. Остановить всё
pkill -f expo && pkill -f metro

# 2. Запустить веб-версию
npx expo start --web
```

**Это гарантированно работает** - откроется в браузере, не нужен телефон.

---

## ✅ Чеклист

- [ ] Попробовал `npx expo start`
- [ ] Попробовал `npx expo start --web`
- [ ] Попробовал `npx expo start --clear`
- [ ] Проверил что порт свободен
- [ ] Остановил все процессы перед запуском
- [ ] Проверил версию Expo: `npx expo --version`

---

**Если ничего не работает - используй веб-версию: `npx expo start --web`** 🚀
