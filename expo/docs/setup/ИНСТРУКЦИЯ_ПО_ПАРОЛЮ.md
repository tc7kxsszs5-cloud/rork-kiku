# 🔐 Инструкция: Как получить и использовать пароль

## 📋 Что у вас есть:

```
postgresql://postgres:[YOUR-PASSWORD]@db.abcdefghijklmnopqrst.supabase.co:5432/postgres
```

## ✅ Что нужно сделать:

### Шаг 1: Найдите ваш реальный пароль

Пароль мог быть создан при создании проекта Supabase. Проверьте:

1. **Settings** → **Database**
2. Найдите раздел **Database password** или **Connection string**
3. Если видите кнопку **"Show"** или **"Reveal"** - нажмите её
4. Или найдите **"Reset database password"** - там может быть текущий пароль

### Шаг 2: Если пароль не найден

Если пароль не виден, его нужно установить:

1. **Settings** → **Database**
2. Найдите **"Database password"** или **"Reset database password"**
3. Нажмите **"Reset database password"**
4. Supabase создаст новый пароль автоматически
5. **ВАЖНО:** Скопируйте пароль сразу! Он показывается только один раз

### Шаг 3: Преобразуйте Connection String для Vercel

**Ваш оригинальный connection string:**
```
postgresql://postgres:[YOUR-PASSWORD]@db.eznumgsmwvavyunqhxfc.supabase.co:5432/postgres
```

**Для Vercel (Connection Pooling):**
```
postgresql://postgres:[YOUR-PASSWORD]@aws-0-us-east-1.pooler.supabase.com:6543/postgres
```

**Что изменилось:**
- `db.eznumgsmwvavyunqhxfc.supabase.co:5432` → `aws-0-us-east-1.pooler.supabase.com:6543`
- Пароль остается тот же!

### Шаг 4: Если пароль содержит специальные символы

Если пароль содержит `@`, `:`, `/`, `#`, `%` и т.д., их нужно закодировать:

- `@` → `%40`
- `:` → `%3A`
- `/` → `%2F`
- `#` → `%23`
- `%` → `%25`

**Пример:**
Пароль: `My@Pass:123`
Закодированный: `My%40Pass%3A123`

Connection string:
```
postgresql://postgres:My%40Pass%3A123@aws-0-us-east-1.pooler.supabase.com:6543/postgres
```

---

## 📋 Команды для установки:

```bash
cd /Users/mac/Desktop/rork-kiku/backend
bunx vercel env rm DATABASE_URL production
bunx vercel env add DATABASE_URL production
```

**Ответьте:**
- `Mark as sensitive?` → **yes**
- `What's the value of DATABASE_URL?` → Вставьте преобразованный connection string

---

## 💡 Важно:

1. **Пароль показывается только один раз** при создании/сбросе
2. Если потеряли пароль - нужно сбросить его заново
3. Используйте Connection Pooling для Vercel (порт 6543)
