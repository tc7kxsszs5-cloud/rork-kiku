# Шаги для запуска системы регистрации родитель-ребенок

## ✅ Что уже сделано

1. ✅ Создана SQL схема для системы родитель-ребенок
2. ✅ Реализованы backend endpoints для регистрации
3. ✅ Созданы UI компоненты для регистрации родителя и ребенка
4. ✅ Интегрирована проверка первого запуска приложения
5. ✅ Обновлен роутинг приложения

## 📋 Что нужно сделать дальше

### 1. Применить SQL схему в Supabase

1. Откройте файл `SQL_РОДИТЕЛЬ_РЕБЕНОК_ДЛЯ_SUPABASE.sql`
2. Скопируйте весь содержимое
3. Откройте Supabase Dashboard → SQL Editor
4. Вставьте SQL код и выполните (Run)
5. Проверьте, что все таблицы созданы (Table Editor)

Подробная инструкция: `ИНСТРУКЦИЯ_ПРИМЕНЕНИЯ_SQL.md`

### 2. Развернуть backend на Vercel

```bash
cd backend
bunx vercel --prod
```

Или если уже развернут:
```bash
cd backend
bunx vercel --prod --force
```

### 3. Протестировать регистрацию

```bash
cd backend
bun test-registration.ts
```

Ожидаемый результат:
- ✅ Родитель успешно регистрируется
- ✅ Генерируется уникальный код (формат: KIKU-XXXXXX)
- ✅ Код валидируется корректно
- ✅ Ребенок успешно регистрируется с кодом родителя

### 4. Проверить работу в приложении

1. Запустите приложение:
   ```bash
   bun start
   ```

2. При первом запуске должен появиться экран выбора роли

3. **Тест регистрации родителя:**
   - Выберите "Я родитель"
   - Заполните форму (имя, email, согласия)
   - Нажмите "Зарегистрироваться"
   - Должен появиться экран с кодом (например: KIKU-ABC123)
   - Скопируйте код

4. **Тест регистрации ребенка:**
   - В другом устройстве/браузере откройте приложение
   - Выберите "Я ребенок"
   - Введите код родителя (KIKU-ABC123)
   - Заполните имя и возраст
   - Нажмите "Зарегистрироваться"
   - Должен произойти успешный вход в приложение

## 🔍 Проверка работы

### В Supabase

Проверьте, что данные сохраняются:

1. **Таблица `parents`:**
   ```sql
   SELECT * FROM parents ORDER BY created_at DESC LIMIT 5;
   ```

2. **Таблица `parent_codes`:**
   ```sql
   SELECT * FROM parent_codes ORDER BY created_at DESC LIMIT 5;
   ```

3. **Таблица `children`:**
   ```sql
   SELECT * FROM children ORDER BY created_at DESC LIMIT 5;
   ```

4. **Таблица `code_usage_log`:**
   ```sql
   SELECT * FROM code_usage_log ORDER BY used_at DESC LIMIT 5;
   ```

### В приложении

- ✅ При первом запуске показывается экран выбора роли
- ✅ После регистрации родителя показывается код
- ✅ Код можно скопировать
- ✅ Ребенок может зарегистрироваться с кодом
- ✅ После регистрации происходит автоматический вход

## 🐛 Устранение проблем

### Ошибка "Supabase клиент не инициализирован"

Проверьте переменные окружения в Vercel:
- `SUPABASE_URL`
- `SUPABASE_SERVICE_ROLE_KEY`

### Ошибка "Код не найден"

1. Проверьте, что код введен правильно (формат: KIKU-XXXXXX)
2. Проверьте, что код активен в таблице `parent_codes`
3. Проверьте, что не превышен лимит использований (`used_count < max_uses`)

### Ошибка при регистрации ребенка

1. Проверьте логи в консоли браузера/терминала
2. Проверьте, что таблица `children` существует в Supabase
3. Проверьте, что `device_id` генерируется корректно

### Приложение не перенаправляет на экран выбора роли

1. Проверьте, что `AuthContext` правильно загружает состояние
2. Проверьте, что `isAuthenticated` корректно обновляется
3. Проверьте логи в консоли

## 📝 Следующие шаги (опционально)

После успешной регистрации можно реализовать:

1. **Email верификацию для родителей**
2. **PIN код для входа**
3. **Восстановление кода родителя**
4. **Управление несколькими детьми**
5. **Мониторинг активности ребенка**
6. **Родительский чат с ребенком**

## 📚 Документация

- `SQL_РОДИТЕЛЬ_РЕБЕНОК_ДЛЯ_SUPABASE.sql` - SQL схема для Supabase
- `ИНСТРУКЦИЯ_ПРИМЕНЕНИЯ_SQL.md` - Подробная инструкция по применению SQL
- `backend/trpc/routes/auth/register-parent.ts` - Backend регистрации родителя
- `backend/trpc/routes/auth/register-child.ts` - Backend регистрации ребенка
- `backend/trpc/routes/auth/validate-code.ts` - Backend валидации кода
- `app/register-parent.tsx` - UI регистрации родителя
- `app/register-child.tsx` - UI регистрации ребенка
- `app/role-selection.tsx` - UI выбора роли
