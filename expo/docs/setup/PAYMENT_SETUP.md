# 💳 Настройка платежных систем для KIKU

Руководство по настройке Stripe, PayPal и других платежных систем.

## 🎯 Быстрый ответ

**У вас есть PayPal?** Отлично! Для начала этого достаточно.

### Варианты:
1. **PayPal** ✅ (у вас уже есть) - для спонсорства и пожертвований
2. **Stripe** - для подписок в приложении (если нужен)
3. **In-App Purchases** - для мобильных подписок (рекомендуется для iOS/Android)

---

## 📋 Что нужно для вашего проекта?

### Для СПОНСОРСТВА (добровольные пожертвования):
- ✅ **PayPal** - уже есть, этого достаточно!
- Ссылка: `paypal.me/kikustore` (уже настроено)

### Для ПОДПИСОК в приложении:
- **iOS/Android:** In-App Purchases (через App Store/Google Play) - **РЕКОМЕНДУЕТСЯ**
- **Web версия:** Stripe (если планируете web)
- **Все платформы:** Stripe (если нужна единая система)

---

## 💳 Вариант 1: Использовать только PayPal (ПРОСТОЙ)

**Если вам нужны только пожертвования/спонсорство:**

### Что уже настроено:
- ✅ PayPal Business аккаунт
- ✅ Ссылка: `paypal.me/kikustore`
- ✅ Упоминания в документации

### Что делать:
1. **Ничего!** PayPal уже работает для спонсорства
2. Пользователи могут перейти по ссылке и сделать пожертвование
3. Для подписок в приложении используйте In-App Purchases (см. ниже)

**Вывод:** Если нужны только пожертвования - Stripe НЕ нужен! ✅

---

## 💳 Вариант 2: Настройка Stripe (если нужен)

### Когда Stripe нужен:
- ✅ Планируете web версию с подписками
- ✅ Нужна единая система для всех платформ
- ✅ Хотите избежать комиссий App Store/Google Play

### Шаг 1: Создание аккаунта Stripe

1. Перейдите на https://stripe.com
2. Нажмите "Sign up" (Регистрация)
3. Заполните форму:
   - Email
   - Пароль
   - Страна (ваша страна)
   - Тип бизнеса (выберите подходящий)

### Шаг 2: Верификация аккаунта

1. Подтвердите email
2. Заполните информацию о бизнесе:
   - Название компании/проекта
   - Адрес
   - Налоговая информация (если требуется)
3. Добавьте банковский счет для получения платежей

### Шаг 3: Получение API ключей

1. Войдите в Dashboard Stripe: https://dashboard.stripe.com
2. Перейдите в **Developers** → **API keys**
3. Скопируйте ключи:
   - **Publishable key** (начинается с `pk_test_` или `pk_live_`)
   - **Secret key** (начинается с `sk_test_` или `sk_live_`)

⚠️ **Важно:**
- `test_` ключи - для тестирования (не настоящие платежи)
- `live_` ключи - для продакшена (настоящие платежи)
- **НЕ делитесь Secret key** ни с кем!

### Шаг 4: Установка Stripe в проект

```bash
# Установка Stripe SDK
bun add @stripe/stripe-react-native

# Для Expo может понадобиться:
bun add stripe
```

### Шаг 5: Настройка переменных окружения

Создайте файл `.env` (если его нет):

```bash
# .env
STRIPE_PUBLISHABLE_KEY=pk_test_ваш_ключ_здесь
STRIPE_SECRET_KEY=sk_test_ваш_секретный_ключ_здесь

# Для продакшена используйте live ключи:
# STRIPE_PUBLISHABLE_KEY=pk_live_...
# STRIPE_SECRET_KEY=sk_live_...
```

⚠️ **Важно:** Добавьте `.env` в `.gitignore` чтобы не закоммитить ключи!

### Шаг 6: Настройка webhook'ов (для подписок)

1. В Stripe Dashboard: **Developers** → **Webhooks**
2. Нажмите "Add endpoint"
3. URL: `https://ваш-домен.com/api/webhooks/stripe`
4. Выберите события:
   - `payment_intent.succeeded`
   - `customer.subscription.created`
   - `customer.subscription.updated`
   - `customer.subscription.deleted`
5. Скопируйте **Webhook signing secret** (начинается с `whsec_`)

Добавьте в `.env`:
```bash
STRIPE_WEBHOOK_SECRET=whsec_ваш_секрет_здесь
```

### Шаг 7: Интеграция в код (базовый пример)

Создайте файл `utils/stripe.ts`:

```typescript
import { loadStripe } from '@stripe/stripe-react-native';

const STRIPE_PUBLISHABLE_KEY = process.env.EXPO_PUBLIC_STRIPE_PUBLISHABLE_KEY || '';

export const stripePromise = loadStripe(STRIPE_PUBLISHABLE_KEY);

export async function createPaymentIntent(amount: number) {
  // Вызов вашего backend API
  const response = await fetch('/api/create-payment-intent', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ amount }),
  });
  return response.json();
}
```

### Шаг 8: Тестирование

1. Используйте тестовые карты Stripe:
   - Успешный платеж: `4242 4242 4242 4242`
   - Отклоненный: `4000 0000 0000 0002`
   - CVV: любой 3 цифры
   - Дата: любая будущая дата

2. Проверьте в Stripe Dashboard → **Payments** что платежи приходят

---

## 📱 Вариант 3: In-App Purchases (РЕКОМЕНДУЕТСЯ для мобильных)

**Для iOS и Android подписок - это стандартный способ!**

### Преимущества:
- ✅ Встроено в iOS/Android
- ✅ Пользователи доверяют (знакомый процесс)
- ✅ Автоматическое управление подписками
- ✅ Не нужна настройка Stripe

### Установка:

```bash
bun add expo-in-app-purchases
```

### Настройка:

1. **iOS:** Настройте подписки в App Store Connect
2. **Android:** Настройте подписки в Google Play Console
3. Используйте `expo-in-app-purchases` для обработки покупок

### Пример кода:

```typescript
import * as InAppPurchases from 'expo-in-app-purchases';

// Получить доступные продукты
const products = await InAppPurchases.getProductsAsync(['premium_monthly']);

// Купить подписку
await InAppPurchases.purchaseItemAsync('premium_monthly');
```

**Документация:** https://docs.expo.dev/versions/latest/sdk/in-app-purchases/

---

## 🔄 Гибридный подход (ОПТИМАЛЬНО)

**Использовать разные системы для разных целей:**

| Цель | Система | Статус |
|------|---------|--------|
| Спонсорство | PayPal | ✅ Настроено |
| iOS подписки | In-App Purchases | ⚙️ Настроить |
| Android подписки | In-App Purchases | ⚙️ Настроить |
| Web подписки | Stripe | ⚙️ Настроить (если нужен web) |

---

## 📊 Сравнение комиссий

| Система | Комиссия | Когда использовать |
|---------|----------|-------------------|
| PayPal | 2.9% + $0.30 | Спонсорство, пожертвования |
| Stripe | 2.9% + $0.30 | Web подписки, все платформы |
| App Store | 15-30% | iOS подписки (обязательно) |
| Google Play | 15-30% | Android подписки (обязательно) |

---

## ✅ Рекомендации для вашего проекта

### Сейчас (MVP):
1. ✅ **Спонсорство:** PayPal (уже настроено)
2. ⚙️ **Подписки:** In-App Purchases для iOS/Android
3. ❌ **Stripe:** Пока не нужен

### Позже (если появится web):
1. Добавить Stripe для web подписок
2. Оставить In-App Purchases для мобильных

---

## 🚀 Быстрый старт

### Если нужен только PayPal:
**Ничего делать не нужно!** PayPal уже работает. ✅

### Если нужен Stripe:
1. Создайте аккаунт на https://stripe.com
2. Получите API ключи
3. Добавьте в `.env`
4. Установите пакет: `bun add @stripe/stripe-react-native`
5. Интегрируйте в код (см. примеры выше)

### Если нужны In-App Purchases:
1. Настройте подписки в App Store Connect / Google Play Console
2. Установите: `bun add expo-in-app-purchases`
3. Используйте API для обработки покупок

---

## 📚 Полезные ссылки

- **Stripe:** https://stripe.com/docs
- **PayPal:** https://developer.paypal.com
- **Expo In-App Purchases:** https://docs.expo.dev/versions/latest/sdk/in-app-purchases/
- **Stripe React Native:** https://stripe.dev/stripe-react-native/

---

## ❓ Частые вопросы

### Нужен ли Stripe если есть PayPal?
**Для спонсорства - НЕТ.** PayPal достаточно.
**Для подписок в приложении - зависит от платформы:**
- iOS/Android: Лучше In-App Purchases
- Web: Нужен Stripe

### Можно ли использовать только PayPal?
**Да!** Для спонсорства и пожертвований PayPal отлично работает.

### Когда точно нужен Stripe?
- Если планируете web версию с подписками
- Если нужна единая система для всех платформ
- Если хотите избежать комиссий магазинов (но сложнее для пользователей)

---

**Итог:** Начните с PayPal (уже есть) и In-App Purchases для мобильных. Stripe добавьте позже, если появится необходимость! 🚀
