# 🔧 Исправление ошибок запуска

## Проблема 1: Порт 8081 занят

**Ошибка:**
```
› Port 8081 is running this app in another window
```

**Решение:**

### Вариант 1: Остановить процесс на порту 8081

```bash
# Найти и остановить процесс
lsof -ti:8081 | xargs kill -9

# Или вручную найти процесс
lsof -i:8081
# Затем убить процесс по PID
kill -9 <PID>
```

### Вариант 2: Использовать другой порт

```bash
# Запуск на другом порту
bunx expo start --lan --port 8082
```

---

## Проблема 2: ngrok tunnel не подключается

**Ошибка:**
```
CommandError: ngrok tunnel took too long to connect.
```

**Решение:**

### Использовать локальный запуск без туннеля

```bash
# Вместо start-web используйте:
bunx expo start --web

# Или для мобильного:
bunx expo start --lan
```

**Я обновил `package.json` - теперь `start-web` использует локальный запуск без туннеля.**

---

## ✅ Правильные команды для запуска

### Для телефона (локальная сеть):
```bash
bun run start:simple
# или
bunx expo start --lan
```

### Для веб-версии (локально):
```bash
bun run start-web
# или
bunx expo start --web
```

### Если порт занят:
```bash
# Остановите процесс
lsof -ti:8081 | xargs kill -9

# Затем запустите снова
bun run start:simple
```

---

## 🔍 Проверка процессов

```bash
# Проверить, что запущено на порту 8081
lsof -i:8081

# Найти все процессы expo/rork
ps aux | grep -E "expo|rork" | grep -v grep
```

---

## 💡 Рекомендации

1. **Всегда останавливайте предыдущий процесс** перед новым запуском
2. **Используйте локальный запуск** (`--lan` или `--web`) вместо туннеля для разработки
3. **Туннель нужен только** если телефон в другой сети

---

## 🚀 Быстрый старт (после исправления)

```bash
# 1. Остановить старые процессы
lsof -ti:8081 | xargs kill -9 2>/dev/null

# 2. Запустить для телефона
bun run start:simple

# 3. Или для веб-версии
bun run start-web
```
