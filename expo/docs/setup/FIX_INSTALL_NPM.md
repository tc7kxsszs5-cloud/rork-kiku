# 🔧 Исправление через npm (альтернатива bun)

## Проблема
Ошибки при установке через bun (zlib, integrity check).

## ✅ Решение: Использовать npm

npm более стабилен и проверен для Expo проектов.

### Шаг 1: Полная очистка

```bash
cd /Users/mac/Desktop/rork-kiku

# Остановить все процессы
killall node

# Удалить все связанное с bun
rm -rf node_modules
rm -f bun.lockb
rm -rf ~/.bun/install/cache

# Очистить кеш npm
npm cache clean --force
```

### Шаг 2: Установка через npm

```bash
# Установить зависимости
npm install
```

### Шаг 3: Запуск через npm

```bash
# Запустить dev сервер
npx expo start

# Или для симулятора:
npx expo start --ios
```

---

## 🔄 Обновить package.json scripts (опционально)

Если хотите использовать npm по умолчанию, можно обновить scripts в package.json, но это не обязательно - можно использовать npx напрямую.

---

## ✅ Команды для запуска после установки

```bash
# Запуск dev сервера
npx expo start

# Для симулятора iOS
npx expo start --ios

# Для веб
npx expo start --web
```

---

## 💡 Почему npm лучше в данном случае

- ✅ Более стабильная работа с Expo
- ✅ Меньше проблем с кешем
- ✅ Лучше поддерживается сообществом
- ✅ Работает "из коробки"
