# ⚙️ Настройка терминала

## ✅ Что было настроено:

1. **Автоматический переход в проект** - при открытии нового терминала вы автоматически будете в директории `/Users/mac/Desktop/rork-kiku`

2. **Alias `kiku`** - вы можете ввести `kiku` в любой момент, чтобы быстро перейти в проект

## 🔄 Применить изменения:

Выполните в текущем терминале:

```bash
source ~/.zshrc
```

Или просто **откройте новый терминал** - настройки применятся автоматически!

## 🎯 Использование:

### Автоматический переход:
- Просто откройте новый терминал - вы будете в директории проекта

### Быстрый переход (alias):
```bash
kiku  # Перейдет в ~/Desktop/rork-kiku
```

### Проверка:
```bash
pwd  # Должно показать: /Users/mac/Desktop/rork-kiku
```

## 🔙 Откатить изменения (если нужно):

Если вы хотите вернуть старую настройку:

```bash
cp ~/.zshrc.backup ~/.zshrc
source ~/.zshrc
```

Или удалите последние строки из `~/.zshrc` вручную.

---

**Теперь каждый новый терминал будет открываться в директории проекта! 🚀**






