# 🚀 Быстрый запуск

## ⚠️ ВАЖНО: Вы должны быть в директории проекта!

Всегда сначала переходите в директорию проекта:

```bash
cd ~/Desktop/rork-kiku
```

Проверьте, что вы в правильной директории:

```bash
pwd
```

**Должно показать:** `/Users/mac/Desktop/rork-kiku`

---

## 🏃 Запуск проекта

После того как вы в правильной директории:

```bash
bun run start -- --clear
```

---

## 🔍 Если получаете ошибку "Script not found"

Это означает, что вы **НЕ** в директории проекта. 

**Решение:** Выполните `cd ~/Desktop/rork-kiku` перед запуском команды.

---

## 💡 Полезная команда (скопируйте и вставьте):

```bash
cd ~/Desktop/rork-kiku && pwd && bun run start -- --clear
```

Эта команда:
1. Перейдет в проект
2. Покажет текущую директорию
3. Запустит проект






