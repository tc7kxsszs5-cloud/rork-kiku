# ✅ ТОЧНАЯ установка Connection String

## ❌ Проблема:

Username все еще `postgres` вместо `postgres.[PROJECT-REF]`

## ✅ Решение:

Connection string должен быть установлен **точно** в таком формате.

## 📋 Шаг 1: Убедитесь в Project Reference

Из вашего connection string: `db.eznumgsmwvavyunqhxfc.supabase.co`

**Project Reference = `eznumgsmwvavyunqhxfc`**

## 📋 Шаг 2: ТОЧНЫЙ Connection String для копирования

**СКОПИРУЙТЕ ЭТОТ СТРОКУ ЦЕЛИКОМ:**

```
postgresql://postgres.eznumgsmwvavyunqhxfc:gerkom-tYbpek-2cochi@aws-0-us-east-1.pooler.supabase.com:6543/postgres
```

**Обратите внимание:**
- Username: `postgres.eznumgsmwvavyunqhxfc` (с точкой и project reference!)
- НЕ просто `postgres`

## 📋 Шаг 3: Удалите старый и установите новый

```bash
cd /Users/mac/Desktop/rork-kiku/backend
```

```bash
bunx vercel env rm DATABASE_URL production
```

**Подтвердите удаление (если спросит)**

```bash
bunx vercel env add DATABASE_URL production
```

**Ответьте:**
- `Mark as sensitive?` → **yes**
- `What's the value of DATABASE_URL?` → **ВСТАВЬТЕ ТОЧНО ЭТУ СТРОКУ:**
  ```
  postgresql://postgres.eznumgsmwvavyunqhxfc:gerkom-tYbpek-2cochi@aws-0-us-east-1.pooler.supabase.com:6543/postgres
  ```

## 📋 Шаг 4: Деплой

```bash
bunx vercel --prod
```

## 📋 Шаг 5: Проверка

```bash
curl "https://backend-three-mauve-67.vercel.app/api/trpc/test.dbCheck"
```

**Ожидаемый результат:**
- `"username": "postgres.eznumgsmwvavyunqhxfc"` (не просто `postgres`)
- `"success": true`

---

## ⚠️ ВАЖНО:

1. **Скопируйте connection string целиком** - не редактируйте вручную
2. **Проверьте, что username содержит точку:** `postgres.eznumgsmwvavyunqhxfc`
3. **После установки обязательно сделайте деплой** (`bunx vercel --prod`)

---

## 🔍 Если все еще не работает:

Пришлите результат:
```bash
curl "https://backend-three-mauve-67.vercel.app/api/trpc/test.dbCheck" | grep -o '"username":"[^"]*"'
```

Это покажет, какой username используется.
