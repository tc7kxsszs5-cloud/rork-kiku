# ⚡ Быстрое создание проекта в Vercel

## 🚀 Самый простой способ

### Запустите одну команду:

```bash
cd backend
bunx vercel --prod
```

### Ответьте на вопросы:

1. **Set up and deploy?** → `Y` (да)
2. **Which scope?** → выберите ваш аккаунт (обычно первый)
3. **Link to existing project?** → `N` (нет, создать новый)
4. **What's your project's name?** → нажмите `Enter` (оставит по умолчанию)
5. **In which directory is your code located?** → `./` (точка слэш)
6. **Want to override the settings?** → `N` (нет)

После этого проект будет создан!

---

## 📋 После создания

### Добавьте переменные через CLI:

```bash
# DATABASE_URL
echo "postgresql://postgres:ВАШ_ПАРОЛЬ@db.eznumgsmwvavyunqhxfc.supabase.co:5432/postgres" | bunx vercel env add DATABASE_URL production

# NODE_ENV
echo "production" | bunx vercel env add NODE_ENV production
```

**Замените `ВАШ_ПАРОЛЬ` на ваш реальный пароль!**

---

## ✅ Готово!

После этого все будет работать!
