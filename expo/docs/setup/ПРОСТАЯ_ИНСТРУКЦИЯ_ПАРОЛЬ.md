# 🔑 Простая инструкция: Как заменить [YOUR-PASSWORD]

## 🎯 Коротко

`[YOUR-PASSWORD]` - это **заглушка**, которую нужно заменить на **реальный пароль**.

## 📋 Шаги:

### 1. Откройте Supabase Dashboard
https://supabase.com/dashboard → выберите проект

### 2. Получите пароль
**Settings** → **Database** → найдите **Database password**

Если пароль не виден:
- Нажмите **Reset database password**
- Скопируйте новый пароль (показывается только один раз!)

### 3. Получите Connection String
В том же разделе **Database** → **Connection string** → **Session mode**
Скопируйте строку (она будет с `[YOUR-PASSWORD]`)

### 4. Замените [YOUR-PASSWORD] на реальный пароль

**Было:**
```
postgresql://postgres:[YOUR-PASSWORD]@db.eznumgsmwvavyunqhxfc.supabase.co:5432/postgres
```

**Стало (пример):**
```
postgresql://postgres:ваш_реальный_пароль@db.eznumgsmwvavyunqhxfc.supabase.co:5432/postgres
```

### 5. Используйте в Vercel

```bash
cd /Users/mac/Desktop/rork-kiku/backend
bunx vercel env rm DATABASE_URL production
bunx vercel env add DATABASE_URL production
# Вставьте connection string С РЕАЛЬНЫМ ПАРОЛЕМ
bunx vercel --prod
```

## ⚠️ Важно

- ❌ **НЕ используйте** `[YOUR-PASSWORD]` как есть
- ✅ **Замените** на реальный пароль
- 💾 **Сохраните** пароль - он показывается только один раз!

## 🧪 Проверка

```bash
curl "https://backend-three-mauve-67.vercel.app/api/trpc/test.dbCheck"
```

Если `"success": true` - всё правильно! ✅
