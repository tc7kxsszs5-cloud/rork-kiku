# 📱 Запуск на симуляторах iOS и Android

## ❌ Проблема

При запуске `bun run start:simple`:
- Открывается только веб-браузер в Cursor
- При нажатии 'i' или 'a' в терминале получаете ошибку "не найдена пригодная команда"

## ✅ Решение

Добавлены прямые команды для запуска на симуляторах.

---

## 🚀 Команды для запуска

### Для iOS Simulator:

```bash
# Вариант 1: Прямой запуск (рекомендуется)
bun run ios:sim

# Вариант 2: Через expo run:ios (требует сборку)
bun run ios
```

### Для Android Emulator:

```bash
# Вариант 1: Прямой запуск (рекомендуется)
bun run android:emulator

# Вариант 2: Через expo run:android (требует сборку)
bun run android
```

### Для веб-версии:

```bash
bun run start-web
```

---

## 📋 Пошаговая инструкция

### iOS Simulator:

1. **Убедитесь, что Xcode установлен:**
   ```bash
   xcodebuild -version
   ```

2. **Запустите iOS Simulator вручную (опционально):**
   ```bash
   open -a Simulator
   ```

3. **Запустите приложение:**
   ```bash
   bun run ios:sim
   ```

   Или:
   ```bash
   bunx expo start --ios
   ```

### Android Emulator:

1. **Убедитесь, что Android Studio установлен**

2. **Запустите Android Emulator вручную:**
   - Откройте Android Studio
   - Tools → Device Manager
   - Запустите эмулятор

3. **Запустите приложение:**
   ```bash
   bun run android:emulator
   ```

   Или:
   ```bash
   bunx expo start --android
   ```

---

## 🔍 Проверка симуляторов

### Проверить доступные iOS симуляторы:

```bash
xcrun simctl list devices available | grep iPhone
```

### Запустить конкретный симулятор:

```bash
xcrun simctl boot "iPhone 14"
```

### Проверить запущенные симуляторы:

```bash
xcrun simctl list devices booted
```

---

## ⚠️ Важные замечания

### Для Expo Go (рекомендуется для разработки):

1. **Установите Expo Go на устройство:**
   - iOS: [App Store](https://apps.apple.com/app/expo-go/id982107779)
   - Android: [Google Play](https://play.google.com/store/apps/details?id=host.exp.exponent)

2. **Запустите dev сервер:**
   ```bash
   bun run start:simple
   ```

3. **Отсканируйте QR код** в Expo Go

### Для симуляторов:

- **iOS Simulator**: Работает с `expo start --ios`
- **Android Emulator**: Работает с `expo start --android`
- **Требуется**: Xcode для iOS, Android Studio для Android

---

## 🎯 Рекомендуемый способ

**Для быстрой разработки:**
1. Используйте **Expo Go на реальном устройстве**
2. Запустите `bun run start:simple`
3. Отсканируйте QR код

**Для тестирования на симуляторах:**
1. Используйте `bun run ios:sim` или `bun run android:emulator`
2. Симулятор должен быть запущен заранее

---

## 🔧 Устранение проблем

### Если симулятор не запускается:

1. **Проверьте, что симулятор запущен:**
   ```bash
   xcrun simctl list devices booted
   ```

2. **Запустите симулятор вручную:**
   ```bash
   open -a Simulator
   ```

3. **Попробуйте другой симулятор:**
   ```bash
   xcrun simctl list devices available
   ```

### Если команда не найдена:

1. **Убедитесь, что Expo CLI установлен:**
   ```bash
   bunx expo --version
   ```

2. **Переустановите зависимости:**
   ```bash
   bun install
   ```

---

## ✅ Быстрый старт

```bash
# iOS Simulator
bun run ios:sim

# Android Emulator  
bun run android:emulator

# Веб-версия
bun run start-web

# Expo Go (рекомендуется)
bun run start:simple
```

---

**Теперь симуляторы должны работать! 🚀**
