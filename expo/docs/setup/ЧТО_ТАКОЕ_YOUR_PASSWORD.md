# 🔑 Что значит "заменить [YOUR-PASSWORD] на реальный пароль"

## 📋 Объяснение

Когда вы копируете Connection String из Supabase Dashboard, он выглядит так:

```
postgresql://postgres:[YOUR-PASSWORD]@db.eznumgsmwvavyunqhxfc.supabase.co:5432/postgres
```

**`[YOUR-PASSWORD]`** - это **заглушка** (placeholder), которую Supabase вставляет вместо реального пароля для безопасности.

## ✅ Что нужно сделать

Вы должны **заменить** `[YOUR-PASSWORD]` на **реальный пароль** от вашей базы данных Supabase.

### Пример:

**Неправильно (с заглушкой):**
```
postgresql://postgres:[YOUR-PASSWORD]@db.eznumgsmwvavyunqhxfc.supabase.co:5432/postgres
```

**Правильно (с реальным паролем):**
```
postgresql://postgres:мой_пароль_123@db.eznumgsmwvavyunqhxfc.supabase.co:5432/postgres
```

## 🔍 Где найти реальный пароль?

### Вариант 1: Если вы помните пароль

Просто замените `[YOUR-PASSWORD]` на ваш пароль в connection string.

### Вариант 2: Если забыли пароль (самый частый случай)

1. Откройте https://supabase.com/dashboard
2. Выберите ваш проект
3. Перейдите в **Settings** → **Database**
4. Найдите раздел **Database password**
5. Нажмите кнопку **Reset database password** (или **Reset**)
6. Скопируйте новый пароль (он показывается только один раз!)
7. Сохраните пароль в безопасном месте
8. Используйте этот пароль в connection string

## 📝 Пошаговая инструкция

### Шаг 1: Получите Connection String

1. Supabase Dashboard → ваш проект → **Settings** → **Database**
2. Найдите **Connection string**
3. Выберите **Session mode**
4. Скопируйте строку (она будет с `[YOUR-PASSWORD]`)

### Шаг 2: Получите или сбросьте пароль

1. В том же разделе **Database** найдите **Database password**
2. Если пароль не виден, нажмите **Reset database password**
3. Скопируйте новый пароль

### Шаг 3: Замените [YOUR-PASSWORD]

**Было:**
```
postgresql://postgres:[YOUR-PASSWORD]@db.eznumgsmwvavyunqhxfc.supabase.co:5432/postgres
```

**Стало (пример с паролем "mypassword123"):**
```
postgresql://postgres:mypassword123@db.eznumgsmwvavyunqhxfc.supabase.co:5432/postgres
```

### Шаг 4: Используйте в Vercel

```bash
cd /Users/mac/Desktop/rork-kiku/backend

# Удалите старую переменную
bunx vercel env rm DATABASE_URL production

# Добавьте новую
bunx vercel env add DATABASE_URL production
# Вставьте connection string С РЕАЛЬНЫМ ПАРОЛЕМ (без [YOUR-PASSWORD])
```

## ⚠️ Важные моменты

1. **Пароль показывается только один раз** при создании/сбросе - сохраните его!
2. **Не используйте `[YOUR-PASSWORD]` как есть** - это не работает
3. **Пароль может содержать специальные символы** - если есть `@`, `:`, `/` в пароле, их нужно закодировать (URL encode)
4. **Пароль чувствителен к регистру** - `Password123` ≠ `password123`

## 🔐 Примеры правильных connection strings

### Пример 1: Простой пароль
```
postgresql://postgres:abc123@db.eznumgsmwvavyunqhxfc.supabase.co:5432/postgres
```

### Пример 2: Пароль со специальными символами
Если пароль `my@pass:word`, нужно закодировать:
```
postgresql://postgres:my%40pass%3Aword@db.eznumgsmwvavyunqhxfc.supabase.co:5432/postgres
```

Где:
- `@` → `%40`
- `:` → `%3A`

## 🧪 Как проверить, что пароль правильный?

После обновления DATABASE_URL в Vercel:

```bash
cd /Users/mac/Desktop/rork-kiku/backend
bunx vercel --prod
# Подождите 20-25 секунд
curl "https://backend-three-mauve-67.vercel.app/api/trpc/test.dbCheck"
```

Если увидите `"success": true` - пароль правильный! ✅
Если увидите `"password authentication failed"` - пароль неправильный ❌

## 📸 Визуальный пример

В Supabase Dashboard вы увидите:

```
Connection string:
postgresql://postgres:[YOUR-PASSWORD]@db.eznumgsmwvavyunqhxfc.supabase.co:5432/postgres
                    ^^^^^^^^^^^^^^^^
                    ЭТО нужно заменить на реальный пароль!
```

И ниже:

```
Database password: [Reset] [Show]
```

Нажмите **Reset** или **Show**, чтобы получить пароль, затем замените `[YOUR-PASSWORD]` на этот пароль.

---

**Коротко:** `[YOUR-PASSWORD]` - это заглушка. Замените её на реальный пароль из Supabase Dashboard → Settings → Database → Database password.
