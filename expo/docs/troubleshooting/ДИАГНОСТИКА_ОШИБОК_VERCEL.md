# 🔍 Диагностика ошибок Vercel

## ❓ Какая ошибка?

Пожалуйста, скопируйте **полный текст ошибки** из Vercel, чтобы я мог помочь.

---

## 🔧 Частые ошибки и решения:

### 1️⃣ "Cannot find module" или "Module not found"

**Ошибка:**
```
Error [ERR_MODULE_NOT_FOUND]: Cannot find module
```

**Решение:**
- Проверьте, что все файлы закоммичены
- Убедитесь, что `includeFiles` в vercel.json включает все файлы

---

### 2️⃣ "Build failed"

**Ошибка:**
```
Build failed
```

**Решение:**
- Проверьте логи в Vercel (View Build Logs)
- Убедитесь, что Root Directory = `backend`
- Проверьте, что Install Command = `cd backend && bun install`

---

### 3️⃣ "Function failed" или "Runtime error"

**Ошибка:**
```
Function failed
```

**Решение:**
- Проверьте логи функции (View Function Logs)
- Убедитесь, что Environment Variables настроены
- Проверьте, что DATABASE_URL правильный

---

### 4️⃣ "Invalid configuration"

**Ошибка:**
```
Invalid configuration
```

**Решение:**
- Проверьте формат vercel.json
- Убедитесь, что все поля правильные

---

### 5️⃣ "TypeScript compilation error"

**Ошибка:**
```
TypeScript compilation error
```

**Решение:**
- Проверьте tsconfig.json
- Убедитесь, что все типы правильные

---

## 📋 Что проверить:

1. ✅ **Root Directory в Vercel:**
   - Settings → General
   - Root Directory должен быть: `backend`

2. ✅ **Build Command:**
   - Должен быть пустым или: `cd backend && bun install`

3. ✅ **Output Directory:**
   - Должен быть пустым

4. ✅ **Framework Preset:**
   - Должен быть: `Other`

5. ✅ **Environment Variables:**
   - DATABASE_URL должен быть добавлен
   - NODE_ENV должен быть `production`

---

## 🆘 Если не знаете, какая ошибка:

1. Идите в Vercel Dashboard
2. Deployments → выберите последний deployment
3. View Build Logs (для ошибок сборки)
4. View Function Logs (для ошибок выполнения)
5. Скопируйте полный текст ошибки

---

## 📝 Скопируйте ошибку

Пожалуйста, скопируйте **полный текст ошибки** из:
- Build Logs
- Function Logs
- Или из сообщения об ошибке

И я помогу исправить!
