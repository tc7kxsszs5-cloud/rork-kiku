# ✅ Решение: Используйте прямое подключение

## ❌ Проблема:

Connection Pooling не работает (ошибка "Tenant or user not found")

## ✅ Решение:

Используйте **прямой connection string** вместо pooling.

## 📋 Прямой Connection String:

```
postgresql://postgres:gerkom-tYbpek-2cochi@db.eznumgsmwvavyunqhxfc.supabase.co:5432/postgres
```

**Что изменилось:**
- Убрали `.eznumgsmwvavyunqhxfc` из username (для прямого подключения не нужно)
- Изменили хост: `aws-0-us-east-1.pooler.supabase.com:6543` → `db.eznumgsmwvavyunqhxfc.supabase.co:5432`
- Порт: `6543` → `5432`

## 📋 Команды:

```bash
cd /Users/mac/Desktop/rork-kiku/backend
bunx vercel env rm DATABASE_URL production
```

**Ответьте:** `y`

```bash
bunx vercel env add DATABASE_URL production
```

**Ответьте:**
- `Mark as sensitive?` → **yes**
- `What's the value of DATABASE_URL?` → Вставьте:
  ```
  postgresql://postgres:gerkom-tYbpek-2cochi@db.eznumgsmwvavyunqhxfc.supabase.co:5432/postgres
  ```

```bash
bunx vercel --prod
```

```bash
curl "https://backend-three-mauve-67.vercel.app/api/trpc/test.dbCheck"
```

---

## 💡 Почему это должно работать:

Прямое подключение часто работает, когда Connection Pooling не работает из-за:
- Неправильного региона
- Настроек проекта
- Ограничений Vercel

---

**Попробуйте прямой connection string!**
