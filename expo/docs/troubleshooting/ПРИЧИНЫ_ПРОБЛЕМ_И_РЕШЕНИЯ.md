# Причины проблем запуска и решения

## 🔍 Найденные причины проблем

### 1. Проблема: Порт занят

**Причина:**
- Другой процесс использует порт 8081
- Expo не может запуститься в неинтерактивном режиме

**Решение:**
- ✅ Добавлен флаг `--port 8082` в команду запуска
- ✅ Создан скрипт `kill-ports.sh` для освобождения портов
- ✅ Автоматическое освобождение портов в `quick-fix.sh`

### 2. Проблема: Ошибки инициализации блокируют запуск

**Причина:**
- `initializeTestCustomEmojis()` может упасть с ошибкой
- Ошибка блокирует установку `isReady = true`
- Приложение остается в состоянии загрузки

**Решение:**
- ✅ Добавлена обработка ошибок с `.catch()`
- ✅ Инициализация не блокирует запуск
- ✅ Ошибки логируются, но не останавливают приложение

### 3. Проблема: Отсутствие обработки ошибок в компонентах

**Причина:**
- `useThemeMode()` может вернуть undefined
- Компоненты падают при обращении к `theme.property`
- Нет fallback значений

**Решение:**
- ✅ Добавлена обработка ошибок во всех экранах
- ✅ Fallback значения для темы
- ✅ Проверки на undefined перед использованием

### 4. Проблема: Неправильный порядок провайдеров

**Причина:**
- `AuthProvider` использует `UserContext`
- Но может быть размещен до `UserProvider`
- → Ошибка "Context is not defined"

**Решение:**
- ✅ Правильный порядок провайдеров в `_layout.tsx`
- ✅ Тесты проверяют порядок
- ✅ Документация порядка провайдеров

### 5. Проблема: Отсутствующие файлы

**Причина:**
- Вкладки объявлены в `_layout.tsx`, но файлы отсутствуют
- → Ошибка при навигации

**Решение:**
- ✅ Созданы все недостающие файлы (achievements, lessons)
- ✅ Тесты проверяют наличие файлов
- ✅ Автоматическое создание при необходимости

## 🛠️ Комплексная диагностика

### Запуск диагностики:

```bash
bun run diagnose
```

**Что проверяет:**
1. ✅ Критические файлы инициализации
2. ✅ Импорты в _layout.tsx
3. ✅ Зависимости контекстов
4. ✅ Асинхронная инициализация
5. ✅ Платформо-специфичный код
6. ✅ Состояние готовности
7. ✅ ErrorBoundary
8. ✅ Экспорты

## 📋 Чеклист для предотвращения проблем

### Перед запуском:
- [ ] Запустить `bun run diagnose`
- [ ] Проверить тесты: `bun run test:all-config`
- [ ] Освободить порты: `bun run kill-ports`

### При разработке:
- [ ] Всегда обрабатывать ошибки
- [ ] Проверять на undefined
- [ ] Использовать fallback значения
- [ ] Тестировать изменения
- [ ] Проверять порядок провайдеров

### При добавлении нового функционала:
- [ ] Проверить зависимости
- [ ] Добавить обработку ошибок
- [ ] Обновить тесты
- [ ] Проверить на всех платформах

## 🎯 Ключевые принципы

### 1. Защита от ошибок
```typescript
// ❌ Плохо
const { theme } = useThemeMode();
const color = theme.textPrimary;

// ✅ Хорошо
let theme;
try {
  theme = useThemeMode().theme;
} catch (error) {
  theme = fallbackTheme;
}
const color = theme?.textPrimary || '#000';
```

### 2. Не блокировать запуск
```typescript
// ❌ Плохо
await initializeSomething(); // Может упасть
setIsReady(true);

// ✅ Хорошо
initializeSomething().catch(err => console.warn(err));
setIsReady(true); // Не ждем
```

### 3. Проверка зависимостей
```typescript
// ❌ Плохо
const data = useContext(); // Может быть undefined

// ✅ Хорошо
const context = useContext();
if (!context) return <FallbackUI />;
const data = context.data;
```

## 🚀 Правильный запуск

```bash
# 1. Диагностика
bun run diagnose

# 2. Освобождение портов
bun run kill-ports

# 3. Запуск
bun run start:web
```

## ✅ Результат

После всех исправлений:
- ✅ Приложение запускается без ошибок
- ✅ Все компоненты защищены от крашей
- ✅ Есть диагностика проблем
- ✅ Есть автоматическое исправление
