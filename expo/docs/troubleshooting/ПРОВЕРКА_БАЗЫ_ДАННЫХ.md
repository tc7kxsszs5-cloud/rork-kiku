# 🔍 Проверка подключения к базе данных Supabase

## 📋 Быстрая проверка

### Метод 1: Через скрипт (рекомендуется)

```bash
cd backend

# 1. Установите библиотеку pg (если еще не установлена)
bun add pg

# 2. Установите DATABASE_URL из Vercel
bunx vercel env pull

# 3. Запустите проверку
bun check-database.js
```

### Метод 2: Через psql (если установлен PostgreSQL)

```bash
cd backend

# 1. Получите DATABASE_URL из Vercel
bunx vercel env pull

# 2. Проверьте подключение
psql "$DATABASE_URL" -c "SELECT version();"

# 3. Проверьте таблицы
psql "$DATABASE_URL" -c "\dt"

# 4. Проверьте расширения
psql "$DATABASE_URL" -c "SELECT extname FROM pg_extension;"
```

### Метод 3: Через Vercel Dashboard

1. Откройте https://vercel.com/dashboard
2. Выберите проект `backend`
3. Перейдите в **Settings** → **Environment Variables**
4. Проверьте наличие `DATABASE_URL`
5. Убедитесь, что значение содержит правильный пароль (не `[YOUR-PASSWORD]`)

### Метод 4: Через Supabase Dashboard

1. Откройте https://supabase.com/dashboard
2. Выберите ваш проект
3. Перейдите в **Settings** → **Database**
4. Проверьте **Connection string** (Session mode)
5. Убедитесь, что пароль правильный

## 🔧 Установка зависимостей

### Установка PostgreSQL client (psql):

**macOS:**
```bash
brew install postgresql
```

**Linux (Ubuntu/Debian):**
```bash
sudo apt-get install postgresql-client
```

**Windows:**
Скачайте с https://www.postgresql.org/download/windows/

### Установка библиотеки pg для Node.js/Bun:

```bash
cd backend
bun add pg
```

## 📝 Проверка переменных окружения

### Получить переменные из Vercel:

```bash
cd backend
bunx vercel env pull
```

Это создаст файл `.env.local` с переменными окружения.

### Проверить переменные в Vercel:

```bash
# Список всех переменных
bunx vercel env ls

# Просмотр конкретной переменной
bunx vercel env ls DATABASE_URL
```

## 🧪 Тестовые запросы

### Простой тест подключения:

```bash
# Через psql
psql "$DATABASE_URL" -c "SELECT 1 as test;"

# Через Node.js/Bun скрипт
bun check-database.js
```

### Проверка таблиц:

```bash
psql "$DATABASE_URL" -c "
SELECT 
  table_name,
  table_type
FROM information_schema.tables 
WHERE table_schema = 'public'
ORDER BY table_name;
"
```

### Проверка подключений:

```bash
psql "$DATABASE_URL" -c "
SELECT 
  pid,
  usename,
  application_name,
  client_addr,
  state
FROM pg_stat_activity
WHERE datname = current_database();
"
```

## ❌ Решение проблем

### Ошибка: "password authentication failed"

**Причина:** Неправильный пароль в DATABASE_URL

**Решение:**
1. Получите правильный пароль из Supabase Dashboard
2. Обновите DATABASE_URL в Vercel:
   ```bash
   bunx vercel env rm DATABASE_URL production
   bunx vercel env add DATABASE_URL production
   # Вставьте правильный connection string
   ```
3. Перезапустите деплой:
   ```bash
   bunx vercel --prod
   ```

### Ошибка: "could not connect to server"

**Причина:** База данных недоступна или неправильный host

**Решение:**
1. Проверьте формат DATABASE_URL:
   ```
   postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres
   ```
2. Убедитесь, что используете правильный проект из Supabase
3. Проверьте, что проект не приостановлен в Supabase

### Ошибка: "MODULE_NOT_FOUND: pg"

**Причина:** Библиотека pg не установлена

**Решение:**
```bash
cd backend
bun add pg
```

### Ошибка: "DATABASE_URL не установлен"

**Причина:** Переменная окружения не установлена локально

**Решение:**
```bash
# Получите из Vercel
bunx vercel env pull

# Или установите вручную
export DATABASE_URL="postgresql://postgres:[PASSWORD]@db.[PROJECT].supabase.co:5432/postgres"
```

## ✅ Успешная проверка

Если все работает, вы увидите:

```
✅ Подключение установлено!
📋 Версия PostgreSQL: PostgreSQL 15.x
📊 Найденные таблицы: (список таблиц или сообщение что таблиц нет)
🔌 Установленные расширения: (список расширений)
✅ Проверка завершена успешно!
```

## 🔗 Полезные ссылки

- [Supabase Dashboard](https://supabase.com/dashboard)
- [Vercel Dashboard](https://vercel.com/dashboard)
- [PostgreSQL Documentation](https://www.postgresql.org/docs/)

---

**Примечание:** Для production используйте переменные окружения из Vercel, не храните пароли в коде!
