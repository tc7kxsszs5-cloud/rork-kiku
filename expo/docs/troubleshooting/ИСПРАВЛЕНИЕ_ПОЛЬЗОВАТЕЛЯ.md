# 🔧 Исправление: Используйте простого пользователя `postgres`

## ❌ Текущая проблема

Ошибка: `Tenant or user not found`

Используется: `postgres.eznumgsmwvavyunqhxfc` (не работает)

## ✅ Решение: Используйте просто `postgres`

### Шаг 1: Удалите старую переменную

```bash
cd /Users/mac/Desktop/rork-kiku/backend
bunx vercel env rm DATABASE_URL production
```

### Шаг 2: Добавьте новую с простым пользователем

```bash
bunx vercel env add DATABASE_URL production
```

**Ответьте:** `yes`

**Вставьте (замените `[ПАРОЛЬ]` на реальный пароль):**

```
postgresql://postgres:[ПАРОЛЬ]@aws-0-us-east-1.pooler.supabase.com:6543/postgres
```

**Обратите внимание:** используйте просто `postgres` (БЕЗ `.eznumgsmwvavyunqhxfc`)

### Шаг 3: Перезапустите

```bash
bunx vercel --prod
```

### Шаг 4: Проверьте

```bash
sleep 25
curl "https://backend-three-mauve-67.vercel.app/api/trpc/test.dbCheck"
```

## 📋 Примеры

### Если пароль `abc123`:
```
postgresql://postgres:abc123@aws-0-us-east-1.pooler.supabase.com:6543/postgres
```

### Если пароль `MyPass456`:
```
postgresql://postgres:MyPass456@aws-0-us-east-1.pooler.supabase.com:6543/postgres
```

## 🔍 Разница

**Неправильно (с project ref в пользователе):**
```
postgresql://postgres.eznumgsmwvavyunqhxfc:[ПАРОЛЬ]@...
                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^
                Это не работает!
```

**Правильно (простой пользователь):**
```
postgresql://postgres:[ПАРОЛЬ]@...
                ^^^^^^^
                Просто postgres!
```

---

**Используйте просто `postgres` как пользователя, без project ref!**
