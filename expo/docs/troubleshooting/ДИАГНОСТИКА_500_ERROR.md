# 🔍 Диагностика ошибки 500 INTERNAL_SERVER_ERROR

## ❌ Ошибка

```
500: INTERNAL_SERVER_ERROR
Code: FUNCTION_INVOCATION_FAILED
```

## ✅ Причины

Возможные причины:
1. **Проблемы с подключением к базе данных**
2. **Ошибки в коде при выполнении**
3. **Отсутствие зависимостей**
4. **Проблемы с импортами**

---

## 📋 Шаг 1: Проверьте Function Logs

### Через Vercel Dashboard:

1. Откройте https://vercel.com/dashboard
2. Выберите проект **backend**
3. **Deployments** → выберите последний деплой
4. Нажмите **"Function Logs"** или **"View Function Logs"**
5. Найдите строку с ошибкой
6. Скопируйте текст ошибки

### Через CLI:

```bash
cd backend
bunx vercel logs --follow
```

---

## 📋 Шаг 2: Проверьте переменные окружения

Убедитесь, что переменные правильно настроены:

```bash
bunx vercel env ls
```

Должны быть:
- ✅ `DATABASE_URL` - Connection String к Supabase
- ✅ `NODE_ENV` = `production`

---

## 📋 Шаг 3: Проверьте подключение к базе

Возможные проблемы:
1. **Неправильный Connection String** - проверьте пароль
2. **База данных не доступна** - проверьте настройки Supabase
3. **IPv4/IPv6 проблемы** - используйте Connection Pooling (порт 6543)

---

## 🔧 Быстрое исправление

### Если проблема с базой данных:

Попробуйте использовать Connection Pooling:

```bash
# Удалите старую переменную
bunx vercel env rm DATABASE_URL production

# Добавьте новую с портом 6543 (pooling)
echo "postgresql://postgres.xxxxx:ВАШ_ПАРОЛЬ@aws-0-eu-central-1.pooler.supabase.com:6543/postgres" | bunx vercel env add DATABASE_URL production
```

**Или найдите Connection String с портом 6543 в Supabase Dashboard.**

---

## 📋 Шаг 4: Проверьте код

Убедитесь, что в `backend/index.ts` и `backend/hono.ts` нет ошибок:

```bash
cd backend
bunx tsc --noEmit
```

---

## ✅ После исправления

После исправления передеплойте:

```bash
bunx vercel --prod
```

---

## 🆘 Если не можете найти причину

Пришлите мне:
1. Текст ошибки из Function Logs
2. Результат `bunx vercel env ls`
3. Что показывает `bunx tsc --noEmit`

---

## ✅ Готово!

Проверьте логи и найдите причину ошибки!
