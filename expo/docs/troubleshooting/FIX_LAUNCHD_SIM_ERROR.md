# 🔧 Исправление ошибки launchd_sim

## Проблема

При запуске iOS симулятора возникает ошибка:
```
Error: xcrun simctl boot <UUID> exited with non-zero code: 60
An error was encountered processing the command (domain=NSPOSIXErrorDomain, code=60):
Unable to boot the Simulator.
launchd failed to respond.
Underlying error (domain=com.apple.SimLaunchHostService.RequestError, code=4):
	Failed to start launchd_sim: could not bind to session, launchd_sim may have crashed or quit responding
```

## Причина

Эта ошибка возникает когда:
- Процесс `launchd_sim` завис или упал
- Симулятор находится в нестабильном состоянии
- Конфликт между процессами симулятора
- Проблемы с правами доступа

## Решения

### ✅ Решение 1: Автоматическое исправление (РЕКОМЕНДУЕТСЯ)

Используйте скрипт для автоматического исправления:

```bash
bun run ios:sim:fix
```

Этот скрипт:
1. Закрывает все зависшие процессы симулятора
2. Выключает все симуляторы
3. Очищает зависшие процессы `launchd_sim`
4. Запускает симулятор вручную
5. Ждёт полной загрузки

После исправления запустите проект:
```bash
bun run ios:sim:safe
# или
bun run start
# затем нажмите 'i' в терминале
```

### ✅ Решение 2: Безопасный запуск (обход проблемы)

Используйте безопасный скрипт, который открывает симулятор вручную:

```bash
bun run ios:sim:safe
```

Этот скрипт:
- Открывает симулятор вручную (не через `simctl boot`)
- Запускает Expo без флага `--ios`
- Вы избегаете ошибки, так как Expo не пытается загрузить симулятор

Когда появится меню Expo, нажмите **`i`** для запуска в iOS симуляторе.

### ✅ Решение 3: Ручное исправление

Если автоматические скрипты не помогли:

1. **Закройте все процессы симулятора:**
   ```bash
   killall Simulator
   killall com.apple.CoreSimulator.CoreSimulatorService
   pkill -9 -f launchd_sim
   ```

2. **Выключите все симуляторы:**
   ```bash
   xcrun simctl shutdown all
   ```

3. **Подождите 2-3 секунды**

4. **Откройте симулятор вручную:**
   ```bash
   open -a Simulator
   ```

5. **Подождите 15-20 секунд**, пока симулятор полностью загрузится

6. **Выберите устройство** в меню Simulator: `File → Open Simulator → iPhone 14`

7. **Запустите проект:**
   ```bash
   bun run start
   # затем нажмите 'i' в терминале
   ```

### ✅ Решение 4: Очистка кеша симулятора (если ничего не помогает)

⚠️ **Внимание:** Это удалит все данные симуляторов (приложения, настройки и т.д.)

```bash
# Закройте все процессы
killall Simulator
killall com.apple.CoreSimulator.CoreSimulatorService

# Очистите кеш (ОПЦИОНАЛЬНО, только если другие методы не помогли)
rm -rf ~/Library/Developer/CoreSimulator/Caches/*

# Перезапустите симулятор
open -a Simulator
```

### ✅ Решение 5: Перезагрузка Mac

Если ничего не помогает, перезагрузите Mac. Это очистит все зависшие процессы и восстановит состояние системы.

## Профилактика

Чтобы избежать этой проблемы в будущем:

1. **Всегда используйте безопасный запуск:**
   ```bash
   bun run ios:sim:safe
   ```

2. **Перед запуском проверяйте, что симулятор закрыт:**
   ```bash
   killall Simulator 2>/dev/null || true
   ```

3. **Не используйте `bun run ios:sim` напрямую**, если возникают проблемы

## Команды для быстрого доступа

```bash
# Исправить проблему
bun run ios:sim:fix

# Безопасный запуск (рекомендуется)
bun run ios:sim:safe

# Обычный запуск (может вызвать ошибку)
bun run ios:sim
```

## Дополнительная информация

- Скрипт исправления: `./scripts/fix-simulator.sh`
- Безопасный скрипт: `./scripts/ios-sim-safe.sh`
- Документация по симулятору: `docs/setup/RUN_IOS_SIMULATOR.md`
