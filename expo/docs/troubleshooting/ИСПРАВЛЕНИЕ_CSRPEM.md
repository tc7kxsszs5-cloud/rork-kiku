# 🔧 Исправление ошибки "csrPEM: Required"

## ❌ Проблема

При запуске Metro Bundler получаете ошибку:
```
ApiV2Error: "csrPEM": Required.
```

## 🔍 Причина

Эта ошибка возникает когда:
1. Expo CLI не может создать сертификат для подключения
2. Проблема с кэшем Expo
3. Конфликт между `rork start` и обычным `expo start`
4. Проблема с конфигурацией проекта

---

## ✅ Решения

### Решение 1: Очистка кэша (рекомендуется)

```bash
# Очистить кэш Expo
rm -rf .expo
rm -rf node_modules/.cache

# Перезапустить
bun run start:simple
```

### Решение 2: Использовать обычный Expo вместо Rork

Если используете команду с `rork start`, попробуйте обычный `expo start`:

```bash
# Вместо
bun run start  # (использует rork)

# Используйте
bun run start:simple  # (использует expo)
```

### Решение 3: Переустановка зависимостей

```bash
# Удалить node_modules и переустановить
rm -rf node_modules
bun install

# Очистить кэш
rm -rf .expo node_modules/.cache

# Запустить снова
bun run start:simple
```

### Решение 4: Использовать локальный запуск без сертификатов

```bash
# Локальный запуск без сертификатов
EXPO_NO_DOTENV=1 bunx expo start --lan --no-dev --minify
```

---

## 🚀 Быстрое исправление

```bash
# 1. Остановите текущий процесс (Ctrl+C)

# 2. Очистите кэш
rm -rf .expo node_modules/.cache

# 3. Запустите снова
bun run start:simple
```

---

## 🔍 Проверка

После исправления проверьте:

1. **Запуск без ошибок:**
   ```bash
   bun run start:simple
   ```

2. **Должен появиться QR код** без ошибок

3. **Можно подключиться** через Expo Go или симулятор

---

## ⚠️ Если проблема сохраняется

### Вариант 1: Использовать Expo Go напрямую

```bash
# Запустить без сертификатов
bunx expo start --lan --no-dev
```

### Вариант 2: Проверить версию Expo

```bash
bunx expo --version
# Должна быть актуальная версия
```

### Вариант 3: Обновить Expo CLI

```bash
bunx expo install --fix
```

---

## 📝 Дополнительная информация

**csrPEM** - это Certificate Signing Request, который Expo использует для безопасного подключения.

**Причины ошибки:**
- Проблема с сетью
- Конфликт версий Expo
- Поврежденный кэш
- Проблема с Rork платформой

**Решение:**
- Использовать стандартный `expo start` вместо `rork start`
- Очистить кэш
- Использовать локальный запуск (`--lan`)

---

**После очистки кэша проблема должна решиться! 🚀**
