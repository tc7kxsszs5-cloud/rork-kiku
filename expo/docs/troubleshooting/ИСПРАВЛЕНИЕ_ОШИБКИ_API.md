# 🔧 Исправление ошибки API

## ❌ Ошибка

```
A server error has occurred
FUNCTION_INVOCATION_FAILED
```

## ✅ Причина

Скорее всего, не хватает переменных окружения или есть ошибка в коде.

---

## 📋 Решение

### Шаг 1: Добавьте Environment Variables

```bash
cd backend

# DATABASE_URL с вашим паролем
echo "postgresql://postgres:ВАШ_ПАРОЛЬ@db.eznumgsmwvavyunqhxfc.supabase.co:5432/postgres" | bunx vercel env add DATABASE_URL production

# NODE_ENV
echo "production" | bunx vercel env add NODE_ENV production
```

**Замените `ВАШ_ПАРОЛЬ` на ваш реальный пароль!**

---

### Шаг 2: Проверьте Function Logs

1. Откройте Vercel Dashboard
2. Выберите проект
3. **Deployments** → выберите последний деплой
4. Нажмите **"Function Logs"** или **"View Function Logs"**
5. Посмотрите, какая именно ошибка

---

### Шаг 3: Передеплойте

После добавления переменных:

```bash
bunx vercel --prod
```

---

## 🔍 Возможные причины ошибки

1. **Отсутствуют переменные окружения** - добавьте DATABASE_URL и NODE_ENV
2. **Ошибка в коде** - проверьте Function Logs
3. **Проблемы с импортами** - проверьте, что все модули правильно импортированы

---

## ✅ После исправления

После добавления переменных и передеплоя проверьте:

```bash
curl https://backend-three-mauve-67.vercel.app/
```

Должно показать:
```json
{"status": "ok", "message": "API is running"}
```

---

## 📋 Проверка логов

Если ошибка сохраняется, проверьте логи:

1. Vercel Dashboard → Deployments → последний деплой
2. Function Logs
3. Найдите строку с ошибкой
4. Сообщите мне, что там написано

---

## ✅ Готово!

Добавьте переменные и передеплойте - это должно исправить ошибку!
