# ⚡ Простое решение ошибки "ENOTFOUND base"

## ❌ Проблема

Ошибка `getaddrinfo ENOTFOUND base` означает, что connection string неправильно сформирован.

## ✅ Быстрое решение

### Вариант 1: Если пароль простой (только буквы и цифры)

Убедитесь, что connection string выглядит **точно так**:

```
postgresql://postgres:ВАШ_ПАРОЛЬ@db.eznumgsmwvavyunqhxfc.supabase.co:5432/postgres
```

**Проверьте:**
- Нет лишних пробелов
- Нет лишних символов
- Пароль идет сразу после `postgres:` без пробелов

### Вариант 2: Если пароль содержит специальные символы

Закодируйте специальные символы:

**Символы для кодирования:**
- `@` → `%40`
- `:` → `%3A`
- `/` → `%2F`
- `#` → `%23`
- `%` → `%25`
- `&` → `%26`
- `?` → `%3F`
- `=` → `%3D`
- Пробел → `%20`

**Пример:**
Пароль `pass@123` → `pass%40123`

## 📋 Команды для исправления

```bash
cd /Users/mac/Desktop/rork-kiku/backend

# Удалите старую
bunx vercel env rm DATABASE_URL production

# Добавьте новую
bunx vercel env add DATABASE_URL production
# Вставьте: postgresql://postgres:ВАШ_ПАРОЛЬ@db.eznumgsmwvavyunqhxfc.supabase.co:5432/postgres
# (замените ВАШ_ПАРОЛЬ на реальный пароль, закодируйте спецсимволы если есть)

# Перезапустите
bunx vercel --prod

# Проверьте
sleep 25
curl "https://backend-three-mauve-67.vercel.app/api/trpc/test.dbCheck"
```

## 🔍 Проверка формата

Правильный формат должен быть:
```
postgresql://postgres:[ПАРОЛЬ]@db.eznumgsmwvavyunqhxfc.supabase.co:5432/postgres
```

**Все части должны быть на месте:**
- ✅ `postgresql://` - протокол
- ✅ `postgres` - пользователь
- ✅ `:` - разделитель
- ✅ `[ПАРОЛЬ]` - пароль (без пробелов)
- ✅ `@` - разделитель
- ✅ `db.eznumgsmwvavyunqhxfc.supabase.co` - хост
- ✅ `:5432` - порт
- ✅ `/postgres` - база данных

---

**Проверьте connection string на правильность формата и специальные символы в пароле!**
