# Диагностика проблем

## 🔍 Как проверить, что не работает

### 1. Проверка консоли браузера

Откройте консоль браузера (F12) и проверьте:

**Критические ошибки:**
- `Cannot read property 'theme' of undefined`
- `Provider is missing`
- `Context is not defined`
- `Module not found`

**Если видите ошибки:**
1. Скопируйте текст ошибки
2. Проверьте, какой файл вызывает ошибку
3. Используйте инструкции ниже для исправления

### 2. Проверка запуска

```bash
# Остановите текущий процесс (Ctrl+C)
# Очистите кэш и запустите заново
bun run start:clear
```

### 3. Проверка тестов

```bash
# Запустите тесты
bun run test:all-config

# Если тесты проходят, но приложение не работает:
# - Проверьте консоль браузера
# - Проверьте, что все провайдеры правильно обернуты
```

## 🐛 Частые проблемы и решения

### Проблема: "Cannot read property 'theme' of undefined"

**Причина:** `useThemeMode()` возвращает undefined или ошибку

**Решение:** Уже исправлено - добавлена обработка ошибок во всех экранах

### Проблема: Приложение не открывается

**Проверьте:**
1. Порт не занят: `lsof -ti:8081 | xargs kill -9`
2. Зависимости установлены: `bun install`
3. Кэш очищен: `bun run start:clear`

### Проблема: Белый экран

**Причина:** Ошибка в одном из компонентов

**Решение:**
1. Откройте консоль браузера (F12)
2. Найдите ошибку
3. Проверьте ErrorBoundary - он должен показать сообщение об ошибке

### Проблема: Вкладки не переключаются

**Проверьте:**
1. Все файлы вкладок существуют
2. Роутинг правильно настроен в `(tabs)/_layout.tsx`
3. Нет ошибок в консоли

## 🔧 Быстрое исправление

Если приложение не запускается:

```bash
# 1. Остановите все процессы
# Ctrl+C в терминале

# 2. Очистите кэш
bun run start:clear

# 3. Проверьте тесты
bun run test:all-config

# 4. Запустите заново
bun run start:web
```

## 📝 Логи для диагностики

Если проблема не решается, соберите информацию:

1. **Вывод тестов:**
   ```bash
   bun run test:all-config > test-output.txt
   ```

2. **Ошибки из консоли браузера:**
   - Откройте F12
   - Скопируйте все ошибки

3. **Вывод запуска:**
   ```bash
   bun run start:web 2>&1 | tee startup-output.txt
   ```

## ✅ Проверочный список

- [ ] Тесты проходят: `bun run test:all-config`
- [ ] Нет ошибок в консоли браузера
- [ ] Все файлы вкладок существуют
- [ ] Все провайдеры правильно обернуты
- [ ] Зависимости установлены: `bun install`
- [ ] Кэш очищен: `bun run start:clear`

## 🆘 Если ничего не помогает

1. Проверьте версию Node.js/Bun: `bun --version`
2. Переустановите зависимости: `rm -rf node_modules && bun install`
3. Очистите все кэши: `bun run start:clear`
4. Проверьте, что порт свободен
