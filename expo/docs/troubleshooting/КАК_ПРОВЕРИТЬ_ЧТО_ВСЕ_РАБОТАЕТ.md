# ✅ Как проверить, что все работает

## 🔍 Проверка работы - пошаговая инструкция

---

## 📋 ШАГ 1: Проверка Backend API

### 1.1. Откройте URL вашего API

1. Идите в Vercel Dashboard
2. Найдите ваш проект
3. Скопируйте URL (например: `https://kiku-backend.vercel.app`)
4. Откройте этот URL в браузере

### 1.2. Что должно вернуться:

**✅ Правильный ответ:**
```json
{
  "status": "ok",
  "message": "API is running"
}
```

**❌ Если ошибка:**
- `404 Not Found` - проблема с маршрутизацией
- `500 Internal Server Error` - проблема с кодом
- `Cannot connect` - проблема с деплоем

---

## 📋 ШАГ 2: Проверка tRPC endpoint

### 2.1. Откройте tRPC endpoint

Откройте в браузере:
```
https://your-backend.vercel.app/api/trpc/example.hi
```

### 2.2. Что должно вернуться:

**✅ Правильный ответ:**
- JSON ответ от tRPC
- Или HTML страница с данными

**❌ Если ошибка:**
- `404` - проблема с маршрутизацией tRPC
- `500` - проблема с кодом tRPC

---

## 📋 ШАГ 3: Проверка логов в Vercel

### 3.1. Откройте логи

1. Vercel Dashboard → ваш проект
2. Deployments → выберите последний deployment
3. View Function Logs

### 3.2. Что искать:

**✅ Хорошие логи:**
- Нет ошибок
- Видны запросы к API
- Все работает

**❌ Плохие логи:**
- `Error: Cannot find module` - проблема с зависимостями
- `Error: Database connection failed` - проблема с DATABASE_URL
- `Error: ...` - другие ошибки

---

## 📋 ШАГ 4: Проверка Environment Variables

### 4.1. Проверьте переменные

1. Vercel Dashboard → ваш проект
2. Settings → Environment Variables
3. Убедитесь, что есть:
   - ✅ `DATABASE_URL` - должен быть заполнен
   - ✅ `NODE_ENV` - должен быть `production`

### 4.2. Проверьте формат DATABASE_URL

Должен выглядеть так:
```
postgresql://postgres:ПАРОЛЬ@db.xxxxx.supabase.co:5432/postgres
```

**Важно:**
- ✅ Пароль должен быть заменен (не `[YOUR-PASSWORD]`)
- ✅ Формат правильный
- ✅ Нет лишних пробелов

---

## 📋 ШАГ 5: Проверка Frontend

### 5.1. Проверьте lib/trpc.ts

Откройте файл `lib/trpc.ts` и проверьте:

```typescript
const PROJECT_BASE_URL = 'https://your-backend.vercel.app';
```

**Важно:**
- ✅ URL правильный (ваш реальный URL из Vercel)
- ✅ Нет старого URL (`rork.live`)

### 5.2. Запустите Frontend

```bash
bun run start:simple
```

Проверьте, что приложение запускается без ошибок.

---

## 🔧 Если что-то не работает

### Проблема 1: 404 Not Found

**Решение:**
1. Проверьте Root Directory в Vercel (должен быть `backend`)
2. Проверьте vercel.json
3. Передеплойте проект

### Проблема 2: 500 Internal Server Error

**Решение:**
1. Проверьте логи в Vercel (View Function Logs)
2. Проверьте Environment Variables
3. Проверьте DATABASE_URL (правильный ли формат)

### Проблема 3: Cannot find module

**Решение:**
1. Проверьте, что все файлы закоммичены
2. Проверьте package.json
3. Передеплойте проект

### Проблема 4: Database connection failed

**Решение:**
1. Проверьте DATABASE_URL в Environment Variables
2. Убедитесь, что пароль заменен
3. Проверьте формат connection string

---

## ✅ Чеклист проверки

- [ ] Backend API возвращает `{"status":"ok"}`
- [ ] tRPC endpoint работает
- [ ] Нет ошибок в логах Vercel
- [ ] Environment Variables настроены
- [ ] DATABASE_URL правильный (с замененным паролем)
- [ ] Frontend обновлен (PROJECT_BASE_URL)
- [ ] Frontend запускается без ошибок

---

## 🆘 Если все еще не работает

**Напишите:**
1. Какой URL вы открываете?
2. Что возвращается (какая ошибка)?
3. Что показывают логи в Vercel?

И я помогу исправить!
