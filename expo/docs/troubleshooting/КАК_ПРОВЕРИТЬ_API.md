# 📋 Как проверить работу API

## ✅ Способ 1: Через браузер (самый простой)

### Откройте в браузере:

https://backend-three-mauve-67.vercel.app/

**Должно показать:**
```json
{"status": "ok", "message": "API is running"}
```

---

## ✅ Способ 2: Через curl (в терминале)

```bash
curl https://backend-three-mauve-67.vercel.app/
```

**Должно показать:**
```json
{"status": "ok", "message": "API is running"}
```

---

## ✅ Способ 3: Через Postman или Insomnia

1. Откройте Postman или Insomnia
2. Создайте новый GET запрос
3. Введите URL: `https://backend-three-mauve-67.vercel.app/`
4. Нажмите Send
5. Должен вернуться JSON с `{"status": "ok", "message": "API is running"}`

---

## ✅ Способ 4: Проверка tRPC endpoint

### Проверьте tRPC endpoint:

```bash
curl https://backend-three-mauve-67.vercel.app/api/trpc/example.hi
```

Или в браузере:
https://backend-three-mauve-67.vercel.app/api/trpc/example.hi

---

## ❌ Если видите ошибку

### Ошибка 500:

1. Проверьте Function Logs в Vercel Dashboard
2. Найдите строку с ошибкой
3. Скопируйте текст ошибки

### Ошибка 404:

- Проверьте, что URL правильный
- Убедитесь, что деплой завершен успешно

---

## ✅ Что должно работать

После успешного деплоя:

1. ✅ **Корневой endpoint** (`/`) должен возвращать:
   ```json
   {"status": "ok", "message": "API is running"}
   ```

2. ✅ **tRPC endpoints** (`/api/trpc/*`) должны работать

3. ✅ **CORS** должен быть настроен правильно

---

## 📋 Проверка через Vercel Dashboard

1. Откройте https://vercel.com/dashboard
2. Выберите проект **backend**
3. **Deployments** → выберите последний деплой
4. Проверьте статус - должно быть **Ready**
5. Нажмите на URL деплоя - откроется в браузере

---

## ✅ Готово!

Используйте любой из способов для проверки API!
