# 🔧 Исправление ошибки ngrok tunnel

## ❌ Ошибка

```
CommandError: ngrok tunnel took too long to connect.
error: script "start" exited with code 1
```

## 🔍 Причина

Команда `bun run start` использует Rork платформу с ngrok туннелем, который не может подключиться.

---

## ✅ Решения

### Решение 1: Использовать стандартный Expo (рекомендуется)

**Запуск через локальную сеть (LAN):**

```bash
bunx expo start --lan
```

**Преимущества:**
- ✅ Не требует туннеля
- ✅ Быстрее
- ✅ Работает в локальной сети

**Требования:**
- Устройство и ноутбук в одной Wi-Fi сети

---

### Решение 2: Использовать локальный адрес

**Запуск без туннеля:**

```bash
bunx expo start --localhost
```

Или просто:

```bash
bunx expo start
```

---

### Решение 3: Использовать команду из package.json

**Уже есть команда для локального запуска:**

```bash
bun run start:local
```

Это запустит: `bunx expo start --lan`

---

## 📱 Подключение устройства

После запуска вы увидите QR код и адрес типа:

```
› Metro waiting on exp://192.168.x.x:8081
```

**Для iPhone:**
1. Откройте приложение **Камера**
2. Наведите на QR код
3. Нажмите на уведомление "Open in Expo Go"

**Для Android:**
1. Откройте **Expo Go**
2. Нажмите "Scan QR code"
3. Наведите на QR код

---

## 🔄 Альтернатива: Ввести адрес вручную

Если QR код не работает:

1. Найдите IP адрес в терминале (например: `192.168.1.100`)
2. В Expo Go нажмите "Enter URL manually"
3. Введите: `exp://192.168.1.100:8081`

---

## ⚠️ Если LAN не работает

**Попробуйте:**

1. Убедитесь, что устройство и ноутбук в одной Wi-Fi сети
2. Проверьте файрвол (может блокировать порт 8081)
3. Попробуйте другой Wi-Fi

**Или используйте туннель вручную:**

```bash
bunx expo start --tunnel
```

Но это может быть медленнее.

---

## ✅ Рекомендуемая команда

```bash
bun run start:local
```

Или:

```bash
bunx expo start --lan
```

---

**Статус:** Готово к запуску без туннеля
