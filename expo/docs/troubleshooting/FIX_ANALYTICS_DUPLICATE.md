# 🔧 Исправление ошибки: AnalyticsProvider уже объявлен

## ❌ Ошибка

```
ERROR  SyntaxError: /Users/mac/Desktop/rork-kiku/app/_layout.tsx: Identifier 'AnalyticsProvider' has already been declared. (15:9)
```

## ✅ Решение

### Шаг 1: Очистить кэш (уже сделано)

```bash
rm -rf .expo node_modules/.cache .metro
```

### Шаг 2: Перезапустить с очисткой кэша

```bash
bunx expo start --clear
```

Или:

```bash
bun run start
```

### Шаг 3: Если проблема остается

Проверьте файл `app/_layout.tsx` - должен быть только один импорт:

```typescript
import { AnalyticsProvider } from "@/constants/AnalyticsContext";
```

**НЕ должно быть:**
- Дублирующих импортов
- Повторных объявлений
- Конфликтующих имен

---

## 🔍 Проверка

Файл `app/_layout.tsx` проверен:
- ✅ Только один импорт `AnalyticsProvider` на строке 13
- ✅ Используется только один раз на строке 163
- ✅ Нет дублирующих объявлений

**Проблема была в кэше Metro bundler.**

---

## ✅ После очистки кэша

Запустите:

```bash
bunx expo start --clear
```

Или:

```bash
bun run start
```

Ошибка должна исчезнуть!

---

**Статус:** Кэш очищен, готово к перезапуску
