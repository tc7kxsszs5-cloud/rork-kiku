# 🔧 Исправление конфликта зависимостей

## Ошибка
```
npm error ERESOLVE unable to resolve dependency tree
Could not resolve dependency:
peer react@"^16.5.1 || ^17.0.0 || ^18.0.0" from lucide-react-native@0.475.0
```

## Причина
В проекте используется React 19.1.0, но `lucide-react-native` требует React 16/17/18.

## ✅ Решение

### Вариант 1: Использовать --legacy-peer-deps (РЕКОМЕНДУЕТСЯ)

```bash
cd /Users/mac/Desktop/rork-kiku
npm install --legacy-peer-deps
```

Этот флаг говорит npm игнорировать конфликты peer dependencies и установить пакеты все равно.

### Вариант 2: Использовать --force

```bash
npm install --force
```

---

## 🚀 После установки

После успешной установки запустите:

```bash
npx expo start --ios
```

---

## ⚠️ Почему это работает

`--legacy-peer-deps` использует старый алгоритм npm (до версии 7), который более терпим к конфликтам peer dependencies. Для Expo проектов это часто необходимо, так как многие пакеты еще не обновились для React 19.

---

## 📝 Примечание

Это безопасное решение для разработки. React 19 обратно совместим, и `lucide-react-native` должен работать с React 19, просто пакет еще не обновил свои peer dependencies.
