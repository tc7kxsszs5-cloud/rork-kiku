# 🔧 Исправление ошибки: Node.js версия несовместима

## Ошибка
```
TypeError: Class extends value undefined is not a constructor or null
Node.js v24.11.1
```

## Причина
Node.js v24.11.1 слишком новая версия. Expo лучше работает с Node.js 18 или 20.

## ✅ Решение: Переключить на Node.js 20

### Если у вас установлен nvm (Node Version Manager):

```bash
# Проверить установленные версии
nvm list

# Установить Node.js 20 (если не установлена)
nvm install 20

# Использовать Node.js 20
nvm use 20

# Проверить версию
node --version
# Должно показать: v20.x.x

# Переустановить зависимости
cd /Users/mac/Desktop/rork-kiku
rm -rf node_modules package-lock.json
npm install

# Запустить снова
npx expo start --ios
```

### Если nvm не установлен:

**Вариант 1: Установить nvm (рекомендуется)**

```bash
# Установить nvm
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash

# Перезапустить терминал или выполнить:
source ~/.zshrc

# Установить Node.js 20
nvm install 20
nvm use 20

# Проверить версию
node --version

# Переустановить зависимости
cd /Users/mac/Desktop/rork-kiku
rm -rf node_modules package-lock.json
npm install

# Запустить
npx expo start --ios
```

**Вариант 2: Скачать Node.js 20 с официального сайта**

1. Перейдите на https://nodejs.org/
2. Скачайте LTS версию (Node.js 20.x)
3. Установите
4. Перезапустите терминал
5. Проверьте: `node --version`
6. Переустановите зависимости и запустите

---

## 🔍 Проверка текущей версии

```bash
node --version
```

**Рекомендуемые версии для Expo:**
- ✅ Node.js 20.x (LTS) - рекомендуется
- ✅ Node.js 18.x (LTS) - тоже работает

**Не рекомендуется:**
- ❌ Node.js 24.x - слишком новая, проблемы совместимости
- ❌ Node.js 22.x - может быть нестабильно

---

## 📋 После переключения на Node.js 20

```bash
cd /Users/mac/Desktop/rork-kiku

# Переустановить зависимости
rm -rf node_modules package-lock.json
npm install

# Запустить приложение
npx expo start --ios
```

---

## 💡 Проверка через nvm

Если nvm доступен, проверьте версию:
```bash
nvm current
nvm list
```

Если видите Node.js 24, переключитесь на 20:
```bash
nvm use 20
```
