# 🔧 Исправление ошибки launchd_sim (code 60)

## ❌ Проблема

При запуске iOS симулятора вы получаете ошибку:

```
Error: xcrun simctl boot [DEVICE_ID] exited with non-zero code: 60
An error was encountered processing the command (domain=NSPOSIXErrorDomain, code=60):
Unable to boot the Simulator.
launchd failed to respond.
Underlying error (domain=com.apple.SimLaunchHostService.RequestError, code=4):
	Failed to start launchd_sim: could not bind to session, launchd_sim may have crashed or quit responding
```

## 🔍 Причина

Процесс `launchd_sim` — это системный демон, который управляет iOS симуляторами. Ошибка возникает когда:

1. **Процесс завис или крашнулся** — предыдущий запуск симулятора завершился некорректно
2. **Конфликт с уже запущенными симуляторами** — несколько симуляторов пытаются использовать один и тот же ресурс
3. **Проблемы с правами доступа** — процесс не может связаться с сессией
4. **Симулятор в нестабильном состоянии** — поврежденный кеш или конфигурация

## ✅ Решения

### Решение 1: Автоматическое исправление (РЕКОМЕНДУЕТСЯ)

Скрипт `ios:sim` теперь автоматически исправляет эту проблему:

```bash
bun run ios:sim
```

**Что делает скрипт:**
1. ✅ Очищает зависшие процессы `launchd_sim`
2. ✅ Выключает все симуляторы
3. ✅ Закрывает процессы Simulator
4. ✅ Пытается загрузить симулятор через `simctl boot`
5. ✅ Если не получается — переключается на безопасный режим (открывает Simulator вручную, запускает Expo без `--ios`)

### Решение 2: Ручное исправление

Если автоматическое исправление не помогло:

```bash
# 1. Исправить проблему вручную
bun run ios:sim:fix

# 2. Затем запустить безопасно
bun run ios:sim:safe
# Когда появится меню Expo — нажмите 'i'
```

### Решение 3: Полная очистка (если ничего не помогает)

```bash
# 1. Закрыть все процессы
killall Simulator 2>/dev/null
killall com.apple.CoreSimulator.CoreSimulatorService 2>/dev/null
pkill -f launchd_sim 2>/dev/null

# 2. Выключить все симуляторы
xcrun simctl shutdown all

# 3. Очистить кеш симулятора (ОСТОРОЖНО: это удалит все данные симуляторов)
# rm -rf ~/Library/Developer/CoreSimulator/Caches/*

# 4. Подождать 5 секунд
sleep 5

# 5. Запустить безопасно
bun run ios:sim:safe
```

### Решение 4: Использовать безопасный режим напрямую

Если проблема повторяется часто, используйте безопасный режим:

```bash
bun run ios:sim:safe
```

Этот скрипт:
- ✅ Не использует `simctl boot` (избегает ошибки)
- ✅ Открывает Simulator вручную
- ✅ Запускает Expo без `--ios` флага
- ✅ Вы нажимаете `i` в меню Expo

## 📋 Пошаговая инструкция

### Если ошибка возникла впервые:

1. **Остановите текущий процесс** (Ctrl+C в терминале)
2. **Запустите автоматическое исправление:**
   ```bash
   bun run ios:sim
   ```
3. **Если скрипт переключился на безопасный режим:**
   - Дождитесь появления меню Expo
   - Нажмите `i` для запуска в iOS симуляторе

### Если ошибка повторяется:

1. **Используйте безопасный режим:**
   ```bash
   bun run ios:sim:safe
   ```
2. **Или исправьте вручную:**
   ```bash
   bun run ios:sim:fix
   bun run start:simple
   # Нажмите 'i' в терминале
   ```

## 🔍 Проверка статуса

Проверить, какие симуляторы загружены:

```bash
xcrun simctl list devices | grep Booted
```

Проверить процессы `launchd_sim`:

```bash
ps aux | grep launchd_sim
```

## ⚠️ Важно

- **Не используйте `kill -9`** для процессов `launchd_sim` — это может повредить систему
- **Не удаляйте кеш симулятора** без необходимости — это удалит все данные всех симуляторов
- **Используйте безопасный режим** (`ios:sim:safe`) если проблема повторяется часто

## 🚀 Альтернативные способы запуска

Если проблема с симулятором не решается, можно использовать:

1. **Веб-версию:**
   ```bash
   bun run start-web
   ```

2. **Реальное устройство через Expo Go:**
   ```bash
   bun run start
   # Отсканируйте QR-код в Expo Go
   ```

3. **Android эмулятор:**
   ```bash
   bun run android:emulator
   ```

## 📚 Дополнительная информация

- [Официальная документация Expo по симуляторам](https://docs.expo.dev/workflow/ios-simulator/)
- [Документация Apple по simctl](https://developer.apple.com/documentation/xcode/command-line-tools)

---

**Если проблема не решается**, попробуйте:

### 1. Проверить расположение Xcode

Xcode должен быть установлен в `/Applications/Xcode.app`. Если он в другом месте (например, `/Users/mac/Downloads/Xcode.app`), это может вызывать проблемы.

**Автоматическая проверка:**
```bash
bun run ios:xcode:check
```

**Ручная проверка:**
```bash
xcode-select -p
# Должно показать: /Applications/Xcode.app/Contents/Developer
```

**Если Xcode в неправильном месте:**

**Вариант А: Переместить Xcode (РЕКОМЕНДУЕТСЯ)**
```bash
# 1. Закрыть Xcode если открыт
killall Xcode 2>/dev/null

# 2. Переместить в Applications
sudo mv /Users/mac/Downloads/Xcode.app /Applications/Xcode.app

# 3. Переключить xcode-select
sudo xcode-select --switch /Applications/Xcode.app/Contents/Developer

# 4. Принять лицензию
sudo xcodebuild -license accept
```

**Вариант Б: Переключить xcode-select на текущее расположение**
```bash
sudo xcode-select --switch /Users/mac/Downloads/Xcode.app/Contents/Developer
sudo xcodebuild -license accept
```

**✅ Если вы уже переместили Xcode в /Applications:**
- Проверьте, что `xcode-select` указывает на правильный путь: `bun run ios:xcode:check`
- Примите лицензию: `sudo xcodebuild -license accept`

### 2. Выключить зависшие симуляторы

Если симулятор завис в состоянии "Booting":
```bash
# Выключить все симуляторы
xcrun simctl shutdown all

# Или конкретный симулятор
xcrun simctl shutdown [DEVICE_ID]
```

### 3. Другие решения

1. Перезагрузить Mac
2. Обновить Xcode через App Store
3. Переустановить Command Line Tools: `sudo xcode-select --install`
