# 🔍 Проверка и исправление ошибки

## ❌ Текущая ошибка:

```
Tenant or user not found
username: "postgres"
```

## ✅ Решение:

Username должен быть в формате: `postgres.[PROJECT-REF]`

## 📋 Шаг 1: Проверьте Project Reference

1. Откройте https://supabase.com/dashboard
2. Выберите проект
3. **Settings** → **General**
4. Найдите **Reference ID** (это ваш project reference)

Или из connection string:
- Если видите: `db.eznumgsmwvavyunqhxfc.supabase.co`
- То project reference: `eznumgsmwvavyunqhxfc`

## 📋 Шаг 2: Правильный Connection String

**Формат:**
```
postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-us-east-1.pooler.supabase.com:6543/postgres
```

**Пример (если project reference = eznumgsmwvavyunqhxfc):**
```
postgresql://postgres.eznumgsmwvavyunqhxfc:gerkom-tYbpek-2cochi@aws-0-us-east-1.pooler.supabase.com:6543/postgres
```

## 📋 Шаг 3: Установите правильно

```bash
cd /Users/mac/Desktop/rork-kiku/backend

# Удалить старый
bunx vercel env rm DATABASE_URL production

# Добавить новый (ВАЖНО: скопируйте весь connection string целиком!)
bunx vercel env add DATABASE_URL production
```

**Когда спросит:**
- `Mark as sensitive?` → **yes**
- `What's the value of DATABASE_URL?` → Вставьте **весь** connection string с `postgres.[PROJECT-REF]`

## 📋 Шаг 4: Проверьте, что установилось

```bash
# Проверить переменные (не покажет значение, но покажет что есть)
bunx vercel env ls

# Деплой
bunx vercel --prod

# Проверка
curl "https://backend-three-mauve-67.vercel.app/api/trpc/test.dbCheck"
```

## ⚠️ Важно:

1. **Скопируйте connection string целиком** - не редактируйте вручную
2. **Убедитесь, что username = `postgres.[PROJECT-REF]`**, а не просто `postgres`
3. **После установки сделайте деплой** (`bunx vercel --prod`)

---

## 🔍 Если все еще не работает:

Пришлите:
1. Ваш project reference из Supabase Dashboard
2. Результат `curl "https://backend-three-mauve-67.vercel.app/api/trpc/test.dbCheck"`
