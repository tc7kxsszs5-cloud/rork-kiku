# 🔧 Исправление проблем с запуском

## Проблемы, которые были исправлены

### 1. ⚠️ CocoaPods Installation Error
**Ошибка:** `Failed to install CocoaPods with Homebrew. Install CocoaPods CLI and try again`

**Решение:**
- CocoaPods **не критичен** для Expo Go разработки
- Он нужен только для нативных iOS сборок
- Скрипты теперь игнорируют эту ошибку и продолжают работу

### 2. ⚠️ Tunnel Connection Timeout
**Ошибка:** `ngrok tunnel took too long to connect`

**Решение:**
- По умолчанию используется **LAN режим** (более надежный)
- Tunnel доступен как опция через `bun run start:rork:tunnel`
- LAN работает быстрее и стабильнее для локальной разработки

## 🚀 Новые команды

### Основные команды запуска:

```bash
# Умный запуск (LAN по умолчанию, игнорирует CocoaPods ошибки)
bun run start

# Только LAN (рекомендуется)
bun run start:rork:lan

# Только Tunnel (если нужен)
bun run start:rork:tunnel

# Простой Expo запуск
bun run start:simple
```

### Команды для исправления проблем:

```bash
# Автоматическое исправление всех проблем запуска
bun run fix:startup

# Быстрое исправление (очистка портов и кэша)
bun run fix
```

## 📋 Что делают скрипты

### `scripts/smart-start.sh`
- ✅ Проверяет CocoaPods (не блокирует при ошибке)
- ✅ Останавливает старые процессы
- ✅ Запускает с LAN режимом (надежнее чем tunnel)
- ✅ Показывает понятные сообщения об ошибках

### `scripts/fix-startup-issues.sh`
- ✅ Проверяет и пытается установить CocoaPods (опционально)
- ✅ Очищает старые процессы (rork, expo, metro)
- ✅ Освобождает порты (8081, 8082, 3000)
- ✅ Очищает кэш (.expo, node_modules/.cache)
- ✅ Проверяет зависимости

## 💡 Рекомендации

### Для разработки с Expo Go:
1. **Используйте LAN режим** - он быстрее и надежнее
2. **Убедитесь, что устройство и компьютер в одной Wi-Fi сети**
3. **CocoaPods не нужен** для Expo Go разработки

### Если нужен CocoaPods (для нативных сборок):
```bash
# Вариант 1: Через Homebrew (рекомендуется)
brew install cocoapods

# Вариант 2: Через gem (может требовать Ruby 3.0+)
sudo gem install cocoapods
```

### Если tunnel все еще нужен:
```bash
# Явно указывайте tunnel режим
bun run start:rork:tunnel

# Или напрямую
bunx rork start -p d8v7u672uumlfpscvnbps --tunnel --scheme rork-app
```

## 🐛 Решение проблем

### Проблема: "Порт занят"
```bash
bun run fix:startup
# или
bun run kill-ports
```

### Проблема: "Кэш поврежден"
```bash
bun run start:clear
# или
rm -rf .expo node_modules/.cache
```

### Проблема: "Зависимости не установлены"
```bash
bun install
```

## ✅ Чеклист перед запуском

- [ ] Зависимости установлены (`bun install`)
- [ ] Порты свободны (8081, 8082)
- [ ] Устройство и компьютер в одной Wi-Fi сети (для LAN)
- [ ] Старые процессы остановлены

## 📝 Изменения в package.json

- `start` - теперь использует умный скрипт (LAN по умолчанию)
- `start:rork:tunnel` - явный tunnel режим
- `start:rork:lan` - явный LAN режим
- `fix:startup` - новый скрипт для исправления проблем

---

**Главное:** Используйте `bun run start` - он автоматически использует LAN режим и игнорирует ошибки CocoaPods! 🚀
