# 🔧 Исправление ошибки "Cannot find module '/var/task/backend/hono'"

## ❌ Ошибка

```
Error [ERR_MODULE_NOT_FOUND]: Cannot find module '/var/task/backend/hono'
```

## ✅ Причина

Vercel не может найти файл `hono.ts` при импорте из `index.ts`. Это проблема с разрешением модулей в ESM.

---

## ✅ Решение 1: Исправить импорт (уже сделано)

Я обновил `backend/index.ts` - изменил импорт с `'./hono'` на `'./hono.js'`.

Это нужно для правильного разрешения модулей в ESM на Vercel.

---

## ✅ Решение 2: Обновить vercel.json (уже сделано)

Я обновил `backend/vercel.json` - добавил `includeFiles` для включения всех `.ts` файлов.

---

## 📋 Что сделать дальше

### 1. Передеплойте проект:

```bash
cd backend
bunx vercel --prod
```

---

### 2. Проверьте работу API:

После передеплоя откройте:
https://backend-three-mauve-67.vercel.app/

Должно показать:
```json
{"status": "ok", "message": "API is running"}
```

---

## 🔍 Если ошибка сохраняется

Проверьте, что все импорты используют правильные расширения:

1. В `index.ts` должно быть: `import app from './hono.js';`
2. Убедитесь, что файл `hono.ts` существует в директории `backend/`

---

## ✅ Готово!

После передеплоя ошибка должна исчезнуть!
