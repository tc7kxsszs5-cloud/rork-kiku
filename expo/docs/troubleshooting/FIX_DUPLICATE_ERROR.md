# 🔧 Исправление ошибки "AnalyticsProvider has already been declared"

## ❌ Ошибка

```
ERROR  SyntaxError: /Users/mac/Desktop/rork-kiku/app/_layout.tsx: Identifier 'AnalyticsProvider' has already been declared.
```

## ✅ Решение

Это обычно проблема с кешем Metro bundler. Вот несколько способов исправить:

### Способ 1: Очистка кеша при запуске (РЕКОМЕНДУЕТСЯ)

```bash
# Очистить кеш и запустить
bun run start -- --clear

# Или для Expo
npx expo start --clear
```

### Способ 2: Полная очистка кеша

```bash
# Остановите текущий процесс (Ctrl+C)

# Удалите все кеши
rm -rf node_modules/.cache
rm -rf .expo
rm -rf .metro

# Запустите снова
bun run start
```

### Способ 3: Полная переустановка (если ничего не помогает)

```bash
# Удалите кеши и node_modules
rm -rf node_modules
rm -rf .expo
rm -rf .metro
rm -rf node_modules/.cache

# Переустановите зависимости
bun install

# Запустите
bun run start -- --clear
```

### Способ 4: Перезапуск с очисткой watchman (macOS)

```bash
# Остановить watchman
watchman watch-del-all

# Очистить кеши
rm -rf node_modules/.cache
rm -rf .expo
rm -rf .metro

# Запустить
bun run start -- --clear
```

---

## 🔍 Проверка файла

Файл `app/_layout.tsx` должен содержать только **ОДИН** импорт `AnalyticsProvider`:

```typescript
import { AnalyticsProvider } from "@/constants/AnalyticsContext";
```

Это на строке 13. Если вы видите два таких импорта - удалите дубликат.

---

## 💡 Почему это происходит?

Metro bundler кеширует модули для ускорения сборки. Иногда при изменениях кеш не обновляется корректно, и bundler видит старую версию файла с дубликатами.

---

## ✅ После исправления

После очистки кеша проект должен запуститься без ошибок. Если ошибка повторяется:

1. Проверьте, что в файле только один импорт `AnalyticsProvider`
2. Убедитесь, что файл `constants/AnalyticsContext.tsx` существует
3. Попробуйте полную переустановку зависимостей (Способ 3)

---

**Попробуйте Способ 1 сначала - это самый быстрый! 🚀**

