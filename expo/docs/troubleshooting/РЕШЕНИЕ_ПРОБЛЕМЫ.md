# 🔧 Решение проблемы "getaddrinfo ENOTFOUND base"

## ❌ Проблема:

Ошибка `getaddrinfo ENOTFOUND base` означает, что connection string неправильно разобран парсером URL.

## ✅ Решение 1: Использовать `postgresql://` вместо `postgres://`

Некоторые парсеры URL неправильно обрабатывают `postgres://`. Используйте `postgresql://`:

```
postgresql://postgres.eznumgsmwvavyunqhxfc:gerkom-tYbpek-2cochi@aws-0-us-east-1.pooler.supabase.com:6543/postgres
```

---

## ✅ Решение 2: Проверить формат пароля

Пароль содержит дефис `-`, который обычно не требует кодирования, но проверим.

---

## ✅ Решение 3: Попробовать без project reference в username

```
postgresql://postgres:gerkom-tYbpek-2cochi@aws-0-us-east-1.pooler.supabase.com:6543/postgres
```

---

## 📋 Команды для Решения 1 (рекомендуется):

```bash
cd /Users/mac/Desktop/rork-kiku/backend
bunx vercel env rm DATABASE_URL production
```

**Ответьте:** `y`

```bash
bunx vercel env add DATABASE_URL production
```

**Ответьте:**
- `Mark as sensitive?` → **yes**
- `What's the value of DATABASE_URL?` → Вставьте:
  ```
  postgresql://postgres.eznumgsmwvavyunqhxfc:gerkom-tYbpek-2cochi@aws-0-us-east-1.pooler.supabase.com:6543/postgres
  ```

**Важно:** Используйте `postgresql://` (с `ql`), а не `postgres://`!

После установки:
```bash
bunx vercel --prod
curl "https://backend-three-mauve-67.vercel.app/api/trpc/test.dbCheck"
```

---

## 💡 Если Решение 1 не работает:

Попробуйте Решение 3 (без project reference в username).

---

**Попробуйте Решение 1 - используйте `postgresql://` вместо `postgres://`!**
