# ✅ Быстрый старт (исправлено)

## 🚀 Запуск проекта

```bash
# Основная команда (использует LAN, игнорирует ошибки CocoaPods)
bun run start
```

## 🔧 Если есть проблемы

```bash
# Автоматическое исправление всех проблем
bun run fix:startup

# Затем запустите
bun run start
```

## ⚠️ Важные замечания

### CocoaPods ошибка
- **Игнорируется автоматически** - не критично для Expo Go
- Нужен только для нативных iOS сборок
- Скрипты продолжают работу даже при ошибке

### Tunnel timeout
- **По умолчанию используется LAN** (надежнее)
- Tunnel доступен через `bun run start:rork:tunnel` (но нестабилен)
- **Рекомендуется:** всегда использовать LAN режим

## 📱 Для подключения устройства

1. Убедитесь, что **устройство и компьютер в одной Wi-Fi сети**
2. Запустите `bun run start`
3. Отсканируйте QR код или введите URL вручную в Expo Go

## 🎯 Альтернативные команды

```bash
# Только LAN (рекомендуется)
bun run start:rork:lan

# Простой Expo запуск
bun run start:simple

# Tunnel (нестабилен, не рекомендуется)
bun run start:rork:tunnel
```

---

**Главное:** Просто запустите `bun run start` - всё работает автоматически! 🎉
