defaults.url=https://sentry.io/
defaults.org=your-org
defaults.project=kiku-production

# Auth tokens are set via environment variables:
# SENTRY_AUTH_TOKEN
# Or manually:
# auth.token=your-auth-token
