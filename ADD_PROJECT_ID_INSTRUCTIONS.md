# 📝 Как добавить Expo Project ID в app.json

## ✅ У вас уже есть Project ID из Expo Dashboard

Теперь нужно добавить его в `app.json`.

---

## 📋 Инструкция

### Вариант 1: Я могу добавить (если вы скажете Project ID)

Просто сообщите мне ваш Project ID, и я добавлю его в `app.json`.

### Вариант 2: Добавить самостоятельно

1. Откройте файл `app.json`
2. Найдите секцию `"expo": {`
3. Добавьте секцию `"extra"` (если её нет) или добавьте `"eas"` в существующую:

**Если секции `"extra"` нет:**

```json
{
  "expo": {
    "name": "KIKU",
    "slug": "greeting-project-58uufiz",
    "extra": {
      "eas": {
        "projectId": "ваш-project-id-здесь"
      }
    },
    "version": "1.0.0",
    ...
  }
}
```

**Если секция `"extra"` уже есть:**

Просто добавьте `"eas"` внутри `"extra"`:

```json
{
  "expo": {
    "extra": {
      "eas": {
        "projectId": "ваш-project-id-здесь"
      },
      ... другие поля если есть
    }
  }
}
```

---

## 📝 Формат Project ID

Project ID имеет формат UUID:
```
xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
```

Например:
```
a1b2c3d4-e5f6-7890-abcd-ef1234567890
```

---

## ✅ После добавления

После того, как вы добавите Project ID, мы сможем:

1. ✅ Реализовать Push Notifications
2. ✅ Использовать Expo Push Notification Service
3. ✅ Отправлять push-уведомления родителям

---

**Пришлите ваш Project ID, и я добавлю его в app.json!** 🚀


