# 🔴 Руководство по исправлению ошибок

Как правильно работать с ошибками (красными крестиками) в проекте.

## 📋 Типы ошибок

### 1. TypeScript ошибки (красные подчеркивания в коде)

**Что делать:**
```bash
# Проверить все TypeScript ошибки
bun run typecheck

# Исправить автоматически (если возможно)
bun run lint:fix
```

**Когда перезапускать:**
- ✅ После исправления ошибок
- ✅ После изменения типов
- ❌ Не нужно перезапускать для предупреждений (warnings)

### 2. Линтер ошибки (ESLint)

**Что делать:**
```bash
# Проверить ошибки линтера
bun run lint

# Автоматически исправить
bun run lint:fix
```

**Когда перезапускать:**
- ✅ После исправления критических ошибок
- ❌ Не нужно для стилистических замечаний

### 3. Ошибки времени выполнения (Runtime)

**Что делать:**
1. Проверьте консоль (терминал где запущен Expo)
2. Найдите строку с ошибкой
3. Исправьте код
4. **Обязательно перезапустите:** Нажмите `r` в терминале Expo

### 4. Ошибки сборки (Build errors)

**Что делать:**
```bash
# Очистить кэш и пересобрать
bun run start:clear

# Или полностью перезапустить
# Остановите сервер (Ctrl+C) и запустите снова
bun run start:simple
```

## 🔄 Когда нужно перезапускать

### ✅ Обязательно перезапускать:

1. **Изменения в нативном коде** (iOS/Android)
   ```bash
   # После изменений в ios/ или android/
   # Остановите и перезапустите полностью
   ```

2. **Изменения в конфигурации** (app.json, package.json)
   ```bash
   # После изменения зависимостей
   bun install
   bun run start:simple
   ```

3. **Ошибки импорта/экспорта**
   - Исправьте импорты
   - Перезапустите: `r` в терминале Expo

4. **Ошибки в контекстах/провайдерах**
   - Исправьте код
   - Перезапустите: `r` в терминале Expo

### ❌ НЕ нужно перезапускать:

1. **TypeScript ошибки типов** (если код компилируется)
   - Исправьте типы
   - Hot Reload обновит автоматически

2. **Стилистические замечания линтера**
   - Исправьте при удобном случае
   - Не критично для работы

3. **Предупреждения (warnings)**
   - Не блокируют работу
   - Исправьте позже

## 🛠️ Процесс исправления ошибок

### Шаг 1: Определите тип ошибки

```bash
# Проверьте TypeScript
bun run typecheck

# Проверьте линтер
bun run lint

# Проверьте тесты
bun run test
```

### Шаг 2: Исправьте ошибки

**TypeScript:**
- Исправьте типы
- Добавьте недостающие типы
- Используйте `any` только в крайнем случае

**Линтер:**
```bash
# Автоисправление
bun run lint:fix
```

**Runtime:**
- Проверьте консоль
- Найдите причину ошибки
- Исправьте код

### Шаг 3: Перезапустите (если нужно)

```bash
# В терминале Expo нажмите:
r  # Перезагрузить приложение

# Или полностью перезапустите:
# Ctrl+C (остановить)
bun run start:simple  # Запустить заново
```

## 📝 Чеклист перед коммитом

Перед каждым коммитом убедитесь:

```bash
# 1. Нет TypeScript ошибок
bun run typecheck

# 2. Линтер проходит
bun run lint

# 3. Тесты проходят
bun run test

# 4. Приложение запускается
bun run start:simple
```

## 🚨 Частые ошибки и решения

### Ошибка: "Cannot find module"

```bash
# Решение:
bun install
# Перезапустите сервер
```

### Ошибка: "Type 'X' is not assignable to type 'Y'"

```typescript
// Исправьте типы или используйте type assertion
const value = data as ExpectedType;
```

### Ошибка: "Module not found"

```bash
# Проверьте импорты
# Убедитесь что файл существует
# Перезапустите: r в терминале Expo
```

### Ошибка: "Context is not defined"

```typescript
// Убедитесь что Provider обернут вокруг компонента
<UserProvider>
  <YourComponent />
</UserProvider>
```

## 💡 Советы

1. **Исправляйте ошибки сразу** - не накапливайте их
2. **Используйте автоисправление** - `bun run lint:fix`
3. **Проверяйте перед коммитом** - запускайте проверки
4. **Читайте сообщения об ошибках** - они подсказывают решение
5. **Не игнорируйте предупреждения** - они могут стать ошибками

## 🔍 Автоматическая проверка

Настройте pre-commit hook для автоматической проверки:

```bash
# В package.json уже есть скрипты:
"check": "./scripts/auto-check-and-fix.sh"
"check:fix": "./scripts/auto-check-and-fix.sh --fix"
```

Используйте перед коммитом:
```bash
bun run check
```

---

**Помните:** Красные крестики - это не враги, а помощники! Они показывают проблемы до того, как они станут критическими. 🚀
