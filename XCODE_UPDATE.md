# 🔧 Обновление Xcode

## ⚠️ Проблема

Ошибка:
```
Error: Your Xcode (14.0 => /Applications/Xcode 2.app/Contents/Developer) at /Applications/Xcode 2.app is too outdated.
Please update to Xcode 15.2 (or delete it).
```

Ваша версия Xcode (14.0) слишком старая. Требуется Xcode 15.2 или новее.

## ✅ Решение

### Способ 1: Обновить через App Store (Рекомендуется)

1. **Откройте App Store** на Mac
2. **Перейдите в раздел "Обновления"** (Updates)
3. Найдите **Xcode** в списке обновлений
4. Нажмите **"Обновить"** (Update)
5. Дождитесь завершения загрузки и установки (может занять время, ~10-15 GB)

### Способ 2: Скачать напрямую с developer.apple.com

1. Перейдите на https://developer.apple.com/download/all/
2. Войдите с Apple ID (если требуется)
3. Найдите **Xcode 15.2** (или новее)
4. Скачайте `.xip` файл
5. Откройте `.xip` файл для распаковки
6. Переместите Xcode в `/Applications/`

### Способ 3: Через команду (если установлен Xcode Command Line Tools)

```bash
# Обновить через mas (Mac App Store command line)
brew install mas
mas search Xcode
mas install 497799835  # ID для Xcode (проверьте актуальный ID)
```

## 🔄 После обновления

### 1. Принять лицензию

```bash
sudo xcodebuild -license accept
```

### 2. Установить Command Line Tools

```bash
xcode-select --install
```

Или если уже установлены, переключить на новый Xcode:

```bash
sudo xcode-select --switch /Applications/Xcode.app/Contents/Developer
```

### 3. Проверить версию

```bash
xcodebuild -version
```

Должно показать:
```
Xcode 15.2
Build version 15C500b
```

## 📋 Проверка установки

После обновления проверьте:

```bash
# Версия Xcode
xcodebuild -version

# Путь к Xcode
xcode-select -p

# SDK версии
xcodebuild -showsdks
```

## ⚠️ Важные замечания

1. **Размер**: Xcode занимает много места (~10-15 GB), убедитесь, что достаточно свободного места
2. **Время**: Обновление может занять 30-60 минут (зависит от скорости интернета)
3. **Версия macOS**: Убедитесь, что ваша macOS поддерживает Xcode 15.2
   - macOS Sonoma (14.0) или новее рекомендуется
4. **Множественные версии**: Если у вас несколько версий Xcode, убедитесь, что выбрана правильная:
   ```bash
   sudo xcode-select --switch /Applications/Xcode.app/Contents/Developer
   ```

## 🚀 Альтернатива (для конкретных задач)

Если вам не нужен полный Xcode, а только Command Line Tools для некоторых задач:

```bash
# Установить только Command Line Tools
xcode-select --install
```

Но для разработки iOS/React Native приложений с Expo/EAS обычно требуется полный Xcode.

## 📝 Для проекта KIKU

Если вы работаете над проектом KIKU:

- **Для разработки React Native/Expo**: Xcode нужен для iOS симулятора и сборок
- **Для EAS Build**: Xcode не обязателен локально (сборка на серверах Expo)
- **Для тестирования на симуляторе**: Xcode обязателен

После обновления Xcode:
- iOS симулятор будет работать корректно
- EAS Build сможет использовать новую версию
- Все инструменты для iOS разработки будут актуальны

---

**Рекомендация**: Обновите Xcode через App Store - это самый простой и безопасный способ.


