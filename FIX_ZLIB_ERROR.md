# 🔧 Исправление ошибки zlib

## Ошибка
```
ZlibError: zlib: invalid block type
```

## Причина
Поврежденные файлы в `node_modules` - часто из-за прерванной загрузки или проблем с сетью.

## ✅ Решение

### Шаг 1: Полная очистка (ВАЖНО!)

```bash
cd /Users/mac/Desktop/rork-kiku

# Остановить все процессы
killall node

# Удалить node_modules и lock файлы
rm -rf node_modules
rm -f bun.lockb package-lock.json yarn.lock

# Очистить ВСЕ кеши bun
bun pm cache rm
rm -rf ~/.bun/install/cache
rm -rf ~/.bun/install/global

# Очистить кеши npm (если есть)
npm cache clean --force 2>/dev/null || true
```

### Шаг 2: Переустановка зависимостей

```bash
# Переустановить зависимости с принудительной очисткой
bun install --force
```

### Шаг 2 (альтернатива): Если bun install не работает

```bash
# Попробовать через npm
npm install
```

### Шаг 3: Если все еще ошибка целостности

```bash
# Удалить конкретный проблемный пакет из кеша
rm -rf ~/.bun/install/cache/@babel
rm -rf node_modules/@babel

# Переустановить
bun install --force
```

### Шаг 3: Запуск

```bash
# Запустить приложение
bun run start:expo
```

---

## 🔄 Альтернативный способ (если bun install не работает)

```bash
cd /Users/mac/Desktop/rork-kiku

# Полная очистка
rm -rf node_modules
rm -f bun.lockb

# Очистить кеш npm/bun
bun pm cache rm
rm -rf ~/.bun/install/cache

# Переустановить
bun install --force
```

---

## ⚠️ Если проблема сохраняется

Попробуйте использовать npm вместо bun:

```bash
cd /Users/mac/Desktop/rork-kiku
rm -rf node_modules bun.lockb
npm install
npm start
```

---

## ✅ После успешной установки

Запустите приложение:
```bash
bun run start:expo
# Или для симулятора:
bunx expo start --ios
```
