# 📱 Запуск в iOS симуляторе (без Expo Go)

## ✅ Рекомендуемый способ для симулятора

В iOS симуляторе **не нужно** Expo Go! Запускайте напрямую через Expo CLI.

### Команда для запуска:

```bash
cd /Users/mac/Desktop/rork-kiku
bunx expo start --ios
```

Или:
```bash
bun run start:expo
# Затем нажмите 'i' в терминале
```

---

## 🔄 Если симулятор уже открыт и пытается скачать Expo Go

### Вариант 1: Остановить и запустить заново

1. **Закройте симулятор:**
   ```bash
   killall Simulator
   ```

2. **Запустите напрямую через Expo:**
   ```bash
   cd /Users/mac/Desktop/rork-kiku
   bunx expo start --ios
   ```

### Вариант 2: Дождаться скачивания Expo Go

Если симулятор скачивает Expo Go, можно подождать - это может занять время. После установки приложение откроется автоматически.

---

## 🚀 Правильный запуск для симулятора

```bash
cd /Users/mac/Desktop/rork-kiku

# Остановить текущий процесс (Ctrl+C в терминале где запущен)

# Запустить напрямую в симуляторе
bunx expo start --ios
```

Эта команда:
- ✅ Откроет симулятор автоматически
- ✅ Соберет и запустит приложение напрямую
- ✅ Не требует Expo Go

---

## ⚠️ Важно

- **Для реального устройства (iPhone/iPad):** Используйте Expo Go
- **Для симулятора:** Запускайте напрямую через `bunx expo start --ios`

---

## 🔍 Если все еще не работает

Попробуйте с очисткой кеша:
```bash
cd /Users/mac/Desktop/rork-kiku
bunx expo start --ios --clear
```
