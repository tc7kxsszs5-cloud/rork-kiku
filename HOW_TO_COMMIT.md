# 📝 Как сделать коммит и отправить в GitHub

## Быстрый способ:

### Шаг 1: Проверить статус
```bash
git status
```

### Шаг 2: Добавить файлы (если еще не добавлены)
```bash
git add .cursorrules
```

### Шаг 3: Сделать коммит
```bash
git commit -m "Add Cursor AI agent rules (.cursorrules)"
```

### Шаг 4: Отправить в GitHub
```bash
git push
```

---

## Пошаговая инструкция:

### 1. Проверка изменений
```bash
cd ~/Desktop/rork-kiku
git status
```

Вы увидите файлы, которые готовы к коммиту (в зеленом цвете).

### 2. Коммит
```bash
git commit -m "Add Cursor AI agent rules (.cursorrules)"
```

**Что означает:**
- `git commit` - создает коммит (сохранение изменений)
- `-m "..."` - сообщение коммита (описание того, что вы добавили)

### 3. Отправка в GitHub
```bash
git push
```

Это отправит ваши изменения в удаленный репозиторий на GitHub.

---

## Альтернативные сообщения коммита:

Если хотите более подробное сообщение:
```bash
git commit -m "Add .cursorrules file for Cursor AI agent configuration

- Add project context and coding rules
- Configure AI agent behavior for KIKU project
- Include design principles and patterns"
```

---

## Если нужно отменить добавление файла:

```bash
# Убрать файл из staging area (но оставить изменения)
git restore --staged .cursorrules
```

---

## Полная команда (все в одной строке):

```bash
cd ~/Desktop/rork-kiku && git commit -m "Add Cursor AI agent rules (.cursorrules)" && git push
```

Это сделает коммит и сразу отправит в GitHub.





