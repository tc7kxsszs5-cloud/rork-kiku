# 🌍 Автоматическое определение языка устройства

## 📋 Как это работает

### 1. **Файлы переводов - нужно добавлять ВРУЧНУЮ**

✅ **Что нужно делать:**
- Создавать файл `constants/locales/{код_языка}.ts` для каждого языка
- Добавлять импорт в `constants/i18n.ts`
- Добавлять в `resources` объект

❌ **Что НЕ происходит автоматически:**
- Переводы НЕ генерируются автоматически
- Файлы НЕ создаются автоматически
- Нужно вручную переводить все строки

### 2. **Автоматическое определение языка устройства**

✅ **Что происходит автоматически:**
- При первом запуске приложение определяет язык устройства
- Если язык поддерживается - переключается на него
- Если не поддерживается - использует fallback (английский)

❌ **Что НЕ происходит автоматически:**
- Если файл перевода не создан - язык не будет работать
- Нужно сначала создать файл перевода, потом он будет автоматически определяться

## 🔧 Как настроить автоматическое определение

### Шаг 1: Установить expo-localization (если еще не установлен)

```bash
bun add expo-localization
```

### Шаг 2: Обновить detectDeviceLanguage в i18n.ts

Текущая версия работает только для web. Нужно добавить поддержку React Native.

### Шаг 3: Автоматическое переключение при первом запуске

Добавить в `app/_layout.tsx` или `UserContext.tsx` автоматическое определение при первом запуске.

## 📱 Как это работает на практике

### Сценарий 1: Пользователь из Испании
1. Устройство настроено на испанский (es)
2. Приложение запускается первый раз
3. `detectDeviceLanguage()` определяет "es"
4. Проверяет: есть ли файл `es.ts`? ✅ Да
5. Автоматически переключает на испанский
6. Сохраняет выбор в AsyncStorage

### Сценарий 2: Пользователь из Франции
1. Устройство настроено на французский (fr)
2. Приложение запускается первый раз
3. `detectDeviceLanguage()` определяет "fr"
4. Проверяет: есть ли файл `fr.ts`? ❌ Нет (еще не создан)
5. Использует fallback: английский (en)
6. Пользователь может вручную выбрать русский или испанский

### Сценарий 3: Пользователь из Китая
1. Устройство настроено на китайский (zh-CN)
2. Приложение запускается первый раз
3. `detectDeviceLanguage()` определяет "zh"
4. Проверяет: есть ли файл `zh.ts`? ❌ Нет (еще не создан)
5. Использует fallback: английский (en)
6. После создания `zh.ts` - будет автоматически определяться

## 🎯 Транскрипция vs Перевод

### Транскрипция (Speech-to-Text)
- **Что это:** Преобразование голосовых сообщений в текст
- **Как работает:** Использует API устройства (iOS/Android)
- **Автоматически:** Да, работает на языке устройства
- **Где используется:** В чате, когда пользователь записывает голосовое сообщение

### Перевод (i18n)
- **Что это:** Перевод интерфейса приложения на разные языки
- **Как работает:** Через файлы переводов в `constants/locales/`
- **Автоматически:** Частично - определение языка да, переводы нет
- **Где используется:** Во всем интерфейсе приложения

## ✅ Рекомендации

### Для автоматического определения:
1. ✅ Создавать файлы переводов для популярных языков
2. ✅ Настроить автоматическое определение при первом запуске
3. ✅ Сохранять выбор пользователя в AsyncStorage
4. ✅ Позволить пользователю вручную менять язык в настройках

### Для транскрипции:
1. ✅ Использовать нативный API устройства (автоматически на языке устройства)
2. ✅ Не нужно ничего настраивать - работает автоматически
3. ✅ Если нужно - можно добавить выбор языка для транскрипции отдельно

## 🔄 Процесс добавления нового языка

1. **Создать файл перевода:**
   ```typescript
   // constants/locales/fr.ts
   export default {
     common: { ... },
     tabs: { ... },
     // ... все ключи
   };
   ```

2. **Добавить в i18n.ts:**
   ```typescript
   import fr from './locales/fr';
   const resources = {
     // ...
     fr: { translation: fr },
   };
   ```

3. **Обновить availableLanguages:**
   ```typescript
   { code: 'fr', name: 'French', nameNative: 'Français', flag: '🇫🇷', region: 'Europe' }
   ```

4. **Готово!** Теперь язык будет автоматически определяться для пользователей из Франции

---

**Вывод:** Файлы переводов нужно создавать вручную, но после создания язык будет автоматически определяться для пользователей из соответствующих стран.
