# 📱 Как открыть iPhone 14 в симуляторе

## Быстрый способ:

### Вариант 1: Через команду (самый простой)
```bash
# Запустить iPhone 14 напрямую
xcrun simctl boot "iPhone 14" && open -a Simulator
```

### Вариант 2: Через приложение Simulator
1. Откройте **Simulator** (если еще не открыт):
   ```bash
   open -a Simulator
   ```

2. В меню Simulator выберите:
   - **File** → **Open Simulator** 
   - Найдите **iPhone 14** в списке
   - Кликните на него

### Вариант 3: Через Xcode
1. Откройте **Xcode**
2. **Xcode** → **Settings** → **Platforms** (или **Preferences** → **Components**)
3. Убедитесь, что iOS SDK установлен
4. Запустите симулятор через Xcode

## Если iPhone 14 не открывается:

### Проверка доступных устройств:
```bash
# Посмотреть все доступные iPhone 14
xcrun simctl list devices available | grep "iPhone 14"
```

### Перезагрузка симулятора:
```bash
# Закрыть все симуляторы
killall Simulator

# Запустить заново
open -a Simulator
```

### Если устройство "зависло":
```bash
# Выключить устройство
xcrun simctl shutdown "iPhone 14"

# Запустить заново
xcrun simctl boot "iPhone 14" && open -a Simulator
```

## Запуск проекта в iPhone 14:

После того как симулятор открыт:

```bash
# 1. Очистить кеш (если нужно)
cd ~/Desktop/rork-kiku
rm -rf node_modules/.cache .expo .metro
killall node

# 2. Запустить проект
bun run start

# 3. В терминале нажать 'i' для запуска в симуляторе
```

## Если ничего не помогает:

1. **Перезагрузите Mac**
2. После перезагрузки:
   ```bash
   open -a Simulator
   # File → Open Simulator → iPhone 14
   ```





