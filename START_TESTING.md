# 🚀 Начало адаптации и тестирования

## 📋 Текущая конфигурация системы

- **macOS**: 13.7.8 (Ventura) ✅
- **Xcode**: 14.0 (устарел, требуется 15.2+) ⚠️
- **Bun**: 1.3.4 ✅
- **Свободное место**: ~132 GB ✅
- **iOS симуляторы**: Доступны (iOS 16.0) ✅

## 🎯 Варианты начала тестирования

### Вариант 1: Тестирование на веб-версии (Быстро, не требует Xcode)

**Рекомендуется для быстрого старта!**

```bash
# Запустить веб-версию
cd /Users/mac/Desktop/rork-kiku
bun run start-web
# После запуска откройте браузер вручную по адресу из консоли (обычно http://localhost:8081)
```

**Если браузер не открылся автоматически:**
```bash
# Альтернативный способ через стандартный Expo:
bun run start-web-expo
# Или:
bunx expo start --web
```

**Преимущества:**
- ✅ Работает сразу, не требует обновления Xcode
- ✅ Быстрый запуск
- ✅ Удобно для тестирования UI

**Ограничения:**
- ⚠️ Некоторые функции работают ограниченно (геолокация, камера, микрофон)
- ⚠️ Браузер может не открыться автоматически - нужно открыть вручную по адресу из консоли

---

### Вариант 2: Тестирование на реальном устройстве через Expo Go (Не требует Xcode)

**Рекомендуется для полного тестирования функциональности!**

1. **Установите Expo Go на устройство:**
   - iOS: [App Store](https://apps.apple.com/app/expo-go/id982107779)
   - Android: [Google Play](https://play.google.com/store/apps/details?id=host.exp.exponent)

2. **Запустите dev сервер:**
   ```bash
   cd /Users/mac/Desktop/rork-kiku
   bun run start
   ```

3. **Отсканируйте QR-код** в приложении Expo Go

**Преимущества:**
- ✅ Полный доступ ко всем функциям (камера, геолокация, уведомления)
- ✅ Тестирование на реальном устройстве
- ✅ Не требует обновления Xcode

---

### Вариант 3: Обновление Xcode для тестирования на симуляторе

**Требуется для:**
- Тестирования на iOS симуляторе
- Сборок через EAS Build
- Публикации в App Store

#### Требования для Xcode 15.2+
- ✅ macOS 13.5+ (у вас 13.7.8 - совместимо)
- ✅ ~10-15 GB свободного места (у вас ~132 GB - достаточно)
- ⏱️ Время загрузки: 30-60 минут

#### Шаги по обновлению:

**Способ 1: Через App Store (Рекомендуется)**

1. Откройте **App Store**
2. Перейдите в раздел **"Обновления"** (Updates)
3. Найдите **Xcode** и нажмите **"Обновить"**
4. Дождитесь завершения (может занять время)

**Способ 2: Скачать напрямую**

1. Перейдите на **https://developer.apple.com/download/all/**
2. Войдите с Apple ID (можно создать бесплатно на appleid.apple.com)
3. Найдите **Xcode 15.2** (или новее) в списке
4. Нажмите **"Download"** и скачайте `.xip` файл (~10-15 GB)
5. Дважды кликните на `.xip` файл для распаковки
6. Переместите `Xcode.app` в `/Applications/`

📖 **Подробная инструкция**: См. `XCODE_15_DOWNLOAD.md`

**После обновления Xcode:**

```bash
# Принять лицензию
sudo xcodebuild -license accept

# Переключить на новый Xcode
sudo xcode-select --switch /Applications/Xcode.app/Contents/Developer

# Проверить версию
xcodebuild -version
# Должно показать: Xcode 15.2 или новее
```

**Запуск на симуляторе:**

```bash
cd /Users/mac/Desktop/rork-kiku
bun run start
# Нажмите 'i' для запуска в iOS симуляторе
```

---

## 🎬 Рекомендуемый порядок действий

### Для быстрого старта (СЕЙЧАС):

1. **Запустите веб-версию:**
   ```bash
   cd /Users/mac/Desktop/rork-kiku
   bun run start-web
   ```

2. **Или установите Expo Go на телефон и запустите:**
   ```bash
   bun run start
   # Отсканируйте QR-код в Expo Go
   ```

### Для полноценного тестирования (ПОЗЖЕ):

1. **Обновите Xcode до 15.2+** (через App Store)
2. **Настройте Xcode:**
   ```bash
   sudo xcodebuild -license accept
   sudo xcode-select --switch /Applications/Xcode.app/Contents/Developer
   ```

3. **Запустите на симуляторе:**
   ```bash
   bun run start
   # Нажмите 'i'
   ```

---

## ✅ Чек-лист готовности к тестированию

- [x] macOS совместима (13.7.8)
- [x] Bun установлен (1.3.4)
- [x] Достаточно места на диске (~132 GB)
- [ ] Xcode обновлен до 15.2+ (опционально для веб/Expo Go)
- [ ] Зависимости установлены (`bun install`)
- [ ] Dev сервер запущен (`bun run start`)

---

## 🔍 Проверка текущего состояния проекта

```bash
# Проверить установлены ли зависимости
cd /Users/mac/Desktop/rork-kiku
ls node_modules 2>/dev/null && echo "✅ Зависимости установлены" || echo "❌ Нужно: bun install"

# Проверить версию Xcode
xcodebuild -version

# Проверить доступные симуляторы
xcrun simctl list devices available | head -15
```

---

## 📚 Полезные ссылки

- **XCODE_UPDATE.md** - Детальное руководство по обновлению Xcode
- **QUICKSTART.md** - Быстрый старт проекта
- **IOS_SIMULATOR_GUIDE.md** - Работа с iOS симулятором
- **EXPO_GO_GUIDE.md** - Тестирование через Expo Go

---

## ❓ Что делать дальше?

**Если нужно начать тестирование ПРЯМО СЕЙЧАС:**
→ Используйте **Вариант 1** (веб) или **Вариант 2** (Expo Go)

**Если нужно тестирование на симуляторе:**
→ Используйте **Вариант 3** (обновите Xcode)

**Если возникают проблемы:**
→ См. раздел "Troubleshooting" в QUICKSTART.md

