# 🔧 Решение ошибки "AnalyticsProvider has already been declared"

## ❌ Ошибка продолжается

Если очистка кеша не помогла, попробуйте следующее:

## 🔄 Полное решение

### Шаг 1: Полная очистка

```bash
# Остановите все процессы (Ctrl+C)

# Удалите все кеши
rm -rf node_modules/.cache
rm -rf .expo
rm -rf .metro
rm -rf .rork
watchman watch-del-all 2>/dev/null || true

# Удалите node_modules (если нужно)
rm -rf node_modules

# Переустановите зависимости
bun install
```

### Шаг 2: Проверьте файл вручную

Откройте `app/_layout.tsx` и убедитесь, что импорт `AnalyticsProvider` встречается **ТОЛЬКО ОДИН РАЗ** на строке 13:

```typescript
import { AnalyticsProvider } from "@/constants/AnalyticsContext";
```

**НЕ ДОЛЖНО БЫТЬ:**
- Два таких импорта
- Импорта в комментариях
- Импорта в других местах файла

### Шаг 3: Запуск с очисткой

```bash
bun run start -- --clear
```

### Шаг 4: Если не помогло - альтернативный запуск

```bash
# Попробуйте через npx expo
npx expo start --clear

# Или через npm
npm start -- --reset-cache
```

---

## 🔍 Дополнительная диагностика

### Проверка на дубликаты в терминале:

```bash
# Проверить количество импортов AnalyticsProvider
grep -c "import.*AnalyticsProvider" app/_layout.tsx

# Должно быть: 1
# Если больше - есть дубликат
```

### Проверка файла AnalyticsContext:

```bash
# Проверить экспорты
grep "export.*AnalyticsProvider" constants/AnalyticsContext.tsx

# Должен быть только один экспорт
```

---

## 🆘 Если ничего не помогает

1. **Проверьте, нет ли других файлов _layout.tsx:**
   ```bash
   find . -name "_layout.tsx" -not -path "./node_modules/*"
   ```

2. **Проверьте, нет ли конфликтов в импортах:**
   ```bash
   grep -r "AnalyticsProvider" app/ constants/ --include="*.tsx" --include="*.ts" | grep -v node_modules
   ```

3. **Попробуйте временно закомментировать импорт:**
   - Закомментируйте строку 13 в `app/_layout.tsx`
   - Запустите проект
   - Если ошибка исчезнет - проблема точно в этом импорте
   - Раскомментируйте обратно

---

## 💡 Альтернативное решение (временно)

Если проблема критична и нужно срочно запустить проект:

1. Временно можно закомментировать `AnalyticsProvider` в `AppProviders`
2. Запустить проект
3. Потом разобраться с проблемой

**НЕ РЕКОМЕНДУЕТСЯ для production!**

---

**Попробуйте полную очистку из Шага 1 - это обычно помогает! 🚀**

