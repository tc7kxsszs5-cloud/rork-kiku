# 📝 Быстрая шпаргалка для новичков

Простая памятка с основными командами. Держите под рукой!

## 🚀 Запуск проекта

```bash
# Запустить проект (локально)
bun run start:simple

# Запустить с туннелем (для телефона в другой сети)
bun run start

# В терминале Expo нажмите:
# i - для iOS симулятора
# a - для Android
# w - для веб-версии
```

### 📱 Запуск на реальном телефоне

**Через Expo Go (не требует CocoaPods):**
```bash
# 1. Установите Expo Go на телефон (App Store / Google Play)
# 2. Запустите сервер:
bun run start

# 3. Откройте Expo Go и отсканируйте QR-код
```

**Если видите предупреждение о CocoaPods:**
- ⚠️ Это не критично для Expo Go
- CocoaPods нужен только для нативной сборки (`expo run:ios`)
- Для тестирования через Expo Go он не требуется

### iOS симулятор: ошибки

**Ошибка "launchd_sim" / "could not bind to session":**
```bash
bun run ios:sim:safe
# Когда появится меню Expo — нажмите i
```

**Ошибка 137 (exit code 137) — процесс убит:**
```bash
# 1. Сначала исправьте симулятор:
bun run ios:sim:fix

# 2. Затем запустите безопасно:
bun run ios:sim:safe
# Когда появится меню Expo — нажмите i
```

**Ошибка "Unable to boot the Simulator":**
```bash
# 1. Загрузите симулятор вручную:
bun run ios:sim:boot
# (по умолчанию загружает iPhone 14)
# Или конкретное устройство:
bun run ios:sim:boot "iPhone 15"

# 2. Затем запустите проект:
bun run start:simple
# Нажмите 'i' в терминале
```

**Если всё ещё не работает:**
```bash
# Откройте Simulator вручную:
# Xcode → Open Developer Tool → Simulator
# File → Open Simulator → выберите устройство
# Затем:
bun run start:simple
# Нажмите 'i' в терминале
```

## ✅ Проверка перед коммитом

```bash
# Проверить TypeScript
bun run typecheck

# Проверить линтер
bun run lint

# Обе проверки сразу
bun run typecheck && bun run lint
```

## 💾 Коммит изменений

```bash
# 1. Добавить все изменения
git add .

# 2. Создать коммит
git commit -m "описание что изменили"

# 3. Отправить на сервер
git push
```

**Или одной командой:**
```bash
git add . && git commit -m "описание" && git push
```

## 🔍 Проверить статус

```bash
# Что изменено
git status

# Что будет в коммите
git status --short

# Последние коммиты
git log --oneline -5
```

## 🐛 Если что-то не работает

```bash
# Очистить и переустановить зависимости
rm -rf node_modules bun.lock
bun install

# Очистить кэш Expo
bun run start:clear

# Перезапустить проект
# Ctrl+C (остановить)
bun run start:simple
```

## 📋 Чеклист перед коммитом

- [ ] Проверил: `bun run typecheck`
- [ ] Проверил: `bun run lint`
- [ ] Нет красных ошибок в IDE
- [ ] Проект запускается
- [ ] Закоммитил: `git commit -m "описание"`
- [ ] Отправил: `git push`

## 🎯 Частые задачи

### Начать работу
```bash
git pull              # Обновить код
bun run start:simple  # Запустить проект
```

### Сохранить работу
```bash
git add .
git commit -m "что сделали"
git push
```

### Исправить ошибки
```bash
bun run lint:fix      # Автоисправление
bun run typecheck     # Проверить типы
```

### Тесты и Node
```bash
bun run test          # Все тесты (Jest)
bun run test:unit     # Только unit-тесты
```

- **Node:** рекомендуется Node 18+ (`engines.node` в `package.json`). На Node 20+ после `bun install` автоматически применяется патч для jest-runtime (readonly fix).
- **Jest / stack-utils:** обновлены, патч в `scripts/patch-jest-runtime.js`, запускается в `postinstall`.

## 📚 Где найти помощь

- **Полная настройка:** [SETUP.md](SETUP.md)
- **Работа с ошибками:** [ERROR_HANDLING_GUIDE.md](ERROR_HANDLING_GUIDE.md)
- **Рабочий процесс:** [WORKFLOW_GUIDE.md](WORKFLOW_GUIDE.md)
- **Для разработчиков:** [DEVELOPER_GUIDE.md](DEVELOPER_GUIDE.md)

## 💡 Советы для новичков

1. **Не паникуйте** - ошибки это нормально
2. **Проверяйте перед коммитом** - это сэкономит время
3. **Коммитьте часто** - лучше маленькие коммиты чем большие
4. **Читайте ошибки** - они подсказывают что не так
5. **Используйте эту шпаргалку** - не нужно все помнить наизусть

## ⚠️ Что НЕ делать

- ❌ Не коммитьте с ошибками
- ❌ Не коммитьте файл `.env`
- ❌ Не коммитьте `node_modules`
- ❌ Не игнорируйте предупреждения

---

**Держите эту шпаргалку открытой - она поможет! 📌**
